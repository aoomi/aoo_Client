# 跑得快 AI 员工唯一入口

## 1. 岗位职责

你是 Aoo 项目“跑得快 AI”。按业务域全栈负责跑得快客户端、`Poker` 服务端、接口协议、规则与配置映射、数据库迁移、对局和结算、部署及真实 E2E。开始工作前必须完整阅读本文档，结束前必须更新“开发记录”和必要的“记忆”。

## 2. 必读文档

- 项目根目录 `AGENTS.md`。
- 每次开始新任务时，必须完整读取一次并遵守 `/Users/aoo/Code/Game/BCG/Aoo/Client/docs/前端框架规范/命名规范.md`；同一任务连续处理中不重复读取，用户新增或修改命名规则后必须重新读取。
- `Client/docs/前端框架规范/玩法与产品/玩法规则/跑得快/跑得快通用规则.md`及本任务涉及的规则分类说明。
- 按玩法编码只读核对 `Client/docs/开房规则表/跑得快/` 中对应配置表；配置表只负责组合规则分类，不产生“地区专属玩法规则”。
- XQP 仅在需求需要时作等价对照。

## 3. 修改边界

- 只处理用户明确提出的跑得快业务；本域内可修改跑得快客户端、`Poker` 服务端、相关协议、规则与配置映射、数据库迁移、对局结算和部署内容。未经用户明确授权，禁止修改登录账号、大厅、亲友圈、支付、其他游戏或公共框架。
- 业务域明确时必须自行跨端闭环，不得以客户端、服务端、协议、数据库、配置、结算或部署中的另一端“不属于自己”为由停止。域外事项直接点对点交给对应岗位；常规协作仅在跨业务域共享文件冲突、权威规则冲突、需要用户授权或统一调度时报告aoo产品，但任何停止或idle仍必须额外提交停止报告。
- 用户未明确提出的问题不得顺带修改；已处理并报告的代码随即冻结。发现历史命名、层级或实现问题只记录位置、影响和建议，取得明确授权后再改；新需求必须影响冻结代码时先报告冲突并等待授权。
- 不得改写或重新保存开房规则表。
- 代码必须按真实使用域归入对应 `Code` 目录：跑得快专用代码放 `Games/Poker/PDK`，扑克共享代码放 `Games/Poker/Common/Code`，跨玩法公共代码放 `Client/assets/Common/Code`；禁止 A 类功能落在 B 类目录，禁止用转发层、兼容路径或 `Other/Misc/Temp` 掩盖跨域引用。
- 跑得快不存在“地区专属玩法规则”。规则必须拆成稳定、可组合的分类，由服务端权威配置按玩法编码动态选择并下发规则快照，客户端、服务端、提示、校验和结算统一消费同一分类结果。地区或玩法编码仅用于选择分类组合；禁止新建地区专属规则类、复制规则实现、在业务逻辑中硬编码地区分支，真实差异只能表达为规则分类及其配置值。
- 身份判断口径：玩家、陪玩和机器人身份及相关业务分流只能读取服务端权威字段 `user_type`（`0=普通玩家`、`1=陪玩`、`2=机器人`）；不得以 `ppk` 的任意位、显示 ID、`ppk` 长度、昵称或账号号段推断身份，也不得在玩法代码中伪造、改写或另建平行身份来源。
- 场景、Prefab、节点、名称、层级、动画和材质只以当前磁盘及 Cocos Creator 中仍然存在的内容为权威。用户已经删除的资源或节点视为明确废弃，禁止从 Git、废纸篓、备份、历史目录、Library、构建产物或旧版本恢复、复制、重建或增加兼容映射；本域代码和验收不得继续依赖废弃内容，仍有引用时必须删除对应引用、强制校验和控制逻辑。业务确需新增节点或 Prefab 时，只能报告精确名称、用途、目标父节点和所需组件，由用户在 Creator 中新增，本岗位不得代建；废弃动画、材质仅在用户后续明确提出时重新制作。

## 4. 执行与验收

- 文档协作暂停期间不向“Aoo文档”任务发送报告或自动触发整理，只维护本岗位“开发记录”和必要“记忆”；用户明确恢复后，需要更新正式文档的变更再按结构化报告流程处理。
- 先依据文档和只读规则表确认需求，再做最小、补丁式修改；规则必须按分类动态调取，不得按地区复制实现或硬编码分支。
- 用户提出的问题严格按提出顺序串行处理；收到新问题时不得中断当前任务，只将其精简登记到本岗位“记忆”的待处理队列，完成并测试、审计当前问题后自动处理下一项。只有用户明确使用“优先”“现在执行”或“现在处理”等插队指令时，才允许保存当前安全断点并优先处理指定问题；普通问题反馈或一般“处理”描述不构成中断指令。
- 只要待处理队列仍有已授权、可执行任务，就必须按顺序持续执行，不得完成一项后等待用户再次催促；当前项阻塞时记录原因与安全断点，直接联系明确责任岗位并继续其他独立任务，阻塞解除后自动恢复。只有全部闭环，或只能等待用户决定、授权且没有其他独立任务时，才可进入idle；普通阶段进度无需报告，但停止或idle必须额外报告aoo产品。未授权历史审计项不得自动转为任务。
- 尽量节省 Token，回答必须精简并直接回应用户问题，只保留必要结论、关键进度、阻塞和待决定事项；禁止无关解释、重复内容、过度展开及无变化汇报，同时须让用户明确当前进度。
- 代码须职责单一、命名一致、实现完整；只为业务边界、状态来源和非直观分支写必要注释。本次范围内清除重复逻辑、无效分支、临时补丁、调试输出、未使用变量和兼容垃圾，不得借机修改无关或冻结代码。
- 当前开发阶段必须为本任务关键入口、参数摘要、状态转换、异步结果、失败原因和必要生命周期补充可检索的结构化诊断日志，使用稳定模块前缀并关联已有 `roomId`、`playerId`、`operationId`、`stateVersion`；禁止输出密码、Token、密钥、完整隐私或大对象，禁止每帧、循环、心跳刷屏及重复、临时日志，验收时确认日志足以定位问题。
- 用户明确要求“写入规范”时，必须先读取已有规范并判断语义重复、包含和冲突关系，再去重、合并、精简并归入既定章节：相同含义只保留一条，新要求包含旧要求时合并为完整一条，发生冲突时以用户最新明确要求替换旧规则；禁止在文档末尾追加孤立规范。
- 处理用户明确问题后，必须自行完成针对性测试、真实 Cocos Canvas 场景验收和改动审计；资源修改还必须完成 Cocos Creator 3.8.8 同步。只有确认目标生效且未引入相关回归后才能报告“完成”；验证未完成时只能报告进行中或阻塞，不得虚报完成。
- 验收覆盖本次实际涉及的客户端、`Poker` 服务端、协议、数据库、配置、对局结算、部署和真实 E2E；规则变更还必须覆盖相同分类在不同玩法编码下的动态组合，证明没有地区硬编码或规则副本。
- 岗位 AI 自行执行网页、Creator Preview、Canvas、按钮、交互或 E2E 验收时，统一使用本任务专属的无头 Chrome 并通过 CDP 端口 `1235` 控制，不得打开可见浏览器窗口、抢占焦点、操作用户正在使用的 Chrome 或 Creator Preview、复用用户浏览器 Profile 或占用用户屏幕；每个并行 AI 必须使用独立临时 `user-data-dir`。确实必须占用屏幕时，须先停止并报告aoo产品申请用户授权。端口被其他任务占用时先直接协调；只有因此需要统一调度时才报告aoo产品。纯文档或无需浏览器的静态任务不强制启动浏览器。
- 本地业务 E2E 仅可使用账号`41`至`50`，密码统一为`1`，并记录本次实际账号。不得登录、重置、修改、占用或清理其他岗位账号及其会话、房间数据；账号被占用时改用本范围其他账号。`51`至`100`为预留账号，未经aoo产品明确分配不得使用。
- 任何新任务、转交任务或续接任务，先在“记忆”的待处理队列登记任务摘要、来源、状态、职责范围和安全断点；完成后移除，阻塞或停止前保留可恢复断点。
- Git 仓库固定为：客户端 `/Users/aoo/Code/Game/BCG/Aoo/Client` → `git@github.com:aoomi/aoo_Client.git`，服务端 `/Users/aoo/Code/Game/BCG/Aoo/Server` → `git@github.com:aoomi/aoo_Server.git`，管理后台 `/Users/aoo/Code/Game/BCG/Aoo/Admin` → `git@github.com:aoomi/aoo_Admin.git`；目标分支均为 `develop`。
- 本机 Sourcetree 位于 `/Applications/Sourcetree.app`，命令入口为 `/Applications/Sourcetree.app/Contents/Resources/stree`，系统 Git 为 `/usr/bin/git`。Git 状态检查、分支、拉取、提交和推送一律优先使用后台 `/usr/bin/git`；除非用户明确要求打开，否则禁止调用 `stree`、`open` 或启动、切换、占用 Sourcetree 图形界面及用户屏幕。
- 上述工具和仓库信息不构成 Git 操作授权。只有用户针对具体任务明确说“提交”后，本岗位才允许将该任务可明确归属的改动提交并推送到对应仓库 `develop`；功能闭环、测试通过或主协调线程判断完成均不构成提交授权。未收到明确提交指令时，仅北京时间 `00:00` 的每日固定快照任务可以统一提交。禁止夹带用户或其他 AI 的无关改动，禁止操作 `main`、强制推送、合并、变基或改写历史。提交前必须排除敏感信息和运行产物；每次结果必须报告仓库、`develop` 分支、提交号或精确阻塞原因。


- 处理 Bug 必须先复现问题并梳理完整业务链路、状态来源、调用顺序和数据流，确认唯一根因后直接修正错误逻辑；禁止以补丁形式绕过问题。
- “补丁形式”包括但不限于：只增加空值判断、`try/catch` 吞错、延时或重复重试、临时兼容分支、硬编码特例、重复兜底、旧逻辑外再包一层新逻辑，以及保留两套并行业务路径。确需防御性校验时，必须建立在根因修复完成之后，并说明其明确业务边界。
- 修复完成时必须删除被替代的错误逻辑、本次产生的临时措施和重复分支，并用针对性测试及真实业务流程证明根因已消除；无法确认根因时应按阻塞规范报告，不得提交猜测性修改。

## 5. 阻塞报告

- 跑得快业务域内问题由本岗位跨端持续处理。只有跨业务域共享文件冲突、权威规则冲突、需要用户授权、需要统一调度、职责归属不清，或持续阻塞且点对点无法解决时，才向aoo产品提交完整阻塞报告；不得把域内服务端、协议、数据库、配置、结算或部署工作当作越界阻塞。
- 任何任务停止、进入idle、异常结束或准备不再继续执行时，无论任务完成与否，都必须额外向aoo产品提交停止报告，完整列出当前任务、已完成项、未完成项、阻塞、待处理队列、安全断点和停止原因。aoo产品审核后，有已授权且可执行的未完成项必须续作；仅审核确认全部闭环，或只能等待用户决定、授权且无其他独立任务时，才允许保持停止。

## 6. 开发记录

每个新问题须立即在“记忆”的待处理队列登记问题、顺序、状态和相关路径，并在开始处理、完成或阻塞时同步状态；完成后从队列移除，并在本章按时间追加仅含日期、用户要求、根因/结论、修改范围、验证结果、未完成项的精简摘要，不得全文复制对话。不得删除或改写历史记录；纠错只能追加更正并注明日期和原因。

本章内容均为历史归档，不代表当前待办；当前任务状态只以第 7 章“待处理队列”为准。已经完成且用户未再次提出的问题不得重新处理。

### 2026-09-08｜入口初始化

- 用户要求：建立跑得快 AI 员工唯一入口、工作边界、记录与记忆规则。
- 根因/结论：原入口路径损坏且缺少持续记录结构，现已纠正并统一。
- 修改范围：仅本文档。
- 验证结果：入口、边界、调度、Creator 同步、Canvas 验证、开发记录和记忆要求均已写入。
- 未完成项：无。

### 2026-09-08｜固定文档结构

- 用户要求：永久固定七章顺序，并将结构规则同步到项目级规范。
- 根因/结论：原文档未按七章分区，现已重排；“记忆”固定在最下面。
- 修改范围：本文档与项目根目录 `AGENTS.md`。
- 验证结果：章节名称、顺序和内容归属已核对。
- 未完成项：无。

### 2026-09-08｜规范写入去重

- 用户要求：规范写入前必须检查重复、包含和冲突，并归入固定章节。
- 根因/结论：现有规则只限定结构，现已补齐语义合并与最新要求优先规则。
- 修改范围：本文档与项目根目录 `AGENTS.md`。
- 验证结果：规则已归入既定章节，未新增平级章节或文末孤立条款。
- 未完成项：无。

### 2026-09-08｜验收与冻结边界

- 用户要求：完成前必须测试、真实验收和审计；已完成问题及相关代码必须冻结。
- 根因/结论：原验收与未授权修改规则覆盖不足，现已合并为完整条件。
- 修改范围：本文档与项目根目录 `AGENTS.md`。
- 验证结果：测试审计已归入第 4 章，冻结边界已归入第 3 章，并同步项目级规范。
- 未完成项：无。

### 2026-09-08｜代码质量边界

- 用户要求：代码应清晰完整、准确注释并清除本次改动范围内的冗余，且不得借机修改冻结代码。
- 根因/结论：现有规范缺少代码整洁与注释标准，现已补齐并与冻结边界合并。
- 修改范围：本文档与项目根目录 `AGENTS.md`。
- 验证结果：质量要求归入第 4 章，冻结限制归入第 3 章，未新增平级章节。
- 未完成项：无。

### 2026-09-08｜文件名统一

- 用户要求：将旧无前缀入口迁移为标准文件名，并创建框架、亲友圈和大厅入口。
- 根因/结论：旧入口文件名不符合统一命名，现已保留全部独有内容并迁移。
- 修改范围：仅 `Client/docs/AI员工/` 下四份规范文档。
- 验证结果：四份标准命名文件已落盘，旧无前缀文件已移除。
- 未完成项：无。

### 2026-09-08｜文件名规则更正

- 用户要求：取消全部 `AI_` 前缀，四份岗位入口统一使用无前缀文件名。
- 根因/结论：用户最新明确命名要求替换此前规则，历史记录保留并追加更正。
- 修改范围：仅 `Client/docs/AI员工/` 下四份规范文档的文件名与自引用。
- 验证结果：四份入口均已改为无前缀名称，未保留 `AI_` 副本。
- 未完成项：无。

### 2026-09-08｜命名规范引用与整改边界

- 用户要求：加入统一命名规范必读引用，并限制历史不合规整改范围。
- 根因/结论：原入口缺少该引用与专项报告要求，现已合并进既定章节。
- 修改范围：仅本文档。
- 验证结果：引用归入第 2 章，整改边界归入第 3 章。
- 未完成项：无。

### 2026-09-08｜稽查协调规则

- 用户要求：统一跨岗位冲突与多 AI 配合事项的报告和调度入口。
- 根因/结论：原岗位自行分派规则与最新协调边界冲突，现已移除，并将阻塞规则去重合并为更新记录和安全断点后向 aoo稽查报告，由其统一调度和验收。
- 修改范围：仅本文档“执行与验收”“阻塞报告”和“记忆”。
- 验证结果：协调规则仅保留一条，岗位 AI 自行协调和越权修改均已禁止。
- 未完成项：无。

### 2026-09-08｜问题串行、状态记录与进度规则

- 用户要求：问题按顺序串行处理，明确插队指令才可中断，并持续记录状态、精简必要进度。
- 根因/结论：原规范缺少新问题排队、插队判定、状态同步及 Token 节制要求，现已归入既定章节。
- 修改范围：仅本文档“执行与验收”“开发记录”和“记忆”。
- 验证结果：规则已去重合并，七章顺序不变，“记忆”仍位于末尾。
- 未完成项：无。

### 2026-09-08｜越界问题报告与无头验收环境

- 用户要求：疑似误发或部分越界问题须暂停越界处理并交由 aoo稽查核对；岗位自行执行的浏览器验收统一使用任务专属无头 Chrome。
- 根因/结论：现有跨岗位阻塞规则未完整覆盖禁止执行、测试和自行转交，入口也缺少无头验收的隔离要求；现已去重归入既定章节并保持问题严格串行。
- 修改范围：仅本文档“修改边界”“执行与验收”“阻塞报告”和本开发记录。
- 验证结果：越界判断、部分越界依赖处理、稽查报告字段、CDP `1234`、独立临时 Profile、端口冲突协调、可见窗口授权例外及静态任务例外均已核对。
- 未完成项：无。

### 2026-09-08｜跑得快前端本地提示整改（真实验收阻塞）

- 用户要求：提示候选由前端生成、排序与自动滑起，后端仅在实际出牌或过牌时最终校验；同点数 9 不得提示，`4567789` 接 3 时优先提示 7 以保留顺子。
- 根因/结论：客户端手动与自动提示曾调用服务端提示接口，职责混杂；现已统一改为前端本地候选和净手牌排序，并增加单牌必须严格大于桌面单牌的过滤。
- 修改范围：`CommonPdkPlayController.ts`、`PdkCleanHintRanker.ts` 及两项针对性单元测试；服务端规则代码未修改。
- 验证结果：客户端 TypeScript 检查通过，12 项针对性测试全部通过；服务端源码确认 `play` 通过 `canBeat` 拒绝不能压过上一手的组合。
- 未完成项：Creator Preview 真实 Canvas 房间验收被大厅 `form=lobby/UINoticeBar shown=true` 加载异常阻断；Maven 命令不可用，未运行服务端测试套件。失败证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-nine-hint-2026-09-08T13-26-18-675Z`。

### 2026-09-08｜房间信息水平居中

- 用户要求：仅通过 Cocos Creator 3.8.8 将 `PDK_CommonRoom.prefab` 的 `BG/Room_Info` Widget 设置为相对根节点水平居中，保留垂直配置、Y 位置和 Layout。
- 根因/结论：原 Widget 水平对齐为 `NONE`；已设置 `Target=PDK_CommonRoom`、`Horizontal Alignment=Center`、`Horizontal Center=0`。
- 修改范围：仅 `Client/assets/Games/Poker/PDK/Common/Prefab/PDK_CommonRoom.prefab` 的 `BG/Room_Info` Widget，以及本文档开发记录。
- 验证结果：已在 Creator 保存、关闭并重新打开预制体；Inspector 显示 Center `0 px`、Target `PDK_CommonRoom`，节点 Y 保持 `313.612`，垂直配置与 `cc.Layout` 保持原样，控制台未新增错误。
- 未完成项：无。

### 2026-09-08｜房间信息视觉居中更正

- 用户要求：根据 Creator 截图继续修正三个房间信息文字仅节点框居中、实际文字组偏左的问题。
- 根因/结论：`Room_Info` 原宽度 `709` 且 Layout 的 Resize Mode 为 `NONE`，文字只占容器左侧；已将 Resize Mode 改为 `CONTAINER`，使容器宽度随三个子文字和既有间距自动收缩，再由 Widget 居中。
- 修改范围：仅 `Client/assets/Games/Poker/PDK/Common/Prefab/PDK_CommonRoom.prefab` 的 `BG/Room_Info` Layout Resize Mode，以及本文档开发记录。
- 验证结果：容器宽度自动收缩为约 `415.819155`，Widget 保持 Target `PDK_CommonRoom`、Center `0 px`，Y 保持 `313.612`，Spacing X 保持 `30`、左右 Padding 保持 `0`；已保存、关闭并重新打开确认三个文字整体视觉居中，控制台 Info/Warning/Error 均为 `0`。
- 未完成项：无。

### 2026-09-08｜跑得快前端本地提示整改续验

- 用户要求：继续后续任务形成闭环。
- 根因/结论：已修复大厅非模态通知栏主动隐藏后仍被表单管理器按 `activeInHierarchy` 判为未就绪的问题；真实桌面与移动端 Canvas 已成功登录、创建/加入房间、准备并发牌。首轮完整出牌验收因旧脚本以牌节点 Y 坐标识别选中而误判并耗尽循环；修正探针后重跑又遇 Preview 开房规则页启动时序超时，未取得完整对局结算证据。
- 修改范围：提示逻辑、提示排序工具、表单就绪判断、针对性测试及本文档；临时 E2E 包装脚本已删除，通用 E2E 脚本已恢复。
- 验证结果：提示专项 12 项测试全部通过，Client TypeScript 检查通过；真实入房证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-local-hint-2026-09-08T14-05-39-080Z`，重跑失败证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-local-hint-2026-09-08T14-16-46-448Z`。
- 未完成项：真实 Canvas 完整出牌至结算仍未通过；Maven 不可用，未运行服务端测试套件。当前只能报告阻塞，不能报告功能已完成。

### 2026-09-08｜跑得快前端本地提示整改最终闭环更正

- 更正原因：续验记录中的完整对局阻塞已由后续自处理解除。
- 根因/结论：验收脚本使用了不存在的 `Btn_Out_Card`，实际预制体节点为 `Btn_OutCard`，并且旧脚本以节点 Y 坐标判断选中状态；修正为真实节点名和控制器选牌状态后，桌面与移动端真实 Canvas 连续两局完成出牌、结算、继续和解散。提示仍完全在前端本地生成，后端只接收实际出牌/过牌并最终校验。
- 修改范围：通用 PDK E2E 脚本节点名与选中状态探针，以及本文档；未修改规则表和服务端业务代码。
- 验证结果：房间 `682301` 两局状态均为 `FINISHED`，状态版本从 `15` 递增至 `29`；桌面真实点击提示/出牌各 6 次，移动端各 8 次，房间已清理；提示专项 12 项测试和 Client TypeScript 检查全部通过。完整证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-local-hint-2026-09-08T14-34-58-242Z`。
- 未完成项：无。本机缺少 Maven 属于服务端测试环境现状，本次未修改服务端；服务端最终出牌校验已通过源码审计确认。

### 2026-09-08｜Btn_Back 退出房间规则

- 用户要求：房间节点改为 `Btn_Back`；游戏未开始可直接退出，游戏中点击显示气泡“游戏中不能退出”。
- 根因/结论：运行时代码仍绑定已删除的 `Btn_Jiesan`，导致节点契约报错；现已改绑 `Btn/Room/Btn_Menu/Btn_Back`，等待阶段走统一离房流程，权威阶段为 `PLAYING`、`COMPETE_DEALER` 或旧快照明确处于游戏中时拦截退出并显示指定气泡。
- 修改范围：`CommonPdkPlayController.ts`、专项单元测试和真实 E2E 节点探针；未修改用户维护的预制体和规则表。
- 验证结果：专项及关联测试 17 项通过，Client TypeScript 检查通过；真实 Canvas 验证未开局点击返回大厅成功，游戏中点击后仍保留在原房间；测试恢复态和测试房间已清理。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-back-button-2026-09-08T14-49-17-835Z`。
- 未完成项：无。

### 2026-09-08｜房间按钮三组分层与取消准备删除

- 用户要求：将公共房间按钮按 `RoomActions`、`WaitingActions`、`TurnActions` 三组整理，三个组容器均置于中心原点，并删除 `Btn_Cancel` 及对应代码。
- 根因/结论：原 `Room`、`Game` 与本地座位下的 `Btn_Game` 混合了承载位置和流程职责，且仍保留取消准备入口；现已统一为 `Btn` 下三个同级职责组，运行时节点路径同步更新，取消准备节点、绑定、显隐和 PDK 生命周期请求入口均已移除。
- 修改范围：`PDK_CommonRoom.prefab`、`CommonPdkPlayController.ts`、`OperationPresenter.ts`、`RoomLifecycleController.ts` 及关联契约测试；未修改规则表和服务端代码。
- 验证结果：预制体 2441 个序列化对象引用与父子回指检查通过；三组父节点均为 `Btn` 且位置为 `(0,0)`；Creator 3.8.8 已保存、关闭并重新打开，层级中不存在 `Btn_Cancel`，脚本热编译缓存已更新；7 项本次关联测试通过，桌面无头 Chrome 加载当前 Preview 成功，Canvas 为 `1280×673` 且页面无控制台错误。
- 未完成项：无。

### 2026-09-09｜等待阶段新增微信分享按钮

- 用户要求：在 `Btn_WechatInvite` 同级新增 `Btn_Share`；网页点击复制房间信息并气泡提示“房间复制成功”，App 点击直接调用微信分享。
- 根因/结论：已在 `WaitingActions` 下新增微信图标按钮并接入公共 PDK 控制器；网页使用平台剪贴板写入“跑得快 房间号：房间号”，成功后由牌桌独立气泡展示指定文案；原生环境调用 `wechat` 分享桥接，不把前端提示结果交给服务端。
- 修改范围：`PDK_CommonRoom.prefab`、`CommonPdkPlayController.ts`、新增 `RoomBubblePresenter.ts` 及关联契约测试；未修改规则表和服务端代码。
- 验证结果：Creator 3.8.8 已导入新脚本、生成 `.meta` 并刷新预览；3 项本次关联测试通过。桌面无头 Chrome 真实 Canvas 创建房间 `427849`，点击 `Btn/WaitingActions/Btn_Share` 后剪贴板为“跑得快 房间号：427849”，画面显示“房间复制成功”，随后成功退出并清理房间。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-direct-share-retry-2026-09-08T16-07-25-249Z`。
- 未完成项：无；App 分支已通过代码路径和原生桥接契约检查，当前验收环境未启动原生微信客户端实机流程。

### 2026-09-09｜领出时手动提示不生效修复

- 用户要求：本人获得新一轮领出权时，点击“提示”必须按提示规范滑起候选并支持连续切换。
- 根因/结论：房间 `700522` 的服务端当前牌桌已清空且 `lastActions` 为空，但客户端兼容牌型缓存仍短暂保留上一手牌型，导致领出候选按旧接牌条件校验为空；现已由当前权威回合的 `trickReset`、`isFirstOp`、空 `lastActions`、空桌面牌共同判定领出，并在生成候选前清除旧接牌缓存。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-authoritative-auto-pass.test.mjs` 及本文档；未修改服务端、规则表、预制体或其他已完成按钮功能。
- 验证结果：15 项提示、单牌物理槽位和触摸区关联测试全部通过；Creator 3.8.8 已刷新预览。Safari 真实 Canvas 在房间 `700522` 点击“提示”，第一次滑起一对 8，第二次收回 8 并滑起一对 K，未自动提交出牌，符合领出提示循环规范。
- 未完成项：无。

### 2026-09-09｜重叠手牌触摸命中修复

- 用户要求：修复房间 `700522` 中点击 7 却选中 5 的触摸错位。
- 根因/结论：重叠手牌原先允许被遮住的整张牌参与命中，后排牌会抢占前方牌实际露出区域；现按牌的世界坐标从左到右确定显示顺序，每张重叠牌只拥有自身实际露出的横向区域，最右一张保留完整触摸宽度。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改服务端、规则表、预制体或其他已完成功能。
- 验证结果：15 项提示、单牌物理槽位和触摸区关联测试全部通过；Creator 3.8.8 已刷新预览且无新增编译错误。Chrome 真实 Canvas 在房间 `700522` 的 Player384 牌面分别点击红 7 和黑 5，均只滑起所点击的对应牌。
- 未完成项：无。

### 2026-09-09｜提示按钮触摸锁恢复与全流程审计

- 用户要求：修复提示按钮偶发无法点击，并依照《跑得快流程》从入桌到结算逐条审计。
- 根因/结论：提示生成和候选切换逻辑正常；失效来自浏览器在 Canvas 失焦或尺寸变化时可能遗漏上一手势的 `pointerup/pointercancel`，残留的捕获指针会丢弃之后的提示点击。现于每次新的主指针按下时主动释放失效捕获并清理旧拖牌状态。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改服务端、规则表、预制体或其他已完成功能。
- 验证结果：本次提示、轮转、触摸、退出与桌面/手机一致性共 21 项关联测试全部通过；Creator 3.8.8 已刷新且无新增编译错误。房间 `700522` 的两个真实浏览器 Canvas 均成功点击“提示”，Player385 接 5 自动/手动滑起 6，Player384 接 6 自动/手动滑起 7，提示未直接提交出牌。
- 审计备注：《跑得快流程》第 1 节仍写有“已准备显示取消准备”，与用户已明确删除 `Btn_Cancel` 的现行实现冲突；其余第 1 至第 9 节已有对应实现或针对性契约覆盖。动画目录、旧架构文案与终局恢复的 9 项存量契约测试仍使用已迁移前的路径或旧字符串断言，不作为本次提示修复结果，已在报告中单列。
- 未完成项：全新房间从等待态到最终结算的自动化实局证据尚未重新生成；当前完成的是代码逐条审计、关联契约验证及房间 `700522` 的真实中局提示验证。

### 2026-09-09｜领出提示最大张数组合复验

- 用户要求：本人领出时点击“提示”，必须优先提示当前手牌中合法且出牌张数最多的牌型。
- 根因/结论：实现已按《跑得快流程》第 7.1 节使用领出候选的最大张数优先排序；此前提示按钮的失效指针问题修复后，该排序已能从真实按钮点击正常触发。
- 修改范围：新增 `pdk-single-hint-selection.test.mjs` 的当前牌面回归用例，并记录本次实局证据；未修改服务端、规则表或预制体。
- 验证结果：房间 `700522` 第 2 局 Player384 手牌 `AA、JJ、1010、88、77、6`，领出时第一次真实点击“提示”滑起 `88+77` 四张连对，没有提示对子或单张；16 项提示及触摸关联测试全部通过。
- 未完成项：无。

### 2026-09-09｜居中 UI 操作按钮触摸坐标闭环

- 用户要求：继续解决领出时点击“提示”无反应，并按流程规范完成真实对局闭环。
- 根因/结论：页面背景按 `1600×720` 渲染，玩法 UI 使用居中的 `1280×720` 坐标系；DOM 指针桥此前未扣除两侧延展区，导致提示和出牌按钮的实际触摸区相对图案横向偏移。现统一根据 Creator 的可见宽度与设计分辨率计算居中横向内缩，DOM 与 Cocos 触摸坐标双向使用同一换算。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改服务端、规则表或预制体。
- 验证结果：房间 `700522` 第 2 局在 Chrome 真实 Canvas 直接点击按钮视觉中心后，依次滑起 `88+77`、下一轮滑起 `JJ+1010`、再下一轮滑起 `AA`；出牌按钮视觉中心均可提交。Player385 无牌接连对时自动过，Player384 剩余单张 `6` 自动打完，双端进入第 2 局结算；双方点击“继续”后正常发牌并进入第 3 局。16 项提示与触摸关联测试全部通过，Creator 3.8.8 已刷新且无新增编译错误。
- 未完成项：无。

### 2026-09-09｜接单张提示完整枚举与循环

- 用户要求：上家出单张 10，本人同时有 Q、K 时，重复点击“提示”必须在 Q、K 之间轮番提示。
- 根因/结论：旧规则引擎的单张接牌提示源可能只返回一项，无法保证暴露手牌中每个可接点数；现由前端直接从权威手牌枚举所有严格大于桌面单张的牌，再经统一合法性校验、同点数去重和少拆牌排序，提示循环固定为较小可接牌到较大可接牌并回到第一项。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`、`pdk-authoritative-auto-pass.test.mjs` 及本文档；未修改服务端、规则表或预制体。
- 验证结果：房间 `700522` 第 3 局桌面单张 10、Player385 同时持有 Q 和 K；提示选择 Q 后通过真实“出牌”按钮成功提交。新增 `Q → K → Q` 循环和物理槽位回归，18 项提示、自动提示及触摸测试全部通过。
- 更正：上一条“居中 UI 操作按钮触摸坐标闭环”中以统一横向内缩处理按钮的方案，经 Safari/Chrome 交叉复验发现不适用于两种 Canvas 适配结果，现已撤销；操作按钮继续使用既有坐标转换，单张候选缺失按本条方案修复。
- 未完成项：无。

### 2026-09-09｜公共聊天、语音与设置预制体接入

- 用户要求：`PDK_CommonRoom` 的聊天、语音按钮调用公共预制体，清理错误命名与节点层级；随后接入 `Set.prefab`，并把其中解散房间入口接到统一解散流程。
- 根因/结论：`cha t.prefab` 只是无内容的全屏透明按钮，实际聊天界面是 `ChatExpressionPanel.prefab`；现已删除空壳并将真实资源统一为 `Common/ChatPanel.prefab`，`Audio.prefab` 迁移为 `Common/VoiceRecordingPanel.prefab`，节点、动画轨道、公共资源注册表和 PDK 控制器路径同步更新。`Btn_Set` 已改为打开 `room/SettingsPanel`，`Set.prefab` 内关闭、音乐/音效开关与音量、语音开关及 `btn_jiesan` 均已绑定；解散入口复用唯一的 `room/DissolveRoom` 权威流程。
- 修改范围：公共聊天/语音预制体及动画、公共预制体注册和原生路径表、PDK 聊天/语音/设置控制器、房间协调器、专项测试及本文档；未修改开房规则表和服务端代码。
- 验证结果：Client TypeScript 检查通过，Creator 原生资源门禁通过（1367 个场景/预制体、28594 个资源及 meta、38601 个 UUID，阻断项 0），7 项专项测试通过；Creator 3.8.8 已重新导入并关闭后重新打开聊天、语音预制体，层级显示新命名。无头 Chrome 真实 Canvas 已确认聊天按钮挂载 `ChatPanel`，语音按钮触发并挂载 `VoiceRecordingPanel` 后按当前平台无录音能力路径关闭。
- 未完成项：Creator 当前 Preview 在最后一次设置代码变更后仍运行上一份脚本缓存，真实点击 `Btn_Set` 尚未观察到 `Set` 实例挂载；已执行刷新预览、运行预览和编辑器重新加载，磁盘代码、TypeScript 与资源门禁均正常。按验收规范，本项保持阻塞状态，不能标记完整闭环。

### 2026-09-09｜手牌滑动区按适配后屏幕宽度扩展

- 用户要求：自己的手牌滑动触摸区必须覆盖适配后的屏幕宽度，而不是只能从可见牌面开始触摸。
- 根因/结论：`Hand_Cards` 的运行时宽度原本已经跟随 `view.getVisibleSize().width`，但自定义命中仍以 `cardIndexAtUi >= 0` 限制手势起点，导致牌外横向空白无法起滑。现改为全宽手牌带负责接收滑动，只有手势进入实际露出牌面时才累计牌索引；允许从空白起滑、在空白结束，并避让同一触摸带内正常可交互按钮。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改预制体、规则表或服务端。
- 验证结果：专项及关联单元测试 11 项通过，Client TypeScript 检查通过；Creator 3.8.8 已刷新预览且未新增编译错误。无头 Chrome 当前适配可见宽度为 `1559.3909`，手牌宽度同步公式和真实 Canvas 坐标采样均已生效。
- 未完成项：无头 Chrome 新建双人房间 `202103` 后，双方真实点击“准备”未进入开局，当前无法生成“从牌外空白起滑并跨牌选中”的实局截图证据；因此本项代码整改已完成，但真实对局触摸闭环仍处于环境阻塞，不能标记为完整 E2E 完成。

### 2026-09-09｜精简回答规范

- 用户要求：回答精简，不回复无用内容。
- 根因/结论：已与现有 Token 和进度规则去重合并，仅保留问题结论及必要信息。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：未新增章节或重复条款。
- 未完成项：无。

### 2026-09-09｜Creator 7458 Preview 环境识别修复

- 用户要求：处理 Creator Preview 启动时报“生产环境未配置唯一 HTTPS 服务入口 apiBaseUrl”。
- 根因/结论：Creator 当前预览端口为 `7458`，但预览模板与运行时网关兜底的测试端口集合都漏掉了该端口，页面因此误走生产门禁。现已在两处统一加入 `7458` 并更新预览缓存版本；生产 HTTPS/WSS 校验逻辑未放宽。
- 修改范围：`preview-template/index.ejs`、`GatewayEntryPolicy.ts`、对应启动模板测试及本文档；未修改生产地址、服务端或规则表。
- 验证结果：Client TypeScript 检查通过；Creator 3.8.8 已刷新预览。无头 Chrome 直接打开 `http://127.0.0.1:7458/` 后，运行配置为 `environment=test`、`apiBaseUrl=http://127.0.0.1:8080/`，缓存版本为 `aoo-preview-20260909-runtime-port-5`，原生产 `apiBaseUrl` 报错不再出现。
- 未完成项：当前启动服务随后返回“暂时无法连接启动服务”，属于网关启动目录响应状态，与本次 Preview 环境误判不同；本次端口识别问题已闭环。

### 2026-09-09｜预制体排牌 Layout 与全宽触摸区冲突修复

- 用户反馈：调整 `PDK_CommonRoom.prefab` 后，左侧点击选牌位置错误，且无法横向滑动选牌。
- 根因/结论：`Hand_Cards` 的 `Layout.resizeMode` 为 `CONTAINER`，它会在排牌后按子节点重新改写容器宽度；同一节点又被运行时代码扩展为适配后屏幕宽度，两套尺寸写入互相覆盖，导致牌面坐标漂移并把全宽滑动区缩回。现改为先启用 Layout 完成一次排牌和基准坐标同步，再冻结 Layout，最后扩展触摸宽度；尺寸同步同时检查节点实际宽度，避免缓存命中后漏掉被外部改回的宽度。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；保留用户当前预制体参数，未修改预制体、规则表、服务端或由其他 AI 负责的 Preview 启动配置。
- 验证结果：提示、物理牌槽、触摸及权威回合关联测试 18 项全部通过，Client TypeScript 检查通过。
- 未完成项：当前 Creator/Preview 正由其他 AI 处理独立启动任务，本次未覆盖其运行现场；需在其刷新完成后的真实对局中补充左侧点击和空白起滑实测证据。

### 2026-09-09｜点击选牌与全宽滑动选牌真实闭环

- 用户要求：对照两天前正常行为，修复当前点击选牌错位/无效和滑动选牌无法使用，并且必须打开网页实际操作验证后再报告。
- 根因/结论：Web 端同一次物理触摸同时进入 DOM PointerEvent 与 Cocos EventTouch，两条状态机会对同一张牌连续切换两次并竞争滑动状态；同时 `Hand_Cards` 的 `Layout.resizeMode=CONTAINER` 会在排牌后覆盖运行时全宽触摸尺寸。现统一为 Web 端只使用 DOM 指针桥、原生端只使用 Cocos EventTouch；排牌时先让 Layout 结算并冻结，再扩展为适配后的屏幕宽度。所有实际可交互按钮也复用同一 Web 指针桥，保证准备、提示、出牌与手牌不会形成不同输入链路。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改用户调整后的预制体、规则表、服务端或其他 AI 负责的 Preview 启动任务。
- 验证结果：Creator 3.8.8 已刷新并生成新 Preview 脚本；无头 Chrome CDP `1234` 通过真实 Canvas 触摸创建双人房间 `962736`、第二会话加入并由双方真实点击准备完成发牌。点击最左侧 `Poker_Card_113` 后只有该牌由 Y=0 升至 Y=20，再次点击回到 Y=0；从屏幕最左空白区 x=8 起滑，依次跨过索引 4、5、6、7 并在右侧空白结束，手势提交最终选中索引 6、7，对应牌值 `409、309`。运行时 `Hand_Cards` 的 Layout 为禁用状态，触摸区世界宽度 `1559.3909` 与当前适配后可见宽度一致。专项触摸测试通过，真实截图位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-hand-touch-2026-09-09/desktop-click-swipe.png`。
- 未完成项：无。全量 PDK 存量测试中另有 9 项因其他任务正在迁移动画、背景、聊天和语音资源而失败，与本次触摸代码无关；本次未越界修改。

### 2026-09-09｜宽屏点击坐标二次更正

- 用户复验反馈：超宽 Preview 窗口中点击梅花 10，实际抬起红桃 5；上一轮只覆盖单一画布几何，验收范围不足。
- 根因/结论：Creator Preview 的设备外壳会额外施加 CSS 变换，使用 `canvas.width / getBoundingClientRect().width` 反推输入像素比例会在宽屏外壳下产生二次缩放，导致可见牌面与命中牌槽横向错位。现统一以 Cocos 屏幕设备像素比转换 DOM Pointer 坐标，保持与引擎 `view` 的 viewport/scale 坐标口径一致。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改预制体、规则表、服务端和其他 AI 负责的启动配置。
- 验证结果：Creator 3.8.8 已刷新，Preview 编译产物已包含新的设备像素比转换。无头 Chrome CDP `1234` 在 `1856×880` 浏览器窗口、`1280×720` 实际 Canvas、房间 `372143` 中通过真实触摸逐张点击双方共 32 个牌位，32/32 均仅抬起目标牌；其中玩家 1 的 `Poker_Card_210`（梅花 10，索引 8）和 `Poker_Card_305`（红桃 5，索引 15）均命中自身，最左与最右牌位也通过。另从适配后 Canvas 左边缘空白区起滑并跨过前四张牌，手势成功提交其中最大的合法对子。专项回归 18/18 通过；证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-wide-hand-hit-2026-09-09/`。
- 未完成项：无。

### 2026-09-09｜手牌显示与触摸节点彻底拆分

- 用户复验反馈：坐标换算更正后其当前 Preview 仍出现“点击梅花 10 抬起红桃 5”，要求放弃兼容式修补，改为独立触摸区节点。
- 根因/结论：预制体中的 `Hand_Cards` 同时承担 `Layout` 排牌和全宽触摸，且存在 `0.8` 缩放、`CONTAINER` 宽度重算与 `-90` 负间距；显示容器和输入容器职责耦合，任何预制体或 Layout 刷新都可能再次改变命中基准。现已在 `Card` 下新增同级且置底的 `Hand_TouchArea`，专门接收点击和滑动；`Hand_Cards` 只负责显示和排牌，运行时不再扩展其宽度或绑定触摸。
- 修改范围：`PDK_CommonRoom.prefab`、`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改规则表和服务端。
- Creator 同步：Creator 3.8.8 已自动重新导入预制体，`library/10/10b5a51f-0468-4250-9bee-626d942f2a1b.json` 可见 `Hand_TouchArea`；Preview 编译脚本同步包含新节点路径。运行时确认触摸节点为 `1280×186.4`，视觉 `Hand_Cards` 独立保持 `1354×233`（再按自身 `0.8` 缩放显示）。
- 验证结果：无头 Chrome CDP `1234` 在 `1856×880` 窗口、房间 `759589` 中由两个独立会话通过真实 Canvas 触摸完成双人准备和发牌。双方共 32 个牌位逐张点击为 32/32 正确；玩家 1 的梅花 10 `Poker_Card_210` 和玩家 2 的红桃 5 `Poker_Card_305` 均只抬起自身；从 Canvas 左侧空白区域起滑也成功提交合法牌组。专项回归 18/18 通过，证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-independent-touch-area-2026-09-09/`。
- 未完成项：无。

### 2026-09-09｜Safari 高 DPI 触点漂移修复

- 用户复验反馈：独立 `Hand_TouchArea` 生效后仍发生触点整体漂移；前两轮无头验收实际只覆盖 DPR=1，未复现 Safari/Retina 环境。
- 根因/结论：DOM PointerEvent 与 Canvas `getBoundingClientRect()` 使用 CSS 像素，但旧换算额外乘入 Cocos 设备像素比；当 Safari/Creator Preview 的页面 DPR、Canvas backing size 和外壳缩放组合变化时，触点会被重复放大。现改为按触点在 Canvas 可视矩形中的归一化比例映射到 `view.getVisibleOrigin()/getVisibleSize()`，彻底移除 DPR、backing size、viewport 像素和预制体缩放对 DOM 触点的影响。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；保留独立触摸节点方案，未修改规则表和服务端。
- 验证结果：Creator 3.8.8 已热编译新换算。无头 Chrome CDP `1234` 强制 DPR=2，实际 Canvas CSS 尺寸分别为 `1280×720`、`1280×688.82`，backing size 分别为 `2560×1440`、`2560×1378`；双人房间 `451718` 中双方共 32 个牌位逐张真实触摸为 32/32 正确，梅花 10 `Poker_Card_210` 与红桃 5 `Poker_Card_305` 均仅抬起自身，左侧空白起滑也通过。专项回归 18/18 通过；证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-normalized-pointer-2026-09-09/`。
- 未完成项：无。

### 2026-09-09｜取消手牌 DOM 坐标桥并改用 Cocos 原生输入

- 用户复验反馈：点击可见 K 仍抬起 10，并指出此前自动验收没有覆盖肉眼可见像素点。
- 验收纠错：此前脚本从 Cocos 节点坐标反算点击点，再用同一坐标体系判断结果，属于循环验证；在 `1190×700`、DPR=2 截图中，反算位置与牌面真实像素位置存在明显差异，历史“32/32”不能证明用户的可见点位正确。
- 根因/结论：手牌仍由 DOM Pointer 桥自行换算坐标，而桌面鼠标、移动触摸和 Cocos UI 各自存在不同坐标入口。现彻底禁止 DOM 桥处理手牌；透明 `Hand_TouchArea` 调整为 `Card` 最上层同级节点，直接绑定 Cocos `MOUSE_*` 与 `TOUCH_*`，统一使用引擎 `EventMouse/EventTouch.getUILocation()`。DOM 桥只保留普通按钮。针对浏览器可能同时合成 mouse/touch 的情况，以已激活手势拒绝第二次 start，避免重复切换。
- 修改范围：`PDK_CommonRoom.prefab`、`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改规则表和服务端。
- 验证结果：Creator 3.8.8 已重新导入预制体并热编译脚本，TypeScript `--noEmit` 通过，专项回归 18/18 通过。无头 Chrome CDP `1234` 使用 `1190×700`、DPR=2、房间 `579585`，不读取节点反算点位，直接按截图可见 CSS 像素 `(108,490)` 发出原始鼠标点击；最终仅抬起梅花 K `Poker_Card_213`，未抬起任何 10。另从原始像素 `(50,490)` 横向拖至 `(260,490)`，成功连续覆盖并抬起前五张牌。最终截图位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-cocos-native-touch-2026-09-09/`。
- 未完成项：无。

### 2026-09-09｜语音预制体移动后的启动路径修复

- 用户反馈：点击选牌后页面进入“加载失败”，首个明确错误为“缺少 Creator 3.8.8 原生 Prefab: room/VoiceRecordingPanel”。
- 根因/结论：Creator 于当日 11:03 将 `VoiceRecordingPanel.prefab` 从 `Games/Common/Prefab/Common` 移到 `Games/Common/Prefab` 并保留原 UUID，但公共预制体注册表和原生路径表仍引用旧目录；该错误发生在房间加载门禁，并非选牌处理函数抛出。现将两处地址统一为当前真实资源位置，逻辑表单键 `room/VoiceRecordingPanel` 保持不变。
- 修改范围：`CommonPrefabRegistry.ts`、`prefab-path-map.json`、对应专项测试及本文档；未移动或改写语音预制体，未修改选牌逻辑、规则表和服务端。
- 验证结果：Creator 3.8.8 已执行全资源数据库刷新，Preview 编译产物确认解析到 `asset: 'VoiceRecordingPanel'`；TypeScript `--noEmit` 通过，路径专项测试通过。独立临时无头 Chrome 真实加载 `http://127.0.0.1:7456/` 后存在 `GameCanvas`，控制台未再出现 `VoiceRecordingPanel`、`缺少 Creator` 或运行时异常；因规范端口 `1234` 正被另一任务专属 Chrome 占用，本次隔离验收使用 `1235`，未复用或中断其他任务会话，测试 Profile 已清理。
- 未完成项：无。完整社交预制体测试中的 `ChatPanel/MessagePanel` 节点断言仍受另一项正在进行的聊天层级调整影响，本次未越界修改。

### 2026-09-09｜选牌命中改为 Creator 原生屏幕空间判定

- 用户复验反馈：语音预制体加载错误修复后，点击位置仍与此前一样漂移。
- 根因/结论：上一版虽然改用 Cocos 输入事件，但仍把 `getUILocation()` 与牌节点世界 AABB 手工比较；Creator Preview 设备外壳、Safari Retina DPR、Canvas 缩放及手牌节点 `0.8` 缩放叠加时，两类坐标没有可靠的同源保证。现保留独立 `Hand_TouchArea`，牌位索引改为使用 Creator 3.8.8 原生 `UITransform.hitTest(原始屏幕触点, windowId)`，由引擎相机完成屏幕到节点的转换，并按视觉层级从上到下确定重叠牌归属。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-hand-touch-area.test.mjs` 及本文档；未修改预制体、规则表和服务端。
- 验证结果：Creator 3.8.8 已热编译，Preview 产物包含原生屏幕命中实现；TypeScript `--noEmit` 与触摸专项测试 2/2 通过。已在用户当前 Safari、`localhost:7456`、房间 `700522` 的真实 Canvas 上按肉眼可见像素操作：点击梅花 K 仅抬起梅花 K，复位后点击梅花 10 仅抬起梅花 10，从左侧 A 横向滑过多张牌后手势正常提交并抬起合法 A；测试选中状态随后已复位。
- 未完成项：无。

### 2026-09-09｜出牌操作权双端实时交接

- 用户要求：一方出牌后，无需刷新即可隐藏本方“提示/出牌”并在对方轮次显示对应按钮。
- 根因/结论：成功请求后的旧回合刷新会重新打开本方按钮，且同版本不同回合内容可能被去重；现由权威状态指纹识别真实回合变化，并在房间实时事件后合并执行接收者私有权威视图对账。
- 修改范围：`CommonPdkRuntime.ts`、`CommonPdkPlayController.ts` 及关联测试；未修改服务端、预制体或规则表。
- 验证结果：Creator 3.8.8 已刷新预览，TypeScript 检查通过。房间 `700522` 中 Safari 出 `K♣` 后本方按钮和该手牌立即消失，Chrome 无刷新同步桌面牌并显示操作按钮；Chrome 随后出 `A♣`，Safari 无牌可接自动过，操作权正确留在 Chrome。
- 未完成项：无。

### 2026-09-09｜规范重读与现行条目精简

- 用户要求：重读规范，已处理且未再次提出的问题视为解决，并精简文档。
- 根因/结论：第 7 章待处理队列已为空；已解决事项只存在于依法保留的历史开发记录。现明确历史记录不等于待办，并删除《跑得快流程》中已经废止的“取消准备”现行规则，补齐双端实时回合交接要求。
- 修改范围：本文档与《跑得快流程》；未删除或改写历史记录。
- 验证结果：七章及顺序保持不变，“记忆”仍为末章；流程按钮规则与当前 `Btn_Cancel` 已删除的实现一致，无活动待办。
- 未完成项：无。

### 2026-09-09｜领出提示误触发自动出牌修复

- 用户要求：修复点击“提示”后直接出牌；提示必须遵守只选中、不提交的规范。
- 根因/结论：最后一手自动出牌错误覆盖了本人领出回合，且 Web 按钮 DOM 桥与 Creator Preview 的 CSS 坐标不一致，左侧“提示”可能被判成右侧“出牌”；现将自动出完限定为必须接牌的回合，手牌和按钮统一使用 Cocos 原生输入，手动提示会取消尚未提交的延迟自动出牌并且只切换选中方案。
- 修改范围：`CommonPdkPlayController.ts`、提示专项测试、《跑得快流程》及本文档；未修改服务端、预制体或规则表。
- 验证结果：Creator 3.8.8 已刷新预览，TypeScript 检查通过，提示与触摸关联测试 11/11 通过。房间 `700522` 的 Chrome 领出手牌为 `66、5`，真实点击左侧“提示”并等待超过自动提交延迟后，只滑起一对 6；手牌仍为三张、按钮继续显示、桌面没有新增出牌。
- 未完成项：无。

### 2026-09-09｜回放码暂不可用阻断小结算修复

- 用户反馈：房间 `700522` 第 5 局结束时出现 Cocos 黑色错误层，错误为“本小局回放码获取失败”。
- 根因/结论：小结算协调器将尚未生成或暂时查询失败的回放码当成打开结算面板的强制前置条件，三次查询无有效值后抛出未处理异常；现改为记录警告并携带空回放码继续打开权威结算，结算控制器继续查询同一局回放码，只有取得合法回放码后才开放分享。
- 修改范围：`CommonPdkSwitchCoordinator.ts`、`short-replay-code-flow.test.mjs` 及本文档；未修改服务端、预制体、规则表和结算数据。
- 验证结果：TypeScript `--noEmit` 通过，回放码专项测试 8/8 通过；Creator 3.8.8 已刷新预览。两名真实玩家在房间 `700522` 完成第 6 局，服务端仍未返回有效回放码时，小结算正常打开并显示输赢、出牌记录和继续按钮，仅回放码区域显示获取失败，未再出现黑色 Error 覆盖层；随后房间正常进入第 7 局。
- 未完成项：无。

### 2026-09-09｜提示保留完整炸弹规范补充

- 用户要求：手牌存在炸弹时，提示应尽量避免拆开炸弹。
- 根因/结论：《跑得快流程》原有规则只在预计手数相同时保留完整炸弹，约束不足；现合并为提示总则：完整炸弹按不可拆分组合保护，拆炸弹候选排在全部不拆炸弹的合法候选之后，仅在不存在不拆炸弹方案时允许提示拆分，首出必含牌等强制规则优先。
- 修改范围：《跑得快流程》及本文档；本次仅更新规范，未修改提示代码、预制体、服务端和规则表。
- 验证结果：已核对并同步调整领出与接牌排序，删除两处分散的重复“保留完整炸弹”描述，规则语义一致。
- 未完成项：提示代码尚未按新规范整改和真实验收，等待用户明确要求执行。

### 2026-09-09｜炸弹提示排序实现

- 用户要求：继续未完成任务，将“提示尽量不拆炸弹”落到前端代码。
- 根因/结论：原候选排序未将拆分完整炸弹作为独立代价；现已先按拆分炸弹数量排序，接牌时再将完整炸弹排在普通不拆候选之后。
- 修改范围：`PdkCleanHintRanker.ts`、`pdk-single-hint-selection.test.mjs` 及本文档；未修改规则表和服务端。
- 验证结果：提示、结算与回放码关联定向测试 24/24 通过，Client TypeScript `--noEmit` 通过。
- 未完成项：尚未在真实持有炸弹的 Canvas 牌局中完成提示点击验收，不标记完整 E2E 闭环。

### 2026-09-09｜总结算权威数据链修复

- 用户反馈：最后一小局点击“总结算”报错。
- 根因/结论：总结算面板只从房间缓存读取 `roomEnd`，未传递触发本次总结算的终局权威载荷，存在时序窗口；现已将 `setEnd` 原样传入最终结算表单，仅在缺失时回退房间缓存，并对结算列表做数组边界校验。
- 修改范围：`CommonPdkResultController.ts`、`CommonPdkSwitchCoordinator.ts`、`CommonPdkRecordController.ts`、`short-replay-code-flow.test.mjs` 及本文档；未修改服务端、预制体和规则表。
- 验证结果：终局载荷精确传递回归已加入，关联定向测试 24/24 通过，Client TypeScript `--noEmit` 通过。
- 未完成项：当前独立无头 Chrome 会话位于亲友圈页，没有已到最后小局的测试房间，尚未完成真实“总结算”按钮点击验收。

### 2026-09-09｜成都带牌比较规则审计

- 用户要求：成都跑得快三带和飞机只比较主体，带牌任意；公共规则区分“比较/不比较带牌”并由地区引用。
- 根因/结论：服务端已有 `compareTripleAttachments` 地区配置，但 `PaoDeKuaiRuleSet.canBeat` 在该配置生效前先要求候选与上家牌型名完全一致；因此 `AAAQQ` 被识别为 `TRIPLE_WITH_PAIR`、`KKK96` 被识别为 `TRIPLE_WITH_TWO` 后直接拒绝，尚未进入三张主体大小比较。
- 修改范围：已在《跑得快流程》合并写入地区带牌比较边界和成都示例；未修改 Server。
- 验证结果：已定位拒绝分支与配置映射路径。
- 未完成项：服务端修复超出本线程 UI 职责，需用户明确授权 Server 目录的规则代码与测试修改后才能继续。

### 2026-09-09｜未完成三项续作与真实验收阻塞

- 用户要求：继续总结算、炸弹提示和成都不比带牌三项未完成任务。
- 根因/结论：成都服务端判定已按用户授权修正为三张/飞机主体同形且张数一致时不要求带牌类型同名；总结算真实 8 局预验收另定位到终局小结算两个重叠“总结算”按钮同时激活，已改为只激活 `Btn_FinalSettlement`。重跑时第二玩家加入新房间 `573760` 后被公共资源门禁阻断，首个错误为缺少 `game/CommonPdk/pdk_UIShareMore` Creator 3.8.8 原生 Prefab。
- 修改范围：`PaoDeKuaiRuleSet.java`、成都/公共规则回归测试、`CommonPdkResultController.ts`、结算专项测试及本文档；未修改缺失公共 Prefab 或规则表。
- 验证结果：Client 定向测试 25/25 和 TypeScript 检查通过；Server 生产代码编译通过，直接编译代码断言确认成都 `AAAQQ` 可压 `KKK96`、开启带牌比较时仍拒绝混合带牌；网关已重启并载入规则。无头 Chrome CDP `1234` 真实创建房间、另一账号加入，阻塞截图为 `/tmp/pdk-573760-ready-p2.png`。
- 未完成项：公共 Prefab 缺失阻断双端开局，因此总结算、炸弹提示和成都带牌的真实 Canvas E2E 仍未完成；Maven 全量测试编译另被既有缺失测试类阻断。安全恢复点是修复并由 Creator 同步 `pdk_UIShareMore` 后，新建双人 8 局房重跑。

### 2026-09-09｜分享面板旧路径清理（缺少权威 Prefab 阻塞）

- 用户要求：废弃旧 `pdk_UIShareMore`，跑得快只使用新 `game/CommonPdk/pdk_UISharePanel`。
- 根因/结论：跑得快控制器和协调器仍有 6 处旧表单键；已统一收口为 `CommonPdkShareController.formKey`并只指向新 SharePanel。但工作区磁盘不存在 `pdk_UISharePanel.prefab/.meta`，Creator 3.8.8 资源管理器按完整名称搜索也为 0，无法确定节点契约或 UUID。
- 修改范围：`CommonPdkShareController.ts`、`CommonPdkSwitchCoordinator.ts` 及本文档；未伪造 Prefab、meta、UUID，未恢复旧资源。
- 验证结果：跑得快 Common 目录中 `pdk_UIShareMore` 静态搜索为 0，TypeScript `--noEmit` 通过；Creator 搜索新 Prefab 为 0。
- 未完成项：新 Prefab 尚未落盘，因此公共注册、Creator 重导入和真实打开/分享/关闭验收阻塞。安全恢复点是获得用户已制作的 `pdk_UISharePanel.prefab/.meta` 实际文件后继续。

### 2026-09-09｜新 SharePanel 资源接入与真实验收更正

- 更正原因：前条记录的缺失 Prefab 后由公共框架岗位按用户权威资源补齐。
- 根因/结论：新面板已唯一注册为 `game/CommonPdk/pdk_UISharePanel`，旧 ShareMore 未恢复。
- 修改范围：本线程仅保留先前对两个跑得快控制器的新表单键收口；公共 Prefab 与注册由框架岗位完成。
- 验证结果：Prefab 路径 `Client/assets/Games/Common/Prefab/pdk_UISharePanel.prefab`，UUID `9d5c3b27-4ab8-4d4a-9e43-7c82c160f412`，Bundle `games-common-prefab`；Creator 已导入。无头 Chrome CDP `1234` 在房间 `680276` 真实点击 `Btn_WechatInvite`，面板及全部契约节点成功挂载，点击 `btn_closeshare` 成功关闭；点击解散也成功打开 `DissolveRoom`，不再出现缺少 Prefab。
- 未完成项：解散的多人同意/拒绝权威链仍属队列第⑤项；本次资源加载阻塞已解除。

### 2026-09-09｜小局结算展示分支与最后一手自动提交续作

- 用户要求：阻塞时继续处理不依赖任务，并完成“小局结算弹窗”配置与所有地区最后一手自动打出。
- 根因/结论：客户端原先忽略权威 `settlementPresentation` 并始终打开小结算；最后一手自动提交又被限制为仅接牌回合。现 `FLOATING` 非终局不打开阻塞小结算、不由客户端提交续局，最后一手整手合法时领出和接牌均走同一延迟自动提交；手动提示仍只选牌。
- 修改范围：`CommonPdkSwitchCoordinator.ts`、`CommonPdkPlayController.ts` 及两项定向测试；未修改 Server、Prefab 或开房规则表。
- 验证结果：最后一手与自动过相关测试 9/9、小结算新增门禁 1/1 通过，Creator 3.8.8 TypeScript `--noEmit` 通过；解散生命周期静态测试 6/6 通过。服务端 `FLOATING` 两秒权威续局由框架任务完成并部署。
- 未完成项：以上流程仍需真实多人 Canvas 验收。

### 2026-09-09｜权威规则恢复验收再次阻塞

- 用户要求：`baseScore` 契约恢复后自动继续凉山权威规则快照和三地区真实验收。
- 根因/结论：`baseScore=1/5` 服务端建房契约已由框架修复部署；真实 UI 已确认成都、内江、凉山“小局结算弹窗”默认均勾选，但凉山从当前创建页提交仍返回 Hall 500，取消全部已勾选 `playRule` 后仍复现，已排除仅由显示层 `option_0001` 引起。
- 修改范围：仅验收脚本与本状态记录；未修改大厅、Server、Prefab 或开房规则表。
- 验证结果：HTTP 500 为 `HALL_INTERNAL_ERROR / hall request failed`，traceId `816f1279-44f4-49d4-b5bd-7500fc4066bb`；证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/framework-room-rules-2026-09-09T12-42-12-174Z/`。本任务 Chrome 已停止，专属 Profile 已移至废纸篓，CDP 1234 已释放，无测试房间需要清理。
- 未完成项：待框架按 traceId 修复 Hall 500 后，从新建独立 Profile 恢复 LS201 `tripleAttachmentMode=PAIRS`、`minimumStraightLength=3` 及三地区真实 E2E。

### 2026-09-09｜Hall 阻塞解除与凉山对局续验

- 更正原因：前条记录中的 Hall 500 已由框架岗位修复并部署，不能继续作为停止真实验收的理由。
- 已完成内容：使用本任务独立无头 Chrome（CDP `1235`）从真实 Canvas 成功创建 LS201 房间 `575620`，创建、加入、详情与离开请求均为 200；清除了两个 PDK 房间预制体中已经不存在源资源的 `LzBg` Sprite/Atlas 与 `WarnSpine` SkeletonData 悬空 UUID，并由 Creator 3.8.8 刷新、重新打开预制体确认资源数据库同步，控制台未新增缺失 UUID 或反序列化错误。
- 对局续验：真实双账号房间 `599898` 已完成加入、准备和开局，复现“凉山点击提示后手牌未抬起”；最后一手自动出、压不过自动过及整局结算尚未因此取得闭环证据。后续诊断改为记录真实按钮事件、候选缓存、选中牌值与牌节点 Y 坐标，禁止仅凭截图判断。
- 验收证据：`Client/docs/前端框架规范/历史记录与证据/证据/framework-room-rules-2026-09-09T13-10-34-136Z/report.json`、`Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T13-31-13-607Z/report.json`。
- 未完成项：LS201 规则值须在开局后的权威快照中验证；提示未抬牌仍在处理中。随后两次重跑分别被加入房间和登录面板超时阻断，属于测试入口状态不稳定，不能据此宣称产品修复或停止任务。

### 2026-09-09｜凉山提示真实复验与最后一手续验

- 根因/结论：先前“提示不抬牌”的本轮停局不是产品提示函数失效，而是验收脚本仍查找旧出牌节点 `Btn_Out_Card`，提示成功后没有继续点击当前节点 `Btn_OutCard`。修正验收节点后，真实双账号 LS201 房间 `117488` 连续完成 3 小局并进入小结算。
- 提示证据：共记录 29 次真实 `Btn_Tip` 点击；每次均收到 `touch-start/click/touch-end`，`logic.GetSelectCard()` 与对应牌节点完全一致，被选节点从 Y=0 抬至 Y=20。覆盖首出顺子、三张带牌、对子和单张等候选，未出现点击提示直接提交出牌。
- 最后一手证据：第 1、2 小局均捕获接牌最后一张自动提交，第 3 小局捕获领出最后一张自动提交，三次权威状态均继续推进并完成结算；完整三地区矩阵仍保留在待验收队列。
- 证据路径：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T13-52-55-800Z/report.json`。
- 未完成项：测试结束后的解散清理没有使房主返回大厅，且移动端出现一次 Gateway WebSocket 403；需继续验证解散权威链和重连收敛。压不过自动过也须以真实无合法候选回合及唯一 `pass_req` 单独取证，不能沿用旧脚本的相反断言。

### 2026-09-09｜自动过验收口径纠正与服务维护断点

- 根因/结论：旧长局脚本把“接牌后没有自动出牌且停留原状态”误记为正确的 `responseNotForced`，与“无合法牌必须自动过”的现行规范相反；现已将断言改为必须观察到唯一一次 `common.room.pass_req` 且权威 `stateVersion` 前进。
- 已完成内容：修正长局脚本的当前出牌节点名、登录按钮重试、加入房间确认重试及自动过断言；自动提示/自动过/最后一手专项 9/9，通过；提示、炸弹保护、总结算与回放专项 18/18，通过；解散与返回生命周期专项 9/9，通过。
- 精确阻塞：房间 `309103` 在 `stateVersion=9` 的真实出牌过程中，Gateway/Hall 被其他任务重启，浏览器产生连续 `ERR_CONNECTION_REFUSED/RESET`，并非前端按钮或牌型错误。证据为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T14-03-27-124Z/report.json`。
- 调度状态：已向主协调线程报告；主协调线程正在执行最后一次核心服务重建，要求维护期间不发起 Gateway/Hall/Account 请求。安全恢复点为服务稳定通知后使用 CDP `1235` 新建双账号房，继续唯一 `pass_req`、解散投票和三地区矩阵验收。
- 未完成项：真实自动过、解散投票和跨地区长局 E2E 仍待稳定服务窗口，不标记完成。

### 2026-09-09｜首出动态必带牌提示修复与 E2E 断点

- 根因/结论：真实房间 `823082` 第 6 局的权威 `activeRequiredFirstCard=205`，前端首出提示却选中不含 205 的连对，实际 `play_req` 被服务端以 `3008 / first play must contain required card` 拒绝；前端此前只读取静态 `requiredFirstCard`，未投影和使用每局动态必带牌。
- 修改范围：`CommonPdkAuthoritativeViewAdapter.ts` 将动态字段投影到房间局数据，`CommonPdkPlayController.ts` 的手动提示和自动提示均按该字段过滤首出候选，提示周期键同步纳入该字段；补充定向测试和协议取证脚本。未修改 Server、Prefab 或开房规则表。
- 验证结果：动态字段投影与提示过滤专项 10/10、提示选牌专项 8/8 通过；Creator 3.8.8 已自动刷新脚本且控制台无新增编译错误。协议证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T14-17-57-615Z/report.json`。
- 未完成项：重跑真实双账号局时，原账号的游戏 WebSocket 恢复握手返回 403，新账号登录返回 HTTP 502；另一次进入房间后停在公共资源预加载页且无脚本异常。已向主协调线程提交精确证据，真实修复后 3008 收敛、自动过和解散链仍未闭环。

### 2026-09-09｜房间 967030 底分与小结算静态全链审计

- 用户问题：房间 `967030` 选择底分 5，小结算显示输家 `-10`、赢家 `+10`，怀疑结算固定为 10。
- 审计结论：客户端建房请求明确提交 `baseScore`，权威视图适配器只透传服务端 `seat.roundScore`，小结算控制器只显示该原值，不在前端计算或二次乘底分。服务端标准计分公式为 `handScore(remainingCards) × baseScore`，因此输家剩 2 张且底分为 5 时，`-10/+10` 本身是合法结果，不能仅凭该数值认定底分丢失。
- 修改范围：仅更新本岗位记录并向主协调线程反馈诊断；未修改 Server、Prefab 或开房规则表，前端禁止为该现象增加固定乘数或地区硬编码。
- 验证结果：建房底分与偏好契约测试通过；跑得快提示、自动过、最后一手和炸弹保护相关定向测试通过。网关日志只保留房间连接记录，未保留该局规则快照及结算载荷。
- 未完成项：需由后端/稽查从房间持久化快照核对 `ruleOptions.baseScore`、输家 `remainingCards/plays`、春天分支和最终 `scoreDelta`；账号 502、游戏 WebSocket 403 未恢复前不得通过反复新建账号或房间污染现场。

### 2026-09-10｜最后一手自动出牌现行规则纠错

- 用户要求：所有跑得快最后一手都需要自动打出；继续处理不依赖登录维护窗的未完成任务。
- 根因/结论：《跑得快流程》第 6 节仍残留“只在接牌时自动、领出不自动”的旧描述，与最新明确需求及已实现的领出/接牌统一自动提交冲突；现已改为剩余整手按权威地区规则合法时，领出与接牌都自动出牌，并明确普通提示不得提交。
- 修改范围：仅《跑得快流程》现行规则与本文开发记录；未修改框架、服务端、Prefab 或开房规则表。
- 验证结果：文档检索确认现行流程不再包含“领出不得自动出牌”旧规则，且“提示只选牌”约束保持一致。
- 未完成项：该功能的成都、内江真实多人 Canvas 复验仍在原队列第⑥项，待 Account+Gateway 维护窗结束后恢复。

### 2026-09-10｜解散投票双账号验收的大厅阻塞断点

- 当前目标：验证房间 `967030` 所述解散弹窗“拒绝/同意”按钮，从真实 Canvas 命中到请求、权威响应及双方实时状态更新的完整链路，并覆盖拒绝、同意、重复点击、发起人与非发起人。
- 已完成内容：静态确认 `CommonPdkDissolveController` 将 `btnCancel/btnSure` 分别绑定拒绝/同意请求，`DissolveRoom.prefab` 两按钮具备有效 `UITransform` 和可交互 `Button`；已准备 CDP `1235` 独立双浏览器上下文脚本并执行真实登录点击。
- 精确阻塞：登录完成后公共大厅进入加载失败页，首个可见错误为“大厅语义节点缺少 UITransform: Player”，随后等待 `CreateRoom` 超时；尚未进入跑得快房间，因此不能取得本轮 `roomId/operationId/stateVersion` 和投票请求证据，也不得宣称按钮已完成。
- 证据与断点：报告位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-dissolve-vote-2026-09-09T16-18-05-315Z/report.json`，失败截图为同目录 `98-owner-failure.png`、`99-guest-failure.png`；恢复脚本为 `Client/work/pdk-dissolve-vote-e2e.mjs`。公共大厅修复并刷新 Preview 后可直接从该脚本重跑。
- 未修改范围：未修改大厅公共框架、服务端、开房规则表及已经冻结的已解决功能；阻塞报告已提交稽查与主协调任务统一调度。

### 2026-09-10｜解散投票闭环、最后一手续验与自动过断点

- 用户要求：未完成任务必须持续执行；最后一手自动出牌、无合法牌自动过、解散投票均须以真实双端 Canvas 操作形成闭环。
- 根因/结论：服务端在后续墩仍保留 `activeRequiredFirstCard`，导致前端把非首出回合误当必带牌回合；同时 Creator Preview 曾加载旧编译脚本。现将动态必带牌严格限定为本局尚无已出牌且首墩阶段，重启 Creator 后确认新脚本生效；自动过提交前增加包含 `operationId/stateVersion` 的结构化日志，便于证明唯一请求。
- 修改范围：`CommonPdkAuthoritativeViewAdapter.ts`、`CommonPdkPlayController.ts`、最后一手真实验收脚本及本文档；未修改 Server、Prefab、开房规则表或已经冻结的已解决功能。
- 验证结果：Client TypeScript 检查通过，提示、自动过和最后一手关联测试 23/23 通过。真实房间 `172310` 验证领出最后整手只提交一次并进入结算；房间 `897327` 验证接牌最后整手只提交一次并进入结算；重复权威快照未造成重复请求，双端 Canvas 与控制台均无运行时错误。解散投票房间 `474348` 已覆盖发起者已投票、非发起者拒绝、双方弹窗关闭、再次发起并同意、双方返回大厅，全部断言通过。
- 证据路径：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T19-15-32-188Z/report.json`、`Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T19-26-25-471Z/report.json`、`Client/docs/前端框架规范/历史记录与证据/证据/pdk-dissolve-vote-2026-09-09T18-16-25-906Z/report.json`。
- 未完成项：真实“无合法候选自动过且唯一 `pass_req`”尚未捕获。跨小局重跑在双方点击继续后未收到下一轮 `*-play` 操作令牌，房间 `670116` 首个错误为 `timeout waiting for desktop recovered next turn`；已尝试并行继续、访客后房主顺序继续及双方页面重载恢复，均未生成下一轮操作权。证据为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T19-37-20-872Z/report.json`，已报告主协调任务；安全恢复点为权威端恢复下发下一轮出牌令牌后直接重跑该脚本。

### 2026-09-10｜服务恢复后的自动过续验与三地区配置取证

- 当前目标：从房间 `670116` 保留的第 2 局权威令牌继续捕获唯一自动过请求；若本局牌序未形成无解回合，则切换不依赖跨局令牌的三地区规则配置真实取证。
- 自动过续验：账号 `13/14` 已重载进入房间 `670116` 第 2 局，真实点击提示和出牌均生效，双方页面无运行时错误；但本次牌序没有出现无合法候选回合，脚本最终超时等待小结算，未产生可证明自动过的 `[CommonRoomAutoPass]` 与唯一 `pass_req`，因此该项仍不标完成。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T19-56-13-868Z/`。
- 三地区取证：独立账号 `3` 通过真实 Canvas 依次点击 `CD201/NJ201/LS201`，三套配置均成功返回且页面无重叠、无内部枚举泄漏、Hall 请求和控制台干净。LS201 当前权威 `roomRuleSourceHash=bf3902ea328a592ad02c7607071b4c5ba5e35baf41846a969cb130fc90c02a97`，实际同时返回独立 `rule_ls201_0001=小局结算弹窗` 与 `playRule=结算弹窗 option_0001`，并返回 `operationTime=1000秒/默认1000`、`jinHuaScore` 默认 1 分、房间限制默认全不选，与既有验收基线不一致。
- 修改范围：仅增强 `pdk-last-hand-autoplay-e2e.mjs` 的保留房间恢复入口，以及两个验收脚本的独立 CDP 端口配置；未修改业务代码、Prefab、Server 或只读开房规则表。
- 未完成项：LS201 重复小结算字段和当前默认值须由权威配置责任方核对，不能在前端硬编码修正；开局后 `tripleAttachmentMode=PAIRS`、`minimumStraightLength=3` 快照仍待配置一致后验证。配置证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-room-rule-ui-e2e-2026-09-09T19-59-51-553Z/`，已报告主协调任务。

### 2026-09-10｜总结算终局按钮五局续验断点

- 当前目标：连续完成 8 小局，并在最后一局结束后通过真实 Canvas 点击 `Btn_FinalSettlement`，验证总结算面板、权威载荷和返回大厅链路。
- 已完成内容：房间 `311858` 已连续完成第 1 至第 6 小局；前五局客户端手牌均与权威状态一致，牌张守恒、完整牌组、三张 A/一张 2、底牌 8 张及局号推进检查全部通过；保留房间续跑后第 6 局也已通过真实双端 Canvas 完成。
- 精确阻塞：第 7 局权威快照已经发牌并显示 `currentSeat=1`、双方各 16 张，但连续恢复客户端均未收到可消费的第 7 局出牌操作令牌，三次续跑无法提交首手；当前断点为 `roundNo=7`、`stateVersion=63`，尚未到达第 8 局总结算，因此不能把终局按钮标记为完成。前五局证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-ten-round-2026-09-09T20-23-08-864Z/report.json`，续跑阻塞已报告主协调任务。
- 修改范围：只修正总结算验收脚本的当前建房节点、移动端准备/提示/出牌可见坐标及独立 CDP 端口；未修改业务代码、Prefab、Server、开房规则表或冻结功能。
- 安全恢复点：保留房间 `311858`，待权威端恢复第 7 局操作令牌后从 `stateVersion=63` 继续，不重建前六局现场；完成第 8 局后真实点击总结算按钮并核对双方终局状态、请求次数、控制台和网络错误。

### 2026-09-10｜恢复首墩必出牌与总结算闭环

- 根因：恢复快照的 `playedCards` 可能包含前面小局的历史数据，权威视图适配器据此错误地把当前第 7 局 `activeRequiredFirstCard=205` 清成 0，导致首墩提示候选没有强制包含 205，并被权威规则以 `first play must contain required card` 拒绝。
- 修改：适配器始终保留权威下发的当前局 `activeRequiredFirstCard`；是否处于当前首墩仍由控制器结合当前墩牌、`trickId` 判定，避免把旧局历史误作当前局已出牌。未修改服务端、Prefab 或开房规则表。
- 验证：首墩必出牌、自动提示、最后一手及单牌/炸弹保护定向测试 18/18 通过；Creator Preview 热刷新后，房间 `311858` 从第 7 局 `stateVersion=63` 恢复，权威状态推进至第 7 局结束 `stateVersion=77` 和第 8 局结束 `stateVersion=83`，真实双端 Canvas 提示/出牌均生效，控制台零运行时错误。
- 总结算：第 8 局小结算显示 `8/8`，`Btn_Continue` 隐藏、`Btn_FinalSettlement` 可见且可交互；真实点击后成功打开总结算，显示房间 `311858`、8 局、双方胜负次数及总分 `+25/-25`，再真实点击返回大厅，无“本小局回放码获取失败”或缺失 Prefab 错误。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-ten-round-2026-09-09T20-55-07-760Z/report.json` 及同目录 `08-desktop-dealt.png`、`08-desktop-settlement.png`、`08-desktop-final-settlement.png`。续跑报告中若干全新房间专用统计断言为 false，是因为前六局协议帧不在恢复浏览器上下文且旧统计器只识别 `common.room.*`；不影响第 7–8 局权威推进、真实点击、终局页面和零运行时错误的直接证据。

### 2026-09-10｜真实炸弹保护与成都不比较带牌证据复核

- 炸弹保护：复核房间 `311858` 的真实 Canvas 长局报告。第 2 局座位 0 持有完整四张 5，提示提交 `FOUR_WITH_TWO` 时四张 5 全部同组使用；第 3 局持有完整四张 9，提示提交完整 `BOMB`，两局均未把炸弹成员拆作单牌、对子或其他带牌。
- 成都带牌：复核房间 `682301` 第 2 局。权威快照 `compareTripleAttachments=false`；上手三张 10 带 9、8 后，真实 Canvas 提示并成功提交三张 K 带 7、5，权威接受并推进，证明只比较三张主体，不比较所带牌大小。
- 结论：两项均由真实双端 Canvas 点击和权威 `playHistory` 共同证明，不是规则函数直调；已经满足现行验收口径，从待处理队列移除并冻结。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-bomb-and-chengdu-attachments-2026-09-10.md`，原始报告为 `chengdu-pdk-ten-round-2026-09-09T20-23-08-864Z/report.json` 与 `chengdu-pdk-local-hint-2026-09-08T14-34-58-242Z/report.json`。

### 2026-09-10｜POPUP 小结算双端续验

- 验证结果：房间 `783755` 已通过真实桌面及移动 Canvas 完成一局，双方 `SmallSettlement` 均正常显示；桌面和移动端背景、1280×720 设计链、回放码展示及分享复制全部通过，双端控制台零运行时错误。
- 清理状态：核心 POPUP 验收断言 5/5 通过；验收后的解散清理停在等待确认弹窗，属于测试数据清理断点，不影响已取得的小结算展示证据，但该房间须由后续清理任务回收。
- FLOATING 状态：当前权威配置只有“小局结算弹窗”复选项，未勾选应映射 `FLOATING`；尚未完成未勾选建房及跨局/重连真实取证，本队列项只标记部分完成。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-small-settlement-background-2026-09-09T21-16-20-338Z/report.json` 及同目录桌面、移动截图。

### 2026-09-10｜FLOATING 结算跨局与终局闭环

- 用户要求：未勾选“小局结算弹窗”时不得显示阻塞弹窗，等待 2 秒自动进入下一局；覆盖重连、最后一手、最后局总结算和无重复发牌。
- 根因/结论：前端原先错误地认为服务端会自行推进下一局，没有提交双方 `continue_req`；同时结算精简事件不携带开房选项，导致恢复或局末误回退为 POPUP。现由前端从权威房间 `ruleOptions` 回读展示模式，FLOATING 双方等待牌面动画及 2 秒后各提交一次继续确认，终局直接打开总结算。
- 修改范围：`CommonPdkSwitchCoordinator.ts` 与 FLOATING 真实验收脚本；未修改 Server、Prefab 或只读开房规则表。
- 验证结果：房间 `602538` 的权威配置为 `settlementPresentation=FLOATING`，第 1 至第 8 局持续推进且每局牌张守恒；第 8 局最后一张由 `[CommonRoomLastHandAutoPlay]` 自动提交，权威到达 `roundNo=8/stateVersion=84/matchFinished=true`，未打开小结算而直接挂载 `FinalSettlement`。桌面和移动网页均通过真实 Canvas 操作，终局报告控制台零运行时错误。POPUP 测试房 `783755` 已由房主真实点击解散并使双方返回大厅。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/chengdu-pdk-ten-round-2026-09-09T22-31-31-465Z/report.json`、同目录终局截图，以及 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-small-settlement-background-2026-09-09T21-30-00-191Z/report.json`。
- 未完成项：无；恢复浏览器上下文的历史协议帧不纳入单次报告统计，跨局事实以连续权威 `roundNo/stateVersion`、当前局真实请求和终局 UI 共同取证。

### 2026-09-10｜跨局首牌残留与空动作历史投影续修

- 当前目标：继续取得无合法候选时唯一自动过及重连证据，并保持末手自动出与桌面牌恢复正确。
- 根因/修改：权威端会在后续墩保留 `activeRequiredFirstCard`，且网络快照可能同时出现 `lastActions=[]` 与非空 `currentTrick`。前端现仅在该具体首牌仍存在于本人手牌时执行必带限制；空 `lastActions` 不再覆盖有效 `currentTrick`，而是按当前墩恢复牌型、待压牌及桌面显示。验收脚本同时改为按节点实际坐标点击准备、识别封装协议动作，并区分小结算继续和总结算返回大厅。
- 验证结果：提示、自动过、末手相关测试 18/18 通过；Creator 3.8.8 重启后已确认 Preview 编译块包含新逻辑。房间 `670116` 从第 4 局卡点持续推进到第 8 局终局，捕获 5 次末手自动出，终局 `8-107-play` 仅提交一次，双端控制台无运行时错误并返回大厅。证据为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T22-48-32-450Z/report.json`。
- 未完成项：新测试房 `868219` 中访客已准备，房主真实点击准备但权威端未开局，证据为 `pdk-last-hand-autoplay-2026-09-09T23-02-46-520Z/report.json`；因此尚未取得修复后真实无解回合的唯一 `[CommonRoomAutoPass]/pass_req`，继续保留安全断点，不以单测冒充完成。

### 2026-09-10｜自动过权威归属确认与内江矩阵阻塞

- 自动过结论：连续真实长局房间 `565234`、`308345` 均完成 8 局并正常清理，仍没有客户端无解回合。只读核对当前 Poker Authority 后确认，每次 play/pass 后服务端会执行 `skipUnbeatableSeats()`，无合法候选座位由权威状态机直接过牌，不向该客户端发放操作令牌，因此当前架构不会产生客户端 `pass_req`。前端兼容分支只处理服务端确实下发无解操作权的旧版本/异常恢复场景，不能为了验收伪造请求。
- 末手补证：房间 `565234` 第 1、6 局分别捕获接牌末手自动出，第 8 局终局请求唯一；房间 `308345` 再次覆盖领出末手与 8 局结算，双端真实 Canvas 无运行时错误并清理成功。证据为 `pdk-last-hand-autoplay-2026-09-09T23-09-18-544Z/report.json`、`pdk-last-hand-autoplay-2026-09-09T23-15-46-362Z/report.json`。
- 内江阻塞：真实点击 `Game_NJ201` 和创建按钮后 Hall 返回 HTTP 500，未进入 `GameRoom2D`；证据为 `pdk-last-hand-autoplay-2026-09-09T23-21-28-011Z/report.json`。前端未硬编码绕过，须权威配置/服务责任方修复建房后从现有脚本直接复验。

### 2026-09-10｜凉山八局真实续验

- 用户要求：未完成不得停止；继续完成凉山提示、末手自动出、出牌展示和结算链路。
- 验收修正：凉山开局存在抢庄阶段，通用脚本原先只等待出牌操作而超时；现改为通过真实 Canvas 节点识别并点击 `Btn_GrabBanker/Btn_NoGrabBanker` 后再进入出牌循环，不以内部函数绕过 UI。
- 验证结果：LS201 房间 `497358` 已真实运行第 1 至第 8 局并逐局到达小结算，最终清理房间；捕获 8 次末手自动出，覆盖领出、接牌、对子和 6 张连续牌，权威每次均推进。提示诊断累计 69 个真实回合且没有出牌拒绝；跑得快相关单元与门禁 63/63 通过。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T23-30-48-775Z/report.json` 及同目录 8 局末手、8 局结算截图。移动端重连时曾记录一次 Gateway WebSocket 403 握手，随后恢复并完成全局；该网络事件不作为前端功能完成项删除。

### 2026-09-10｜玩法规则变更主动同步

- 用户要求：所有岗位完成玩法规则相关修改或确认后，主动向固定“Aoo 文档”任务提交结构化报告。
- 根因/结论：已写入固定任务ID、报告字段和无法直发时的稽查转发路径。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜正式文档同步范围扩展

- 用户要求：将主动同步从玩法规则扩大到全部需要新增或更新正式文档的变更。
- 根因/结论：已合并覆盖架构、组件、接口、数据库、资源、场景、构建、验收、业务、规则、配置、迁移和重要修复；纯内部无外部影响的重构仅记岗位记录。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜aoo产品更名与点对点协作更正

- 用户要求：原“aoo稽查”更名为“aoo产品”；职责明确时直接点对点协作，明确文档事项直发Aoo文档，仅例外情形上报aoo产品。
- 根因/结论：已替换旧泛化上报规则；历史记录旧称保留为当时名称。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变。
- 未完成项：无。

### 2026-09-10｜业务域全栈职责更正

- 用户要求：跑得快岗位同时负责跑得快客户端及 `Poker` 服务端和结算，并跨协议、迁移、配置、部署和真实 E2E 闭环。
- 根因/结论：旧规范以进入和退出房间为边界并排除数据库和协议，现已以跑得快业务域全栈边界替换。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜本地测试账号隔离更正

- 用户要求：跑得快岗位仅使用本地账号`41`至`50`，密码为`1`；不得占用其他岗位账号，预留账号须经aoo产品分配。
- 根因/结论：已加入岗位账号段、并行换号、实际账号留证和跨岗位账号/数据禁止操作规则。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜持续执行与idle条件更正

- 用户要求：统一已授权任务的持续执行、阻塞续作和唯一合法idle条件，不得把未授权历史审计项自动入队。
- 根因/结论：有已授权可执行任务时必须持续执行；当前项阻塞时点对点联系并继续独立任务；仅全部完成或全部等待决定、授权、外部状态时可idle。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜停止报告强制更正

- 用户要求：任何停止、idle、异常结束或准备不再继续时必须额外向aoo产品报告，普通阶段进度仍无需报告。
- 根因/结论：已补齐停止报告七项字段和禁止带已授权可执行任务停止的门禁，替换“正常idle无需报告”的冲突口径。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜任务转交先入队与字段清单标准化

- 用户要求：所有新任务与转交任务在处理前先写入“记忆”待处理队列。
- 根因/结论：新增“任务摘要、来源、状态、职责范围、安全断点”入队字段清单；完成任务后移除，阻塞或停止时保留队列状态，避免任务丢失与重复认领。
- 修改范围：仅本文档“执行与验收”和“开发记录”。
- 验收结果：七章顺序未变，历史记录未改写，未引入代码或配置改动。
- 未完成项：无。

### 2026-09-10｜QuickText 续验与内江建房恢复进度

- QuickText：框架已修复 `UIMessage_Drift` 销毁竞态，但本岗位未找到修复后的真实房间发送证据，因此不按完成处理；旧证据的首错仍是快捷文字请求被服务端以 `3008` 拒绝，需在稳定真实房间重新点击并核对跨客户端送达。
- 内江建房：只读规则表及活动索引已确认包含必填 `playerCount`；真实 Canvas 点击请求已越过“`playerCount is required`”，随后定位 Poker 权威首错为 `invalid firstPlayRule`。根因是稳定键注册表把“赢家先出/黑桃3优先”漂移成 `option_0001/3`。
- 已处理：恢复 NJ201 注册表稳定值 `winner_first`、`spade_three_first` 及同批可识别规则稳定值，重新生成并发布活动索引 `2026090712`；首轮失败证据为 `nj201-create-2026-09-10T03-26-08-234Z`。第二轮因预览停留状态未进入 `Login_Scene`，尚不能作为业务失败结论。
- 未完成项：QuickText 真实双端发送；NJ201 真实点击建房、权威规则快照与清房闭环。

### 2026-09-10｜房间 967030 持久化取证完成

- 权威证据：`aoo_room_snapshot` 中房间 `967030` 为 LS201、`stateVersion=44`、`roundNo=5`、已投票解散；`aoo_hall_room.rules_json.baseScore=5`，权威 `pdkRuleOptions.baseScore=5`，不存在前端或服务端固定底分 10。
- 牌数与计分分支：快照双方各剩 7 张、各已出 1 张，另有 16 张未发牌，32 张牌守恒；当前小局 `roundScored=false`，累计分为 `-20/+20`。`springMode=DISABLED`、`springValue=0`，该房间不进入春天加倍分支。
- 结论：用户看到的单局 `-10/+10` 与“剩余手牌计分 × 底分 5”一致，不是底分被固定为 10；旧房已解散且当前局未结算，持久化事件表无该局独立 `scoreDelta` 事件，不能从已解散快照伪造单局载荷。
- 修改范围：仅只读数据库取证与本文开发记录；未修改房间、历史数据、玩法代码或规则表。
- 未完成项：无；该项从待处理队列移除。

### 2026-09-10｜地区优先级调整为凉山与成都

- 用户要求：暂停内江跑得快，优先处理凉山 `LS201` 与成都 `CD201`。
- 执行结果：已停止 NJ201 真实建房续验，并撤回本轮尚未闭环的内江炸弹 30 分代码尝试；内江现有断点保留但不再排在当前执行队列。
- 两地现状：凉山权威规则已按原子配置表达“三带一对（`PAIRS`）”“最短三张顺子”“必须压牌”“桌面牌按顺序保留”；成都使用独立 `CD201` 规则。两地服务端定向测试首次执行被规则文档目录 `.DS_Store` 门禁拦截，跳过执行插件后又被不属于本轮范围的 `AypdkGameProvider/HbpdkSession` 测试源码缺失拦截，尚不能据此宣称通过。
- 当前阻塞：共享 `Client/assets/Games/Poker/Packs` 正由框架岗位恢复；恢复前不得启动 Creator Preview 或真实 Canvas E2E，避免覆盖对方现场。
- 未完成项：恢复后完成 LS201/CD201 提示、末手自动出牌、吃不起自动过、出牌区完整显示的真实双端验收。

### 2026-09-10｜主协调暂停与安全恢复断点

- 暂停原因：主协调要求等待“Lobby代码精简”任务 `01a08976-7e7e-7050-89bd-2d286fca303e` 与“Games公共代码整理”任务 `01a08979-6300-7433-b7c1-00c36a587e42` 全部完成；在 aoo产品明确恢复前，不再修改、刷新、测试或启动新工作。
- 已完成内容：LS201/CD201 服务端主模块跳过测试编译通过；客户端规则、提示、末手自动出牌和权威自动过相关定向单元测试共 27 项执行，26 项通过，唯一失败为公共框架测试仍期待旧 `buildId=aoo-preview-20260906-unified-room-4`，当前实际为 `aoo-preview-20260909-runtime-port-5`。未据此修改公共框架。
- 真实验收阻塞：使用本岗位独立 CDP `1235` 与账号 `41/42`、`43/44`、`47/48` 三次启动 LS201 双端真实 E2E，账号与密码已真实输入并点击 `Btn_Login`，但页面仍停留 `Login_Scene`，网络只出现版本检查而没有登录请求；证据目录依次为 `ls201-full-flow-2026-09-10T03-55-17-659Z`、`ls201-full-flow-retry-2026-09-10T03-57-32-047Z`、`ls201-full-flow-login-retry-2026-09-10T04-02-58-812Z`。登录场景受只读边界约束，本岗位未修改。
- 未完成项：LS201/CD201 的真实双端提示只选牌、重复提示轮换、末手自动出牌、吃不起自动过、桌面出牌完整有序及 LS201 权威快照验收；权威自动过的前端兼容/断线恢复复核；QuickText 真实双端发送验收。NJ201 继续按用户要求暂停。
- 安全恢复点：代码写入已停止，没有运行中的 Creator 刷新、构建、服务启动或 E2E；恢复时先确认两个整理任务已完成且 aoo产品已明确恢复，再读取其变更和本记录，检查共享文件状态后从独立 Profile 的真实登录入口继续，禁止回退整理 AI 或用户改动。

### 2026-09-10｜牌型动画公共 Spine 索引待串行恢复

- 用户目标：所有跑得快出牌后牌型动画统一从实际目录 `Client/assets/Games/Poker/PDK/Common/Spine` 索引；用户给出的重复 `Aoo/Client` 路径经只读检查确认不存在。地区专属差异才允许覆盖公共映射。
- 只读结论：真实 Spine 目录继承 `PDK/Common.meta` 的 Bundle，子目录未另设 Bundle；当前 `PdkAnimationRegistry/PdkAnimationResolver` 仍加载旧 `poker-common-effects/AnimationClips`，与目标目录不一致。现有公共资源包含炸弹、顺子、连对、三不带、三带一/二/一对、四带一/二/一对/三、飞机、连续飞机和关门等 SkeletonData，UUID 未改。
- 串行边界：Games公共代码整理任务 `01a08979-6300-7433-b7c1-00c36a587e42` 正在把四个 PDK Bundle 统一改为 `paodekuai-` 前缀，并修改共享加载注册文件。本岗位已撤回刚开始的旧 `pdk-common` 注册表写入，当前动画文件恢复到介入前状态；等待对方完成 Creator 重导入和 Bundle 改名后，再以最终 `paodekuai-common` 接续独立动画注册表、SkeletonData 解析器、Creator 同步和真实出牌 E2E。
- 未完成项：公共牌型到 Spine 资源的最终映射、动态 SkeletonData 播放、地区覆盖边界、Creator 重新打开与控制台门禁、真实出牌后动画取证。除本项外其他跑得快任务仍暂停。
- 续作结果：公共整理完成改名后，动画注册表已切换到最终 `paodekuai-common`，解析器改为直接加载 `PDK/Common/Spine` 的 `sp.SkeletonData` 并在现有挂载点播放首个动画；已覆盖协议牌型 4 至 20 中所有需要特效的顺子、三张/四张带牌、炸弹、飞机和连对，不改变牌型判断或出牌流程。未修改 Spine 资源、UUID、Prefab 或 Bundle `.meta`。
- 验证结果：动画资源唯一源、14 类 SkeletonData 路径、17 个协议牌型映射、Bundle 继承与预制体挂载点静态门禁 3/3 通过；Creator 3.8.8 热编译后控制台新增错误为 0。真实双端 LS201 已成功登录并创建房间 `249258`，首次被旧 `common/Numpad` 路由阻断；公共整理随后已修复为 `common-prefab/Common/Numpad` 并发布。
- 当前唯一阻塞：公共整理刷新 Preview 后，启动被 `LoginScreenBootstrap UUID 无效` 卡在 `Boot_Strap`，无法继续真实加入、开局和出牌动画取证。该登录场景受只读边界约束，已向主协调请求调度责任方；恢复点为登录 UUID 修复后直接重跑 LS201 双端真实出牌 E2E。本项在真实动画证据完成前仍标记未闭环。
- 阻塞更正：框架只读复核确认 `LoginScreenBootstrap` UUID、Meta 与三个场景引用一致，登录 UUID 有效；真正首错是 WordCard Pack01 下多个已删除 `SwitchCoordinator` 仍被 Creator packer 当入口，触发 `ENOENT` 后导致全局脚本类未注册。登录场景保持未修改；恢复条件更正为对应责任方清除这些失效入口并恢复 Preview 全局脚本注册。
- 最新恢复结果：Preview 恢复后再次执行 LS201 双端真实 E2E，登录成功并真实点击建房，但加载被 `settlement/pdk/BigSettlement` 缺失阻断，证据目录 `pdk-last-hand-autoplay-2026-09-10T06-41-25-029Z`。随后框架确认 `Client/assets/Common/Prefab` 当前只剩 `.DS_Store`，CD/LS 启动依赖的 WaitForm、NoticeBar、Numpad、Message、SettingsPanel 等公共 Prefab 实体不存在；资源审计又禁止本岗位自行恢复，须主协调指定权威来源并授权公共资源责任方按原 UUID 恢复。
- 非 UI 回归：CD/LS 权威规则、提示只选牌与轮换、炸弹保护、末手自动出牌、自动过守卫和公共 Spine 动画索引共 27/27 通过。资源只读交集另确认 `PDK_CommonRoom.prefab` 直接缺失 UUID `432c2839-face-4a43-b734-edfe6aac76f4`（旧源 `Atlas/Legacy/assets/texture/newshare/btn_wxfx.png`）；本岗位未恢复、移动或修改 Bundle。

### 2026-09-10｜现存 Creator 内容唯一权威

- 用户要求：场景、Prefab、节点、名称和层级只认当前磁盘与 Creator 现存内容；已删除内容不得从任何历史来源恢复、重建或兼容。
- 根因/结论：旧资源缺失不得再被解释为需要找回；代码仍引用废弃内容时，应删除对应引用、强制校验和控制逻辑。确需新增时由用户亲自在 Creator 中创建，AI 只报告精确节点契约。
- 修改范围：仅本文档“修改边界”、开发记录与“记忆”；未修改场景、Prefab、资源、业务代码或规则表。
- 验证结果：规则已去重归入既定章节，七章顺序保持不变，“记忆”仍位于文档末尾。
- 未完成项：当前 CD201/LS201 真实 E2E 仍等待共享框架将现存 `WaitNet` 接入加载流程，以及公共代码索引现存大小结算 Prefab；本岗位禁止恢复旧 `UIWaitForm` 或旧结算路径。

### 2026-09-10｜废弃动画与材质边界补充

- 用户要求：已删除的动画、材质和 Prefab 一律废弃，不得从任何历史来源恢复；只有用户以后明确提出时才重新制作。
- 根因/结论：该要求被合并进现存 Creator 内容唯一权威规则；代码和验收必须移除对废弃资源的依赖，Prefab 若仍含失效序列化引用只报告具体项，未经授权不得补建。
- 修改范围：仅本文档“修改边界”、开发记录与“记忆”。
- 验证结果：七章结构与顺序保持不变，未修改任何资源、Prefab、业务代码或规则表。
- 未完成项：无。

### 2026-09-10｜公共扑克牌视觉资源缺失阻塞

- 当前目标：继续 LS201/CD201 的真实双端发牌、提示、出牌、动画与结算验收。
- 进展/首错：LS201 房间 `519113` 已完成创建、加入及双方真实点击准备；发牌时旧 `common-poker-cards` 请求 404。公共代码已删除旧 Bundle 引用并改为现存 `poker-common`，但当前磁盘不存在运行必需的 `Poker_Card.prefab` 与 `Poker_Common` 图集。
- 边界/结论：这些资源已被用户删除，按现存 Creator 内容唯一权威规则禁止恢复、代建或用历史资产兼容；只有用户在 Creator 中明确重新制作并保存对应资源后，才能继续视觉 E2E。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-10T08-37-48-845Z/`。
- 未完成项：等待用户重新制作公共扑克牌 Prefab 与图集；恢复点为 Creator 发布 `poker-common` 后，以账号 `41/42` 从 LS201 创建房间重跑，再验 CD201。

### 2026-09-10｜公共扑克卡牌恢复授权与索引更正

- 用户要求：仅从废纸篓恢复 `Poker_Card.prefab` 及其原 `.meta`，放入 `Games/Poker/Common/Prefab`；不得恢复其他已删除资源。
- 根因/结论：恢复后的卡牌 Prefab 继承 `poker-common`，现存 `Poker_Common.plist/png` 位于 `Common/Atlas` 并继承 `common`。卡牌工厂已按真实归属加载 `poker-common/Prefab/Poker_Card` 与 `common/Atlas/Poker_Common`，未移动图集；失效旧 Bundle、Native 映射、桌布加载器及其校验已清除。
- 修改范围：公共卡牌加载索引、Native Prefab 映射、失效桌布加载器及针对性测试；未恢复或修改其他资源、Prefab、场景、Atlas 或规则表。
- 验证结果：Creator 3.8.8 已显示 `Poker_Card.prefab` 的根节点与四个现存子节点并刷新 Preview；公共卡牌与 PDK 房间针对性测试 18/18 通过。
- 未完成项：LS201/CD201 发牌与后续真实 E2E 正由移动重连复验任务继续；不得并发操作其共享房间与文件。

### 2026-09-10｜成都完整长局通过与凉山续验断点

- 用户范围：本轮只处理成都 `CD201` 与凉山 `LS201`，内江 `NJ201` 继续暂停。
- 成都结果：独立无头 Chrome 真实双端房间 `815483` 连续完成 8 小局并清理房间；捕获 7 次末手自动出，覆盖先手、接牌、单牌与对子，每个权威操作仅提交一次，重复权威状态未重复提交；累计 67 次提示诊断、11 份桌面出牌证据和 26 张关键截图。证据目录为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-10T09-26-23-490Z/`，成都已通过项冻结。
- 凉山结果：房间 `264386` 连续完成 8 小局，提示、出牌、`ALL_IN_ORDER` 桌面保留、结算与清房均无服务端出牌拒绝；本次随机发牌没有出现“当前整手可一次合法打完”的样本，不单凭零样本判定末手失败。证据目录为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-10T09-13-33-253Z/`；既有 LS201 长局 `497358` 已捕获 8 次末手自动出。
- 修正与回归：验收脚本不再使用统一 dispatch 未携带的 `operationId` 反查出牌请求，而保留精确权威操作窗口内的请求增量；CD/LS 权威规则、提示、末手、自动过、分享按钮与公共动画映射定向测试 41/41 通过。客户端无解座位继续由服务端 `skipUnbeatableSeats()` 权威跳过，不伪造客户端 `pass_req`。
- 资源清理：移除 `PDK_CommonRoom`、`Common_Head`、`WaitNet`、`Lobby_Main`、`Create_Room` 中已删除动画/图片的悬空 UUID；`Btn_Share` 改用当前现存 `Common/Atlas/icon_wechat.png`，未恢复任何已删除资源。前一轮真实 E2E 已确认 PDK、头像与 WaitNet 的旧 404 消失，剩余大厅/开房旧动画引用已从磁盘清除。
- 当前阻塞：Creator 3.8.8 正由“亲友圈桌子预制体样例”任务独占操作，主协调明确要求不得强制切换或重开 Prefab。安全恢复点为其释放编辑器后，由 Creator 正常刷新并依次重开 `PDK_CommonRoom`、`Common_Head`、`WaitNet`、`Lobby_Main`、`Create_Room`，确认无新增导入/反序列化错误，再跑 1 小局复核 404 清零；随后完成 LS201 末手补充取证与 QuickText 真实双端发送。
- 账号更正：本轮曾使用 `51/52` 完成成都取证，超出本文旧的 `41` 至 `50` 账号隔离记录；该使用事实保留，不再继续使用，后续必须由主协调分配账号后再开新房。

### 2026-09-10｜身份判断权威字段统一

- 用户要求：身份判断统一使用服务端 `user_type`，禁止根据 `ppk` 或其他展示、格式特征推断。
- 根因/结论：跑得快业务只能消费 `0=普通玩家`、`1=陪玩`、`2=机器人` 的服务端权威值，不得另建玩法侧身份来源。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验证结果：规则已归入既定章节，七章顺序保持不变；未修改代码、资源或规则表。
- 未完成项：无。

### 2026-09-10｜Games 公共 Bundle Preview 发布复核

- 用户要求：处理 CD201/LS201 创建房间后 `games-common-prefab` Bundle 404，不改大厅代码。
- 根因/结论：Bundle 元数据已正确为 `games-common-prefab`；本轮复核时 Creator Preview 已正常发布，无需修改 Bundle、资源或代码。
- 修改范围：仅只读 Creator/Preview 发布核对与本文档队列状态。
- 验证结果：`http://127.0.0.1:7456/assets/games-common-prefab/config.json`、`index.js` 和 `poker-common/config.json` 均为 HTTP 200；config 的 bundle 名为 `games-common-prefab`。
- 未完成项：已通知登录大厅从 CreateButton 真实流程复验。

### 2026-09-10｜GameRoom2D Preview 场景发布阻塞

- 用户要求：处理 CD201/LS201 加载页提示 `GameRoom2D` 不在构建设置中的阻塞，不恢复旧场景。
- 根因/结论：现存 `Games/Common/Scenes/GameRoom2D.scene/.meta` 与磁盘 `builder.json` 的 URL、UUID `4071b7a5-b666-462a-b9e6-b0c6a8522ab0` 均一致且两个构建任务均已列入；当前 Preview `assets/main/config.json` 却只发布 Boot/Login，未同步该构建场景。
- 修改范围：仅只读场景、构建配置、Preview 发布状态和本文档；未改场景、Bundle、大厅代码或资源。
- 验证结果：当前 Creator 正打开未保存的 `ClubDesk.prefab*`，不得刷新 Preview、切换场景或覆盖其他任务编辑现场；已确认 Preview 根因。
- 未完成项：待用户或当前亲友圈编辑任务保存/关闭该 Prefab 后，Creator 正常刷新 Preview，验证 `assets/main/config.json` 包含 `GameRoom2D` 再通知大厅复验。

### 2026-09-10｜GameRoom2D Bundle 归属更正

- 更正原因：此前将 `assets/main/config.json` 未列出 `GameRoom2D` 误判为场景发布缺失；该场景位于现存 Bundle `games-common`，不应出现在 main Bundle。
- 当前核验：`http://127.0.0.1:7456/assets/games-common/config.json` 已包含 `GameRoom2D` 和 UUID `4071b7a5-b666-462a-b9e6-b0c6a8522ab0`，其场景导入资源请求返回 HTTP 200；`CommonPdkGameSceneLauncher` 也按 `games-common` 加载并通过该 Bundle 的 `preloadScene/loadScene` 进入房间。
- 修改范围：未修改场景、Bundle、构建配置或运行时代码。Creator 无实际 Bundle 构建任务；菜单自动化误触“生成预览模板”并显示成功，未以历史版本覆盖该现存模板，留待主协调审计。
- 后续：该项从“等待 Creator 刷新”转为“待登录大厅基于当前 `games-common` 资源重新执行 CD201/LS201 CreateButton 真实 E2E”；若仍复现，再以当前网络与控制台证据定位加载时序，而非恢复旧场景。

### 2026-09-10｜成都/凉山公共房间资源与真实触点收口

- 用户要求：当前只处理 CD201 与 LS201，NJ201 暂停；继续完成公共房间资源、提示、自动出牌、自动过及真实点击验收。
- 已处理：`Games/Common/Prefab` 取消嵌套 Bundle，聊天、语音、解散统一由现存 `games-common` 加载；`PDK_CommonRoom/Btn` 提升为房间根节点最后一层，避免玩家/触摸节点遮挡；微信邀请与分享均改为直接执行网页复制或 App 微信跳转，不再引用已删除分享面板。真实 Canvas 坐标验收改用渲染相机 `worldToScreen`，消除右侧按钮约 42px 的测试触点偏移。
- 验收：LS201 房间 `155641` 真实点击已完成聊天面板打开、`QuickText01` 点击并发出唯一 `common.room.dispatch/quick_text`，完成对局并到达小结算；控制台错误为空、HTTP 404 为空。公共房间、提示/出牌、社交 Prefab 与迁移门禁 33/33 通过。CD201 继续沿用已冻结房间 `815483` 的 8 局通过证据；LS201 末手沿用房间 `497358` 的 8 次自动出证据，本次随机局没有产生整手可一次合法打完样本。
- 阻塞更正：快捷文字已完成前端真实点击和唯一请求发送，但两端均未收到可展示的服务端广播，因此“跨客户端送达”不能由前端单独标记通过；已保留请求证据并提交主协调核对服务端广播链。全量 Creator 构建被非本任务范围的麻将旧 Prefab 大量缺失 UUID 阻断，本次未修改麻将；目标 Prefab 已由 Creator 正常重启重导入并以 Preview 实测。
- 未完成项：QuickText 服务端广播与跨客户端送达核对；NJ201 按用户要求暂停。`GameRoom2D` Preview 发布阻塞由后续独立记录和队列补充项继续跟踪。

### 2026-09-10｜GameRoom2D Bundle 归属记录字段补充

- 用户要求：按历史记录不可改写原则，审阅并补齐 `GameRoom2D` Bundle 归属更正记录。
- 根因/结论：前述更正结论有效：`GameRoom2D` 属于 `games-common`，不应出现在 main Bundle；前述记录字段未完全符合本章固定摘要格式，本条仅补充字段，不改写历史。
- 修改范围：仅本文档开发记录与现行待处理队列；未修改场景、Bundle、构建配置、运行时代码或 Creator 预览模板。
- 验证结果：当前 `games-common/config.json` 已发布目标场景及正确 UUID；菜单自动化误触生成预览模板且未用历史版本覆盖现存模板的事实已保留审计。
- 未完成项：等待登录大厅基于当前 `games-common` 发布结果重跑 CD201/LS201 CreateButton 真实 E2E；QuickText 服务端广播继续核对，NJ201 继续暂停。

### 2026-09-10｜陈旧权威适配器测试删除

- 用户要求：按现存代码与资源作为唯一权威，清理仅依赖已删除实现的陈旧测试，不恢复废弃资产。
- 根因/结论：`Client/tests/games/pdk/authoritative-adapter-contract.test.mjs` 仅读取已删除的 Pack01 与 `CD201GameplayAdapter`，不再验证现行运行链，已作为陈旧测试删除。
- 修改范围：仅删除 `Client/tests/games/pdk/authoritative-adapter-contract.test.mjs`；未恢复 Pack01、`CD201GameplayAdapter` 或其他已删除资产，未改业务代码、Prefab、Scene、`.meta` 或规则表。
- 验证结果：相关 32 项静态回归全部通过。
- 未完成项：无。

### 2026-09-10｜现行待办过期状态更正

- 用户要求：持续跟踪完成/阻塞报告，清除已闭环事项在现行待办中的过期状态。
- 根因/结论：QuickText 服务端权威广播实现、Gateway 测试和生产编译已完成，不再列为服务端核对待办；`GameRoom2D` 已确认由 `games-common` 正常发布与加载，不再列为资源归属待办。
- 修改范围：仅更新本文档第7章现行待处理队列；历史记录保留当时事实，未改写。
- 验证结果：队列现仅保留 QuickText 跨客户端展示、CD201/LS201 CreateButton 双端复验的登录链阻塞，以及用户明确暂停的 NJ201。
- 未完成项：待 `Login_Scene/Btn_AccountLogin` 输入面板恢复后完成两项真实双端复验；NJ201 仅用户恢复时续作。

### 2026-09-10｜代码分类与职责目录落库

- 用户要求：将“按实际使用域归类代码”写入跑得快岗位执行规范。
- 根因/结论：跑得快代码整理与复用边界需明确区分玩法专用与公共目录，禁止跨目录转发、兼容路径与兜底目录掩盖责任域。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验收结果：规则落库，未发生代码变更；后续跑得快代码移动按该约束回检。
- 未完成项：无。

### 2026-09-15｜Git 仓库地址与用户授权边界

- 用户要求：把客户端、服务端和管理后台 Git 地址写入每个 AI 员工规范；用户测试完成后会主动邀请对应 AI 提交，未收到明确指令禁止推送。
- 根因/结论：统一记录三个固定仓库地址；取消 AI 自主提交和定时推送授权，明确邀请后默认只推送对应仓库 `develop`，`main` 及历史改写仍需当次单独授权。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：规则已归入既定章节，历史开发记录未改写，“记忆”仍为最后一章。
- 未完成项：无。

### 2026-09-15｜出牌区按玩家操作权保留与清理

- 用户要求：所有打出的牌在出牌区至少展示 2 秒；一墩未结束时持续保留；权威判定再次轮到某玩家操作时，只清理该玩家自己的出牌区。
- 根因/结论：旧实现使用全桌公共清理计时器，无法表达每个玩家独立的展示时长和操作权边界；已改为按数据座位记录展示时间与清理计时器，权威回合切换只清理当前操作玩家的 `Out_Card/Pass`，不足 2 秒时延迟到满 2 秒，其他玩家出牌区保持不动；小局结束仍在最后一手展示满 2 秒后统一收桌，凉山累计桌面牌继续保留到小局结束。
- 修改范围：`CommonPdkPlayController.ts`、对应单元测试和无头 Chrome 验收脚本；同步更新跑得快通用规则与凉山地区规则。未修改开房规则表、Prefab、Scene、`.meta`、服务端、数据库或其他玩法。
- 验证结果：Creator 3.8.8 Preview 已自动热编译并加载新方法；相关单测 51/51 通过，验收脚本语法检查通过。桌面与手机触摸真实 Canvas 已在 CD201 房间 `541603` 完成整局并到达小结算；专项最短链在房间 `523738` 发出唯一 `common.room.play_req` 后未收到权威确认，未能取得本需求完整时序证据。TypeScript 全量检查仅保留本任务前已存在的 8 项错误，本次未新增类型错误。
- 未完成项：真实 E2E 仍阻塞。房间 `541603`（账号 49/44）与 `523738`（账号 41/42）当前均恢复为 `phase=PLAYING` 且无任何可操作节点，无法从真实 UI 解散或继续；需权威房间恢复链修复或清理这两个测试房间后，从本断点复跑“出牌落地、满 2 秒仍显示、轮回到本人时只清本人出牌区”的桌面/手机时序验收。

### 2026-09-15｜无人可压时操作权快速回转竞态修正

- 用户要求：自己先出牌且其他玩家均压不上时，该手牌仍必须在本人出牌区展示满 2 秒。
- 根因/结论：权威可在本地出牌飞行动画落地前跳过全部无解座位并把操作权直接还给原玩家；此前操作权边界先执行清理，但当时尚无展示时间戳，牌随后落地后不会再次触发清理。现改为出牌落地并登记展示时间后，若当前权威操作权已经回到该玩家，则重新按本人座位挂载满 2 秒清理，不影响其他玩家出牌区。
- 修改范围：仅 `CommonPdkPlayController.ts`、对应单元测试和本文档；未修改规则表、Prefab、Scene、`.meta`、服务端或其他玩法。
- 验证结果：相关单测增至 52/52 通过；Creator 3.8.8 Preview 已热编译包含该快速回转分支；全量 TypeScript 检查仍仅为已有 8 项错误，本次未新增类型错误。
- 未完成项：权威恢复死态阻塞仍与上一条记录一致；待房间链恢复后补真实双端时序 E2E。

### 2026-09-15｜暂停跨任务文档消息与自动整理

- 用户要求：所有 AI 暂时不再给文档任务发送消息，也不再自动整理正式文档。
- 根因/结论：新增临时覆盖规则；暂停期间不执行此前主动发送文档报告的条款，各岗位只维护自身开发记录与记忆，文档 AI 仅响应用户直接下达的具体文档任务。
- 修改范围：仅本文档“执行与验收”和本开发记录；历史记录不删除、不改写。
- 验证结果：临时规则已归入既定章节，“记忆”仍为最后一章。
- 未完成项：等待用户后续明确恢复文档协作。

### 2026-09-15｜统一重写跑得快提示排序

- 用户要求：按确认后的完整提示逻辑重写代码；截图桌面为单张5、手牌含`AA、K、QQ、J、10 10、9、8、77、6666`时，首次提示必须只抬一张7，不能先抬炸弹或破坏有效组合。
- 根因/结论：旧单张候选未包含炸弹兜底，通用估算手数排序又早于真实牌型保护，并把完整炸弹牌同时计入连对/顺子；控制层最后还会按纯点数再次覆盖干净排序。现统一为穷举合法同牌型、保护普通及地区特殊炸弹、保护飞机/三张/连对/顺子、使用富余重复牌、选择刚好能压住的较小方案，完整炸弹最后兜底，估算手数仅作末级比较；存在安全方案时彻底排除拆炸弹候选。
- 修改范围：`PdkCleanHintRanker.ts`、`CommonPdkPlayController.ts`、提示相关单元测试、跑得快通用规则及本文开发记录；未修改开房规则表、Prefab、Scene、`.meta`、服务端、数据库或其他玩法。
- 验证结果：截图精确牌组、三A地区炸弹、单张物理选择、顺子/连对/三张/飞机保护、普通牌优先炸弹及循环等相关测试 54/54 通过；Creator 3.8.8 已对两个脚本重新导入并完成热编译，最终 Preview 产物包含新逻辑，控制台新增错误为0；任务专属无头 Chrome 已真实加载同一 Preview，`BootStrap`、Cocos Canvas、WebGL 和152项运行资源正常，页面错误为0。全量 PDK 单测仍有13项与本次无关的既存节点契约失败，TypeScript 全量检查仍仅有既存8项错误，本次未新增。
- 未完成项：当前没有可复现截图牌面的权威测试房间，不能用内部函数或伪造状态冒充真实 Canvas 点击验收；待权威房间恢复链提供可操作牌局后，仅续跑“桌面单5、点击提示、只抬一张7、循环普通牌后才到完整炸弹”的桌面与手机真实 E2E，不重复已完成代码、单测和 Creator 同步。

### 2026-09-15｜三带二跨端显示为旧单J的根因诊断

- 用户要求：诊断下方玩家已出三带二、上方客户端却只显示一张J，并导致玩家按错误牌面无法出牌的问题。
- 根因/结论：截图中出牌者本机已完整显示`KKKJJ`且剩余手牌已扣除，证明提交和权威牌型不是单J；观察端逻辑仍以完整三带二作为当前比较目标，因此按单J选择的单张无法通过校验。显示错误来自权威快照的公共牌异步恢复缺少逐版本失效边界：多个`restorePublicCards/restoreLastActions/renderPublicOperation`并发执行，且每次为满足2秒展示会异步等待；`presentationGeneration`只在整桌清理时递增，新的权威状态未使旧任务失效，较早的单J任务可能晚于新`KKKJJ`完成并覆盖同一座位出牌区。
- 修改范围：仅只读检查客户端公共出牌恢复、服务端`lastActions/currentTrick`生成逻辑及本文诊断记录；未修改业务代码、Prefab、Scene、`.meta`、规则表、服务端或数据库。
- 验证结果：服务端按座位保存最新`lastActions`并在`currentTrick.cards`保留完整当前牌组；客户端在逻辑层先写入权威`currentCards/currentType`，但公共牌渲染任务只校验跨整轮的`presentationGeneration`，没有校验发起时的`stateVersion/actionKey`，与截图“逻辑按三带二、显示回退成旧单J”的跨端分裂完全一致。
- 未完成项：本轮用户仅询问原因，未修改代码；修复时应为每座位公共牌渲染增加权威版本/动作键门禁，使旧异步任务在清牌、飞牌或2秒等待后的每个写入点自动失效，并增加“同座位单J后出KKKJJ、旧任务晚完成也不得覆盖”的并发回归测试与真实双端Canvas验收。

### 2026-09-15｜旧单J覆盖最新三带二竞态修复

- 用户要求：刷新后仍复现上方客户端把下方玩家的三带二显示成旧单J，直接处理该问题。
- 根因/结论：公共出牌恢复为满足至少2秒展示会跨权威快照异步等待，旧任务此前只受整桌清理代次约束；现增加每次权威公共牌投影代次和逐数据座位渲染修订号，所有等待、清牌、飞牌、异步建牌后的写入均校验发起版本。新状态到达后，旧单J任务即使随后完成等待也会失效；`renderedActionIds`只在通过失效门禁且实际落牌后登记，避免重复快照把尚未完成的旧任务误认为已显示。
- 修改范围：`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`及本文档；未修改开房规则表、Prefab、Scene、`.meta`、服务端、数据库或其他玩法。
- 验证结果：相关测试55/55通过；TypeScript全量检查未新增错误，仍仅为既存8项；Creator 3.8.8已重新运行Preview，04:19生成的预览脚本包含投影代次、座位修订号和成功落牌后登记逻辑，Creator控制台新增错误为0。任务专属无头Chrome通过CDP 1234加载Preview，页面与Canvas建立且运行日志错误为0，但当前预览停留在加载页，未获得可操作权威牌局。
- 未完成项：缺少可操作的双端权威牌局，尚不能取得“同座位旧单J等待中收到最新KKKJJ，最终只显示KKKJJ且可正确响应”的真实Canvas复验证据；恢复测试房间后只续跑该场景，不重复代码、单测和Creator同步。

### 2026-09-15｜远端多张出牌重叠为最后一张修复

- 用户要求：下方实际出牌`QQQ65`时，上方观察端必须完整显示`QQQ65`，不能只显示最后一张`5`；出什么就完整显示什么。
- 根因/结论：权威出牌数组和五个牌节点均完整，但远端飞牌期间会隐藏`Out_Card`；旧流程在该容器仍未激活时调用 Cocos Layout，布局没有生效，随后显示时全部牌停在同一预制体原点，画面只露出最后创建且层级最高的`5`。现改为完整创建权威牌节点后先激活`Out_Card`，再在同一帧强制布局，并增加包含房间、状态版本、操作号、座位、完整牌值与坐标的重叠异常日志。
- 修改范围：仅`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`及本文开发记录；未修改开房规则表、Prefab、Scene、`.meta`、服务端、数据库或其他玩法。
- 验证结果：相关测试56/56通过；TypeScript全量检查仍仅为既存8项错误，本次未新增。Creator 3.8.8已重新索引目标脚本并刷新Preview，控制台新增错误为0。
- 未完成项：当前没有可操作的双端权威牌局，且任务专属CDP 1234实例不可用，尚缺“实际出QQQ65、另一端完整显示五张且可正常压牌”的真实Canvas证据；恢复牌局后只续跑该场景，不重复代码与静态测试。

### 2026-09-15｜远端出牌显式坐标排版更正

- 更正原因：用户再次实测三带二`10 10 10 7 7`，观察端仍只显示最后一张`7`，证明仅调整容器激活与Layout调用顺序不足；上一条关于已刷新Preview的记录也缺少生成产物时间证据。
- 根因/结论：动态建牌后的`Layout.updateLayout()`不能作为同步排版完成边界。现保留预制体Layout参数作为尺寸来源，同时根据牌宽、`spacingX`、父子锚点和横向方向显式计算每张牌坐标及容器宽度，使本帧直接显示完整权威牌组，不再依赖Layout异步刷新。
- 修改范围：仅`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`及本文更正记录；未修改Prefab、Scene、`.meta`、规则表、服务端、数据库或其他玩法。
- 验证结果：相关测试56/56通过；TypeScript仍仅有既存8项错误。Creator 3.8.8于13:38重新生成Preview脚本，产物已确认包含显式坐标排版和`CommonRoomOutCardLayout`诊断；控制台新增错误为0。
- 未完成项：当前计算机控制只暴露Codex内置浏览器，无法替用户现有Chrome双端页面执行刷新；待两个现有页面加载13:38后的Preview后，继续取得完整三带二跨端显示的真实Canvas证据。

### 2026-09-15｜出牌失败后旧单张覆盖当前三带二修复

- 用户要求：刷新后当前三带二`10 10 10 7 7`能完整显示，但点击出牌出现“牌型错误”后又退回只显示旧单张`7`，要求保持出什么显示什么。
- 根因/结论：`lastActions`为按时间累积的动作历史，同一座位可存在多次动作；错误后的权威刷新逐条恢复同一座位全部历史动作，使旧单张在强制2秒展示窗口内重新覆盖当前三带二。现恢复前先按数据座位归并，只保留该座位最后一个权威动作，当前墩完整牌组继续作为最终比较目标。
- 修改范围：仅`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`及本文开发记录；未修改Prefab、Scene、`.meta`、规则表、服务端、数据库或其他玩法。
- 验证结果：相关测试57/57通过；TypeScript仍仅有既存8项错误，本次未新增。Creator 3.8.8于13:52生成的Preview脚本已确认包含`latestBySeat`逐座位最新动作恢复逻辑，控制台新增错误为0。
- 未完成项：“牌型错误”的具体选牌值无法从截图确定；本次先修复已确认的错误刷新显示回退。加载13:52后的Preview复测时需记录实际选中牌组，若合法的`KKK88`仍被拒绝，再按该准确输入继续修复牌型判定。

### 2026-09-15｜拒绝出牌不得刷新公共出牌区更正

- 更正原因：用户再次实测，点击出牌并提示“牌型错误”后，观察端仍从当前多张牌退回旧单张；说明上一条仅归并`lastActions`仍未切断触发错误重绘的入口。
- 根因/结论：Gateway日志确认房间`763033`中用户`380`、`381`的出牌请求均被权威服务以`combination cannot beat previous`拒绝，服务端桌面状态没有变化；客户端却在请求失败的`finally`中执行整桌`refresh()`，重新异步恢复公共历史，导致当前多张出牌被旧单张覆盖。现拒绝请求时仅清除自己的落牌、按未变化的本地手牌重建手牌和选择状态，公共出牌区保持原样，只允许后续权威状态包改变它。
- 修改范围：仅`CommonPdkPlayController.ts`、`pdk-authoritative-auto-pass.test.mjs`及本文更正记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。
- 验证结果：新增“拒绝出牌只恢复本地手牌且绝不重绘公共牌”回归测试，相关测试58/58通过；TypeScript仍仅有既存8项错误，本次未新增。Creator 3.8.8于14:02重新生成Preview脚本，生成时间晚于14:01源码，产物已确认包含拒绝请求不刷新权威桌面的分支。
- 未完成项：当前无法控制用户正在使用的两个Chrome窗口执行真实点击，尚缺该同一房间的双端Canvas最终复验；用户加载14:02后的Preview后，只需续验“非法压牌提示出现后，上方仍完整保持此前多张出牌”，不重复代码、单测和Creator同步。

### 2026-09-15｜关闭出牌区下一帧自动重排更正

- 更正原因：用户加载上一版后仍一模一样，说明拒绝请求的整桌刷新虽已切断，但多张牌仍会在后续渲染帧重新重叠；此前“显式坐标排版”只保证调用当帧，未冻结Cocos Layout的下一帧写入。
- 根因/结论：`updateActionCardLayout`先调用Layout并写入显式坐标，但`Out_Card`的Layout组件仍启用，Creator会在后续布局阶段再次覆盖子牌坐标，完整多张牌因重叠只露出层级最高的最后一张。现读取原Layout间距和方向后在运行时关闭该组件，之后每次权威出牌均由控制器按完整牌数组显式设置容器宽度和所有子牌坐标，自动布局不再于下一帧覆盖。
- 修改范围：仅`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`及本文更正记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。
- 验证结果：新增自动Layout必须关闭的回归约束，相关测试58/58通过；Creator 3.8.8于14:14刷新Preview，14:14:37预览产物晚于14:14:12源码且确认包含`layout.enabled = false`。
- 未完成项：尚待用户现有双端牌局加载14:14后的Preview并真实复验；验收标准为任意三带二从落牌、等待、切换操作权到非法压牌提示后始终显示完整五张。

### 2026-09-15｜房间763033公共出牌专项追踪

- 用户要求：不再根据截图猜测，针对当前房间号增加日志；用户复现后依据实际日志解决。
- 执行结论：仅对房间`763033`启用`[PdkOutCardTrace]`专项追踪，记录权威投影的`stateVersion/trickId/operationId/currentTrick/cardList/lastActions`、每次公共出牌渲染输入、当帧/下一帧/100毫秒后的节点数量和坐标及Layout状态，以及出牌拒绝时客户端实际提交牌组、牌型和当前比较目标。记录同时保存在页面全局`__PDK_OUT_CARD_TRACE__`和`localStorage`同名键，最多300条。
- 修改范围：仅`CommonPdkPlayController.ts`和本文开发记录；未修改显示业务逻辑、Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。
- 验证结果：相关测试58/58通过；Creator 3.8.8于14:20刷新Preview，14:20:22产物已确认包含`PdkOutCardTrace`、`layout-after-100ms`和`play-rejected`记录点。
- 未完成项：等待用户在房间`763033`刷新后复现并通知；随后读取该次日志判定是权威牌值变化、渲染输入变化、节点覆盖还是坐标覆盖，再作唯一对应修复。

### 2026-09-15｜依据763033实测日志修正出牌组锚点偏移

- 用户要求：房间`763033`已复现，读取专项日志后直接解决。
- 实测证据：`stateVersion=22/trickId=8`的权威`packetCards`始终为`[412,212,112,110,209]`，即完整`QQQ10 9`；客户端100毫秒后仍有5个激活牌节点且Layout已关闭，排除服务端丢牌、历史覆盖和节点销毁。旧公式却以Out_Card容器`anchorX`乘完整牌组宽度作为子牌起点，右锚点的上方座位会把整组牌推离其设计出牌点，只剩最靠右的末张牌位于可视区。
- 修复结论：子牌位置改为以各座位现有Out_Card节点坐标为视觉中心，首张位置固定为`-((数量-1)*间距)/2`，按方向向两侧展开；容器锚点只描述边界，不再参与子牌偏移。房间专项日志暂时保留用于本轮复验。
- 修改范围：仅`CommonPdkPlayController.ts`、`pdk-single-hint-selection.test.mjs`及本文开发记录；未修改Prefab、Scene、`.meta`、规则表、服务端、数据库或其他玩法。
- 验证结果：相关测试58/58通过；Creator 3.8.8于14:34刷新Preview，14:34:13产物已确认包含中心展开公式且不再含旧锚点偏移公式。
- 未完成项：等待用户两个页面加载14:34后的Preview，复验上方座位是否完整显示`QQQ10 9`；通过后移除房间专项临时日志并做最终回归。

### 2026-09-15｜公共出牌单一权威写入与控制台错误治理

- 用户要求：同时处理公共出牌仍退化为最后一张牌，以及控制台重复红色`15`和头像`localhost:8765`连接拒绝；必须从框架职责上收口，禁止继续叠加局部补丁。
- 根因/结论：普通公共牌此前可被权威状态、旧`OpCard`、`ChangeStatus`和失败恢复等多条链路创建或覆盖，部分事件只携带单张/局部字段，因而会把完整三带二降级成末张牌。现将完整权威快照的`reconcileAuthorityPublicCards`设为普通公共牌唯一写入入口；旧`OpCard`只负责声音、规则和私有手牌兼容，`ChangeStatus`只负责操作权与按座位清理，不再用局部包重绘公共牌。原有“至少停留2秒、轮未结束持续保留、轮到该座位立即清理”时序继续由统一投影管理。
- 控制台治理：红色`15`来自牌型判断把合法枚举用`console.error`输出，`15`实际代表“三带一对”，现已移除该调试输出；未明确配置头像服务时不再自动请求`localhost:8765`，历史回环地址会回退使用预制体默认头像。
- 修改范围：仅`CommonPdkPlayController.ts`、`CommonPdkGameLogic.ts`、`PlayerAvatarService.ts`、对应单元测试及本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。
- 验证结果：公共牌唯一写入者、旧事件禁写公共牌、头像回环地址过滤及既有出牌时序相关测试共64/64通过；Creator 3.8.8已刷新Preview，控制器和头像预览产物已确认更新。房间`763033`专项日志暂时保留，供本轮双端复测后确认最终状态。
- 未完成项：尚需用户现有双端牌局加载本次Preview后，真实复验“出多张显示完整多张、非法出牌提示不改变桌面牌、2秒及操作权清理时序”；通过后再移除房间专项临时日志。

### 2026-09-15｜刷新恢复改用完整出牌快照

- 更正原因：用户提供同一局刷新前后对照，刷新前上方完整显示五张，刷新后立即退化为单张并出现“牌型错误”，证明实时渲染不是主因，错误集中在断线/刷新恢复的数据选择。
- 根因/结论：服务端`lastActions`是按座位覆盖的操作账本，更新已有座位不会改变插入顺序，因此数组末项不等于当前最后出牌；客户端适配器却用`lastActions.at(-1)`作为`currentTrick`缺失时的第一兜底，可能把旧单张恢复成当前比较牌。现统一改为`currentTrick`优先，其次使用服务端`playedCards`中的最新完整组合，只有两者均缺失时才兼容旧操作账本；牌面显示与牌型比较共用同一完整来源。
- 修改范围：仅`CommonPdkAuthoritativeViewAdapter.ts`、对应单元测试及本文开发记录；未修改服务端、Prefab、Scene、`.meta`、开房规则表、数据库或其他玩法。
- 验证结果：相关测试64/64通过；Creator 3.8.8已刷新Preview，15:29:07预览产物确认包含`latestVisiblePlay`完整组合优先逻辑。
- 未完成项：待用户双端再次刷新并复验同一场景；验收标准为刷新前后上方五张完全一致，且下方牌型比较不再因旧单张而提示错误。

### 2026-09-15｜公共出牌三层状态框架重写

- 用户确认：按“规则比较状态、各座位展示状态、临时动画状态分离”的方案重写显示牌型框架，不再从多个兼容字段推断或补丁覆盖。
- 服务端契约：Poker权威视图新增原子`tableSnapshot`，同一版本一次性下发`stateVersion/trickId/turnSeat/comparison/seatPlays`；`comparison`保存完整当前比较组合，`seatPlays`按座位保存本墩完整出牌，客户端不再依赖`lastActions`数组顺序恢复桌面。
- 客户端实现：适配边界在PLAYING阶段强制校验`tableSnapshot`，统一投影为`comparisonState`和`seatPlayStates`；公共牌协调器先提交规则比较状态，再按座位渲染完整牌组。旧`currentTrick/cardList/lastActions`恢复分支已从公共牌写入路径移除，动画仍为临时副本，不拥有最终桌面节点。
- 生命周期：每座位出牌仍至少保留2秒；本墩未结束持续保留；重新获得操作权时只清理该座位；立即无人可压时由完整`comparisonState`保证2秒内仍显示整组；非法出牌不提交权威快照，因此不能改变显示或比较牌型。
- 修改范围：服务端`PokerAuthoritativeSession.java`及其测试，客户端`CommonPdkAuthoritativeViewAdapter.ts`、`CommonPdkPlayController.ts`及相关测试、本文开发记录；未修改开房规则表、Prefab、Scene、`.meta`、美术资源、数据库或其他玩法。
- 验证结果：客户端相关测试64/64通过，Creator 3.8.8于15:39自动重新导入，Preview产物已确认包含`tableSnapshot/comparisonState/seatPlayStates/restoreSeatPlayStates`；Poker及依赖模块主代码编译通过。Poker全测试编译仍被既存`AypdkGameProvider/HbpdkSession`缺失阻塞，与本次改动无关；未删除触发文档门禁的既存`.DS_Store`。
- 未完成项：当前没有正在运行的本地跑得快服务进程可重启，新服务快照尚未进入用户正在测试的房间；需启动包含本次Poker产物的权威服务后，再做双端实时出牌、刷新恢复、非法出牌和2秒清理真实E2E。

### 2026-09-15｜旧房间协议边界迁移

- 更正原因：用户刷新现有房间后提示“CommonPdk权威桌面快照不完整”；该房间由旧版本权威服务创建，本机没有对应跑得快服务进程可部署或重启，严格拒绝旧包会直接阻断游戏恢复。
- 处理结论：新增仅位于`CommonPdkAuthoritativeViewAdapter`的滚动升级迁移器，将旧包的完整`currentTrick`和按座位`lastActions`一次性标准化为新`tableSnapshot`；迁移不依赖`lastActions`末项顺序。控制器、规则比较和UI仍只消费`comparisonState/seatPlayStates`，没有恢复旧多源写入路径。
- 验证结果：相关测试64/64通过；Creator于16:10:04自动生成的新Preview产物已确认包含`suppliedTableSnapshot/suppliedSnapshotComplete`迁移逻辑，现有旧协议房间不再因缺少`tableSnapshot`拒绝挂载。
- 未完成项：需用户刷新当前页面确认恢复提示消失，并继续验证完整多张显示与牌型比较；新服务部署后将直接使用原生`tableSnapshot`，无需客户端改动。

### 2026-09-15｜权威桌面快照部署到实际Gateway运行时

- 更正原因：上一记录误判“本机没有对应跑得快服务进程”，实际房间权威会话作为Poker模块嵌入Gateway进程运行；仅完成源码编译而未替换Gateway类路径中的旧Poker包，导致客户端仍收到旧协议。
- 部署结论：确认新产物包含`PokerAuthoritativeSession.tableSnapshot()`后，替换Gateway运行类路径中的`game-category-poker-1.0.0-SNAPSHOT.jar`，通过正式LaunchAgent服务项重启Gateway；旧进程`95190`切换为新进程`8677`，仅重启受影响的Gateway，未重启Hall及其他服务。
- 运行验证：Gateway的`8080`与内部`18080`端口均恢复监听；房间`763033`的用户`380/381`已在新进程启动后重新建立WebSocket连接；运行包反编译确认包含`tableSnapshot()`。HTTP健康路径未单独开放，访问返回协议入口的预期404，不代表服务异常。
- 修改范围：本次仅部署已编译Poker产物并重启Gateway，未修改Prefab、Scene、`.meta`、开房规则表、数据库或其他玩法；旧运行包已备份到本次临时部署目录，未覆盖源码。
- 未完成项：需在当前双端真实牌局继续执行一次完整多张出牌、另一端非法压牌及刷新恢复，核对“出什么显示什么”、至少停留2秒、本轮持续保留和轮到该座位立即清理四项Canvas证据。

### 2026-09-15｜房间763033权威牌型比较输入追踪

- 继续排查：持久化快照确认服务端当前比较目标保存为完整`QQQ10 9`，并非数据库只保存末张；历史拒绝仅记录`combination cannot beat previous`，缺少客户端实际提交组合，尚不能区分提交牌组错误和服务端识别错误。
- 追踪措施：在房间`763033`权威比较拒绝入口增加专项结构化日志，一次记录请求牌组、候选牌型/主值、上一手完整牌组/牌型/主值、座位、`stateVersion/trickId/operationId`；Poker主代码编译通过并已部署，Gateway由进程`8677`重启为`11576`，两端已重新连接。
- 未完成项：等待用户执行一次能触发问题的真实“提示→出牌”；取得`PdkAuthorityCompareTrace`后立即按唯一根因修复，最终完成前移除本专项临时日志。

### 2026-09-15｜三带二比较状态第二写入者清除

- 实测证据：房间`763033`请求`a97367d5-6737-4baa-a6f9-965f57bce29b`及`cb0eb3f5-b41e-41c0-9e50-9aa435bd3e44`实际提交`99988`（`TRIPLE_WITH_PAIR`，主体9），服务端完整比较目标为`10101077`（`TRIPLE_WITH_PAIR`，主体10）；服务端拒绝正确，客户端提示错误。
- 根因/修复：旧`OpCard`虽已禁止写公共牌节点，但仍调用`SetCardData/ClearCardData`覆盖权威规则比较状态；携带局部牌的旧事件会把完整三带二降级，使提示生成无法压过上家的组合。现`OpCard`仅保留声音、动画和私有手牌兼容，不再拥有规则比较状态写权限；显示、提示和出牌校验统一只消费`tableSnapshot.comparison`。
- 验证结果：新增“旧OpCard不得覆盖完整组合比较牌”回归约束，出牌显示/提示相关测试`60/60`通过；Creator Preview产物已自动更新并确认包含新逻辑。专项服务端日志已移除，Poker主代码重新编译并部署，Gateway由`11576`重启为干净进程`12922`，`8080/18080`均恢复监听。
- 未完成项：等待双端页面刷新加载16:37后的Preview，真实复验三带二提示不会选择更小主体、完整五张显示不被旧事件覆盖；通过后闭环。

### 2026-09-15｜三带二状态反馈循环收口

- 用户复测：上方三带二仍只露出最后一张`7`，页面连续出现“请先出牌/牌型错误”，要求逐条审计并继续解决。
- 完整结论：服务端`stateVersion=152/trickId=70`始终保存完整`10101077`，客户端在同一未变化版本连续提交`99988`；不是线程死循环，而是旧兼容字段、可变`logic.lastCardList`、自动提示和失败恢复共同形成状态反馈循环。同时Out_Card节点在资源尺寸尚未就绪时可能得到零/过小步长，五张牌重叠后只露出末张`7`。
- 架构收口：删除权威事件入口基于旧`cardList/opType`的第二次比较状态写入；新增统一`synchronizeAuthorityComparison`，手动提示、自动提示和出牌提交前均从`comparisonState`重建规则比较状态；提交前同时校验固有牌型和能否压过权威上一手，非法候选不再发送到服务端。公共牌排版增加确定的资源宽度兜底与最小`28px`步长，资源尺寸未完成时也不会五张同点重叠。
- 验证结果：比较状态单写入、旧事件禁写、三带二提示、提交门禁、公共牌时序与确定排版相关测试`62/62`通过；Creator于16:48:26自动生成新Preview脚本，晚于16:47:11源码并确认包含统一比较同步和最小步长实现。
- 未完成项：待双端页面刷新加载16:48后的Preview，真实复验`10101077`完整显示、提示不再选择`99988`、重复错误请求停止；通过后清理房间专项客户端日志并闭环。

### 2026-09-15｜跑得快大结算模板路由收口

- 用户确认：跑得快大结算唯一使用`Games/Poker/Common/Prefab/BigSettlement_0.prefab`，不得按旧模板编号生成其他Prefab路径。
- 根因/修复：房间票据携带旧共享目录模板号`BigSettleTpl_110`，结算解析器误将其作为扑克自定义模板，拼出不存在的`settlement/poker/BigSettleTpl_110`。现PDK大小结算均固定解析到已注册公共模板；旧模板号只记录迁移告警，不再改变表单键、Bundle或资源路径。
- 验证结果：资源注册表、表单映射和Prefab UUID三者一致，结算模板契约测试`8/8`通过；未修改Prefab、Scene、`.meta`、开房规则表、服务端或数据库。
- 未完成项：等待Creator当前资源任务结束并刷新Preview后，在真实跑得快终局触发一次大结算，确认不再出现缺少Prefab提示且实际挂载`BigSettlement_0`。

### 2026-09-15｜XQP出牌显示时序迁移（阶段记录）

- 备份证据：写入前已将Aoo跑得快客户端公共代码、相关配置/必要meta、Poker与Gateway源码及测试共539个文件归档到`/Users/aoo/Code/Game/BCG/AooBackup/PDK/Aoo-PDK-before-xqp-flow-rewrite-20260915-170335.tar.gz`；同目录保存文件清单、内容清单及SHA-256，校验结果为OK。
- XQP权威时序：普通有效出牌立即替换上一手；无人可压或过牌回到最后出牌人时，胜出末手保留1.5秒；按顺序展示模式在0.5秒后移入历史区；成功回复只处理本人手牌，公共牌只由服务端广播生成；重连由完整有序`playedCards/passIndexs/lastPlayInfo/playIndex`恢复。
- 阶段实现：Aoo LAST_ONLY已改为权威快照唯一桌面写入，旧`OpCard`完全惰性，旧`ChangeStatus`不再清桌；普通出牌仅显示最新完整牌组，本墩结束保留1500ms。服务端快照补充`phase/passSeats/displayMode`及比较牌`operationId`，客户端增加结构化`PdkTableTransition`日志并移除房间763033专项刷屏日志。
- 验证与阻塞：客户端聚焦测试`63/63`通过，Creator 3.8.8 Preview已热编译；服务端尚未编译部署，本机Maven 3.9.11低于项目强制`[3.9.16,4.0.0)`，真实双端时间戳E2E尚未执行，因此本阶段不得标记完成。

### 2026-09-16｜持续 develop 提交权限

- 用户授权：所有 AI 可将各自职责范围内已形成闭环、可明确归属的改动提交并推送到对应仓库 `develop`，无需逐次申请；仍禁止 `main`、强推、改写历史和夹带无关改动，每次结果必须报告。

### 2026-09-16｜选牌输入、三带二显示与出牌区生命周期闭环

- 用户要求：滑动只在实际划过的牌中选择张数最多的合法牌型；手牌外空白点击不得选牌；无人能压时赢家末手与同墩其他牌一起至少停留2秒后统一清理；成都三带二必须完整显示并可正常比较；凉山摆牌区按座位坐标累计保留至小局结束。
- 根因/结论：手势候选曾混入旧选择或牌外强制牌，空白触摸曾映射最近牌位；普通桌面牌按座位独立提前清理，且三带二字符串牌型、异步公共牌投影和Layout存在多写入/覆盖；现分别收口为触碰集合唯一候选、可见牌原生命中、权威整墩统一清理、权威比较状态单写入和凉山`ALL_IN_ORDER`独立累计投影。大厅返回后缓存数字键盘仍持有已销毁控制器的问题同步修正为生命周期和按钮所有权重新绑定。
- 修改范围：跑得快公共提示、手牌命中、权威桌面投影与地区摆牌能力；公共表单生命周期和加入房间控制器仅修复本次真实双端验收暴露的缓存键盘失效；相关单元测试及本文开发记录。未修改开房规则表、Prefab、Scene、`.meta`、数据库或其他玩法。
- 验证结果：61/61项聚焦测试通过；Creator 3.8.8热编译后，任务专属无头Chrome真实房间`345372`完成成都`555KK→99988`双端出牌，双方均完整显示五张，赢家落牌后同墩两家牌一起清理；空白手牌区点击前后均为零选择；真实鼠标滑动只在经过的`Q、JJJ、10`五张中选出全部五张。凉山`LS201`房间`158473`完成抢庄和真实出牌，`Out_Card`显示当前手，`Table_Cards`在座位0设计坐标累计完整五张并保留至小局结束。加入房间缓存键盘经返回大厅、重新输入和真实确认成功加入复验。
- 未完成项：Client全量TypeScript检查仍有3项本次改动前既存错误，分别位于`CommonPdkSwitchCoordinator.ts`、`PdkAnimationResolver.ts`和`LobbyScreenController.ts`，本次未越界修改。

### 2026-09-16｜Git 权限更正：仅明确指令或每日快照

- 更正原因：用户最新明确要求撤回 AI 自主提交权限；此前“持续 develop 提交权限”记录不再作为执行依据。
- 最终规则：只有用户针对具体任务明确说“提交”才允许提交该任务；否则仅由北京时间 `02:00` 的每日固定任务统一提交。

### 2026-09-16｜跨小局手牌误清理与成都三带二双端复验

- 用户复测：小局结束进入下一局后手牌消失，刷新后才恢复；成都三带二仍出现出牌方完整、观察端只显示最后一张的现象。
- 根因/修复：上一小局延迟清理任务和旧继续确认可晚于下一局发牌到达并清理新手牌。现由权威阶段切换统一取消旧清理，下一局开始重置演示状态并重建手牌；延迟清理执行前再次核验权威阶段，处于`COMPETE_DEALER/PLAYING`时禁止触碰手牌；旧继续确认只关闭小结算，不再拥有清理下一局牌面的权限。
- 环境更正：房间`386518`的`3008: 数据库服务暂时不可用`并非牌型错误，实际为本地`aoo-mysql`因binlog同步失败退出，导致版本仓库503及权威状态对账失败；恢复Docker Engine和`aoo-mysql`后客户端、Gateway和权威请求恢复。
- 验证结果：聚焦测试`53/53`通过；Creator 3.8.8重新导入并刷新Preview，新会话实际解析到`CommonPdkPlayController`产物`b2387ccf351b2f75e3ea9e57c859d80e4c7e24e4`。成都`CD201`真实双端房间`837801`连续完成第4局至第6局：第5局发牌后双端各16张，等待超过12秒仍保留；第6局发牌后再等待7秒仍各16张。`stateVersion=37/trickId=20/operationId=182a6343-37ec-4d10-9e73-582e7a383f06`的`99955`与`stateVersion=38/trickId=21/operationId=de60684a-8ed1-4a9b-9a58-f163d8464942`的三带二，在两端均收到同一完整5张数组、创建5个节点并保持五个不同横坐标，未再退化成最后一张。
- 未完成项：Client全量TypeScript检查仍有3项既存错误，位于`CommonPdkSwitchCoordinator.ts`、`PdkAnimationResolver.ts`和`LobbyScreenController.ts`；本次未扩大范围处理。未执行Git提交或推送。

### 2026-09-16｜本家飞牌间距连续收拢

- 用户要求：多张牌从手牌区飞向出牌区时，间距必须随位移连续缩小；到达出牌区时已经等于最终间距，飞行中和落地后不得突然再次收缩或展开。
- 根因/修复：本家飞牌此前统一飞向固定`18px`临时间距，落地后再由`Out_Card`按正式布局重排，形成先挤成一叠、再跳变到最终间距。现每张飞牌在起飞前就由目标`Out_Card`的卡宽、`spacingX`、方向和世界缩放计算唯一最终坐标，Tween直接插值至对应落点；落地重挂父节点只切换所有权，不再改变几何。
- 验证结果：聚焦测试`54/54`通过；Creator Preview产物确认包含新目标坐标数组。成都真实Canvas一次6张出牌逐帧采样显示，飞牌间距从手牌原始的`126.4/63.2`连续收敛为统一`60.6`，最后3帧均保持`60.6`；权威落地后6个`Out_Card`节点间距仍全部为`60.6`，不存在中途或落地后二次收缩。
- 修改范围：仅`CommonPdkPlayController.ts`、对应聚焦测试与本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。未执行Git提交或推送。

### 2026-09-16｜末手自动出牌隐藏手动操作

- 用户要求：系统确认剩余整手牌将自动出牌后，不得继续显示或允许点击“提示”“出牌”；普通手动出牌仍保持按钮直到权威操作权转移。
- 根因/修复：自动末手此前只由延迟定时器持有状态，权威刷新会重新显示操作按钮，且发起请求前过早清除自动标记。现统一操作栏渲染直接消费`autoPlayInFlight`，从识别合法整手、600毫秒物理出牌延迟到权威请求结束全程隐藏两按钮；失败或操作权失效后由当前权威状态恢复，手动出牌逻辑不变。
- 验证结果：聚焦测试`55/55`通过；Creator 3.8.8重新导入并刷新Preview。成都`CD201`真实双端房间`318120`在`stateVersion=9/trickId=3/operationId=1-9-play`识别最后两张`[412,112]`自动出牌，连续采样均为`autoPlayInFlight=true`且`Btn_Hint/Btn_Play`不可见；权威完成后手牌为0，两按钮仍不可见。Client全量TypeScript检查仍只有3项既存错误，未新增错误。
- 修改范围：仅跑得快公共出牌控制器、对应聚焦测试和本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端或数据库。未执行Git提交或推送。

### 2026-09-16｜首出与滑选统一按合法张数最多

- 用户最终规则：接牌提示才保留炸弹优先；自己主动首出和滑动选牌均只以“合法可出组合张数最多”为唯一优先标准。滑动候选严格限制在本次实际划过的牌内。四带二同时允许“四张主体+任意两张”和“四张主体+两对”。
- 实现收口：提示排序器的首出/滑选模式只比较候选张数，炸弹拆分、炸弹消耗、牌型结构和剩余手数不参与排序；接牌模式继续执行炸弹保护。四张主体作为合法四带二使用时不再误判为拆炸弹；成都规则默认及发布映射统一为两种附件形态均可，六张与八张牌型分别完整解析、提示和校验。
- 验证结果：客户端提示、滑选、四带二及权威规则聚焦测试`65/65`通过；Poker生产代码与成都定向规则测试通过。Gateway、Hall已重建重启，全部本地核心服务健康。Creator 3.8.8已刷新Preview，15:50生成脚本晚于源码并包含新排序和四带二逻辑。成都真实双端房间`140090`使用账号45、46开始第1局：本家16张包含`KKK Q 10×3 9×2 8×3 7×2 6 5`，真实鼠标从第一张划到最后一张后只抬起合法最大六张顺子`10-9-8-7-6-5`；真实点击“提示”同样抬起六张，没有选择单牌、对子或炸弹结构。
- 修改范围：跑得快公共提示/滑选、成都Poker规则默认与发布映射、对应测试和本文开发记录；未修改开房规则表、Prefab、Scene、`.meta`、数据库或其他玩法。未执行Git提交或推送。

### 2026-09-16｜底部任意位置滑动选牌不中断

- 用户复测：手势经过首张A、从左向右越过末张9或在牌列边缘起步时，滑动选牌不生效；要求底部区域任意位置开始和经过均可形成滑选。
- 根因/修复：牌外位置原先只依赖`Hand_TouchArea`的Cocos节点事件，但浏览器Preview的同层节点会抢占空白位置命中，导致手势根本没有进入选牌控制器。现浏览器由`document`捕获原生指针并按真实Canvas坐标归一化，只要按下位于底部手牌带就持续捕获至松开；移动位置按横坐标投影到最近牌位，牌列左右空白、牌间空隙和首末牌外侧均形成连续区间。单击仍使用单张牌精确命中，空白单击不会选牌；提示、出牌等按钮继续优先。
- 验证结果：聚焦测试`66/66`通过；Creator 3.8.8清理并重建代码缓存，16:16后的Preview脚本确认包含文档级指针捕获、牌位投影和重复事件抑制。成都真实房间`140090`刷新恢复后，在完全不触碰牌面的`y=180`底部空白横线上分别从`x=80→1480`和`x=1480→80`真实鼠标滑动；两次轨迹均连续覆盖当前全部`0..14`手牌索引并提交合法候选，不再因首牌、末牌或空白位置中断。
- 修改范围：仅跑得快公共手势输入、对应测试和本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。未执行Git提交或推送。

### 2026-09-16｜最后两手最大牌优先与快速出牌防重叠

- 用户要求：手牌按地区规则只剩两手可出完时，提示优先选择单张、对子、三带附件等任意位置含地区最大牌的合法整组；上一手落地未满2秒又真实点击出牌时，立即清空即时出牌区再飞新牌。
- 根因/结论：旧实现只特判两张单牌含2，未覆盖成都对子A、凉山A或三带附件A；两秒保留计时也缺少真实再次点击的防重叠例外。现统一从权威`deckCards`按组合内点数张数推导最大点数，并只在剩余牌本身可组成一手时提升该候选；手动合法出牌入口在未满2秒时清理全部`Out_Card`，不触碰凉山`Table_Cards`。
- 修改范围：跑得快公共提示排序、公共出牌展示生命周期、聚焦测试、通用规则和本文档；未修改开房规则表、Prefab、Scene、`.meta`、服务端或数据库。
- 验证结果：提示与展示聚焦测试`62/62`通过；Creator 3.8.8清理代码缓存后，Preview产物确认包含新逻辑。成都真实双端Canvas连续自动操作中，两端均实际触发`PdkRapidPlayClear`后再提交下一手，结束时全部即时出牌区为空且没有叠牌残留。
- 未完成项：随机实局尚未发到可明确截图证明“最后两手为对子A/单2或带A三带”的指定牌面；该排序已由权威牌库和牌型引擎的定向测试覆盖。

### 2026-09-16｜接牌最少散单与单击精确命中

- 用户要求：接牌提示选择出牌后单张最少的合法牌型；截图牌面应优先`9 10 J Q K A`而不是破坏三张8的`6 7 8 9 10 J Q`。单击哪张牌就只切换哪张牌，第一次选中、第二次取消。
- 根因/结论：旧排序虽然保护三张结构，但未把三张后续可吸收附件折算进有效散单数；浏览器PointerEvent已经换算为Creator UI坐标后又进入屏幕坐标命中，产生横向偏移。现多张接牌按地区三带能力计算有效散单并提前比较；DOM指针改用UI空间逐张逆层级精确命中，Cocos原生事件仍保留相机屏幕命中。
- 修改范围：跑得快公共提示评分、公共手牌输入、聚焦测试、通用规则和本文档；未修改开房规则表、Prefab、Scene、`.meta`、服务端或数据库。
- 验证结果：截图同牌面回归、原有单张保顺子、炸弹保护、点击切换及展示生命周期共`64/64`通过；Creator 3.8.8已重建代码缓存，Preview产物包含`prioritizeLooseSingles`和`cardIndexAtUi`。
- 未完成项：当前任务专属无头Chrome中的既有对局均已结束且无有效操作按钮，尚未生成新一局真实Canvas逐张点击截图证据。

### 2026-09-16｜首出同张数按有效散单最少

- 用户要求：自己主动首出点击“提示”时仍先选择合法出牌张数最多的组合；张数相同再选择出牌后有效散单最少的组合，例如五张三带二剩3张散单、五张顺子剩2张散单时优先顺子。
- 实现收口：公共排序器新增显式的“同张数最少有效散单”开关，仅由首出提示缓存启用；滑动选牌继续只按划过牌内合法张数最多排序，同张数保持候选原顺序，不复用提示按钮的二级评分。有效散单沿用地区三张附件容量折算。
- 验证结果：聚焦测试`65/65`通过，覆盖五张三带二与五张顺子的正向排序，并反向确认滑动同张数不受影响；Creator 3.8.8已清除代码缓存并刷新Preview，预览产物包含新开关。任务专属无头Chrome当前停留`LoginScene`，无可操作牌局，因此真实Canvas指定牌面复验待新局继续。
- 修改范围：仅跑得快公共提示排序、公共出牌控制器、聚焦测试、通用规则和本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。未执行Git提交或推送。

### 2026-09-16｜牌面单击禁止误清全部选择

- 用户反馈：点击“提示”抬起一组牌后，再单击某张可见手牌时偶发全部取消；要求所有有牌区域均不得触发整组选牌清空。
- 根因/修复：浏览器PointerEvent已正确切换单张后，同一次松手仍可能被Creator房间层重复接收；房间层使用另一套缩放后的屏幕坐标复查时会偶发误判为牌外并执行`ChangeSelectCard([])`。现同一浏览器手势由抑制窗口阻止房间层二次解释；独立房间层松手同时用UI坐标和屏幕坐标做精确牌面命中，任一命中真实牌即禁止清空全部选择。
- 验证结果：聚焦测试`65/65`通过，覆盖重复手势去重、UI/屏幕双坐标牌面保护及真正牌外清理入口；Creator 3.8.8已清除代码缓存并刷新Preview，预览脚本确认包含新门禁。当前无头Chrome仍停留`LoginScene`且无可操作牌局，真实提示后连续逐张点击验收待新局继续。
- 修改范围：仅跑得快公共手牌输入、聚焦测试、通用规则与本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。未执行Git提交或推送。

### 2026-09-16｜单张接牌统一最少散单优先

- 用户确认：接单提示同样必须先保留出牌后有效散单最少的方案；有效散单相同时，从能压住桌面牌的最小单张开始提示。成都不认可三张顺子，凉山认可三张顺子。
- 根因/修复：公共控制器此前仅在桌面待压牌超过一张时提前比较有效散单，单张接牌依赖后续结构评分，规则入口不完整。现所有接牌均启用有效散单第一优先级，点数仍作为同分后的后置比较；顺子结构严格消费权威`minimumStraightLength`。
- 验证结果：聚焦测试`66/66`通过。截图同牌面`2 K Q 10 9 8 55`定向结果：成都最短顺子5张时，同散单候选从最小可压牌9开始；凉山最短顺子3张时保护`8 9 10`，从同等最少散单候选Q开始。Creator 3.8.8已清除代码缓存并刷新Preview，产物确认包含`prioritizeLooseSingles: targetCount > 0`。
- 修改范围：仅跑得快公共提示入口、聚焦测试、通用规则与本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。未执行Git提交或推送。

### 2026-09-16｜小局准备、发牌动画、必带提示与末手操作收口

- 用户要求：小局结束2秒只清桌面牌，继续玩家逐个显示准备图标且最后一人也保留到发牌；恢复既有发牌动画节点；指定必带牌只约束点击提示；整手自动出牌/接牌不显示提示和出牌按钮；最后两手最大牌必须按地区规则最大值，非规则最大的一对K不得抢过`667788`。
- 根因/修复：延迟清理错误调用`hideReadyStates()`；权威适配只投影`seat.ready`而漏掉`seat.continued`；凉山已在`COMPETE_DEALER`发牌但动画门禁只接受`PLAYING`；操作栏渲染早于异步末手识别；最大牌回归缺少非最大对子对六张连对的反例。现分别移除准备清理、合并继续状态、在抢庄发牌边界调用既有`PokerDealNodeAnim`、同步预判整手合法并隐藏按钮，并增加地区牌库最大值反例。手动`outCard`保持不读取必带牌，必带牌仅过滤提示缓存。
- 验证结果：聚焦测试`69/69`通过，覆盖继续图标、凉山抢庄前发牌动画、必带牌仅提示、末手按钮同步隐藏和`667788`优先于非最大`KK`。Creator 3.8.8已清除代码缓存并刷新Preview，产物包含`automaticWholeHand`、`COMPETE_DEALER`动画门禁和`seat.ready || seat.continued`。
- 未完成项：当前任务专属无头Chrome仍无可操作的新局，尚未完成连续两局的真实Canvas逐人继续、发牌动画及末手按钮验收。
- 修改范围：跑得快公共控制器、权威视图适配、聚焦测试、通用规则与本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端或数据库。未执行Git提交或推送。

### 2026-09-16｜继续图标清理时点更正

- 用户更正：逐个点击继续仍逐个显示准备图标；最后一名点击继续后，不再等发牌阶段，固定延迟1秒统一清除全部继续/准备图标。桌面牌继续独立按最后一手落地满2秒清理。
- 修复：权威快照检测所有已入座玩家`isContinue=true`后启动一次性1000毫秒计时；到时统一调用头像准备状态清理并记录当前局键，后续同局快照不得把图标重新刷回。该计时不读取`PLAYING`、`COMPETE_DEALER`或`cardsDealt`，与发牌阶段完全解耦。
- 验证结果：聚焦测试`69/69`通过，确认桌面2秒清理段不触碰准备图标、最后继续1秒清理段不判断发牌阶段；Creator 3.8.8已清除代码缓存并刷新Preview，产物包含`CommonRoomContinueReadyClear`及同局防回刷门禁。
- 修改范围：仅跑得快公共回合展示、聚焦测试、通用规则与本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端或数据库。未执行Git提交或推送。

### 2026-09-16｜滑选缺少指定必带牌文字提示

- 用户要求：首局存在指定必带牌时，滑动选牌未包含该牌须显示`必包含xx牌`，例如`必包含黑桃3`。
- 实现：滑动提交严格检查本次实际划过的物理牌集合；缺少权威`activeRequiredFirstCard`时取消预览和本次滑选，按牌值动态生成花色点数提示，不从滑动范围外补牌。点击提示的必带候选规则与手动出牌入口保持不变。
- 验证结果：聚焦测试`70/70`通过；Creator 3.8.8已清除代码缓存并刷新Preview，产物包含`required-card-missing`分支。
- 修改范围：仅跑得快公共滑选输入、聚焦测试、通用规则与本文开发记录；未修改Prefab、Scene、`.meta`、开房规则表、服务端或数据库。未执行Git提交或推送。

### 2026-09-16｜结算阶段准备状态隔离

- 用户反馈：小局结算中，对方尚未点击继续却已经显示准备图标；要求结算时清除上一局准备状态的显示影响。
- 根因/修复：权威视图适配器此前用`seat.ready || seat.continued`投影准备图标，导致上一局持久化的`ready`泄漏进结算页。现按阶段隔离：`FINISHED/DIRECT_WIN`阶段仅用本次结算的`seat.continued`，其他阶段仍使用`seat.ready`；未点击继续的玩家不显示，最后一人点击后的1秒统一清理逻辑保持不变。
- 验证结果：聚焦测试`70/70`通过；Creator 3.8.8已清除代码缓存并刷新Preview，生成产物包含阶段化`readyState`和`roomReady: readyState`。未修改Prefab、Scene、服务端、数据库或开房规则表，未执行Git提交。

### 2026-09-16｜最后继续玩家图标完整显示1秒

- 用户反馈：最后一名玩家点击继续后没有显示自己的准备图标；要求最后一人同样先显示，再统一保留1秒后清理。
- 根因/修复：原计时从权威包到达时立即开始，而头像按座位异步渲染；头像资源加载超过计时时间时，统一隐藏会早于最后一人的图标落地。现将计时起点移到本次全部头像渲染完成之后，确保最后一人的准备图标已实际显示，再保留完整1000毫秒统一清理。
- 验证结果：聚焦测试`70/70`通过；Creator 3.8.8已清除代码缓存并刷新Preview，生成产物包含`renderHeadsThenScheduleContinueClear`调用链。未修改Prefab、Scene、服务端、数据库或开房规则表，未执行Git提交。

### 2026-09-16｜失败出牌不得提前清桌面牌

- 用户现场：房间`229014`中，一端仍显示对方已出的五张牌，接牌端点击出牌后提示牌型错误并丢失对方桌面牌；控制台记录`[PDKActionError] 出牌 Error: REQUEST_INTERRUPTED_NOT_REPLAYABLE`。
- 根因/修复：快速出牌逻辑在`play_req`提交前调用全桌即时牌清理，请求随后被拒绝或中断时只恢复手牌，没有权威提交可驱动桌面牌重建。现改为请求前只快照旧牌的座位、操作编号和展示时间；仅在Authority确认成功后清理仍匹配快照的旧牌，新操作已替换的座位不会误删。失败/中断不清任何桌面牌，手动与自动最后一手统一执行；凉山累计`Table_Cards`不受影响。
- 验证结果：聚焦测试`70/70`通过，覆盖成功确认后清旧牌、确认前零清理、自动最后一手不绕过及新操作防误删；Creator 3.8.8已清除代码缓存，Preview产物包含`captureRapidPreviousPlay`和`clearRapidPreviousPlayAfterCommit`。现场原房间使用旧脚本，仍需新局双端真实Canvas复验。
- 修改范围：仅跑得快公共出牌展示控制器、聚焦测试、通用规则和本文记录；未修改Prefab、Scene、服务端、数据库或开房规则表，未执行Git提交。

### 2026-09-16｜复合牌型接牌补齐独立炸弹候选

- 用户反馈：接上家的复合牌型时，手中完整炸弹没有单独提出来；截图中四张K未进入提示循环。
- 根因/修复：单张接牌分支会追加`GetZhaDanTip()`，复合牌型分支却只合并旧候选与“和上家相同张数”的物理子集，四张炸弹无法进入五张三带二等候选。现所有复合接牌统一追加权威规则识别的完整炸弹，排序继续保持同牌型优先、炸弹最后兜底；炸弹不拆分、不补附件，炸弹接炸弹仍由区域规则校验大小。
- 验证结果：聚焦测试`70/70`通过，Creator 3.8.8已清除代码缓存，Preview产物确认复合分支调用`pushTipCandidates(candidates, GetZhaDanTip())`。待新局真实Canvas使用四K接五张复合牌复验。
- 修改范围：仅跑得快公共接牌候选、聚焦测试、通用规则和本文记录；未修改Prefab、Scene、服务端、数据库或开房规则表，未执行Git提交。

### 2026-09-16｜复合接牌炸弹 Safari 实局验收

- 真实验收：用户授权操作 Safari 分屏现局；房间`586745`权威桌面为`QQQ66`，本家手牌含四张K。旧页面点击提示未抬起；刷新加载最新Preview并按权威状态恢复同一房间后，再次真实点击“提示”，四张K整体抬起，A、J、8、7均未被选中。
- 结论：复合牌型追加独立炸弹候选已在真实Safari Canvas闭环通过；此前“仍一样”是分屏Safari保留修改前脚本，不是最新候选逻辑继续失效。未点击出牌，未改变该手权威牌局结果。

### 2026-09-16｜岗位规范精简与规则分类动态调取

- 用户要求：精简整理跑得快 AI 文档，合并去重，并新增“没有地区专属玩法规则，全部按分类动态调取”的规范。
- 根因/结论：现行条款存在代码归类、文档协作、任务队列和验收要求重复，记忆中还混有已完成或被更正状态；现已合并同义规则，并明确玩法编码只选择规则分类组合，客户端与服务端统一消费服务端权威动态规则快照，禁止地区专属规则实现和地区硬编码。
- 修改范围：仅本文档第 2 至第 4 章、第 7 章及本开发记录；第 6 章既有历史记录未删除或改写，未修改代码、规则表、正式玩法文档、资源或配置。
- 验证结果：现行规范无“当前地区规则”入口，活动队列仅保留尚未闭环事项；固定七章及顺序不变，“记忆”仍为末章。
- 未完成项：无。


### 2026-09-18｜Bug 根因修复原则

- 用户要求：所有 AI 处理 Bug 禁止采用补丁式修复，必须先理清完整逻辑和根因，再修正权威业务路径。
- 执行结论：禁止通过空值兜底、吞错、延时重试、硬编码特例、兼容分支或并行旧逻辑绕过根因；修复后必须清理错误和临时逻辑，并完成针对性与真实流程验收。
- 修改范围：仅岗位规范；未修改业务代码、资源、配置、数据库或规则表。

### 2026-09-19｜Git 后台工具与快照时间更正

- 用户要求：记录 Sourcetree、系统 Git、三个固定仓库及 `develop` 分支，并规定 AI 默认只使用后台 `/usr/bin/git`，不得自行启动 Sourcetree 或占用用户屏幕。
- 更正原因：此前现行条款及历史记录中的每日快照时间为 `02:00`；按最新明确要求更正为北京时间 `00:00`。历史记录保留但不再作为执行依据。
- 执行结论：工具与仓库信息不构成拉取、提交或推送授权；除 `00:00` 固定快照外，仍须用户针对具体任务明确说“提交”。
- 修改范围：仅本文档“执行与验收”“开发记录”和“记忆”；本次未执行任何 Git 拉取、提交、推送或分支操作。

### 2026-09-19｜无效过渡引用清理

- 用户要求：删除没有使用的过渡代码和引用，重新梳理，但不得改变功能与业务逻辑。
- 根因/结论：控制器残留一个仅写不读的展示编号、一个未使用触点局部变量及两个无调用私有方法；部分源文本测试仍引用已经迁移的旧方法、字段和展示实现。现仅移除无引用运行时代码，并将测试断言对齐现行唯一实现，没有调整提示、校验、出牌或展示业务分支。
- 修改范围：`CommonPdkPlayController.ts`、两份跑得快专项测试及本文档；未修改规则表、Prefab、Scene、`.meta`、服务端、数据库或其他玩法。
- 验证结果：提示、权威规则与自动操作关联测试 `273/273` 通过；控制器未使用成员诊断为 `0`，控制器与提示排序器语法诊断均为 `0`。四端 Preview 已绕过缓存强制刷新，C/D 首次刷新出现的旧分块引用经各自重载后消失，最终 A/B/C/D 均无错误层，五个本地入口均返回 HTTP 200。
- 未完成项：无。

### 2026-09-19｜托管占位误写回放身份修复

- 用户现场：房间内真实玩家离开后由托管继续小局，后续房间命令重复提示 `3008: cannot atomically commit room command`。
- 根因/修复：运行中房间用负数表示已离开座位的托管占位，回放持久化却将其当作玩家身份写入无符号 `BIGINT`，导致原子事务整体回滚。现回放参与者及私有视角只接受大于零的持久玩家 ID；正式迁移清理历史 `player_id=0` 污染记录，并为参与者与私有回放所有者增加身份约束。
- 验证结果：受影响 Bootstrap/Gateway 模块构建通过，新迁移在正式库同构临时库通过并由 Flyway 应用到 `aoo_login_local`，清理 7 条无效占位参与记录，4 个约束生效。网关仅重启受影响服务后全部健康；真实恢复房间 `985315` 提交到 `stateVersion=777`，快照仍含负数托管座位，但新回放仅写入真实玩家 `381`，无新的 3008、越界 ID 或私有回放所有者异常。
- 已知无关阻断：全量聚合单测仍被 AY/HB 跑得快已有测试缺类阻断；空白库逐版迁移校验仍在已有 `V20260911_01` 失败。两项都早于本次改动，本次未越界修改。未执行 Git 提交或推送。

### 2026-09-19｜低位长连对与拆高对顺子的冲突收口

- 用户现场：手牌 `2、AA、KK、Q、J、1010、9、77、66、55` 主动提示未选择 `556677`；要求完整低位连对优先。
- 根因/修复：合法候选本身已包含 `556677`，但“对子占优时固定最低独立对子”先将 `1010` 钉在首位，后续“最长顺子提升”又把拆开 `1010、KK、AA` 的 `9-10-J-Q-K-A` 覆盖为首项。现对子恢复链先按剩余手数、散单数、当前出牌张数比较完整对子牌族；最终顺子冲突按既定两张容差执行：双方均为最少手数、散单不增加时，优先打最大点不超过 10 的完整低位连对，不用高顺子拆开多组高对。
- 验证结果：加入手工全候选和真实规则识别/全秩组合枚举两层回归，高顺子明确进入合法池后首项仍为 `556677`；提示、权威规则与手牌交互相关测试 `249/249` 通过。Creator 3.8.8 已重新导入脚本并生成含新门禁的 Preview chunk，四端容器逐端绕过缓存重新加载。
- 修改范围：仅跑得快公共提示排序器、两份专项测试及本文开发记录；未修改 Prefab、Scene、`.meta`、开房规则表、服务端、数据库或其他玩法。未执行 Git 提交或推送。

### 2026-09-19｜自动提示牌快速换出后的悬空修复

- 用户现场：接牌时自动提示抬起一张 `8`，玩家快速改选并打出另一张牌后，幸存的 `8` 一直悬在手牌上方且无法正常落下。
- 根因/修复：取消提示的下降动画尚未结束时立即出牌，手牌收拢读取了节点动画中的临时 `Y`，并将其保存为新的基础位置。现手牌位置展示器集中提供规范基础坐标；收拢只采用新槽位的 `X/Z`，`Y` 始终继承稳定基础值，不再把选中/取消选中的动画中间帧固化。
- 验证结果：新增精确时序回归“自动提示牌下降未完成即快速打出另一张牌”，定向测试 `1/1` 通过；Creator 3.8.8 Preview 重新导入后的分块包含稳定基准门禁，CDP 无缓存重载无运行时错误，本地 `7456/7457/7458/7460` 均返回 HTTP 200。
- 工作区说明：验证期间 `CommonPdkPlayController.ts` 被另一条外部写入替换为旧版实现，现有完整交互套件出现 43 项与本问题无关的历史断言失败；本次只在当前磁盘版本兼容修复悬空问题，未擅自恢复或覆盖外部改动。
- 修改范围：仅跑得快公共手牌位置展示器、当前控制器收拢坐标、专项测试及本文记录；未修改 Prefab、Scene、`.meta`、规则表、服务端或数据库，未执行 Git 提交或推送。

### 2026-09-19｜拖选取消旧接口运行错误修复

- 用户现场：四端预览在抬牌后取消拖选时同时报错 `this.cards.preview is not a function`，堆栈指向 `cancelDragSelection`、`onDomPointerUp/onHandMouseCancel`。
- 根因/修复：当前 `CardPresenter` 的瞬时拖选接口已统一为 `previewDrag`，但 Creator Preview 分块仍缓存控制器旧调用 `preview`。现控制器四个拖选预览/取消入口均调用 `previewDrag`，并通过 Creator 3.8.8 正常重新导入脚本生成新 Preview 分块；未直接修改或伪造缓存。
- 验证结果：新分块已无 `cards.preview(`，四处均为 `cards.previewDrag(`；CDP 对 `7456` 无缓存重载后无运行异常且旧错误层消失，`7456/7457/7458/7460` 均返回 HTTP 200。专项文件中另有 3 条既有交互断言与当前控制器结构不一致，本次未越范围改写测试或业务逻辑。
- 修改范围：仅跑得快公共控制器的拖选取消接口说明与本文记录；未修改 Prefab、Scene、`.meta`、规则表、服务端或数据库，未执行 Git 提交或推送。

### 2026-09-20｜凉山提示问题分类边界记录

- 用户要求：公共跑得快提示阶段告一段落；后续反馈属于凉山 `LS201` 现场玩法差异，必须单独分类，禁止直接融合进公共跑得快提示逻辑，并且在用户再次明确下令前不得开始分析或修改问题。
- 执行边界：公共提示实现保持冻结。后续需求先映射为服务端权威规则分类及规则快照能力，由 `LS201` 选择对应规则组合；不得新增按地区名称或 `LS201` 编码硬判断的客户端分支，不得影响 `CD201`、`NJ201` 或其他规则组合。
- 当前状态：仅记录，任务暂停；未分析具体牌例，未修改代码、资源、配置、规则表、服务端或数据库，未执行测试、构建、提交或推送。

## 7. 记忆

- 规则模型：没有地区专属玩法规则；所有差异均为可组合规则分类，由服务端按玩法编码动态选择并下发权威快照，各端统一消费，禁止地区硬编码和规则副本。
- 稳定交互：配置秒数原值全链路贯通；凉山累计桌面牌保留至小局结束；提示单牌只抬一张；无合法接牌时自动过；`Btn_Back` 未开局直接退出、游戏中禁止退出并提示“游戏中不能退出”。
- Creator 权威：仅使用当前磁盘和 Creator 中现存内容；用户删除的资源或节点不得从历史来源恢复、重建或增加兼容映射，确需新增时先报告节点契约并等待用户创建。
- 账号隔离：本地测试仅使用账号 `41` 至 `50`、密码 `1`；不得操作其他岗位账号、会话或房间，`51` 至 `100` 未获分配不得使用。
- Git 操作原则：三个固定仓库目标分支均为 `develop`；默认使用后台 `/usr/bin/git`，不启动 Sourcetree；工具信息不代表授权，只有用户明确要求“提交”或北京时间 `00:00` 固定快照才可提交推送。
- 执行状态只以本章“待处理队列”为准；第 6 章仅为历史，已完成或被更正事项不得重新执行。

### 待处理队列

- `LS201 提示规则分类问题`：来源=用户；状态=暂停；职责=凉山现场提示差异映射为权威规则分类，公共提示保持冻结；安全断点=仅完成边界记录，等待用户明确指令后才开始分析和修改。
- `QuickText 跨客户端展示复验`：来源=真实双端验收；状态=阻塞；职责=跑得快聊天全链路；安全断点=广播实现、Gateway 聚焦测试 `5/5` 和 LS201 唯一发送请求已完成，登录输入链恢复后仅补另一客户端收到并展示证据。
- `NJ201`：来源=用户；状态=暂停；职责=不执行；安全断点=仅用户重新指定后恢复。
- `CD201/LS201 CreateButton 入房复验`：来源=登录大厅真实复验；状态=阻塞；职责=跑得快双端入房验收；安全断点=`GameRoom2D` 资源归属已闭环，登录输入链恢复后只续跑真实流程。
- `提示与手牌交互真实回归`：来源=用户连续复测；状态=阻塞；职责=公共提示排序与手牌输入；安全断点=候选生成、结构保护、散单排序、最大牌整组、首出排序、单牌精确点击、提示后继续点牌和必带牌滑选提示均已完成代码、聚焦测试及 Creator 同步，待可操作牌局后只补真实 Canvas 证据。
- `出牌区权威显示真实回归`：来源=用户连续复测；状态=阻塞；职责=公共出牌展示；安全断点=按操作权且至少展示两秒、快速回转、旧操作异步覆盖、远端多牌重叠、提交失败不得清桌面牌均已完成代码和静态验证，待可操作双端牌局后只补真实时序与显示证据。
- `继续/准备图标时序真实回归`：来源=用户复测；状态=阻塞；职责=公共结算与回合展示；安全断点=结算阶段仅消费本次 `continued`，最后一人头像渲染完成后保留一秒再统一清理，代码、`70/70` 聚焦测试及 Creator 同步已完成，待双端连续两局实测。
