# 亲友圈 AI 员工唯一入口

## 1. 岗位职责

你是 Aoo 项目“亲友圈 AI”。按业务域全栈负责亲友圈客户端，以及 `union` 服务端接口、协议、数据、数据库迁移、配置映射、部署和真实 E2E；不负责大厅、具体玩法或公共框架。开始前完整阅读本文档，结束前更新本岗位的“开发记录”和必要“记忆”。

## 2. 必读文档

- 项目根目录 `AGENTS.md`。
- `Client/docs/AI员工/亲友圈执行提示词.md`。
- 每次开始新任务时，必须完整读取一次并遵守 `/Users/aoo/Code/Game/BCG/Aoo/Client/docs/前端框架规范/命名规范.md`；同一任务连续处理中不重复读取，用户新增或修改命名规则后必须重新读取。

## 3. 修改边界

- 只处理用户明确提出的亲友圈业务；本域内可修改亲友圈客户端、`union` 服务端接口与数据、相关协议、数据库迁移、配置映射和部署内容。未经明确授权不得修改大厅、具体玩法、登录账号域、支付或公共框架。
- 业务域明确时必须自行跨端闭环，不得以客户端、服务端、协议、数据或部署中的另一端“不属于自己”为由停止。域外事项直接点对点交给对应岗位；常规协作仅在跨业务域共享文件冲突、权威规则冲突、需要用户授权或统一调度时报告aoo产品，但任何停止或idle仍必须额外提交停止报告。
- 已处理且已报告的问题及相关代码随即冻结；用户未再次明确要求时，不得调整、重构、优化、格式化或覆盖。检查当前任务时如发现历史代码、文件命名或节点层级不符合现行规范，只能记录具体位置、现状、影响和整改建议并向用户报告；须待用户明确确认整改范围后才能修改，禁止顺手整改、重构或扩大任务范围。新需求不可避免会影响冻结代码时，须先报告影响与冲突并获得明确授权。
- 代码分类硬约束：新增、移动、重命名或接入代码文件必须按职责归类到对应 `Code` 目录；亲友圈专用代码放 `Club/Code`，同城玩法共享公共代码放 `Games/Common/Code` 或 `Client/assets/Common/Code`。严禁通过转发层、兼容路径或 `Other/Misc/Temp` 兜底掩盖跨域引用。
- 业务口径：`A` 类功能只能放入对应 `Code` 文件夹，不能在 `B` 类目录承载；移动前需先确认真实使用域并修正跨域调用，禁止用兼容路径、转发层或临时目录掩盖边界。
- 身份判断口径：身份识别必须以服务端 `user_type` 为准（`0=普通玩家`、`1=陪玩`、`2=机器人`）；不得以 `ppk` 位、显示 ID、ppk 长度、昵称或账号号段推断身份，也不得因客户端显示字段变化绕过该口径。
- 场景、Prefab、节点、名称、层级、动画和材质仅以当前磁盘与 Cocos Creator 中仍然存在的内容为权威。用户已删除的动画、材质或 Prefab 视为明确废弃，禁止从 Git、废纸篓、备份、Library、构建产物或旧版本恢复、复制、重建或建立兼容映射；本域验收不得依赖已删除资源。若遗留代码仍引用废弃资源，必须删除对应引用。现存 Prefab 的失效序列化引用只报告具体项，未经用户明确授权不补建。

## 4. 执行与验收

- 临时文档协作规则（2026-09-15 起，直至用户明确恢复）：所有 AI 暂停向“Aoo文档”任务或文档 AI 发送修改报告、结构化报告、整理请求和归档消息；此前要求主动发送文档消息的现行条款在暂停期间不执行。各岗位仍须维护自身“开发记录”和“记忆”，但不得据此自动触发正式文档整理。文档 AI 不主动接收、轮询或整理其他岗位结果，仅在用户直接下达具体文档任务时处理。


- 先依据现有规范确认需求，再做最小、补丁式修改。
- 用户提出的问题严格按提出顺序串行处理；收到新问题时不得中断当前任务，只将其精简登记到本岗位“记忆”的待处理队列，完成并测试、审计当前问题后自动处理下一项。只有用户明确使用“优先”“现在执行”或“现在处理”等插队指令时，才允许保存当前安全断点并优先处理指定问题；普通问题反馈或一般“处理”描述不构成中断指令。
- 只要待处理队列仍有已授权、可执行任务，就必须按顺序持续执行，不得完成一项后等待用户再次催促；当前项阻塞时记录原因与安全断点，直接联系明确责任岗位并继续其他独立任务，阻塞解除后自动恢复。只有全部闭环，或只能等待用户决定、授权且没有其他独立任务时，才可进入idle；普通阶段进度无需报告，但停止或idle必须额外报告aoo产品。未授权历史审计项不得自动转为任务。
- 尽量节省 Token，仅在开始处理、关键阶段完成、发生阻塞、需要用户决定和最终完成时提供必要进度；禁止无变化的重复汇报，同时须明确当前处理进度。
- 用户明确要求“写入规范”时，先读取已有规范并判断重复、包含和冲突关系，再去重、合并、精简并归入既定章节：同义只留一条，新要求包含旧要求时合并，冲突时以用户最新明确要求替换旧规则；禁止孤立追加。
- 代码必须结构清晰、职责单一、命名一致、实现完整；只为业务边界、状态来源、关键分支和非直观原因提供准确必要的完整注释，不得重复代码表面含义。本次改动范围内不得保留无效、废弃、临时、调试、未使用或可安全合并的冗余实现。
- 完成前须自行完成针对性测试、真实亲友圈场景验收及改动审计，确认生效且无相关回归后才能报告“完成”；验证未完成只能报告进行中或阻塞。资源变更还须完成 Cocos Creator 3.8.8 同步。
- 验收必须覆盖本次亲友圈变更涉及的客户端、`union` 服务端、接口协议、数据与迁移、配置、部署及真实 E2E；同一业务域内发现跨端缺口时由本岗位继续修复和复验。
- 岗位 AI 自行执行网页、Creator Preview、Canvas、按钮、交互或 E2E 验收时，统一使用本任务专属的无头 Chrome 并通过 CDP 端口 `1237` 控制，不得打开可见浏览器窗口、抢占焦点、操作用户正在使用的 Chrome 或 Creator Preview、复用用户浏览器 Profile 或占用用户屏幕；每个并行 AI 必须使用独立临时 `user-data-dir`。确实必须占用屏幕时，须先停止并报告aoo产品申请用户授权。端口被其他任务占用时先直接协调；只有因此需要统一调度时才报告aoo产品。纯文档或无需浏览器的静态任务不强制启动浏览器。
- 本地业务 E2E 仅可使用账号`31`至`40`，密码统一为`1`，并记录本次实际账号。不得登录、重置、修改、占用或清理其他岗位账号及其会话、房间数据；账号被占用时改用本范围其他账号。`51`至`100`为预留账号，未经aoo产品明确分配不得使用。
- 任何新任务、转交任务或续接任务，必须先在“记忆”的待处理队列登记字段：任务摘要、来源、状态（待处理/进行中/阻塞）、职责范围、安全断点，随后方可处理；完成后移除，阻塞/停止前保留状态并同步接续人。

- 任何修改、修复或确认会要求新增或更新正式文档时，完成职责内工作后必须直接向“Aoo文档”任务 `01a088d4-fe1b-7e92-a939-04785e1bec61` 报告修改内容、修改前后、影响范围、文件或接口路径、验证证据及建议更新的唯一正式文档，不得先交aoo产品或等待提醒。纯内部且不改变外部契约、使用方式或运维方式的重构可不更新正式文档，但仍须精简写入本岗位开发记录。


- 只有用户针对具体任务明确说“提交”后，本岗位才允许将该任务可明确归属的改动提交并推送到对应仓库 `develop`；功能闭环、测试通过或主协调线程判断完成均不构成提交授权。未收到明确提交指令时，仅北京时间 `02:00` 的每日固定快照任务可以统一提交。禁止夹带用户或其他 AI 的无关改动，禁止操作 `main`、强制推送、合并、变基或改写历史。提交前必须排除敏感信息和运行产物；每次结果必须报告仓库、`develop` 分支、提交号或精确阻塞原因。


- 处理 Bug 必须先复现问题并梳理完整业务链路、状态来源、调用顺序和数据流，确认唯一根因后直接修正错误逻辑；禁止以补丁形式绕过问题。
- “补丁形式”包括但不限于：只增加空值判断、`try/catch` 吞错、延时或重复重试、临时兼容分支、硬编码特例、重复兜底、旧逻辑外再包一层新逻辑，以及保留两套并行业务路径。确需防御性校验时，必须建立在根因修复完成之后，并说明其明确业务边界。
- 修复完成时必须删除被替代的错误逻辑、本次产生的临时措施和重复分支，并用针对性测试及真实业务流程证明根因已消除；无法确认根因时应按阻塞规范报告，不得提交猜测性修改。

## 5. 阻塞报告

- 亲友圈业务域内问题由本岗位跨端持续处理。只有跨业务域共享文件冲突、权威规则冲突、需要用户授权、需要统一调度、职责归属不清，或持续阻塞且点对点无法解决时，才向aoo产品提交完整阻塞报告；不得把域内服务端、协议、数据、配置或部署工作当作越界阻塞。
- 任何任务停止、进入idle、异常结束或准备不再继续执行时，无论任务完成与否，都必须额外向aoo产品提交停止报告，完整列出当前任务、已完成项、未完成项、阻塞、待处理队列、安全断点和停止原因。aoo产品审核后，有已授权且可执行的未完成项必须续作；仅审核确认全部闭环，或只能等待用户决定、授权且无其他独立任务时，才允许保持停止。

## 6. 开发记录

只记录本岗位任务，禁止跨岗位混写。每个新问题须立即在“记忆”的待处理队列登记问题、顺序、状态和相关路径，并在开始处理、完成或阻塞时同步状态；完成后从队列移除并按时间在本章追加日期、用户要求、根因/结论、修改范围、验证结果、未完成项。记录须精简，不复制全文，不删除或改写历史记录，纠错须追加注明。

### 2026-09-08｜入口初始化

- 用户要求：创建亲友圈 AI 独立入口和固定七章规范。
- 根因/结论：此前无本岗位入口，现按项目模板建立。
- 修改范围：仅本文档。
- 验证结果：岗位边界、最小必读文档、执行验收、冻结与记录规则已核对。
- 未完成项：无。

### 2026-09-08｜文件名规则更正

- 用户要求：取消入口文件的 `AI_` 前缀。
- 根因/结论：最新明确命名要求替换此前文件名。
- 修改范围：仅本文档文件名和自引用。
- 验证结果：已改为 `亲友圈执行提示词.md`，无前缀副本。
- 未完成项：无。

### 2026-09-08｜命名规范引用与整改边界

- 用户要求：加入统一命名规范必读引用，并限制历史不合规整改范围。
- 根因/结论：原入口缺少该引用与专项报告要求，现已合并进既定章节。
- 修改范围：仅本文档。
- 验证结果：引用归入第 2 章，整改边界归入第 3 章。
- 未完成项：无。

### 2026-09-08｜稽查协调规则

- 用户要求：统一跨岗位冲突与多 AI 配合事项的报告和调度入口。
- 根因/结论：原阻塞规则缺少统一协调归属，现已去重合并为更新记录和安全断点后向 aoo稽查报告，由其统一调度和验收。
- 修改范围：仅本文档“阻塞报告”。
- 验证结果：协调规则仅保留一条，岗位 AI 自行协调和越权修改均已禁止。
- 未完成项：无。

### 2026-09-08｜问题串行、状态记录与进度规则

- 用户要求：问题按顺序串行处理，明确插队指令才可中断，并持续记录状态、精简必要进度。
- 根因/结论：原规范缺少新问题排队、插队判定、状态同步及 Token 节制要求，现已归入既定章节。
- 修改范围：仅本文档“执行与验收”“开发记录”和“记忆”。
- 验证结果：规则已去重合并，七章顺序不变，“记忆”仍位于末尾。
- 未完成项：无。

### 2026-09-08｜越界问题报告与无头验收环境

- 用户要求：疑似误发或部分越界问题须暂停越界处理并交由 aoo稽查核对；岗位自行执行的浏览器验收统一使用任务专属无头 Chrome。
- 根因/结论：现有跨岗位阻塞规则未完整覆盖禁止执行、测试和自行转交，入口也缺少无头验收的隔离要求；现已去重归入既定章节并保持问题严格串行。
- 修改范围：仅本文档“修改边界”“执行与验收”“阻塞报告”和本开发记录。
- 验证结果：越界判断、部分越界依赖处理、稽查报告字段、CDP `1234`、独立临时 Profile、端口冲突协调、可见窗口授权例外及静态任务例外均已核对。
- 未完成项：无。

### 2026-09-09｜亲友圈战绩入口改用 Records 模块（阻塞）

- 用户要求：亲友圈内的战绩按钮打开 `Client/assets/Modules/Records/Prefab/Records.prefab`。
- 根因/结论：两个亲友圈战绩按钮共用 `LegacyClubMainController.openClubRecord()`；已将该入口改为调用映射至目标预制体的 `UILobbyRecords`。
- 修改范围：`LegacyClubMainController.ts`、对应入口契约测试及本文档；未修改目标预制体、其他亲友圈战绩实现或大厅代码。
- 验证结果：入口、模块注册表、预制体根节点三方映射检查通过；Creator Preview 地址可访问。
- 未完成项：CDP `1234` 被其他任务的独立无头 Chrome 占用，按隔离规范未接管或终止；须待端口释放后完成真实按钮点击验收。现有三项相关测试还分别受缺失历史文件和两处旧断言阻塞，均非本次改动导致且未越权修复。

### 2026-09-09｜亲友圈战绩入口真实验收完成

- 用户要求：继续完成亲友圈战绩按钮打开 Records 模块的真实点击验收。
- 根因/结论：CDP `1234` 释放后已使用独立临时 Profile，从游客登录、创建测试亲友圈、进入亲友圈到点击底部“战绩”完成真实 Canvas 操作，目标 `Records` 界面正常打开。
- 修改范围：无新增业务代码；保留此前 `LegacyClubMainController.ts` 和入口契约测试修改，仅更新本文档状态。
- 验证结果：目标界面截图为 `/tmp/aoo-club-records-e2e.iZfv2H/records-opened.png`；测试亲友圈“战绩验收圈”（ID `309407`）已通过 UI 解散并回到大厅。
- 未完成项：无。

### 2026-09-09｜赛事创建 MessageTip 加载失败（阻塞）

- 用户要求：修复创建赛事后“统一消息预制体缺少模板：MessageTip”的加载失败。
- 根因/结论：`LegacyUnionCreateController.tip()` 调用了统一 `Message.prefab` 中不存在的 `Template_MessageTip`；已在亲友圈控制器内改用现有 `UIMessage_Drift`，避免修改公共消息预制体和公共框架。
- 修改范围：仅 `Client/assets/Club/Code/Runtime/LegacyUnionCreateController.ts` 与本文档。
- 验证结果：源码静态契约检查通过；Creator Preview 仍运行 9 月 8 日的旧编译块，真实复验仍复现旧错误。
- 未完成项：Creator 3.8.8 热编译未刷新，须经正常编辑器同步后重测；测试亲友圈“赛事验收圈”（ID `338284`）的解散确认点击已发出，但因工具调用中断未取得最终清理画面，状态待核对。

### 2026-09-09｜赛事创建 MessageTip 修复验收完成

- 用户要求：继续完成创建赛事提示错误修复并自行清理测试数据。
- 根因/结论：Creator 已正常热编译 `LegacyUnionCreateController.ts`，赛事创建校验提示现统一使用 `UIMessage_Drift`。
- 修改范围：沿用上一条记录中的控制器修改，仅更新本文档状态。
- 验证结果：无头 Chrome 真实进入创建赛事表单并点击空名称提交，界面正常显示“赛事名称不能为空”，未再进入加载失败页；截图 `/tmp/aoo-club-records-e2e.iZfv2H/task2-fixed.png`。此前测试亲友圈 ID `338284` 已确认不存在；本次测试亲友圈 ID `747639` 已通过 UI 解散并回到大厅。
- 未完成项：无。

### 2026-09-09｜加入赛事统一公共数字键盘

- 用户要求：加入赛事只使用 `Client/assets/Common/Prefab/Common/Numpad.prefab`，不得叠加两个输入框。
- 根因/结论：`UIJoinUnion` 原先加载旧 `JoinUnion.prefab`，控制器又在其上实例化公共 Numpad，导致两个输入界面叠加；现将逻辑表单直接映射到唯一公共 Numpad，并改为原地绑定事件和六位赛事号显示。
- 修改范围：`Client/assets/Common/Code/Runtime/ui/ModulePrefabRegistry.ts`、`Client/assets/Club/Code/Runtime/LegacyJoinUnionController.ts` 与本文档；未修改任何预制体。
- 验证结果：Creator Preview 热刷新后，无头 Chrome 真实创建测试亲友圈（ID `471226`）、点击“赛事→加入赛事”，只显示一个公共数字键盘及一个输入显示区；截图 `/tmp/aoo-task3-fixed.png`。测试亲友圈已通过 UI 解散并回到大厅；专属 Chrome PID `96110` 及 Helper 已终止，CDP `1234` 无监听，Profile 已移至废纸篓，可恢复。
- 未完成项：两个既有 Club 测试分别因缺失历史文件 `LegacyRoleGateway.ts` 和旧创建房间断言失败，均发生在本次断言前且非本次修改引入，未越权修改。

### 2026-09-09｜亲友圈顶部栏画布适配核查完成

- 用户要求：检查亲友圈顶部栏为何适配到 `1280×720` 画布外。
- 根因/结论：三个历史主界面预制体根节点均按 `1360×760` 制作并启用全边 Widget，顶部和左右按钮继承旧坐标后会越出 1280 设计区；现有 `LegacyClubLandscapeAdapter` 已在主界面创建、显示及尺寸变化时把根节点归一到 `1280×720` 并刷新 Widget，当前无需修改预制体。
- 修改范围：仅核查 `LegacyClubLandscapeAdapter.ts`、`LegacyClubMainController.ts` 和三个 ClubMain 预制体，未新增业务或资源修改；更新本文档。
- 验证结果：本轮同一 Creator Preview 的真实亲友圈主界面截图 `/tmp/aoo-task3-clubmain.png` 显示左侧返回/切换/身份区与右侧赛事至更多按钮均完整处于画布内。
- 未完成项：按最新调度不再占用浏览器，最终视觉由用户统一测试。

### 2026-09-09｜赛事建房下一步与 2.22 参数界面接通

- 用户要求：从亲友圈赛事创建房间时隐藏“创建房间”、显示预制体已有“下一步”，点击后接通 2.22 同位置界面，并把对应预制体迁入 `Client/assets/Club/Prefab`。
- 根因/结论：统一创建房间控制器此前不区分赛事上下文，也未绑定已存在的 `NextButton`；2.22 权威对照确认下一页为 `UIUnionPLSet`。现以 `unionId > 0` 切换按钮，普通大厅和非赛事亲友圈仍显示“创建房间”；下一步携带当前玩法规则打开赛事参数页并可提交 `union.CUnionCreateRoom`。
- 修改范围：迁入 `Club/Prefab/UnionPLSet.prefab(.meta)` 并保留 UUID `003909af-c8c3-4bba-afad-b2b79e6c5e20`；新增 `LegacyUnionPLSetController.ts`，更新 `LegacyUnionManagerController.ts`、`LegacyClubMainController.ts`、`UnifiedPlaySelectorController.ts`、定向测试和本文档。未修改用户的 `Create_Room.prefab`。
- 验证结果：Creator 3.8.8 已刷新资产数据库和 Preview，搜索可见并重新打开 `UnionPLSet.prefab`，控制台无新增导入/反序列化错误（仅历史字体回退 Warning）；最终热编译块包含 NextButton 显隐、`UIUnionPLSet` 跳转和控制器注册。定向测试 1/1、Creator 原生资产门禁（1369 个场景/预制体，blocking=0）通过。
- 未完成项：按用户要求由用户统一做真实 UI 测试；全项目组件注册门禁仍有 42 项既有重复/缺失 UUID，均不指向本次新增资源；全项目未安装 `tsc`，未越权处理。

### 2026-09-09｜创建赛事成功后界面输入卡死

- 用户要求：修复创建赛事成功后界面卡住、所有按钮无法点击。
- 根因/结论：成功回调误关闭不存在的 `ui/club/UIClubMain`；旧版实际调用 `CloseClubFrom()` 关闭当前皮肤主界面并在下次进入时刷新赛事身份。现改为关闭三个真实的亲友圈主界面路径，保证全屏输入层随主界面一并退出。
- 修改范围：`LegacyUnionCreateController.ts`、新增定向契约测试及本文档；未修改公共表单管理器、预制体或服务端。
- 验证结果：定向测试 1/1 通过；旧版源码行为和当前三个真实主界面注册路径已交叉核对。两个既有 Club 测试仍分别受上一任务新增控制器清单未更新和旧创建房间断言阻塞，均在本次断言之外。
- 未完成项：Creator 当前 Preview 编译块仍为修改前缓存，须刷新 Preview 后由用户按约定统一做真实 UI 测试。

### 2026-09-09｜赛事创建房间两步流程补全

- 用户要求：亲友圈赛事创建房间第一页显示“下一步”，第二页设置房间名称、入场分数等参数后再创建。
- 根因/结论：第一页的 `NextButton` 和第二页 `UIUnionPLSet` 已接通，但第二页预制体中的 `JoinGameEditBox` 仅有视觉节点，控制器未加载、校验、缓存或提交该字段；现已把入场分数完整接入 `JoinGamePoint` 配置。
- 修改范围：`LegacyUnionPLSetController.ts`、`club-union-create-room-flow.test.mjs` 与本文档；未修改预制体、规则表、服务端或公共框架。
- 验证结果：定向两步流程测试与创建赛事退出回归测试均通过；测试同时校验第一页“下一步”、第二页房间名称与入场分数节点、跳转以及创建协议接线。
- 未完成项：Creator 当前 Preview 尚未生成本次脚本的新编译块，用户测试前需刷新 Preview。

### 2026-09-09｜阻塞任务自动续作规则核对

- 用户要求：任务阻塞时保存原因和安全断点，立即继续下一项不依赖任务；阻塞解除后自动恢复原任务。
- 根因/结论：该要求已完整并入“执行与验收”的队列持续执行规则，语义与现有部分越界处理边界一致；按去重规范不再重复追加规则正文。
- 修改范围：仅本文档开发记录。
- 验证结果：已完整重读七章，确认阻塞原因、安全断点、不依赖任务续作、解除后自动恢复及向 aoo稽查报告均在同一条规则中闭环。
- 未完成项：无。

### 2026-09-09｜亲友圈真实验收端口更正

- 用户要求：最新要求覆盖此前不使用无头浏览器的临时调度；亲友圈真实验收改用独立无头 Chrome CDP `1237` 和独立临时 Profile，有待验收断点时自动恢复，不等待用户手动测试。
- 根因/结论：“执行与验收”已统一为 CDP `1237`；历史记录中的 `1234` 仅作为当时事实保留，不再作为后续执行依据。
- 修改范围：仅本文档执行约定与本开发记录。
- 验证结果：已完整重读七章，确认当前唯一亲友圈验收端口为 `1237`，浏览器和 Profile 隔离规则保持不变。
- 未完成项：创建赛事退出与赛事建房两步流程仍等待 Creator 正常生成最新 Preview 编译块；编译块更新后自动用 `1237` 恢复真实验收。

### 2026-09-09｜赛事建房隐藏按钮误提交（阻塞）

- 用户要求：赛事创建房间第一页只显示“下一步”，点击后进入房间名称、入场分数等赛事参数页。
- 根因/结论：真实点击发现 `CreateButton` 与 `NextButton` 重叠；赛事模式虽隐藏前者，但统一指针捕获未检查节点激活和按钮交互状态，一次“下一步”点击仍会触发隐藏的创建按钮并直接进入游戏。现已在捕获入口增加 `activeInHierarchy` 与 `interactable` 门禁。
- 修改范围：`UnifiedPlaySelectorController.ts`、`club-union-create-room-flow.test.mjs` 与本文档；未修改预制体、服务端或玩法代码。
- 验证结果：定向两步流程与创建赛事退出测试均通过；CDP `1237` 真实创建赛事后已确认正常退出大厅、界面可继续操作且控制台无错误。首轮点击“下一步”复现隐藏按钮误提交并进入测试房间，截图 `/tmp/aoo-club-cdp1237.RCKFQC/union-settings2.png`。
- 未完成项：新增点击门禁尚未进入 Creator Preview 编译块，待热编译后自动从真实“下一步”点击恢复验收；测试亲友圈 `TestClub927`（ID `408982`）及误建房间的最终服务端清理状态待重连核对。

### 2026-09-09｜赛事建房两步流程真实验收完成

- 用户要求：完成脚本同步后自动恢复“下一步”真实点击，确认只进入赛事第二步且不创建普通房，并清理测试数据。
- 根因/结论：通过后台文件事件触发 Creator 3.8.8 正常脚本热编译，Preview 编译块已包含隐藏按钮激活/交互门禁；全新 CDP `1237` 会话真实点击“下一步”后停留在 `MainScene` 并只打开“比赛分设置”第二步，未进入普通游戏房。
- 修改范围：沿用上一条的 `UnifiedPlaySelectorController.ts` 与定向测试修改；本条仅更新验收记录。
- 验证结果：第一步仅显示“下一步”，第二步显示房间名称、比赛分门槛、倍数、自动解散、消耗、成本和保存等赛事参数；截图已随可恢复 Profile 移至 `/Users/aoo/.Trash/aoo-club-cdp1237-retest.Nzjifr/second-step.png`。点击期间无“开始提交”日志，场景保持 `MainScene`。CDP `1237` 已释放。
- 未完成项：测试亲友圈 `验收圈`（ID `705400`）及赛事 `验收赛事` 未能安全清理；赛事管理界面收到的上下文显示 `ID:0`，并报 `3008: unsupported union action: union.CUnionRoomCfgCount`，无法通过真实 UI 定位赛事解散入口。此前测试亲友圈 `TestClub927`（ID `408982`）、赛事 `Union927` 及误建房间状态同样待服务端/赛事管理链路恢复后核对；未调用内部函数冒充 UI 清理。

### 2026-09-10｜赛事测试数据清理续作仍阻塞

- 用户要求：从最近安全断点继续赛事建房相关测试数据清理。
- 根因/结论：使用保留的任务专属测试 Profile 重新登录账号 `105613` 并进入测试亲友圈 `验收圈`（ID `705400`）；赛事管理仍显示 `ID:0`，对应链路尚未恢复。退出亲友圈时又触发大厅启动错误 `大厅语义节点缺少 UITransform: Player`，无法继续通过真实 UI 返回亲友圈列表执行解散。
- 修改范围：仅更新本文档；未修改亲友圈业务代码、服务端、账号网关、大厅或测试数据。
- 验证结果：两项赛事建房定向契约测试均通过；Creator Preview `http://127.0.0.1:7456` 可访问，CDP `1237` 独立无头 Chrome 已真实复现赛事管理 `ID:0`，证据 `/tmp/aoo-club-cleanup-union.png`；大厅失败证据 `/tmp/aoo-club-cleanup-close-retry.png`。
- 未完成项：测试亲友圈 `705400`、`408982` 及其赛事/误建房间仍待赛事管理服务端动作与大厅启动链路恢复后，通过真实 UI 核对并清理。安全断点为账号 `105613` 的保留测试 Profile；未调用内部函数或直接接口冒充 UI 清理。

### 2026-09-10｜主界面代码适配迁回预制体

- 用户要求：修复亲友圈上下左右内容越界，把可由 Creator 组件表达的代码适配迁回预制体。
- 根因/结论：`adaptClubMainLandscape()` 会禁用根 Widget、强制缩放背景并重写底部按钮和关闭命中区，和三个主界面已有 Widget 形成双重适配。现已删除主界面运行时坐标覆盖，并把三个预制体根设计尺寸及底部基准由 `1360×760` 统一为 `1280×720`；弹窗历史兜底暂不在本项扩大修改。
- 修改范围：`LegacyClubLandscapeAdapter.ts`、`LegacyClubMainController.ts`、`ClubMainDefault.prefab`、`Skin1ClubMain1.prefab`、`Skin2ClubMain2.prefab` 及两项布局契约测试；节点命名、层级、Meta、UUID和业务逻辑未变。
- 验证结果：布局、赛事两步和赛事退出三项定向测试通过；Creator Preview 已加载不含主界面适配调用的新脚本块。CDP `1237` 真实桌面与宽屏验收显示顶部、左右及底部处于画布内，证据 `/tmp/club-main-layout-desktop2.png`、`/tmp/club-main-layout-wide2.png`。
- 未完成项：Preview 在 `844px` 窄窗口仍固定生成 `1280px` Canvas（左边界 `-218px`）而由宿主页裁切，须使用保持 `1280×720` 逻辑画布的移动触摸仿真补充最终移动验收；该限制不由亲友圈节点坐标造成。

### 2026-09-10｜主界面移动触摸补验完成

- 用户要求：补充亲友圈主界面移动端真实触摸适配验收。
- 根因/结论：窄 Preview 宿主页裁切不能代表逻辑画布适配；改用 Android 移动 UA、5 点触摸和 `1280×720` 逻辑画布执行真实 Canvas touch。
- 修改范围：无新增代码或资源修改，仅更新本文档状态。
- 验证结果：通过真实触摸进入亲友圈，返回、切换亲友圈、顶部功能、底部战绩与快速加入均完整位于画布内；证据 `/tmp/club-main-layout-mobile-touch.png`。CDP `1237` 已释放。
- 未完成项：无。

### 2026-09-10｜主界面按钮全部失效回归修复

- 用户要求：修复亲友圈所有按钮点击不生效。
- 根因/结论：完全移除主界面适配后，预制体根 Widget 挂载时未刷新，视觉坐标与输入命中坐标分离；同时左侧视觉容器保留历史 Button。现恢复仅负责根节点归一和 Widget 刷新的最小适配，并禁用左侧容器 Button；未恢复背景缩放、按钮坐标或命中区硬编码。
- 修改范围：`LegacyClubLandscapeAdapter.ts`、`LegacyClubMainController.ts` 和布局契约测试；未修改业务协议、节点层级、Meta 或 UUID。
- 验证结果：布局、赛事两步、赛事退出三项定向测试通过；CDP `1237` 真实重新进入亲友圈并点击顶部“赛事”，`UIUnionManager` 完成加载和挂载，证据 `/tmp/club-buttons-restored.png`。专用 Chrome 已停止，端口已释放。
- 未完成项：无。

### 2026-09-10｜亲友圈模板房协议与刷新链路补全（验收暂停）

- 用户要求：按 2.22 权威实现亲友圈模板房两步创建，保存后返回亲友圈并刷新显示新桌子，普通大厅不受影响。
- 根因/结论：2.22 `UIUnionPLSet.js` 向 `union.CUnionCreateRoom` 平铺提交模板字段，当前实现误包在 `bRoomConfigure` 且成功后未刷新桌子列表。现已按旧版语义平铺 `roomName`、`roomSportsThreshold`、`sportsDouble`、消耗/奖池/密码及 `bigWinnerConsumeList` 等字段，并在成功回调请求 `union.CUnionGetAllRoomMin` 后重新渲染亲友圈桌子。
- 修改范围：`LegacyUnionPLSetController.ts`、`LegacyClubMainController.ts` 和模板房契约测试；未修改预制体层级、坐标、普通大厅创建流程或服务端。
- 验证结果：模板房、赛事退出和布局三项定向测试通过；Creator 脚本热编译已触发。CDP `1237` 已进入测试亲友圈并保存截图 `/tmp/template-step1.png`。
- 未完成项：按维护窗协调停止 8080 请求，尚未真实点击“房间管理→下一步→保存”及确认新桌子显示。安全断点为账号 `105613`、亲友圈 `705400`、大厅主界面；Gateway 部署完成后从该断点恢复，记录 `union.CUnionCreateRoom` 完整请求/响应并清理测试数据。

### 2026-09-10｜亲友圈模板房客户端闭环，服务端动作阻塞

- 用户要求：亲友圈模板创建入口固定显示“下一步”，第二步设置房间名称、入场分数等参数，保存后返回亲友圈并显示新桌子；普通大厅创建不受影响。
- 根因/结论：入口此前以 `unionId` 猜测模式，第二步旧版 `1560×760` 根节点未归一导致点击偏移，缓存表单还会残留重复区间行。现改为亲友圈入口显式传递模板房模式，第二步执行亲友圈弹窗适配，并清除复用残留；提交协议已按 2.22 使用平铺 `union.CUnionCreateRoom` 字段，成功后刷新 `union.CUnionGetAllRoomMin`。
- 修改范围：`UnifiedPlaySelectorController.ts`、`LegacyClubMainController.ts`、`LegacyUnionManagerController.ts`、`LegacyUnionPLSetController.ts` 与本文档；未修改预制体层级/坐标、普通大厅创建、规则表或服务端。
- 验证结果：Creator 3.8.8 已执行“刷新预览”，新编译块包含最终修改；CDP `1237` 真实 Canvas 点击确认第一步仅显示“下一步”、第二步可点击保存。请求 `requestId=2e3ad80d-7f6d-42e9-9a4b-0658114aa582` 已携带 `clubId=705400`、`unionId=161336`、`gameCode=CD201` 及完整模板字段发出。
- 未完成项：Gateway 返回 `code=3008, message=unsupported union action: union.CUnionCreateRoom`，因此服务端未创建模板桌，客户端无法验证“新桌子显示及点击”。安全断点为账号 `105613`、亲友圈 `705400`、CDP `1237` 保留 Profile；服务端支持该动作后从保存按钮恢复。

### 2026-09-10｜玩法规则变更主动同步

- 用户要求：所有岗位完成玩法规则相关修改或确认后，主动向固定“Aoo 文档”任务提交结构化报告。
- 根因/结论：已写入固定任务ID、报告字段和无法直发时的稽查转发路径。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜正式文档同步范围扩展

- 用户要求：将主动同步从玩法规则扩大到全部需要新增或更新正式文档的变更。
- 根因/结论：已合并覆盖架构、组件、接口、数据库、资源、场景、构建、验收、业务、规则、配置、迁移和重要修复；纯内部无外部影响的重构仅记岗位记录。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜aoo产品更名与点对点协作更正

- 用户要求：原“aoo稽查”更名为“aoo产品”；职责明确时直接点对点协作，明确文档事项直发Aoo文档，仅例外情形上报aoo产品。
- 根因/结论：已替换旧泛化上报规则；历史记录旧称保留为当时名称。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变。
- 未完成项：无。

### 2026-09-10｜业务域全栈职责更正

- 用户要求：亲友圈岗位同时负责亲友圈客户端及 `union` 服务端接口和数据，并跨协议、迁移、配置、部署和真实 E2E 闭环。
- 根因/结论：旧规范按客户端阶段划界并排除数据库和协议，现已以业务域全栈边界替换；域内跨端问题不得再转为岗位阻塞。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜本地测试账号隔离更正

- 用户要求：亲友圈岗位仅使用本地账号`31`至`40`，密码为`1`；不得占用其他岗位账号，预留账号须经aoo产品分配。
- 根因/结论：已加入岗位账号段、并行换号、实际账号留证和跨岗位账号/数据禁止操作规则。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜模板房服务端动作接通更正

- 用户要求：按业务域全栈职责补充亲友圈模板房服务端实现和验证状态。
- 根因/结论：`ClubDispatchService`已接通四个模板房动作并由`JdbcClubService`权威持久化，赛事ID和管理员权限校验已补齐，`CD201`映射历史`gameId=201`；原`3008`未支持动作阻塞已解除。
- 修改范围：仅本文档开发记录和记忆；正式流程另由Aoo文档同步。
- 验证结果：Club影响模块测试通过，Gateway单模块重建重启后核心服务健康。
- 未完成项：使用本岗位`31`至`40`账号完成模板桌显示、点击和测试数据清理真实 E2E。

### 2026-09-10｜账号31真实登录坐标阻塞

- 用户要求：按新账号规范继续模板房真实 E2E，不得把服务端接通标记为真实 UI 完成。
- 根因/结论：已使用账号`31`、密码`1`和独立CDP`1237` Profile；Creator Preview在`1280×720`模式下报告`1280×380` viewport，可见渲染与事件命中坐标错位，未能完成登录。
- 修改范围：仅本文档开发记录和记忆；账号`31`未创建或清理任何业务数据。
- 验证结果：输入链可达，但可见“确定登录”坐标不命中，按Camera坐标点击反而命中密码框并清空密码。
- 未完成项：修复或恢复权威画布命中后，从账号`31`重新登录并继续模板房真实 E2E。

### 2026-09-10｜阻塞后空闲违规更正

- 用户要求：追加记录账号`31`登录阻塞后进入空闲的执行违规，并恢复点对点上报与独立队列续作。
- 根因/结论：发现`1280×380` viewport和事件命中错位后曾进入idle，不符合“向明确责任岗位直报并继续独立任务”的规范；现已将完整阻塞证据发送框架AI任务`01a0810d-3983-73a3-a0d3-ea24936e4c63`，并继续只读核对遗留测试数据。
- 修改范围：仅本文档开发记录和记忆；未登录、重置、占用或清理其他岗位账号及其数据。
- 验证结果：亲友圈`408982`（owner playerId`560`，union`245351`）与`705400`（owner playerId`565`，union`161336`）均为`ACTIVE`且templates/tables为`0`；`309407`、`338284`已为`DISSOLVED`。
- 未完成项：现有亲友圈归属旧账号数据，受账号`31`至`40`边界限制不能越权清理；已向aoo产品请求用户授权或重新分配，等待决定。

### 2026-09-10｜持续执行与idle条件更正

- 用户要求：统一已授权任务的持续执行、阻塞续作和唯一合法idle条件，不得把未授权历史审计项自动入队。
- 根因/结论：队列规则已收敛为“有已授权可执行任务即持续执行；当前项阻塞时点对点联系并继续独立任务；全部完成或全部等待决定、授权、外部状态时方可idle”。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜停止报告强制更正

- 用户要求：任何停止、idle、异常结束或准备不再继续时必须额外向aoo产品报告，普通阶段进度仍无需报告。
- 根因/结论：已补齐停止报告七项字段和禁止带已授权可执行任务停止的门禁，替换“正常idle无需报告”的冲突口径。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜模板房下一步节点更正

- 用户要求：复查实际未进入“下一步”的问题，并使用用户指定节点名`btn_next`。
- 根因/结论：`Create_Room.prefab`和`UnifiedPlaySelectorController`错误使用`NextButton`；现已恢复`MainPanel/Actions/btn_next`并只绑定该节点，赛事模式仅显示`btn_next`，非赛事模式显示`CreateButton`，上下文或规则未就绪时改为明确提示。
- 修改范围：相关Prefab、控制器及本记录；正式模板房流程已由Aoo文档同步。
- 验证结果：Creator 3.8.8已刷新Preview，编译块确认`NEXT_BUTTON_PATH`和提示生效，Node测试通过。
- 未完成项：等待框架岗位修复Preview viewport和命中错位后，自动恢复真实“下一步”点击验收。

### 2026-09-10｜任务转交先入队与字段清单标准化

- 用户要求：所有新任务与转交任务在处理前先写入“记忆”待处理队列。
- 根因/结论：新增“任务摘要、来源、状态、职责范围、安全断点”入队字段清单；完成任务后移除，阻塞或停止时保留队列状态，避免任务丢失与重复认领。
- 修改范围：仅本文档“执行与验收”和“开发记录”。
- 验收结果：七章顺序未变，历史记录未改写，未引入代码或配置改动。
- 未完成项：无。

### 2026-09-10｜亲友圈美术资源归档（验收待续）

- 用户要求：将亲友圈实际引用且非共享的图片、SpriteFrame、Texture 及配套资源移动到 `Client/assets/Club/Atlas`，消除重复资源并修复引用。
- 根因/结论：525 个直接引用的旧图片中，494 个仅由亲友圈使用、31 个同时被大厅或玩法使用；独占资源已保留 UUID 连同 `.meta` 移至 `Club/Atlas/Legacy`。`Club/Atlas` 已配置为 `club-atlas` bundle，换肤动态路径改至该 bundle。10 份二进制重复图片已合并为等价 SpriteFrame 引用后删除。
- 修改范围：494 项图片与 `.meta` 的移动，10 项重复图片与 `.meta` 删除，18 个 `Client/assets/Club/Prefab/*.prefab` 的等价 UUID 重定向，`Client/assets/Club/Atlas.meta`，`LegacyClubMainController.ts` 及本文档；未移动共享公共资源，未改节点层级、坐标或业务协议。
- 验证结果：移动完整性、配套 `.meta`、悬空 UUID、图片 `.meta` JSON 与 164 个 Club prefab JSON 检查通过；重复图片组为 0；Creator 原生资产门禁通过。Creator 3.8.8 资源管理器已显示 `db://assets/Club/Atlas` 配置为 Bundle，`bg02` SpriteFrame 导入条目存在；未发现本次导入错误。
- 未完成项：Creator 当前由用户操作，自动化再次请求资源搜索时收到应用状态变化保护，未能在不抢占编辑器的前提下关闭重开相关预制体、完成 Preview 真实换肤点击与对应构建；安全断点为资源已移动、静态验证已通过，待 Creator 空闲后从 `ClubMainDefault.prefab` 与 `LegacyClubMainController` 换肤入口复验。

### 2026-09-10｜Club Atlas 全量扁平化（阻塞）

- 用户要求：将 `Club/Atlas` 当前全部子层级的图片、Texture、SpriteFrame、图集及配套 plist 移至 Atlas 直属层级，保留 UUID、现有 `club-atlas` Bundle 与视觉业务。
- 已完成：任务已登记；盘点得到 484 个图片（475 PNG、9 JPG）、0 个 plist，全部均有 `.meta`；确定性旧路径→新文件名映射已生成（55 组重名、135 项使用原相对路径稳定前缀，映射校验哈希 `57183e8da08420c2df251beb0d1b8fafbdb60d42022ebf82a2b6998bfbd98dc4`）。发现 7 张换肤背景仍归属 `Common/Atlas/Legacy` 的 `legacy-ui`，未越权移动。
- 阻塞点：Creator 3.8.8 正打开 `node.prefab`，界面显示“保存”按钮且自动化检测到用户仍在交互；为避免覆盖用户未保存 Prefab，未开始文件移动、代码路径写入或资源重导。
- 安全断点：待 Creator 空闲并确认用户未保存内容已处理后，按上述映射连同 `.meta` 执行移动，更新 Club 自有动态换肤路径，清除空目录并完成 Creator 正常导入、重开预制体与真实 E2E。

### 2026-09-10｜Club Atlas 图片直属层级归档完成

- 用户要求：仅直接移动图片，不修改代码、预制体、场景或 Bundle。
- 修改范围：将 `Client/assets/Club/Atlas` 的 484 张图片连同原 `.meta` 移到 Atlas 直属层级，删除全部已搬空子目录和目录 `.meta`；未移动 Common 共享资源，未改 TypeScript、Prefab、Scene、`Atlas.meta` 或 Bundle 配置。
- 验证结果：根层图片 484、缺失目标 0、UUID 变化 0、旧路径残留 0、子目录 0；55 组同名按原相对路径稳定散列前缀重命名 135 项，完整映射临时审计文件为 `/tmp/club-atlas-flatten-map.json`。Creator 3.8.8 资源管理器已自动刷新，`Club/Atlas` 下不再显示 Legacy 子层级，控制台未出现本次资源导入错误。
- 未完成项：无；按用户本次“只移动图片”指令，未改既有动态加载路径。

### 2026-09-10｜代码分类与职责目录落库

- 用户要求：把“按实际使用域归类代码”写入亲友圈岗位执行规范。
- 根因/结论：亲友圈代码整理需按职责边界落位，禁止用跨目录转发或兼容路径兜底；违规条目需先修正分类与引用再继续后续任务。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验收结果：规则落库，无代码更改；后续整理任务以该条进行自检。
- 未完成项：无。

### 2026-09-10｜已删除动画/材质/Prefab不得恢复

- 用户要求：凡用户已删除的动画、材质、Prefab 资源一律视为废弃，不得从任何历史来源恢复；本域当前代码与验收不得再依赖该类资源。
- 根因/结论：现有失效序列化引用需先报告具体项，未经用户明确授权不得补建或兼容映射。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验收结果：资源禁用与不复原边界已落库；后续整理与验收以该条为依据。
- 未完成项：无。

### 2026-09-10｜亲友圈模板房全链路闭环

- 用户要求：完成房间管理、玩法添加、规则下一步、赛事参数、Common Numpad 与保存模板桌等全链路，并统一数字输入路径。
- 根因/结论：亲友圈玩法与模板房创建流程按指定节点与参数链路已收敛，数字输入统一为 `assets/Common/Prefab/Numpad.prefab`（含 `onShow` 动态字段），`Message_Drift` 按现有 `bg1/LabelMessage` 层级修正。
- 修改范围：仅文档级闭环记录更新；代码与资源改动由已执行范围在其他任务与报告中确认。
- 验收结果：专项测试 `1/1`，`failure=null`，`native gate blocking=0`；证据目录 `Client/docs/前端框架规范/历史记录与证据/证据/club-template-room-2026-09-10T08-57-44-830Z`。
- 未完成项：无。

### 2026-09-10｜保存后大厅模板桌被过滤修复（真实验收阻塞）

- 用户要求：修复模板房保存成功后亲友圈大厅未显示模板桌。
- 根因/结论：普通亲友圈列表把桌号放在`bRoomConfigure.gameIndex`，赛事列表使用顶层`id`，大厅渲染仅接受顶层`roomKey`，导致已返回的模板被过滤；现统一转换两类响应，并在列表短暂延迟时合并本次保存响应。
- 修改范围：`LegacyClubMainController.ts`、模板房专项测试及本文档；未修改预制体、规则表或服务端持久化。
- 验证结果：专项测试`1/1`、Creator原生资源门禁`blocking=0`，Creator 3.8.8已刷新Preview。
- 未完成项：账号`31`真实E2E在进入亲友圈列表时被既有资源错误“缺少Creator 3.8.8原生Prefab: club/default/UIClubList”阻断；证据目录`Client/docs/前端框架规范/历史记录与证据/证据/club-template-room-2026-09-10T09-13-07-331Z`。未把静态通过冒充真实显示完成。

### 2026-09-10｜模板桌真实验收阻塞更正

- 用户要求：资源链恢复后自动续跑保存后模板桌显示验收。
- 根因/结论：`club-prefab`已恢复且账号`31`可进入亲友圈大厅；新的首个阻塞为Preview未发布`club-atlas`，请求`/assets/club-atlas/index.js`失败，导致房间管理无法加载。
- 修改范围：无新增业务修改；临时核对`Club/Atlas.meta`后已保持原样，未留下未同步Bundle改动。
- 验证结果：`club-prefab/config.json`与`index.js`均为200；`club-atlas/config.json`与`index.js`均为404。证据目录`Client/docs/前端框架规范/历史记录与证据/证据/club-template-room-2026-09-10T09-27-49-739Z`，已点对点通知框架岗位。
- 未完成项：待`club-atlas`按当前权威资源重新发布后，从账号`31`进入房间管理继续保存与大厅模板桌真实验收。

### 2026-09-10｜保存后大厅模板桌显示真实验收完成

- 用户要求：继续验证模板房保存后亲友圈大厅显示模板桌。
- 根因/结论：框架岗位恢复`club-prefab`和`club-atlas`发布后，统一模板响应转换与保存响应合并逻辑已在真实流程生效。
- 修改范围：沿用`LegacyClubMainController.ts`及专项测试修改；本条仅更新验收记录。
- 验证结果：账号`31`真实点击“房间管理→添加玩法→下一步→保存”，服务端成功处理`union.CUnionCreateRoom`并刷新`union.CUnionGetAllRoomMin`，返回大厅后显示`CD201`模板桌；证据`Client/docs/前端框架规范/历史记录与证据/证据/club-template-room-2026-09-10T09-44-01-980Z/after-save.png`。专项测试`1/1`、Creator原生资源门禁`blocking=0`。
- 未完成项：无。

### 2026-09-10｜亲友圈桌子预制体样例

- 用户要求：仅在`Client/assets/Club/Prefab/test`制作基础、长桌、圆桌、方桌与公共座位预制体样例，复用现有Club Atlas，不接入业务脚本或正式预制体。
- 根因/结论：四种桌型统一采用`Background/Seats/Information/Status/Actions`根层契约，每桌仅复用5个`ClubDeskSeat`实例，通过实例显隐覆盖2至5人布局；座位内部固定为空位与玩家态语义层级。
- 修改范围：新增`ClubDeskBase.prefab`、`ClubDeskLong.prefab`、`ClubDeskRound.prefab`、`ClubDeskSquare.prefab`、`Parts/ClubDeskSeat.prefab`及配套`.meta`；Atlas、业务脚本、正式预制体和开房规则表均未修改。
- 验证结果：Creator 3.8.8已正常重新导入并逐一重开5份预制体，资源数据库生成对应导入记录；控制台未新增缺失UUID、组件反序列化或脚本注册错误。结构审计确认四桌各5个公共座位实例、根节点Widget居中、座位层级与嵌套节点完整、无自定义脚本；Creator原生资产门禁通过（645 scenes/prefabs、20599 assets/meta、29283 UUIDs、blocking=0）。
- 未完成项：无；样例未接入运行时，因此按任务边界不执行完整构建或业务E2E。

### 2026-09-10｜桌子预制体样例合并调整

- 用户要求：将四份桌子样例合并为唯一`ClubDesk.prefab`，桌面、椅子和玩家信息按三种桌型分层，继续独立嵌套`Parts/ClubDeskSeat.prefab`。
- 根因/结论：合并后根层为`Background/Chairs/Seats/Information/Status/Actions`；`Background/TableVariants`只保存`Long/Round/Square`桌面，`Chairs`与`Seats`分别保存三套同名Layout。长桌组合默认启用，圆桌、方桌组合默认关闭；每套布局各5把椅子和5个公共座位实例，2至5人由对应椅子与座位节点成对显隐。
- 修改范围：新增`Client/assets/Club/Prefab/test/ClubDesk.prefab`及`.meta`，删除被替代的`ClubDeskBase/Long/Round/Square.prefab`及其`.meta`；保留`Parts/ClubDeskSeat.prefab`不变，未修改Atlas、正式预制体、业务代码或规则表。
- 验证结果：结构审计通过（3种桌面、3×5把椅子、3×5个座位实例、15个公共座位Prefab引用、悬空序列化引用0、自定义脚本0）；Creator 3.8.8已正常刷新资源数据库并重开`ClubDesk.prefab`，Library仅保留新桌子UUID与公共座位UUID，控制台未新增导入、缺失UUID、组件反序列化或脚本错误；Creator原生资产门禁通过（642 scenes/prefabs、20593 assets/meta、29280 UUIDs、blocking=0）。
- 未完成项：无；样例未接入运行时，按任务边界不执行完整构建或业务E2E。

### 2026-09-10｜身份判断权威字段统一

- 用户要求：身份判断统一读取服务端 `user_type`，不得根据 `ppk` 或展示、格式特征推断。
- 根因/结论：亲友圈仅消费 `0=普通玩家`、`1=陪玩`、`2=机器人` 的服务端权威值，不得因显示字段变化建立平行来源。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验证结果：规则已归入既定章节，七章顺序保持不变；未修改代码、Prefab、资源或规则表。
- 未完成项：无。

### 2026-09-10｜桌型与人数独立布局重做

- 用户要求：纠正5座显隐复用缺陷，在唯一`ClubDesk.prefab`中为每个允许的桌型人数保留可在Creator独立展开、启用和拖动的完整布局；圆桌仅2至5人、方桌仅2至4人、长桌仅6至12人。
- 根因/结论：头像、椅子、信息和按钮位置随人数变化，不能共享同一套Seat坐标。现统一为`Variants/{Long,Round,Square}`，每个桌型仅保留一份`Table`，并在`Layouts/PlayersN`下独立保存`Chairs/Seats/Information/Status/Actions`；每套仅创建实际人数数量的椅子和`ClubDeskSeat`实例。默认只启用`Long/Players6`，其余人数布局关闭但可在编辑器手动切换。
- 修改范围：仅重构`Client/assets/Club/Prefab/test/ClubDesk.prefab`；`Parts/ClubDeskSeat.prefab`、Atlas、正式Prefab、业务代码和规则表均未修改。
- 验证结果：结构审计通过（3种桌型、14套允许人数布局、范围外布局0、椅子86、座位86、公共座位嵌套引用86、桌面3、自动Layout组件0、其他嵌套Prefab 0、悬空引用0、自定义脚本0）；Creator原生资产门禁通过（642 scenes/prefabs、20593 assets/meta、29280 UUIDs、blocking=0）。Creator 3.8.8已执行“重新导入资源”并重开`ClubDesk.prefab`，Library导入时间已更新；层级中已分别展开确认长桌6至12人、圆桌2至5人、方桌2至4人，并展开`Long/Players6`确认6把椅子、6个座位及独立信息/状态/操作节点可选。
- 未完成项：无；样例未接入运行时，按授权边界不执行完整构建或业务E2E。

### 2026-09-10｜恢复原ClubDeskSeat预制体阻塞

- 用户要求：仅从macOS废纸篓恢复刚删除的原`ClubDeskSeat.prefab`及原`.meta`，不得重建或生成新UUID，并在Creator 3.8.8完成重导入、重开与Missing Node验证。
- 已完成内容：通过Finder“放回原处”恢复原Prefab，并通过Finder自动化从废纸篓恢复同名原`.meta`；未修改`ClubDesk.prefab`布局、图片或业务代码。
- 精确阻塞：恢复出的`.meta` UUID为`bfc91f77-7946-4ea8-9881-dba747486e98`，但当前`ClubDesk.prefab`的86个嵌套座位引用均为`ac7d47f2-58af-46ca-b0f1-6b64797cc101`；随后Creator资源监听期间目标Prefab与`.meta`又从磁盘消失，无法在不改UUID、不重建资源的边界内闭环。
- 已尝试方案：核对恢复文件、JSON可解析性、Library映射和86处引用；关闭`ClubDesk.prefab`返回`MainScene.scene`以释放编辑态。未伪造Library、未编辑UUID、未改桌型布局。
- 未完成项：待用户或aoo产品确认正确原`.meta`（应使用废纸篓恢复值还是当前86处引用值）并排除并发删除后，从安全断点继续恢复、Creator重导入、重开检查、保存关闭及原生资源门禁。

### 2026-09-10｜ClubDeskSeat权威位置更正后的阻塞

- 用户更正：座位预制体权威位置改为`Client/assets/Club/Prefab/test/ClubDeskSeat.prefab/.meta`，后续应以该原UUID恢复`ClubDesk.prefab`的14套布局、86个嵌套实例。
- 核对结果：磁盘上该根目录文件及`.meta`均不存在，`Parts`目录内也无同名文件；仅Creator缓存界面仍显示`ClubDeskSeat`条目，不能把缓存伪装为磁盘原件。
- 修改范围：未修改`ClubDesk.prefab`、UUID、布局、图片或业务代码；已关闭一次Prefab编辑态返回`MainScene.scene`，但外部状态随后再次打开该Prefab。
- 未完成项：待权威根目录原`.prefab/.meta`实际落盘并避免并发编辑后，恢复86个嵌套实例、Creator重导入、Missing Node与数量检查、保存关闭及资源门禁。

### 2026-09-10｜停止ClubDeskSeat融合方案

- 用户最终决定：保留`ClubDesk.prefab`与`ClubDeskSeat.prefab`两个独立预制体，所有桌型与人数布局中的座位继续使用`ClubDeskSeat.prefab`嵌套实例，禁止融合成普通节点。
- 执行结果：收到最终决定时尚未对`ClubDesk.prefab`实施融合写入，因此无须回滚；未修改布局、节点、图片、UUID或业务代码。
- 未完成项：根目录`Client/assets/Club/Prefab/test/ClubDeskSeat.prefab/.meta`仍未落盘，待原件恢复后继续嵌套实例与Creator验收。

### 2026-09-10｜ClubDeskSeat独立预制体恢复闭环

- 用户最终决定：保留`ClubDesk.prefab`与`ClubDeskSeat.prefab`两个独立预制体，86个座位均保持嵌套实例，不展开为普通节点。
- 恢复结果：通过macOS废纸篓恢复原`ClubDeskSeat.prefab/.meta`并迁移到`Client/assets/Club/Prefab/test`；恢复出的原UUID为`ac7d47f2-58af-46ca-b0f1-6b64797cc101`，与`ClubDesk.prefab`现有86处引用完全一致，因此未生成新UUID、未批量改写引用。
- Creator验收：Cocos Creator 3.8.8对`test`目录执行正常“重新导入资源”，分别重开并保存关闭两份Prefab；`ClubDeskSeat`层级为`EmptyState/PlayerState`，`ClubDesk`中原Missing Node已恢复为`ClubDeskSeat`实例，控制台未新增缺失UUID或反序列化错误，最终返回`MainScene.scene`释放编辑态。
- 静态验收：14套布局数量分别为长桌6至12、圆桌2至5、方桌2至4，总计86个嵌套引用且各布局数量与人数一致；Creator原生资源门禁通过（642 scenes/prefabs、20596 assets/meta、29280 UUIDs、blocking=0）。
- 未完成项：无。

### 2026-09-10｜ClubDesk座位方位融合与位置语义化

- 用户要求：保留`ClubDesk.prefab`与`ClubDeskSeat.prefab`两个独立资源，将座位方位能力融合进桌子各人数布局；座位预制体只保留`Top/Bottom/Left/LeftTop/LeftBottom`五个原始方向，不新增`Back`，右侧方向复用左侧视觉并水平镜像。
- 实现结果：`ClubDeskSeat.prefab`新增`ChairVariants`，五个方向各自包含`Chair/PlayerAnchor`，原根级`EmptyState/PlayerState`保持不变；`ClubDesk.prefab`的14套`PlayersN`布局将`Chairs`统一更名为`Positions`，每个语义位置节点直接包含一个原UUID的`ClubDeskSeat`嵌套实例，右侧`Right/RightTop/RightBottom`及其编号位置统一使用`scaleX=-1`。
- 修改范围：仅修改`Client/assets/Club/Prefab/test/ClubDesk.prefab`、`Client/assets/Club/Prefab/test/ClubDeskSeat.prefab`及本开发记录；未修改两份`.meta`、Atlas、正式Prefab、业务代码或规则表。
- 验证结果：静态结构终审确认五个方向准确且无`Back/Front/Side`残留，长桌6至12人、圆桌2至5人、方桌2至4人共14套布局、86个`Positions`与86个原UUID嵌套实例全部匹配，26个右侧位置均水平镜像。Creator 3.8.8已对`test`目录正常重新导入，分别重开两份Prefab；层级确认五方向、`Positions`语义命名和嵌套实例，Inspector确认`Right`位置`Scale X=-1`，控制台未新增缺失UUID或组件反序列化错误；保存关闭后返回`MainScene.scene`释放编辑态。Creator原生资产门禁通过（642 scenes/prefabs、20596 assets/meta、29280 UUIDs、blocking=0）。
- 未完成项：无；样例未接入运行时，按任务边界不执行完整构建或业务E2E。

### 2026-09-10｜ClubDeskSeat椅子与头像预览补全

- 用户要求：仅为测试座位预制体补上现有Club Atlas中的椅子和头像图片；五个方向分别配置有效SpriteFrame，右侧继续通过桌子嵌套实例镜像，头像保留运行时替换能力。
- 实现结果：`Top`使用`chair_5_3`，`Bottom`使用`chair_5_2`，`Left/LeftTop/LeftBottom`使用`chair_5_1`；五个`Chair`节点均新增`cc.Sprite`。现有`PlayerState/AvatarMask/Avatar`继续使用`icon_tx46`作为默认头像，并将样例默认状态切为玩家态可见、空位态隐藏；未向`PlayerAnchor`添加图片或重复头像结构。
- 修改范围：仅修改`Client/assets/Club/Prefab/test/ClubDeskSeat.prefab`及本开发记录；`ClubDesk.prefab`只读打开检查，未修改Atlas、正式Prefab、业务代码、规则表或资源meta。
- 验证结果：静态审计确认五个Chair各有且仅有一个有效SpriteFrame、头像SpriteFrame有效、86个原UUID嵌套引用保持不变。Creator 3.8.8重新打开`ClubDeskSeat.prefab`后Inspector确认`Top/Chair`的SpriteFrame为`chair_5_3`，玩家态及头像层级正常；重新打开`ClubDesk.prefab`确认嵌套`ClubDeskSeat`可展开且无Missing Node。控制台错误计数为0，未新增缺失UUID或反序列化错误；保存座位Prefab并关闭两份Prefab后返回`MainScene.scene`释放编辑态。
- 未完成项：无；测试样例未接入运行时，按边界不执行完整构建或业务E2E。

### 2026-09-10｜模板桌入房返回链阶段修复与停止报告

### 2026-09-10｜座位公共信息与操作层统一迁移

- 用户要求：去掉`ChairVariants`中间层，并将当前磁盘实际保留的`Information/Actions`迁入`ClubDeskSeat.prefab`统一维护；各桌布局只启用一个座位实例中的信息与操作，其他实例禁用。
- 实现结果：`ClubDeskSeat`根层为`Direction/EmptyState/PlayerState/Information/Actions`，`Direction`原有`Chair/PlayerAnchor`保持不变；`Information`保留`GameName/RoundInfo`，`Actions`保留`Btn_Detail`。按当前权威磁盘的`Variants`与`Variants-001`两套布局，共迁移28组现存桌级节点、覆盖167个座位实例；每布局首个实例启用，其余实例禁用，位置按原桌级坐标换算。
- 修改范围：仅修改测试目录两份Prefab及本开发记录；未恢复用户已删除节点，未修改图片、meta、正式Prefab、业务代码或规则表。
- 验证结果：静态审计确认源座位根节点完整、`ChairVariants`为0、桌内桌级`Information/Actions`均为0；两类实例覆盖各为启用28、禁用139。Creator 3.8.8正常重新导入`test`目录，重开两份Prefab并保存关闭；`ClubDeskSeat`层级可见根级`Information/Actions`，`ClubDesk`无Missing Node，最终返回`MainScene.scene`，控制台Error计数为0。
- 未完成项：无；样例未接入业务运行时，按边界未执行完整构建或业务E2E。

### 2026-09-10｜模板桌入房返回链阶段修复与停止报告

- 用户要求：验证并闭环“从亲友圈模板桌进入游戏房间后，返回亲友圈大厅不报错且按钮可用”。
- 根因/结论：已为 Lobby 销毁后的延迟回调、Club 销毁节点 `off` 增加有效性保护，并在游戏返回时传递 `CLUB/UNION` 来源、恢复亲友圈上下文。
- 修改范围：`LegacyLobbyScreen`、`LegacyClubMainController`、游戏返回来源链与亲友圈流程专项测试；未修改只读登录场景、Prefab、Atlas、规则表或旧赛事数据。
- 验证结果：`club-union-create-room-flow.test.mjs` `1/1` 通过；Creator 原生资源门禁检查 642 个 scene/prefab、20596 个 assets/meta、29280 个 UUID，`blocking=0`。
- 未完成项：最终“模板桌→进入房间→`Btn_Menu`→`Btn_Back`→亲友圈大厅 `btn_roomlist` 及其他按钮可点”真实 Canvas 终验尚未完成。当前 Preview 的 `AccountInput`/`PasswordInput` 运行时 EditBox 为禁用态，最新证据目录为 `Client/docs/前端框架规范/历史记录与证据/证据/club-template-desk-enter-2026-09-10T12-50-36-733Z`；登录链恢复后从亲友圈大厅直接续验，不重复前序修改。

### 2026-09-11｜亲友圈保险箱切换至ClubBox

- 用户要求：亲友圈保险箱统一使用`Client/assets/Club/Prefab/ClubBox.prefab`，删除新预制体不存在的旧`CaseSprots`代码，并补齐新界面已有功能。
- 实现结果：保险箱逻辑表单由`UICaseSprots`切换为`UIClubBox`，接通`ClubBoxController/ClubBoxGateway`的余额、存入、取出和流水列表；存取金额统一调用`Common/Prefab/Common/Numpad.prefab`。旧`PLEditBox`、`btn_Add/btn_Del`、旧余额标签及`CClubGetCaseSprotsChange`调用全部移除，原`CaseSprots.prefab/.meta`未删除或修改。
- 修改范围：`LegacyClubMainController.ts`、`ClubBoxController.ts`注释、原生预制体路径映射及保险箱专项契约测试；未修改`ClubBox.prefab`、`CaseSprots.prefab`、Atlas、规则表或其他业务。
- 验证结果：保险箱专项测试`2/2`通过，Creator原生资源门禁通过（642 scenes/prefabs、20452 assets/meta、28661 UUIDs、blocking=0）；Creator 3.8.8资源管理器可见并打开`ClubBox.prefab`，刷新Preview后控制台Error计数为0；专用无头Chrome刷新Preview后运行时异常与页面错误均为0。
- 未完成项：测试账号当前为无亲友圈的游客账号，只能到达“加入/创建亲友圈”页，无法执行“更多→保险箱→存取”真实Canvas终验；全量TypeScript检查仍有6个既存错误，其中2个在本控制器模板桌加载段、4个在PDK与登录模块，本次新增保险箱代码错误已清零。

### 2026-09-11｜创建房间后模板桌未显示修复

- 用户反馈：亲友圈完成创建房间并保存后，大厅没有显示新模板桌。
- 根因/结论：`CClubGetCreateGameSet`返回的模板关键字段位于`clubCreateGameSets[].bRoomConfigure.baseCreateRoom`，旧转换只读取`bRoomConfigure`第一层，导致`playerNum=0`且玩法身份缺失，桌型选择按契约拒绝实例化。
- 实现结果：模板转换统一展开`clubRoomCfg/baseCreateRoom/bRoomConfigure/room`四层权威数据，补齐`gameIndex`、人数、局数、房间名和Catalog玩法身份；玩法编码可由稳定编码、Catalog数字ID或Catalog展示名解析，但不新增别名或数字范围推断。保存响应仍立即并入刷新结果，避免列表接口短暂延迟丢桌。
- 修改范围：`LegacyClubMainController.ts`及亲友圈创建流程、桌型选择专项测试；未修改Prefab、meta、规则表、保险箱代码或服务端。
- 验证结果：专项测试`2/2`通过，Creator原生资源门禁通过（642 scenes/prefabs、20452 assets/meta、28661 UUIDs、blocking=0）；Creator 3.8.8刷新Preview后Error计数为0；专用无头Chrome强制刷新后运行时异常与页面错误均为0。本次新增代码的TypeScript错误已清零。
- 未完成项：专用无头Chrome当前游客账号无亲友圈，无法执行“创建模板→保存→大厅出现桌子”真实Canvas终验；获得已加入且有管理权限的测试账号后从该步骤续验。

### 2026-09-11｜凉山跑得快2人模板桌验收口径更正

- 用户更正：模板桌显示不需要多个账号测试；创建凉山跑得快2人房间时，直接调用`ClubDesk.prefab`中的`Players_2`布局即可。
- 执行结果：现有权威映射已满足`LS201 + 2人 → POKER → Round/Players_2`，无需修改Prefab或另建布局；专项测试增加对`ClubDesk/Variants/Round/Players_2`实际层级及`LS201` Catalog类别的明确断言。
- 验证结果：单账号验收口径生效，不再以多账号测试作为模板桌显示任务的完成条件。
- 未完成项：无。

### 2026-09-11｜赛事模板桌数据源修复

- 用户反馈：房间管理中已保存并启用多个模板，但亲友圈大厅没有显示桌子，询问是否因房间名称为空。
- 根因/结论：不是名称为空导致；大厅进入、切换及保存后刷新误用了“游戏中房间列表”，当游戏中桌子为0时返回空数组并清空模板桌。模板名称只控制桌面标题，不是实例化条件。
- 实现结果：大厅三条加载链统一改用`CUnionRoomConfigItemList/CClubRoomConfigItemList`模板配置接口；转换层兼容配置项的`configId`与`size`字段，空`roomKey`时使用稳定配置ID；游戏中房间全量推送只触发模板重载，不再覆盖并删除空闲模板桌。
- 修改范围：`LegacyClubMainController.ts`、`LobbyClubEntryController.ts`及两项亲友圈入口/模板流程契约测试；未修改Prefab、meta、规则表、保险箱或服务端。
- 验证结果：模板创建流程与桌型选择专项测试`2/2`通过，凉山跑得快2人仍映射`ClubDesk/Variants/Round/Players_2`；Creator原生资源门禁通过（643 scenes/prefabs、20454 assets/meta、28662 UUIDs、blocking=0），Creator 3.8.8已执行“刷新预览”。全量入口测试另有既存`UIClubRoomList`字符串断言失败；TypeScript仍仅报告此前6个既存错误，本次两条加载链无新增错误。
- 未完成项：当前测试会话无可管理亲友圈，未代替用户创建额外账号或测试数据；由用户在现有赛事中复验模板桌显示。

### 2026-09-11｜Club预制体重复与未引用资源审计

- 用户要求：先还原误删的亲友圈主界面预制体，再检查`Client/assets/Club/Prefab`中界面完全相同但名称不同的预制体；未引用项统一增加`xxx`前缀，界面不同但业务相同者保留。
- 恢复结果：从废纸篓原样恢复`ClubMainDefault.prefab/.meta`，保留原UUID`e3246265-c2ae-423b-8900-1e5214848bc4`；Creator 3.8.8重新导入后可见并可打开，原节点层级完整。
- 审计结果：结构哈希仅发现`ClubMain.prefab`与`ClubMainDefault.prefab`界面完全相同；前者全项目无UUID或路径引用，已按用户此前删除要求移除，后者为原生路径映射和控制器实际引用项并保留。15个`Skin2`预制体均通过`ui/club_2/...`逻辑路径被控制器或原生路径映射引用，未误标、未删除。
- 标记结果：确认无业务引用的旧保险箱`CaseSprots.prefab/.meta`重命名为`xxxCaseSprots.prefab/.meta`，旧合伙说明`x x xClubHeHuoShuoMing.prefab/.meta`规范化为`xxxClubHeHuoShuoMing.prefab/.meta`；两项均保留原UUID并同步Prefab根节点名及meta的`syncNodeName`。
- 验证结果：Creator资源数据库已自动导入两个新路径，`library/.assets-info.json`及对应UUID导入产物均记录`xxx`名称；Creator原生资源门禁通过（585 scenes/prefabs、20334 assets/meta、28602 UUIDs、blocking=0）。未修改界面不同的Prefab、业务代码、Atlas或规则表。
- 未完成项：无。

### 2026-09-11｜赛事创建后保险箱入口默认显示

- 用户要求：创建赛事后进入亲友圈时，保险箱按钮不再沿用旧版本默认隐藏逻辑，改为默认显示。
- 实现结果：亲友圈主界面每次应用普通圈/赛事模式显隐状态时，均显式启用`top/right_btn/moreNode/childMore/btn_caseSprots`，不再受`unionId`、管理员身份或赛事创建状态控制；“更多”面板原有展开/收起交互保持不变。
- 修改范围：仅修改`LegacyClubMainController.ts`，新增保险箱默认显隐专项测试；未修改Prefab、meta、保险箱业务接口、赛事协议或规则表。
- 验证结果：`club-box-default-visibility.test.mjs`通过，确认入口启用逻辑位于统一模式刷新方法内且不存在赛事或权限条件。
- 未完成项：Creator 3.8.8当前同时存在项目进程与启动器模态窗口，无法切换到项目窗口执行Preview刷新；代码与专项测试已完成，待编辑器窗口恢复后进行真实Canvas点击终验。

### 2026-09-11｜赛事模板桌列表路由补全

- 用户反馈：创建房间模板后，亲友圈赛事大厅仍未显示模板桌。
- 根因/更正：此前仅修复了客户端模板转换和刷新覆盖问题，但当前模块化Club服务没有注册客户端实际请求的`union.CUnionRoomConfigItemList`；保存成功后，赛事大厅模板列表请求无法分发，因此没有数据可供`ClubDesk`实例化。截图中的“游戏201”不是房间名称为空导致。
- 实现结果：Club服务新增赛事模板列表路由，校验当前亲友圈已绑定且请求匹配的`unionId`后返回启用模板；响应同时保留稳定`gameType`（例如`LS201`）与内部数字`gameId`，客户端可据此选择`ClubDesk/Variants/Round/Players_2`。
- 修改范围：`Server/server/Club/ClubDispatchService.java`及对应集成测试；未修改客户端Prefab、规则表、模板数据或其他服务。
- 验证结果：Club模块`JdbcClubServiceIntegrationTest`通过，客户端`club-union-create-room-flow.test.mjs`通过；仅重启受影响的Hall进程，健康检查显示Hall PID`5391`及Gateway、Account、Version、Social、Gifting、Room Rules、Authority全部正常。
- 未完成项：需用户在现有赛事中重新进入大厅复验已保存模板显示；不需要创建多个账号。

### 2026-09-11｜模板桌独立Bundle加载修复

- 用户反馈：赛事模板列表路由补全并重启Hall后，大厅仍然没有显示模板桌。
- 根因/更正：只读核对赛事`391758`确认数据库已有5条启用的`CD201`两人模板，房间名称为空不是阻断条件；真正原因是`Club/Prefab.meta`把目录声明为独立Bundle `club-prefab`，运行时代码却从父Bundle `club`按`Prefab/ClubDesk`加载，导致`ClubDesk.prefab`始终返回空。
- 实现结果：模板桌加载优先使用`club-prefab/ClubDesk`，并保留旧构建`club/Prefab/ClubDesk`兼容回退；加载成功后仍按Catalog玩法类别与人数选择精确桌型节点。
- 修改范围：`LegacyClubMainController.ts`及模板桌专项测试；未修改Prefab、meta、规则表、模板数据、保险箱或其他业务。
- 验证结果：模板桌、赛事创建流程和保险箱入口专项测试`3/3`通过；Creator原生资源门禁通过（583 scenes/prefabs、20330 assets/meta、28600 UUIDs、blocking=0）；Creator 3.8.8已执行“刷新预览”，Preview与Editor编译产物均确认包含新Bundle加载路径且旧单一路径代码已消失，刷新后控制台Error计数为0。
- 未完成项：当前可用本地测试账号不属于用户截图中的赛事，未越权登录账号`380`；需用户在现有赛事会话刷新后进行真实Canvas显示终验。

### 2026-09-11｜创建亲友圈弹窗迁入LobbyClubList

- 用户要求：创建亲友圈输入弹窗已融合到`Lobby/Prefab/LobbyClubList.prefab/CreatecCub`，运行时不再引用旧`Club/Prefab/ClubCreate.prefab`；修改后由用户自行测试。
- 实现结果：大厅列表与亲友圈内“创建”入口统一打开`UILobbyClubList`并激活内嵌`CreatecCub`；名称输入改绑`Create/Found/EditBox`，关闭改绑`Popup/Tag/Close`，确认改绑`Confirm`，创建协议、余额刷新、成功提示和进入新圈流程保持不变。移除旧`UIClubCreate`表单注册及原生路径映射。
- 修改范围：`LobbyClubEntryController.ts`、`LegacyClubMainController.ts`、`prefab-path-map.json`及相关契约测试；未修改新旧Prefab、meta、接口或服务端。
- 验证结果：内嵌弹窗专项测试通过，相关控制器TypeScript未新增错误，Creator原生资源门禁通过（584 scenes/prefabs、20332 assets/meta、28601 UUIDs、blocking=0）；Creator 3.8.8已刷新预览，编译产物确认包含内嵌节点逻辑且不再注册旧表单。
- 未完成项：按用户要求不代为执行真实点击，由用户自行测试创建、关闭和确认流程。

### 2026-09-11｜保险箱预制体更名引用同步

- 用户要求：保险箱预制体已由`ClubBox.prefab`更名为`ClubSafePanel.prefab`，同步修改代码引用。
- 实现结果：保险箱表单路径统一由`ui/club/UIClubBox`切换为`ui/club/UIClubSafePanel`，原生预制体映射同步指向`native-ui/club/default/ClubSafePanel`；控制器、接口、公共数字键盘和存取逻辑保持单份不变。
- 修改范围：`LegacyClubMainController.ts`、原生预制体路径映射、控制器说明及保险箱专项测试；未修改预制体、meta、协议或服务端。
- 验证结果：保险箱入口、节点契约与默认显示专项测试`3/3`通过；Creator原生资源门禁通过（582 scenes/prefabs、20318 assets/meta、28591 UUIDs、blocking=0）；Creator 3.8.8已刷新预览，编译产物确认包含新路径且不再包含旧表单路径。
- 未完成项：按用户此前要求，真实点击测试由用户执行。

### 2026-09-11｜亲友圈战绩按钮接通Records模块

- 用户反馈：亲友圈战绩按钮点击无效，要求引用`Client/assets/Modules/Records`中的预制体。
- 根因/结论：亲友圈按钮虽写了`UILobbyRecords`，但直接调用表单管理器挂载预制体，绕过了Records模块`ReplayController.open()`的完整打开与战绩加载流程。
- 实现结果：普通亲友圈底部战绩与赛事顶部战绩统一发送`legacy-open-records`事件，由Records模块控制器接管并调用`open()`；权威映射仍为`records-ui/Prefab/Records`，Club侧不复制战绩业务代码。
- 修改范围：`LegacyClubMainController.ts`、`Modules/Records/Code/ReplayController.ts`及专项契约测试；未修改Records预制体、meta、接口或服务端。
- 验证结果：亲友圈到Records模块的专项测试通过，Creator原生资源门禁通过（582 scenes/prefabs、20318 assets/meta、28591 UUIDs、blocking=0）；Creator 3.8.8已刷新预览，两份编译产物均确认包含事件链且Club旧直接挂载路径已消失。
- 未完成项：按用户此前要求，真实点击测试由用户执行。

### 2026-09-11｜竞技动态消息模板融合到ScoreRecord

- 用户要求：将`MsgNodeMsgType0`至`MsgNodeMsgType16`全部复制融合到`ScoreRecord.prefab`，保留原预制体，并按功能语义重命名融合后的全部节点层级。
- 实现结果：以用户已复制的Type0为样板，将其余16类消息模板完整展开为`ScoreRecord`直属模板；根模板按补偿、处罚、授权、赛事奖励、保险箱、抽水、成员和归属变更等业务语义命名，内部背景、时间、操作者、目标成员、变更值、余额、详情等节点同步语义化；仅Type0样板默认启用，其余模板保持隐藏供运行时选择。
- 资源边界：`MsgNodeMsgType0.prefab`至`MsgNodeMsgType16.prefab`及各自meta全部保留且未修改；`ScoreRecord.prefab.meta`原UUID `785e0063-0fee-44e0-8eb9-ef75c638f945`保持不变。
- 验证结果：融合专项测试`2/2`通过，确认17个模板顺序、语义命名、显隐状态、引用有效性及原预制体保留；Creator原生资源门禁通过（583 scenes/prefabs、20320 assets/meta、28592 UUIDs、blocking=0）。Creator 3.8.8已重新导入并生成1596项、无空对象的UUID缓存，资源预览重新打开后显示语义化层级，控制台Error计数为0。
- 未完成项：无。

### 2026-09-11｜ScoreRecord节点命名精简规范

- 用户要求：`ScoreRecord.prefab`中的节点名过长，统一精简到AI可理解即可，并将该要求写入亲友圈执行规范。
- 规范结果：场景与预制体节点统一采用最短无歧义功能名，允许通用缩写，禁止堆叠冗长描述；末尾编号统一使用`_数字`格式，规则已去重归入“执行与验收”。
- 修改范围：`ScoreRecord.prefab`、融合专项测试及本文档；原17份消息预制体及meta不修改。
- 验证结果：专项测试`2/2`通过，并增加节点长度与`_数字`后缀门禁；Creator原生资源门禁通过（583 scenes/prefabs、20324 assets/meta、28604 UUIDs、blocking=0）。Creator 3.8.8已正常重新导入并打开`ScoreRecord.prefab`，层级显示精简名称，控制台Error计数为0。
- 未完成项：无。

### 2026-09-11｜玩家ID邀请改为直接加入

- 用户要求：亲友圈输入玩家ID邀请后不再等待玩家同意，直接将玩家加入亲友圈并归属在当前邀请人名下。
- 根因/结论：`club.CClubFindPIDAdd`原逻辑只创建七天有效的待接受邀请；现改为服务端单事务直接写入正式成员，并将成员`upPlayerId`设置为操作者ID，同时移除该玩家已有的待审核加入申请，不再生成新的待接受邀请。
- 修改范围：`JdbcClubService.java`、`ClubDispatchService.java`、`LegacyClubMainController.ts`及客户端/服务端专项测试；未修改Prefab、规则表或其他业务域。
- 验证结果：Club服务定向集成测试`4/4`通过，确认正式成员、邀请人归属与待邀请列表为空；客户端提示专项测试`1/1`通过，Creator已正常导入脚本且控制台Error计数为0；Hall已使用最新运行时恢复，PID`39963`，端口`8093`健康。
- 未完成项：未使用真实账号新增测试成员，避免污染用户现有亲友圈；由用户在当前亲友圈输入目标玩家ID复验成员列表与归属显示。

### 2026-09-11｜ScoreRecord统一使用专属图集

- 用户要求：`ScoreRecord.prefab`内所有图片统一替换为`Club/Atlas/ClubScoreRecord.plist`中的图。
- 实现结果：82个Sprite全部按语义替换为专属图集的9张SpriteFrame，覆盖条目背景、比分/剩余徽标、绿/橙/蓝箭头及三色标签框；保留用户最新模板显隐状态，不改节点层级、布局或原17份消息预制体。
- 修改范围：`ScoreRecord.prefab`、融合专项测试及本文档；未修改图集、图片、meta、业务代码或其他Prefab。
- 验证结果：专项测试`3/3`通过，确认82个Sprite均引用`ClubScoreRecord`图集UUID；Creator原生资源门禁通过（583 scenes/prefabs、20286 assets/meta、28540 UUIDs、blocking=0），Creator 3.8.8正常导入，Library缓存1595项、82个Sprite且非目标图集引用为0。
- 未完成项：Creator当前打开的`ClubMainDefault.prefab`存在用户未保存修改，为避免覆盖现场未切换编辑页面；`ScoreRecord`导入已完成，待用户保存当前Prefab后可补充关闭重开检查。

### 2026-09-11｜ClubDiamond节点语义化与代码接通

- 用户要求：精简并语义化`ClubDiamond.prefab`全部子节点名称，同步代码引用，保持实现干净。
- 实现结果：层级统一为`Panel/Header/Title/HeaderClose`、`AdminLimit`、`MemberLimit`、`Confirm/Cancel`等短语义名，通用子节点使用`Bg/Text/Hint/Audience/Threshold`；补齐该弹窗原先缺失的表单注册、关闭/取消/确认绑定及亲友圈/赛事两套钻石提醒协议提交，并统一由入口传递完整上下文。数字输入继续由公共表单管理器自动接入唯一`Common/Prefab/Common/Numpad.prefab`。
- 修改范围：`ClubDiamond.prefab`、`LegacyClubManagementController.ts`、`LegacyUnionManagerController.ts`及专项契约测试；未修改meta、布局、图集、服务端、规则表或其他Prefab。
- 验证结果：专项测试`2/2`通过，Creator原生资源门禁通过（583 scenes/prefabs、20286 assets/meta、28540 UUIDs、blocking=0）；Creator 3.8.8已正常导入Prefab（Library缓存比源文件晚1秒且包含新节点名）并完成Editor/Preview脚本热编译，编译产物包含新节点与两套协议绑定。
- 未完成项：真实账号下的按钮点击和协议返回由用户按当前安排自行测试。

### 2026-09-11｜12个亲友圈Prefab统一Club精简命名

- 用户要求：将指定Prefab统一改为`Club + 精简功能名`，精简内部层级命名，同步代码引用，并清理相关代码冗余与多余注释；重复列出的`ClubUserMessageNew`只处理一次。
- 实现结果：资源依次更名为`ClubFind`、`ClubBan`、`ClubManage`、`ClubMsg`、`ClubPlayerLog`、`ClubRooms`、`ClubRule`、`ClubMembers`、`ClubMemberMsg`、`ClubPromoters`、`ClubPromoterSet`、`ClubPromoterView`；根节点与meta同步名一致，原UUID全部保留。内部清除了`New Label`、`New Sprite`、`BACKGROUND_SPRITE`、`TEXT_LABEL`、`PLACEHOLDER_LABEL`、`RICHTEXT_CHILD`、`tip copy`、`user1/user2`等默认或含糊名称，改为短业务语义名；已简洁且承担动态绑定契约的节点不作无意义改写。
- 引用与整洁审计：12条旧表单兼容键保持不变，原生资源值全部切换到新Prefab名；专项测试中的直接文件引用同步更新。相关控制器未发现可安全删除的重复实现；现有注释均说明状态边界、点击命中、服务端权威数据或兼容原因，不属于重复代码表面的多余注释，因此保留。
- 验证结果：命名与映射专项测试`2/2`通过，Creator原生资源门禁通过（565 scenes/prefabs、20250 assets/meta、28522 UUIDs、blocking=0）；Creator 3.8.8已正常导入全部12个UUID缓存，缓存根节点均为新名称，旧资源文件无残留。
- 未完成项：按用户此前安排，真实界面点击回归由用户执行。

### 2026-09-11｜UIClubMainDefault运行时缺失修复

- 用户反馈：进入亲友圈时报“缺少 Creator 3.8.8 原生 Prefab: club/default/UIClubMainDefault”。
- 根因：`Club/Prefab.meta`虽然声明`bundleName: club-prefab`，但缺少`isBundle: true`，Creator Preview的`projectBundles`未注册该Bundle；`ClubMainDefault.prefab`及原UUID实际均存在，不是文件再次被删除。
- 实现结果：恢复`club-prefab`的Creator Bundle启用声明，不复制Prefab、不重建UUID、不增加加载兼容分支。
- 验证结果：Creator资源数据库已重新导入，Preview设置已包含`club-prefab`；通过真实Preview运行上下文调用`cc.assetManager.loadBundle('club-prefab')`成功，并从该Bundle加载`ClubMainDefault`得到`ok:ClubMainDefault`。Bundle专项测试`3/3`及Creator原生资源门禁通过。
- 未完成项：无。

### 2026-09-15｜Git 仓库地址与用户授权边界

- 用户要求：把客户端、服务端和管理后台 Git 地址写入每个 AI 员工规范；用户测试完成后会主动邀请对应 AI 提交，未收到明确指令禁止推送。
- 根因/结论：统一记录三个固定仓库地址；取消 AI 自主提交和定时推送授权，明确邀请后默认只推送对应仓库 `develop`，`main` 及历史改写仍需当次单独授权。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：规则已归入既定章节，历史开发记录未改写，“记忆”仍为最后一章。
- 未完成项：无。

### 2026-09-15｜亲友圈 ScrollBar 空节点异常修复

- 用户反馈：进入亲友圈持续报 `Cannot read properties of null (reading 'getComponent')`，并怀疑 CommonHead/ClubDesk 席位实例化链路。
- 根因/结论：首个完整栈位于 Creator 3.8.8 `ScrollBar._setOpacity`；`ClubMain/Middle/RoomList/mark` 的 `ScrollView` 仍引用一个已脱离节点、`node=null` 的旧水平 ScrollBar，页面激活时在 CommonHead/ClubDesk 渲染前崩溃；非头像变体或异步销毁根因。
- 实现结果：仅解除该 `ScrollView._horizontalScrollBar` 对旧脱离组件的引用，保留 `UnifiedScroll` 单一滚动策略；未改节点名、层级、布局、UUID、meta、CommonHead、ClubDesk 或业务控制器。
- 验证结果：专项测试 `6/6` 通过；Creator 3.8.8 正常导入并更新 Library，关闭重开 `ClubMain.prefab` 后层级正常，再恢复用户原打开的 `PDK_CommonRoom.prefab`；账号`31`经专属无头 Chrome/CDP `1237` 真实点击首次进入和返回重进，两次 `pageerror=[]`，主页、模板桌和 CommonHead 正常显示。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-commonhead-null-2026-09-15T02-35-00-0800/`。
- 未完成项：无。

### 2026-09-15｜暂停跨任务文档消息与自动整理

- 用户要求：所有 AI 暂时不再给文档任务发送消息，也不再自动整理正式文档。
- 根因/结论：新增临时覆盖规则；暂停期间不执行此前主动发送文档报告的条款，各岗位只维护自身开发记录与记忆，文档 AI 仅响应用户直接下达的具体文档任务。
- 修改范围：仅本文档“执行与验收”和本开发记录；历史记录不删除、不改写。
- 验证结果：临时规则已归入既定章节，“记忆”仍为最后一章。
- 未完成项：等待用户后续明确恢复文档协作。

### 2026-09-15｜刷新后模板桌玩家头像缺失修复

- 用户反馈：刷新页面后，亲友圈模板桌上的玩家头像未渲染，远端头像失败时也不能留下空白。
- 根因/结论：`CommonHead.prefab`迁移后，桌面仍查找已不存在的`Avatar/Nickname`，因此刷新重建座位时头像目标恒为空；统一改由`CommonHeadController`切换`Stat`变体、写入`Lb_PlayerName`并负责异步头像代次，失败时保留预制体默认头像。
- 修改范围：`LegacyClubMainController.ts`、头像统一专项测试、本开发记录与专项证据；未修改`CommonHead.prefab`、`ClubDesk.prefab`、meta、布局、旧头像节点、规则表或服务端。
- 验证结果：专项测试`9/9`通过；Creator 3.8.8对脚本正常重新导入并完成Preview热编译。账号31建桌、账号32观察，活动桌`116065`连续两次整页刷新后`Stat/Img_Avatar`均激活，默认帧`bg_txk_f`与纹理有效，`Lb_PlayerName=玩家580`，`pageerror=[]`；`tx580.png`连接拒绝时未清空默认头像。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-avatar-refresh-2026-09-15T03-05-00-0800/`。
- 未完成项：账号32为双账号复验加入测试亲友圈`549245`；测试房间与浏览器上下文已清理，成员关系因删除操作需要当次确认而保留。

### 2026-09-15｜添加队长输入框无响应修复

- 用户反馈：亲友圈“队长→添加队长”的ID输入框点击后不生效。
- 根因/结论：运行时表单名为`UIClubPromoterLevelAdd`，公共数字键盘白名单却误用预制体根名`ClubPromoterLevelAdd`；EditBox按规范保持禁用后没有获得Numpad点击绑定，形成无响应状态。
- 修改范围：`LegacyFormManager.ts`修正该表单身份映射，`LegacyClubPromotionController.ts`为搜索开始、成功、非法输入和失败补充结构化诊断日志，并更新相关契约测试；未修改Prefab、meta、布局、协议或服务端。
- 验证结果：公共数字键盘契约测试`2/2`通过；Creator 3.8.8已刷新Preview，账号31在亲友圈`549245`真实点击输入框打开唯一公共Numpad，输入并搜索PID`581`成功返回玩家，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-promoter-input-2026-09-15T13-00-00-0800/`。
- 未完成项：全项目TypeScript检查仍有8个既有PDK类型错误，与本次修改无关；未点击最终添加按钮，未改变成员或队长关系。

### 2026-09-15｜队长查看战绩与报表返回结构兼容修复

- 用户反馈：亲友圈“队长”页的“查看战绩”和“查看报表”点击后报错。
- 根因/结论：当前V2兼容网关将两个列表包装为`{items: [], list: []}`，客户端却按原生数组直接调用`forEach`/迭代，分别产生`rows.forEach is not a function`和`rows is not iterable`。
- 实现结果：`LegacyClubPromotionController.ts`新增单一列表解包边界，同时兼容原生数组、`items`数组和`list`数组；战绩和报表请求补充受控的开始/成功/失败诊断日志，只记录亲友圈ID、目标ID、页码和返回条数。
- 验证结果：专项契约测试`2/2`通过；Creator 3.8.8运行Preview完成脚本热编译，服务端编译块已包含新日志与解包逻辑；账号31在亲友圈`549245`通过真实Canvas先后点击两个按钮，空数据页正常打开，`record-list-success`/`report-list-success`均为`rowCount: 0`，`pageerror=[]`。
- 测试数据清理：为定位入口曾将已有成员PID`581`临时设为队长，验收后已定向恢复`promotionManager=false`，未删除该成员或改动其他字段。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-promoter-report-record-2026-09-15T13-18-00-0800/`。
- 未完成项：全项目TypeScript检查仍有8个既有PDK类型错误，与本次修改无关。

### 2026-09-15｜队长子窗口关闭后行按钮失效修复（终验待续）

- 用户反馈：首次打开并关闭“查看报表”或“查看战绩”后，队长列表中其余操作按钮全部无法点击；关闭并重开队长界面后暂时恢复。
- 根因/结论：所有表单的动态行按钮共用单一清理数组，子窗口渲染和关闭时全局清理，误解绑了仍显示的底层队长列表按钮；现改为按表单根节点分组，刷新/关闭只清理当前窗口监听。
- 修改范围：`LegacyClubPromotionController.ts`、队长报表响应契约测试及专项证据；未修改Prefab、Scene、meta、布局、协议、服务端或成员关系。
- 验证结果：专项测试`3/3`通过；Creator 3.8.8已热编译且Preview产物包含`rowDisposers = new Map()`；账号31真实Canvas进入亲友圈`549245`队长列表，`pageerror=[]`。
- 未完成项：当前队长列表只有本人PID`580`，本人行按规则不显示操作按钮，无法在不改变业务数据的前提下执行完整连续按钮终验；安全断点和证据位于`Client/docs/前端框架规范/历史记录与证据/证据/club-promoter-listener-scope-2026-09-15/`。

### 2026-09-15｜队长列表首行本人固定只读

- 用户确认：队长列表第一行固定显示自己，自己不能对自己操作，只展示数据，图中整组操作按钮不得显示。
- 根因/结论：旧实现主要依赖登录玩家ID与列表显示ID比对，字段不一致时可能误把首行识别为他人；现保留服务端原始顺序并将索引`0`固定识别为本人，同时保留身份ID兜底判断。
- 修改范围：`LegacyClubPromotionController.ts`、专项契约测试、验收记录和本开发记录；未修改Prefab、Scene、meta、布局、协议、服务端或业务数据。
- 验证结果：专项测试`4/4`通过；本次文件TypeScript错误为`0`，全项目只剩既有PDK类型错误；Creator 3.8.8执行“刷新预览”，最新产物包含首行只读判断；账号31真实Canvas截图确认PID`580`首行仅展示数据且无操作按钮。
- 未完成项：无。

### 2026-09-15｜房费设置自动填写玩法底分名称

- 用户要求：新建模板房进入房费设置时自动填写房间名称，例如选择凉山跑得快、底分2时显示“跑得快2分”。
- 根因/结论：房费设置只读取已有`roomName`，新建时为空；入口实际已持有权威玩法展示名、地区分类名和底分。现将三者传入参数页，移除地区前缀后生成`玩法名+底分+分`，已有模板名称仍优先保留。
- 修改范围：`PlaySelectorController.ts`、`ClubRoomFeeController.ts`、专项测试、验收证据和本开发记录；未修改Prefab、Scene、meta、规则表、协议或服务端。
- 验证结果：专项测试`1/1`通过；Creator 3.8.8热编译完成；账号31在亲友圈`549245`通过真实Canvas选择凉山跑得快、底分2并进入下一步，房间名称显示“跑得快2分”，`pageerror=[]`。
- 未完成项：无；未点击创建按钮，未产生测试模板房数据。

### 2026-09-15｜玩法场次双层筛选与侧栏开关接入

- 用户要求：`GameTabs`显示“全部+已创建玩法”，`RoomTabs`显示“全部+所选玩法的房间名称”；删除旧`RoomFilter/PlayList`引用，并按最新层级将`Btn_Tabs`接到`FavoriteList`下，实现侧栏切换和点外收回。
- 根因/结论：旧控制器仍递归绑定已删除的`PlayList`且只有单层房间筛选；现改为按稳定玩法身份和模板房名称生成双层筛选，`Btn_Tabs`最终路径为`GameTabs/FavoriteList/Btn_Tabs`。
- 修改范围：`ClubMain.prefab`保存用户已完成的节点调整，修改`LegacyClubMainController.ts`和专项测试；未恢复旧节点，未修改服务端、协议、规则表或业务数据。
- 验证结果：专项测试`3/3`通过，本次控制器TypeScript错误为`0`；Creator热编译产物包含最新嵌套路径。账号32真实Canvas验证亲友圈`549245`中嵌套按钮可打开侧栏、再次/点外收回，日志依次为`open:true`和`open:false`，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/04-btn-tabs-nested-open.png`。
- 未完成项：无。

### 2026-09-15｜场次“全部”按钮恢复当前玩法全部桌子

- 用户要求：选择单个场次后，点击`RoomTabs`的“全部”按钮，应清除场次筛选并显示当前玩法下的全部桌子。
- 根因/结论：预制体内存在两个同名`Btn_Play`，通用递归查找把点击监听绑定到了另一个隐藏按钮；现改为直接在`RoomTabs/View/Content`取得的实际“全部”模板节点上绑定，点击仅清空`selectedRoomFilter`，保留当前玩法筛选并重绘场次和桌子。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、规则表、协议、服务端或业务数据。
- 验证结果：专项测试`5/5`通过；Creator Preview热编译产物包含最新监听。账号32在亲友圈`549245`真实Canvas先选择`Tag1`，再点击“全部”，日志从`selectedRoom: Tag1`恢复为`selectedRoom: all`且`roomCount: 16`，全部桌子重新显示，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/08-single-room-final.png`、`09-all-rooms-final.png`。
- 未完成项：无。

### 2026-09-15｜更正：场次“全部”严格限定当前玩法

- 用户纠正：左侧选择成都跑得快后，右侧“全部”只能显示成都跑得快的全部场次和桌子，不得混入凉山跑得快等其他玩法。
- 根因/结论：活动桌精简投影可能只携带跑得快公共数字`gameId`，成都`CD201`与凉山`LS201`会被数字反查误归为同一地区玩法；现先通过活动桌的`tagId/configId`关联权威模板，再继承模板的稳定地区玩法编码执行筛选。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、规则表、协议、服务端或业务数据。
- 验证结果：专项测试`6/6`通过；Creator 3.8.8执行“刷新预览”，产物包含最新模板归属逻辑。账号32真实Canvas依次选择成都跑得快、单场次和“全部”，日志始终保持`selectedGame: CD201`，仅将`selectedRoom: Tag1`恢复为`all`，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/10-selected-game-all-final.png`。
- 未完成项：测试亲友圈`549245`当前只返回成都跑得快模板，跨地区隔离由稳定编码专项测试覆盖；未创建或修改模板房数据。

### 2026-09-15｜RoomTabs跟随玩法侧栏联动滑动

- 用户要求：`RoomTabs`必须跟随`FavoriteList`一起滑出、收回；再次点击`Btn_Tabs`或点击侧栏外部关闭时均保持同步。
- 实现结果：分别保存两个节点的收起基准横坐标，使用相同`0.2s quadOut`动画和`FavoriteList`宽度作为位移量；展开、按钮收回及点外收回均复用同一联动入口，不改变两个节点原始纵坐标和各自横坐标偏移。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、布局参数、协议、服务端或业务数据。
- 验证结果：专项测试`6/6`通过；Creator 3.8.8执行“刷新预览”。账号32在亲友圈`549245`真实Canvas验证：展开时`FavoriteList.x: 0→250`且`RoomTabs.x: -3.226→246.774`，点外与按钮收回均恢复原坐标，日志`roomTabsFollowing: true`，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/11-room-tabs-follow-open.png`、`12-room-tabs-follow-closed.png`。
- 未完成项：无。

### 2026-09-15｜玩法侧栏仅点击RoomTabs保持展开

- 用户要求：`FavoriteList`展开后，仅点击`RoomTabs`区域不自动收回；点击玩法列表、桌子区域、顶部、底部及其他位置全部自动收回，`Btn_Tabs`继续负责自身切换。
- 根因/结论：原关闭监听使用冒泡阶段，玩法按钮等交互节点会提前停止触摸传播，导致点击后侧栏不收回；现改为根表单触摸捕获监听，并只豁免`RoomTabs`与`Btn_Tabs`目标树。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、布局、协议、服务端或业务数据。
- 验证结果：专项测试`6/6`通过；Creator 3.8.8执行“刷新预览”。账号32在亲友圈`549245`真实Canvas验证：点击`RoomTabs`后两节点保持展开；点击玩法按钮或桌面其他区域后两节点均恢复收起坐标，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/14-favorite-autoclose-capture-final.png`。
- 未完成项：无。

### 2026-09-15｜更正：GameTabs全区域保持展开

- 用户最终确认：`GameTabs`所属的整个界面都不触发自动收回，包括`FavoriteList`、玩法按钮、`RoomTabs`、场次按钮和`Btn_Tabs`；仅点击`GameTabs`外部区域自动收回。
- 实现结果：点外判断统一提升到`GameTabs`根节点包含关系，删除对子区域的分别判断；`Btn_Tabs`仍通过自身监听执行主动展开/收回。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、布局、协议、服务端或业务数据。
- 验证结果：专项测试`6/6`通过，Creator Preview产物包含最新根节点判断。账号32在亲友圈`549245`真实Canvas验证：点击玩法和场次后`FavoriteList.x=250`、`RoomTabs.x=246.774`保持展开；点击桌面外部后恢复`0/-3.226`，`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/15-gametabs-scope-final.png`。
- 未完成项：无。

### 2026-09-15｜删除左上角“全部玩法”文字

- 用户要求：删除返回按钮下方显示的“全部玩法”，代码中不得再向该位置写入此文本。
- 根因/结论：该文字显示在`ClubMain/Top/Lb_PlayName`（旧路径`top/lb_cityName`），由`LegacyClubMainController`初始化和进入亲友圈时两次主动写入，并非预制体默认文字；现已删除两处赋值。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未删除或修改Prefab节点、Scene、meta、布局、协议、服务端或业务数据。
- 验证结果：亲友圈代码目录不再包含“全部玩法”运行文本；专项测试`6/6`通过，Creator 3.8.8执行“刷新预览”。账号32真实进入亲友圈`549245`后`Lb_PlayName.text=""`且`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/16-playname-removed-final.png`。
- 未完成项：无。

### 2026-09-15｜隐藏已开房间勾选项修复

- 用户反馈：`ClubMain/HideRoomToggle`勾选后没有生效。
- 根因/结论：预制体中的`Toggle.checkMark`为空，且旧筛选只按`setId`隐藏已经开局的桌子，仍会保留已创建但等待中的实体房间；现运行时绑定`Checkmark`的`Sprite`并同步显隐，筛选统一按`roomId/roomID > 0`识别已开实体房，永久模板桌始终保留。
- 修改范围：`LegacyClubMainController.ts`、隐藏房间专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、规则表、协议、服务端或业务数据。
- 验证结果：专项及关联测试`7/7`通过；Creator 3.8.8脚本热编译产物包含最新逻辑。账号32在亲友圈`549245`真实Canvas点击后，勾选状态与勾图均由`true`切换为`false`，日志为`[ClubRoomFilter] hide-opened-change`，`entityRoomCount: 0`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/17-hide-room-toggle-final.png`。
- 未完成项：测试亲友圈当前没有实体房间，无法现场展示桌子数量变化；实体房过滤条件由专项测试覆盖，未为验收创建业务数据。

### 2026-09-15｜GameTabs玩法选择会话缓存

- 用户要求：玩家主动选择`GameTabs`玩法后，关闭并重新进入亲友圈时始终保持最后一次选择；只有刷新页面、刷新Preview或杀进程后才默认显示“全部”。
- 实现结果：新增按`playerId + clubId`隔离的JavaScript进程内缓存；选择具体玩法或“全部”时更新缓存，重新打开`ClubMain`时恢复；未写入`localStorage`等永久存储。若权威模板已返回且缓存玩法已不存在，则安全回退并缓存“全部”。
- 修改范围：`LegacyClubMainController.ts`、玩法场次专项测试、本开发记录与验收截图；未修改Prefab、Scene、meta、规则表、协议、服务端或业务数据。
- 验证结果：专项及关联测试`8/8`通过；Creator 3.8.8执行“刷新预览”。账号32真实Canvas选择成都跑得快后退出并重新进入亲友圈`549245`，日志恢复`selectedGame: CD201`；刷新页面后恢复`selectedGame: all`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/18-game-tab-session-selected.png`、`19-game-tab-session-restored.png`、`20-game-tab-refresh-default-all.png`。
- 未完成项：无。

### 2026-09-15｜桌子列表最右列完整显示

- 用户反馈：桌子列表滑到最右侧后，最后一列仍被裁切；`ClubDesk.prefab`中当前启用的`Players_*`节点才是桌子实际宽度来源。
- 根因/结论：旧代码固定使用`317`列间距，并把带`-320`右偏移的1600宽`mark`当作可见区，ScrollView误认为屏幕外区域也可见；现将运行时滚动视口限制为`RoomList`的1280宽度，并按每张桌当前启用的`Players_* / UITransform.contentSize.width`乘根节点缩放计算每列宽度、中心坐标及内容末端留白。
- 修改范围：`LegacyClubMainController.ts`、桌子布局与运行时选择专项测试、本开发记录和验收截图；未修改或保存`ClubDesk.prefab`、`ClubMain.prefab`、Scene、meta、规则表、协议、服务端或业务数据。
- 验证结果：专项及关联测试`10/10`通过；Creator 3.8.8执行“刷新预览”。账号32在亲友圈`549245`通过真实Canvas连续向左拖动到最右端，16张桌完整保留，`Players_2`设计宽400、显示宽280；最后一列右边界`1366.62`小于可视区右边界`1420.46`，保留约`53.83`安全间距。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-tabs-2026-09-15/21-desk-last-column-complete.png`。
- 未完成项：无。

### 2026-09-15｜当前房间详情迁入ClubMain

- 用户已将原`ClubInRoom.prefab`界面迁入`ClubMain/RoomDetails`，要求代码改用新嵌入节点。
- 实现结果：移除`UIClubInRoom`独立表单注册与打开路径，改为`ClubMain`Controller统一绑定、展示、更新和关闭`RoomDetails`；大厅等待座位入口通过`legacy-club-show-current-room`事件打开详情。节点路径与当前预制体一致，不再假设存在`data`中间层。
- Creator同步：通过Cocos Creator 3.8.8保存用户当前的`ClubMain.prefab`编辑内容，预览重载后运行场景已找到`RoomDetails`，默认`active=false`，直接子节为`btn_showmore/table/mask/btn_exitroom/btn_goroom`。
- 修改范围：`LegacyClubMainController.ts`、`LobbyScreenController.ts`、`ClubMain.prefab`、两个当前房间入口专项测试及本开发记录；保留旧`ClubInRoom.prefab`资源文件，运行时已不再加载它。
- 验证结果：当前房间、等待座位及RoomTabs关联测试`13/13`通过；真实Creator Preview确认嵌入节点已载入。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-room-details-2026-09-15/01-embedded-room-details-ready.png`。
- 未完成项：当前测试账号不在等待房间中，未创建或修改业务房间数据，因此本次未对填充后的详情窗口做真实点击终验。

### 2026-09-15｜凉山跑得快桌面占座与二次点击修复

- 用户确认业务规则：首次点击凉山跑得快桌子只占座并在桌面显示玩家头像；同一张桌第二次点击才进入房间；亲友圈右上角返回按钮先退出当前等待房间。
- 根因：第二次点击时旧逻辑只比较实体`roomId`，将模板桌与它创建的实体等待桌误判为两张桌，因此执行退桌再占座并提示“换桌成功”；占座后的权威座位数据仅用于详情层，未立即投影到桌面。
- 实现结果：等待桌点击改按`tagId/configId`、`templateCode`及玩法+房间名识别“同一模板桌/实体桌”，同桌二次点击直接进房，仅真正改选其他桌时提示换桌成功。占座成功或重进亲友圈读到当前房间时，立即合并权威`posList`并重绘桌面头像。
- 返回逻辑：保留已有的`room.CBaseExitRoom`权威退房链路，退房后刷新桌面；未在等待房时才正常关闭亲友圈。
- 诊断日志：新增`[ClubWaitingDesk] tap-routing`与`seated-room-projected`，记录`clubId/playerId/roomId`、点击分流和已投影座位数，不记录Token或大对象。
- 修改范围：`LobbyScreenController.ts`、`LegacyClubMainController.ts`、等待桌契约测试及本开发记录；未修改Prefab、Scene、meta、规则表、服务端或房间配置。
- 验证结果：等待桌、当前房间、桌型与玩法场次关联测试`14/14`通过；Cocos Creator 3.8.8已执行“刷新预览”，真实Preview加载`MainScene`且`pageerror=[]`。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-waiting-desk-2026-09-15/01-preview-synced.png`。
- 未完成项：本次测试账号所在亲友圈`549245`未返回`LS201`凉山跑得快模板，为避免越权新建或改动玩法配置，本次无法执行“首次占座→头像显示→同桌二次进房→返回退房”的完整真实点击终验。

### 2026-09-16｜更正：游客388真实账号验收与启动服务阻塞

- 用户指定测试账号：游客388，ID `870766`；已在本任务隔离的无头Chrome中真实登录，大厅显示`Player388 / ID:870766`，并进入亲友圈`384801`。
- 现场结果：热刷新前已实点凉山跑得快`LS201`的`跑得快2分`桌，服务端分配等待房间`372541`；第二次点击已进入`GameRoom2D`，并非停留在亲友圈。这次真实数据揭示`HideRoomToggle`旧实现错把所有已开等待桌隐藏，已更正为只隐藏已满房间，占座玩家所在的未满实体桌继续显示头像。
- 返回逻辑更正：从玩法返回后的精简房间投影可能不带稳定玩法字段；亲友圈返回现直接依据权威`roomId > 0`调用`room.CBaseExitRoom`，不再被玩法识别阻断。
- Creator同步：Cocos Creator 3.8.8执行“刷新预览”，预览分片已包含`hideFullRooms`、`seated-room-projected`与新返回判断。关联契约测试`5/5`通过。
- 未完成项：最后一轮热刷新后，启动版本检查请求出现`ERR_CONNECTION_REFUSED`，首个明确错误为`[BootstrapStage] version-check:failure：暂时无法连接启动服务`；因此最新热编译版的“首次占座头像→第二次进房→返回退房”连续终验尚未完成，不标记为全部闭环。
- 证据：`Client/docs/前端框架规范/历史记录与证据/证据/club-waiting-desk-2026-09-16/01-player388-login-confirmed.png`、`02-club-384801-server-state.png`。

### 2026-09-16｜凉山跑得快等待状态显示RoomDetails

- 用户确认：凉山跑得快`LS201`第一次点桌后是等待进入状态，必须留在亲友圈并显示`ClubMain/RoomDetails`。
- 根因/修复：`CBaseRoomConfig`的已占座精简投影可能只含`roomId/tagId`和公共数字玩法ID，旧`isWaitingEntry`无法识别`LS201`；现改为通过当前亲友圈模板关联恢复稳定玩法编码，使即时占座回包及重进亲友圈的恢复查询都会打开`RoomDetails`。
- 验证：等待桌、当前房间和隐藏房间关联测试`7/7`通过；Cocos Creator 3.8.8已执行“刷新预览”，预览分片已包含最新`gameFilterKey(room) === 'LS201'`判定。未修改或保存用户当前带`*`的`ClubMain.prefab`内存编辑内容。
- 未完成项：启动服务仍拒绝连接，待服务恢复后从游客388/ID`870766`的安全断点继续真实Canvas首次占座显示终验。

### 2026-09-16｜普通成员点击模板桌权限链修复

- 现象：游客388点击凉山跑得快模板桌时显示“模板桌进入链异常”，Gateway日志的首个明确错误为`HALL_CLUB_MANAGER_REQUIRED / club manager role required to create rooms`。
- 根因/修复：Bootstrap内部经`ClubDispatchService.roomTemplateForEntry`校验的“有效成员点已有模板”与外部新建亲友圈房间共用公共创建命令，被Hall管理员权限拒绝。现增加只由Bootstrap内部调用的`CLUB_TEMPLATE_ENTRY`来源：该来源只要求有效俱乐部成员；普通HTTP/客户端创建即使伪造`_creationOrigin`仍强制解析为`PUBLIC`，继续要求OWNER/MANAGER/ADMIN。Saga持久化保留可审计来源，补偿恢复按原来源执行。
- 变更：`Server/server/Hall/.../RoomCreateSaga.java`、`JdbcHallRepository.java`、`Server/server/Bootstrap/.../JdbcHallWebSocketDispatcher.java`及`RoomCreateSagaTest.java`；未修改数据库、玩法规则表或其他模块业务。
- 验证/部署：Hall+Bootstrap定向编译成功；`local-dev-services.sh restart gateway`已用新Bootstrap产物重启Gateway/Authority，核心服务全部健康。父级文档门禁被既有`.DS_Store`阻塞，全反应器测试又被Poker模块既有缺失`AypdkGameProvider/HbpdkSession`阻塞，均未越界处理。
- 未完成项：规范固定的CDP端口`1234`持续被另一隔离测试PID `3439`占用，本任务未终止或复用其现场；待端口释放后用游客388继续真实Canvas占座与`RoomDetails`终验。

### 2026-09-16｜未满等待房刷新后保持亲友圈

- 用户要求：凉山跑得快`LS201`首次占座进入等待状态后，即使刷新页面或恢复进程，只要人数未满都必须继续停留在亲友圈；房间满员或已经开局后才按正常恢复链进入玩法。
- 根因/修复：旧启动恢复只知道玩家存在活动房间，登录后无条件执行玩法handoff。Hall的`activeRoom`现返回权威`clubId/state/playerNum/occupiedCount/waitingFull`；客户端启动路由识别`LS201 + 未满 + 未开局`后清除旧玩法恢复票据，恢复上次亲友圈并跳过自动进入玩法，同时输出`[StartupRoomRecovery] waiting-club-room-held`诊断日志。
- 修改范围：`HallRoomGateway.ts`、`AuthoritativeSubgameHandoff.ts`、`SceneRouter.ts`、`JdbcHallRepository.java`及等待进入契约测试；未修改Prefab、Scene、meta、规则表或数据库结构。
- 验证/部署：客户端专项测试`5/5`通过；Hall定向编译通过，Hall与Gateway已重启且全部本地核心服务健康。
- 未完成项：规范固定的CDP端口`1234`仍由另一隔离测试PID `3439`占用，本任务未终止或复用其现场；Creator当前也由另一玩法任务占用。待资源释放后仍需执行Creator刷新及游客388真实Canvas“占座→刷新→仍显示ClubMain/RoomDetails”终验，当前不标记为全部闭环。

### 2026-09-16｜满员后两名等待玩家未自动进房

- 用户反馈：凉山跑得快桌面已显示两名玩家，但两个账号都没有被拉入玩法房间。
- 现场证据：数据库确认房间`644575`为`LS201/OPEN`，规则人数`2`、已加入人数`2`（账号`380/381`），权威快照`players={0:381,1:380}`且仍为`WAITING`；占座与桌面投影正常，问题位于满员后的客户端唤醒时序。
- 修复：Gateway在最后一名玩家加入后，不再只延迟`100ms`推送一次；改为在`100/500/1500ms`有界重复向该房全部成员发送幂等`club.waiting_room_ready`。客户端收到任一次通知后仍必须重新读取自己的权威房间和人数，再分别签发一次性票据进入玩法；保留每秒权威查询作为漏推兜底。
- 诊断日志：新增`[WaitingRoomReady] roomId/recipients/published/delayMs`，用于确认满员判定、目标成员数和每轮实际送达连接数，不记录Token。
- 验证/部署：Bootstrap反应器编译通过；Gateway已用标准脚本重新构建并重启，Gateway/Authority及本地核心服务健康。客户端Preview分片已包含满员监听与权威轮询代码。
- 未完成项：当前已经满员的旧房不会重新产生“最后一人加入”事件，两个页面刷新后会由`waitingFull=true`启动恢复直接进入；下一张新桌用于验证重复唤醒实时拉入。规范固定CDP端口`1234`仍由另一隔离测试占用，本任务未终止或复用该现场。

### 2026-09-16｜隐藏满房默认关闭与场次选中占位样式

- 用户要求：`ClubMain/HideRoomToggle`默认不勾选；`GameTabs/RoomTabs/.../Btn_Play`需要明确区分选中和未选中的按钮底板与文字颜色，当前可使用占位效果供后续美术替换。
- 实现：Prefab中`HideRoomToggle._isChecked=false`，Controller每次打开ClubMain时也显式重置为未勾选，避免旧缓存状态污染默认值。`Btn_Play`选中使用金黄底板`#FFCA4A`与深棕文字`#6F390C`，未选中使用蓝灰底板`#487EA6`与白字`#FFFFFF`；同时同步其已有`SelectToggle.isChecked`，后续替换SpriteFrame或颜色无需改选择逻辑。
- 修改范围：`ClubMain.prefab`、`LegacyClubMainController.ts`、两个亲友圈专项测试及本开发记录；未修改玩法规则、协议、服务端或其他Prefab。
- Creator同步：Creator 3.8.8已检测并载入磁盘Prefab变化，Inspector中的`HideRoomToggle/Is Checked`显示未选中；Preview脚本分片已包含新默认值与四色状态逻辑。保留用户当前Prefab未保存标记，未执行覆盖式保存。
- 验证：亲友圈场次与隐藏满房专项测试`9/9`通过。

### 2026-09-16｜Btn_Play与HideRoomToggle统一选择层级

- 用户明确层级：`Btn_Play`、`HideRoomToggle`都采用根节点挂`Toggle`，子节点固定依次为`NoSelect`、`YesSelect`、`Label`；`Toggle.Check Mark`指向`YesSelect`。
- 磁盘实现：`HideRoomToggle`原`Bg/Checkmark`更名为`NoSelect/YesSelect`，默认未勾选并绑定`YesSelect`精灵；`GameTabs/RoomTabs/View/Content/Btn_Play`新增`NoSelect/YesSelect`底板、保留`Label`，根节点新增Toggle并绑定`YesSelect`。占位底板仍为蓝灰未选中、金黄选中，文字由Controller切换白色/深棕色。
- 运行时：统一按`NoSelect.active=!selected`、`YesSelect.active=selected`及根`Toggle.isChecked=selected`更新，不再依赖旧`Icon_Selected/on/SelectToggle`结构。
- 验证：Prefab JSON可完整解析，目标节点路径、子节点顺序、Toggle与Check Mark引用均通过专项断言；亲友圈专项测试`9/9`通过。
- Creator同步阻塞：当前Creator内存中的`ClubMain.prefab*`仍有未保存改动，外部Prefab变化触发“保存/不保存/取消”提示；为避免覆盖用户内容已选择取消，未执行保存或丢弃。磁盘修改已完成，但编辑器最终重载待用户先保存当前内存改动后继续，当前不得标记Creator同步完成。

### 2026-09-16｜更正：统一选择层级已完成Creator同步

- 用户确认已保存Creator内存改动后，以该最新磁盘版本为基线重新应用`Btn_Play`与`HideRoomToggle`层级；关闭旧内存副本并重新打开`ClubMain.prefab`。
- Creator 3.8.8重新打开后，层级管理器已显示两处均为`NoSelect`、`YesSelect`、`Label`；磁盘解析确认根Toggle默认未选中且`Check Mark`分别指向对应`YesSelect`精灵。
- 关联专项测试重新执行`9/9`通过；控制台未新增缺失UUID、组件反序列化或资源导入错误，现有`bg1.jpg/bg1.png`动态加载URL重复警告与本次改动无关。

### 2026-09-16｜满员玩家均已连房但先入玩家仍显示亲友圈

- 用户反馈：凉山跑得快满员后只有最后进入的玩家切到玩法画面，先入玩家仍停留在亲友圈。
- 现场证据：房间`813490`的`100/500/1500ms`三轮`[WaitingRoomReady]`日志均为`recipients=2,published=2`；Gateway随后记录账号`380/381`都建立了该房间的玩法连接，证明满员广播、个人handoff和玩法WebSocket均已成功，故障位于客户端连接成功后的视觉切换边界。
- 根因/修复：`CommonPdkSwitchCoordinator`旧逻辑直到`PDK_ROOM_FORM.onShow`才隐藏大厅根节点；先入玩家若房间Prefab解码或初始呈现较慢，会在已经连房后继续露出`ClubMain`。现改为`gameClient.connect(authorityRoute)`成功后立即隐藏大厅/亲友圈，再进行权威进房和房间层展示；后续初始化失败时恢复大厅显示并沿用既有恢复链。
- 诊断日志：新增`[CommonPdkRoomEntry] authority-connected`，记录`roomId/playerId/gameCode`，不记录票据。
- 验证：房间无缝切换测试中的新增连接边界、顺序及失败回滚断言通过；等待进入契约测试`5/5`通过。关联测试组另有既有`prewarmDefaultRoom`、启动计数和`lastActions`断言失败，未越界修改。
- 未完成项：Creator尚未生成包含新日志的Preview分片；当前`ClubMain.prefab*`存在用户未保存编辑，且规范固定CDP端口`1234`被另一隔离测试占用，未关闭编辑器、未保存预制体、未复用或终止该测试现场。待Creator脚本刷新后执行两账号真实满员同步切换终验。

### 2026-09-16｜持续 develop 提交权限

- 用户授权：所有 AI 可将各自职责范围内已形成闭环、可明确归属的改动提交并推送到对应仓库 `develop`，无需逐次申请；仍禁止 `main`、强推、改写历史和夹带无关改动，每次结果必须报告。

### 2026-09-16｜四人满员时仅最后加入者进入玩法

- 用户现场：亲友圈`722583`的四人桌已满，但仅最后加入者进入玩法，其他已占座玩家仍停留在亲友圈。
- 服务端证据：房间`193273`与`231728`的`100/500/1500ms`三轮`[WaitingRoomReady]`均为`recipients=4,published=4`，证明四名成员都收到定向满员通知；旧客户端中房间`193273`仅最后加入账号建立玩法连接，问题不在服务端收件人计算。
- 根因/修复：桌面满员快照会预先写入`waitingHandoffRoomId`作为临时去重锁，但在Lobby实际消费进房事件前，权威`club.waiting_room_ready`监听又因同一roomId直接返回，导致先入座玩家吞掉全部三轮唤醒。现由权威满员通知先取消临时重试、清除该旧锁，再重新读取当前账号的权威房间并签发独立进房票据；真正的并发保护仍由Lobby和玩法切换协调器负责。
- 诊断日志：新增`[ClubWaitingDesk] ready-releases-stale-handoff`，只记录`clubId/playerId/roomId`，用于确认每个客户端都已释放旧锁。
- 验证：等待进入契约测试`5/5`通过；Cocos Creator 3.8.8已执行“刷新预览”，Preview分片确认包含新日志和修复代码，未保存或改写当前预制体。固定CDP端口`1234`仍被另一隔离测试占用，本任务未复用或终止该现场，因此账号`1`四客户端真实Canvas终验仍待端口释放后执行。

### 2026-09-16｜更正：四人满员后全部未进入玩法

- 新现场证据：房间`116981`三轮满员推送仍为`recipients=4,published=4`，但之后没有任何该房间的玩法连接，说明四个客户端均在签票前返回。
- 补充根因/修复：权威推送处理在核对当前roomId和满员人数后，仍调用`isWaitingEntry(current)`；精简`CBaseRoomConfig`未携带gameCode时会把四名玩家全部拦截。现将`expectedRoomId>0`的权威满员事件直接视为已确认等待房，仅无事件来源的一秒轮询保留LS201玩法判定；仍严格校验当前账号roomId与事件roomId一致以及权威人数已满。
- 诊断日志：增加`ready-room-config-failed`、`ready-room-config-skipped`和`ready-handoff-emitted`，记录房间、玩家、人数及拦截原因，不记录Token或进房票据。
- 验证：等待进入契约测试`5/5`通过；Creator 3.8.8再次刷新预览，Preview分片已确认包含`waitingEntryConfirmed`和`ready-handoff-emitted`。

### 2026-09-16｜Git 权限更正：仅明确指令或每日快照

- 更正原因：用户最新明确要求撤回 AI 自主提交权限；此前“持续 develop 提交权限”记录不再作为执行依据。
- 最终规则：只有用户针对具体任务明确说“提交”才允许提交该任务；否则仅由北京时间 `02:00` 的每日固定任务统一提交。

### 2026-09-16｜更正：满员唤醒后的单玩家一次性票据竞争

- 后续现场证据：房间`443239`三轮`[WaitingRoomReady]`均为`recipients=4,published=4`，账号`380/387/381`先后建立玩法连接，另一个客户端被Gateway以`WS_TICKET_REJECTED`拒绝；证明满员广播和全员handoff已经生效，剩余失败点是一名玩家的单次WebSocket票据在并发恢复期间失效或已被消费。
- 修复：`CommonPdkSwitchCoordinator`首次玩法WebSocket连接失败后，关闭失败连接，为当前玩家重新调用`refreshRoomConnection(roomId)`取得独立新票据，并有界重连一次；重连仍失败则进入既有失败回滚，避免无限重试和多个玩家共用票据。
- 诊断日志：新增`[CommonPdkRoomEntry] authority-ticket-refresh`，记录`roomId/playerId/reason`，不记录票据内容。
- 验证：房间切换专项断言`1/1`、等待进入契约`5/5`通过；Creator 3.8.8已执行“刷新预览”，Preview分片确认包含票据刷新与重连逻辑，未保存或改写`ClubMain.prefab`。固定CDP端口`1234`仍被其他隔离测试占用，未终止或复用该现场，因此四客户端真实Canvas终验仍待新房实测。

### 2026-09-16｜返回大厅前检查并退出当前房间

- 用户要求：点击亲友圈返回大厅按钮时，先检查玩家是否仍在房间；在房间内先退出房间再返回大厅，不在房间则直接返回。
- 根因/修复：原返回处理已查询`room.CBaseRoomConfig`，但退房成功后只刷新亲友圈桌面而未关闭主界面；查询失败时反而直接返回大厅。现改为查询成功后按权威`roomId`分流：有房调用`room.CBaseExitRoom`且成功后关闭亲友圈，无房直接关闭；查询或退房失败均留在当前界面提示重试。
- 诊断日志：新增`[ClubBack] active-room-checked/active-room-check-failed/room-exited/room-exit-failed/no-active-room`，关联`clubId/playerId/roomId`且不记录凭证。
- 验证结果：等待房返回及关联专项测试`6/6`通过；Creator 3.8.8执行“刷新预览”，editor与preview编译分片均包含新逻辑。
- 未完成项：岗位专用CDP端口`1237`当前被既有隔离Chrome PID `96074`占用，未终止或复用该现场；待端口释放后真实点击复验有房、无房两条路径。

### 2026-09-16｜更正：兼容链路无房错误应直接返回

- 用户现场：点击返回按钮出现“房间状态检查失败，请重试”。
- 根因/修复：新Hall链路以`roomID:0`表示无房，旧兼容链路则可能以`PLAYER_NOT_ROOM/玩家不在房间/not in room`错误表示相同状态；此前把后一种情况误当成查询故障。现仅将明确的无房错误归一为无房并直接返回大厅，连接断开、超时及未知错误仍留在亲友圈提示重试。
- 验证结果：返回逻辑及关联专项测试`6/6`通过；Creator 3.8.8再次刷新预览，editor与preview分片均包含兼容无房判定。
- 未完成项：沿用上一条CDP端口占用阻塞，待释放后真实点击确认截图中的提示不再出现。

### 2026-09-16｜再次更正：返回大厅使用双查询兼容

- 用户反馈：加入无房错误归一后仍显示相同检查失败提示，证明现场错误并非已知无房文案，而是该部署环境的`room.CBaseRoomConfig`查询不可用或返回未知错误。
- 修复：返回逻辑优先查询`room.CBaseRoomConfig`；非明确无房错误时再调用大厅启动使用的`game.C1101GetRoomID`获取权威房间号。兜底成功后仍按房间号执行退房或直接返回；只有两种查询都失败才提示检查失败。
- 诊断日志：新增`[ClubBack] active-room-fallback-used`，记录首查询失败原因但不记录凭证。
- 验证：本功能定向契约`1/1`通过，Creator Preview分片已包含双查询代码；完整等待房测试组另有并行改动导致的既有“满员快照调用顺序”断言失败，本任务未修改该逻辑。
- 未完成项：等待用户在已刷新页面复测当前现场；CDP`1237`占用状态不变。

### 2026-09-16｜框架收敛：大厅会话唯一消费满员切场

- 最新证据：房间`860151`满员推送为`recipients=4,published=4`，但只有账号`381/380`建立玩法连接，且连接发生在满员推送前；满员后其余账号没有稳定发起切场。继续调整玩法连接层不能解决上游入口竞争。
- 框架根因：服务端重复推送、ClubMain一秒轮询、桌面快照满员检测及Lobby页面重试都能独立触发签票/切场。同一玩家短时间产生多个handoff会让后签发的一次性票据覆盖前票据，同时页面级去重锁和Prefab生命周期会丢失后续入口。
- 架构修复：`LobbyScreenController`作为Hall会话唯一满员协调器，直接订阅`club.waiting_room_ready`，按当前账号调用`activeRoom()`核对`roomId/waitingFull`并取得独立票据，再通过统一玩法注册表切场；忙碌或权威投影延迟时最多重试10次。`LegacyClubMainController`只记录满员通知并刷新桌面表现，不再签票、切场或维护页面级handoff锁。
- 清理：删除ClubMain的`enterFullWaitingRoom`、`enterCurrentWaitingRoomWhenFull`、页面级重试锁，以及Lobby旧`forceEnter`分支和已无调用的`resumeWaiting`接口，避免并行入口复活。
- 诊断日志：大厅统一记录`[LobbyWaitingRoomReady] handoff-started/handoff-retry`；亲友圈仅记录`[ClubWaitingDesk] ready-observed`。
- 验证：等待进入契约`5/5`、房间切换专项`1/1`通过；Creator 3.8.8已刷新预览，Preview分片确认包含大厅唯一协调器且不含旧页面级入口。本次只改TypeScript和测试，未保存或改写Prefab；四客户端真实Canvas仍需用刷新后的页面新建房间终验。

### 2026-09-16｜第三次更正：查询均失败时使用当前座位投影

- 用户反馈：双查询版本仍出现相同检查失败提示。
- 修复：两种服务查询都失败时，改由亲友圈当前桌面快照检查实体房`posList`是否包含当前`playerId`；包含则按该房间号退出后返回，不包含则直接返回大厅，不再弹出“房间状态检查失败”。明确无房错误和两种服务成功返回的原分支保持不变。
- 诊断日志：新增`[ClubBack] active-room-local-fallback`及`no-active-room source=local-projection`，保留两次查询错误上下文用于定位。
- 验证：本功能定向契约`1/1`通过；Creator 3.8.8刷新预览后分片已包含本地座位兜底逻辑。
- 未完成项：等待用户刷新当前页面后复测返回按钮；未提交、未推送。

### 2026-09-17｜队长列表日期筛选按钮恢复交互

- 用户要求：修复亲友圈“队长”界面左侧“常用操作、今天、昨天、前天及历史日期”按钮点击无效。
- 根因/结论：按钮已有点击监听和接口请求，但未更新预制体既有 `on/off` 选中节点，也未初始化四个历史日期占位文字，导致点击后界面无视觉反馈；现统一由筛选状态驱动选中底板，并在打开界面时生成实际日期。
- 修改范围：`LegacyClubPromotionController.ts`、定向契约测试及本文档；未修改预制体和服务端。
- 验证结果：Creator 3.8.8 已正常热编译至 Preview；定向测试 1/1 通过；使用专属无头 Chrome（CDP `1237`、测试账号 `31`/玩家 `580`）真实进入亲友圈 ID `549245`，点击“昨天”后黄色选中态切换且列表重新渲染，点击“常用操作”后选中态再次正确切换，历史日期显示为实际日期。证据：`/tmp/aoo-club-period-yesterday.png`、`/tmp/aoo-club-period-common.png`。
- 未完成项：无。


### 2026-09-18｜Bug 根因修复原则

- 用户要求：所有 AI 处理 Bug 禁止采用补丁式修复，必须先理清完整逻辑和根因，再修正权威业务路径。
- 执行结论：禁止通过空值兜底、吞错、延时重试、硬编码特例、兼容分支或并行旧逻辑绕过根因；修复后必须清理错误和临时逻辑，并完成针对性与真实流程验收。
- 修改范围：仅岗位规范；未修改业务代码、资源、配置、数据库或规则表。

## 7. 记忆

- 岗位边界：全栈负责亲友圈客户端、`union` 服务端接口与数据、相关协议、数据库迁移、配置映射、部署和真实 E2E；不处理大厅、具体玩法和公共框架。
- 记录边界：开发记录与记忆只保存亲友圈岗位信息，禁止跨岗位混写。
- 待处理队列：① 点击模板桌直接进入游戏房间（来源：用户当前反馈；状态：进行中；职责范围：亲友圈模板桌点击、建桌/进房协议及游戏handoff；安全断点：已复现handoff缺少`gameName`导致切换协调器报错，待核对点击事件与服务端返回字段）。② 赛事测试数据清理（待授权/重新分配）——只读确认`408982`/union`245351`与`705400`/union`161336`均`ACTIVE`且templates/tables为`0`，`309407`、`338284`已`DISSOLVED`；因前两项归属旧账号数据，未越权登录或清理，已向aoo产品请求用户授权或重新分配。③ 亲友圈保险箱真实Canvas终验（来源：用户当前要求；状态：阻塞；职责范围：更多菜单打开保险箱、余额/流水、公共数字键盘、存取与关闭；安全断点：代码与Creator同步已完成，专用无头Chrome当前游客账号无亲友圈，只能到达加入/创建页，获得已加入亲友圈的测试账号后从“更多→保险箱”继续，不重复静态修改）。队列仅保留尚未完成的问题，并按提出顺序记录状态与相关路径；已完成结果移入“开发记录”，过期信息及时清除。
- 上下文与重读：对话过长时自动压缩旧内容，删除重复、过期和无用信息，仅保留已确认需求、架构决定、重要路径、未完成任务及最近 10 轮对话原文，不询问用户或中断任务，压缩后立即继续。仅在上下文压缩后、用户要求写入规范且更新完成后、准备切换新的业务问题时，或记忆不确定、规则冲突、职责边界不清时，重新读取本岗位执行提示词；同一问题连续处理中不得重复读取。
- 账号隔离：本地测试仅用`31`至`40`，密码为`1`；并行任务记录实际账号，被占用时只在本段换号。禁止操作其他岗位账号、会话和房间数据；`51`至`100`未经aoo产品分配不得使用。
- 停止条件：只要存在已授权、可执行任务就持续执行；仅全部闭环，或只能等待用户决定、授权且无其他独立任务时可idle。任何停止、idle、异常结束或准备不再继续都须向aoo产品提交包含当前任务、完成/未完成项、阻塞、队列、安全断点和原因的停止报告；未授权历史审计项不自动入队。
