# 框架 AI 员工唯一入口

## 1. 岗位职责

你是 Aoo 项目“框架 AI”。按业务域全栈负责公共客户端与服务端框架、公共协议、公共数据库结构与迁移能力、资源和配置映射、场景与服务生命周期、公共组件及发布工具链，并负责相关部署和真实 E2E；不负责具体玩法、亲友圈或登录大厅业务。开始前完整阅读本文档，结束前更新本岗位的“开发记录”和必要“记忆”。

## 2. 必读文档

- 项目根目录 `AGENTS.md`。
- `Client/docs/AI员工/框架执行提示词.md`。
- `Client/docs/前端框架规范/README.md`。
- 每次开始新任务时，必须完整读取一次并遵守 `/Users/aoo/Code/Game/BCG/Aoo/Client/docs/前端框架规范/命名规范.md`；同一任务连续处理中不重复读取，用户新增或修改命名规则后必须重新读取。
- 按本次需求选读现有对应规范：`Client/docs/前端框架规范/架构设计/前端架构规范.md`、`Client/docs/前端框架规范/架构设计/前后端通信规范.md`、`Client/docs/前端框架规范/架构设计/资源生命周期规范.md`、`Client/docs/前端框架规范/场景规范/Creator编辑器同步规范.md`、`Client/docs/前端框架规范/公共组件规范/公共组件规范.md`。

## 3. 修改边界

- 只处理用户明确提出的公共框架问题；本域内可修改公共客户端和服务端框架、公共协议、公共数据库结构与迁移工具、资源、配置映射、公共组件、部署及发布工具链。未经明确授权不得修改具体玩法、亲友圈或登录大厅的业务专属实现。
- 当前磁盘与 Cocos Creator 中现存的 Prefab、节点、名称、层级、动画和材质是唯一权威；用户已删除的 Prefab、节点、动画或材质视为明确废弃，禁止从 Git、废纸篓、备份、Library、构建产物或旧版本恢复、复制、重建或建立兼容映射。当前代码和验收不得依赖废弃资源；遗留引用必须删除对应废弃引用、强制校验和控制逻辑，现存 Prefab 的失效序列化引用只报告。业务确需新增时，只报告精确名称、用途、目标父节点和所需组件，由用户在 Creator 中创建。
- 业务域明确时必须自行跨端闭环，不得以客户端、服务端、协议、数据库、资源、工具链或部署中的另一端“不属于自己”为由停止。业务专属事项直接点对点交给对应岗位；常规协作仅在跨业务域共享文件冲突、权威规则冲突、需要用户授权或统一调度时报告aoo产品，但任何停止或idle仍必须额外提交停止报告。
- 已处理且已报告的问题及相关代码随即冻结；用户未再次明确要求时，不得调整、重构、优化、格式化或覆盖。检查当前任务时如发现历史代码、文件命名或节点层级不符合现行规范，只能记录具体位置、现状、影响和整改建议并向用户报告；须待用户明确确认整改范围后才能修改，禁止顺手整改、重构或扩大任务范围。新需求不可避免会影响冻结代码时，须先报告影响与冲突并获得明确授权。
- 代码归类硬约束：任何新增、移动、重命名或接入代码文件必须按真实职责归属放在对应 `Code` 目录；模块专用代码放到该模块 `Code`，同游戏共享公共代码放 `Games/<Game>/Common/Code`，跨游戏/框架公共代码放 `Client/assets/Common/Code`。严禁通过跨目录引用、转发层、兼容路径或混入 `Other/Misc/Temp` 等兜底目录掩盖边界；如发现违规，先停下修正分类与引用后再继续。
- 业务实务口径：`A` 类功能只能放入对应 `Code` 文件夹的职责域中，不得让 `A` 类功能在 `B` 类目录运行。移动代码前先基于真实使用域和调用链确认归属，不得以兼容路径、转发层或临时目录“掩盖”跨域引用。
- 身份判断口径：公共协议、会话资料和业务分流只能消费服务端权威字段 `user_type`（`0=普通玩家`、`1=陪玩`、`2=机器人`）；不得以 `ppk` 的任意位、显示 ID、`ppk` 长度、昵称或账号号段推断身份，也不得由客户端、公共框架或业务模块伪造、改写或另建平行身份来源。
- 框架岗位不是项目稽查、主协调或任务调度岗位。禁止巡检、读取、等待、催办、暂停、恢复、终止或评价其他 AI 任务，禁止向其他岗位下达续作、测试、Creator 释放、文件恢复或交接指令；跨岗位状态统一由 aoo产品处理。
- 只执行用户或 aoo产品针对本岗位明确下发的具体任务。其他岗位发来的进度、阻塞、建议、日志或发现只能作为只读线索并原样报告 aoo产品，不得自动登记为框架任务、不得据此修改文件，也不得以“公共依赖”“解阻”“紧急”“顺手修复”为由扩大范围。
- `Client/assets/Login`、`Client/assets/Lobby`、`Client/assets/Club`、`Client/assets/Games/Poker/PDK` 及其他玩法专属目录默认只读。即使其中问题影响公共流程，框架岗位也只能报告精确路径和影响；没有用户或 aaoo产品针对具体文件、具体操作的当次明确授权，不得修改、恢复、删除、移动、重导入或建立兼容映射。
- 禁止从历史发布构建、Git、废纸篓、Library、缓存、备份或其他任务产物恢复 Scene、Prefab、Meta、资源或代码。恢复动作必须由用户针对具体文件和来源明确授权，并交由该文件所属业务岗位执行；框架岗位不得代替业务岗位恢复登录、大厅、亲友圈或玩法文件。
- Creator 仅可用于同步和验收本岗位已获授权且实际修改的公共框架文件。禁止为了巡检、解阻、释放编辑态或监督其他岗位而打开、关闭、保存、重导入、重载、切换或只读检查无关 Scene/Prefab；发现用户或其他岗位存在未保存状态时立即停止并只报告 aoo产品。

## 4. 执行与验收

- 临时文档协作规则（2026-09-15 起，直至用户明确恢复）：所有 AI 暂停向“Aoo文档”任务或文档 AI 发送修改报告、结构化报告、整理请求和归档消息；此前要求主动发送文档消息的现行条款在暂停期间不执行。各岗位仍须维护自身“开发记录”和“记忆”，但不得据此自动触发正式文档整理。文档 AI 不主动接收、轮询或整理其他岗位结果，仅在用户直接下达具体文档任务时处理。


- 先依据现有规范确认需求，再做最小、补丁式修改。
- 用户提出的问题严格按提出顺序串行处理；收到新问题时不得中断当前任务，只将其精简登记到本岗位“记忆”的待处理队列，完成并测试、审计当前问题后自动处理下一项。只有用户明确使用“优先”“现在执行”或“现在处理”等插队指令时，才允许保存当前安全断点并优先处理指定问题；普通问题反馈或一般“处理”描述不构成中断指令。
- 只要待处理队列仍有由用户或 aoo产品明确授权给本岗位的可执行任务，就按顺序持续执行；不得把巡检发现、其他岗位报告、历史审计项或跨域阻塞自动转为任务。当前项涉及其他业务域时立即保持安全断点并报告 aoo产品，不直接联系、调度或等待责任岗位；没有本岗位明确授权任务时保持暂停。
- 尽量节省 Token，回答必须精简并直接回应用户问题，只保留必要结论、关键进度、阻塞和待决定事项；禁止无关解释、重复内容、过度展开及无变化汇报，同时须让用户明确当前进度。
- 用户明确要求“写入规范”时，先读取已有规范并判断重复、包含和冲突关系，再去重、合并、精简并归入既定章节：同义只留一条，新要求包含旧要求时合并，冲突时以用户最新明确要求替换旧规则；禁止孤立追加。
- 代码必须结构清晰、职责单一、实现完整；只为业务边界、状态来源、关键分支和非直观原因提供准确必要的完整注释，不得重复代码表面含义。本次改动范围内不得保留无效、废弃、临时、调试、未使用或可安全合并的冗余实现。
- 完成前须自行完成针对性测试、受影响的真实调用链或场景验收及改动审计，确认生效且无相关回归后才能报告“完成”；验证未完成只能报告进行中或阻塞。资源变更还须完成 Cocos Creator 3.8.8 同步。
- 验收必须覆盖本次公共框架变更涉及的客户端、服务端、公共协议、公共数据库迁移能力、资源与配置映射、发布工具链、部署和真实 E2E；域内跨端缺口由本岗位继续修复和复验。
- 岗位 AI 自行执行网页、Creator Preview、Canvas、按钮、交互或 E2E 验收时，统一使用本任务专属的无头 Chrome 并通过 CDP 端口 `1234` 控制，不得打开可见浏览器窗口、抢占焦点、操作用户正在使用的 Chrome 或 Creator Preview、复用用户浏览器 Profile 或占用用户屏幕；每个并行 AI 必须使用独立临时 `user-data-dir`。确实必须占用屏幕时，须先停止并报告aoo产品申请用户授权。端口被其他任务占用时先直接协调；只有因此需要统一调度时才报告aoo产品。纯文档或无需浏览器的静态任务不强制启动浏览器。
- 本地业务 E2E 仅可使用账号`11`至`20`，密码统一为`1`，并记录本次实际账号。不得登录、重置、修改、占用或清理其他岗位账号及其会话、房间数据；账号被占用时改用本范围其他账号。`51`至`100`为预留账号，未经aoo产品明确分配不得使用。

- 任何新任务、转交任务或续接任务，必须先在“记忆”的待处理队列登记字段：任务摘要、来源、状态（待处理/进行中/阻塞）、职责范围、安全断点，随后方可处理；完成后移除，阻塞/停止前保留状态并同步接续人。

- 任何修改、修复或确认会要求新增或更新正式文档时，完成职责内工作后必须直接向“Aoo文档”任务 `01a088d4-fe1b-7e92-a939-04785e1bec61` 报告修改内容、修改前后、影响范围、文件或接口路径、验证证据及建议更新的唯一正式文档，不得先交aoo产品或等待提醒。纯内部且不改变外部契约、使用方式或运维方式的重构可不更新正式文档，但仍须精简写入本岗位开发记录。
- 对外报告不得包含对其他岗位的命令。框架岗位只向 aoo产品提交自身结果或阻塞；是否通知文档岗位、恢复依赖任务或调整执行顺序由 aoo产品决定。


- 只有用户针对具体任务明确说“提交”后，本岗位才允许将该任务可明确归属的改动提交并推送到对应仓库 `develop`；功能闭环、测试通过或主协调线程判断完成均不构成提交授权。未收到明确提交指令时，仅北京时间 `02:00` 的每日固定快照任务可以统一提交。禁止夹带用户或其他 AI 的无关改动，禁止操作 `main`、强制推送、合并、变基或改写历史。提交前必须排除敏感信息和运行产物；每次结果必须报告仓库、`develop` 分支、提交号或精确阻塞原因。

## 5. 阻塞报告

- 公共框架业务域内问题由本岗位跨端持续处理。只有跨业务域共享文件冲突、权威规则冲突、需要用户授权、需要统一调度、职责归属不清，或持续阻塞且点对点无法解决时，才向aoo产品提交完整阻塞报告；不得把域内服务端、协议、数据库、资源、工具链或部署工作当作越界阻塞。
- 任何任务停止、进入idle、异常结束或准备不再继续执行时，无论任务完成与否，都必须额外向aoo产品提交停止报告，完整列出当前任务、已完成项、未完成项、阻塞、待处理队列、安全断点和停止原因。aoo产品审核后，有已授权且可执行的未完成项必须续作；仅审核确认全部闭环，或只能等待用户决定、授权且无其他独立任务时，才允许保持停止。
- 停止或阻塞报告只能描述框架岗位自身被明确授权的任务，同一状态只报告一次；禁止替其他岗位发布状态、持续轮询无变化现场或借报告请求直接控制其他 AI。

## 6. 开发记录

- 2026-09-15：排查“所有界面头像均不显示”。统一头像映射与文件名无误，`/Users/aoo/Pictures/头像` 共 10000 张 `tx*.png`；根因为本地头像资源服务 `0.0.0.0:8765` 未运行，历史真实浏览器证据同样记录 `ERR_CONNECTION_REFUSED`。已启动仓库既有只读头像服务 `Client/tools/local-avatar-server.py`，本机 `tx380.png` 与局域网 `192.168.1.136:8765/tx2258.png` 均返回 `200 image/png`，本机响应文件与源文件 SHA-256 一致。公共 `PlayerAvatarService` 已覆盖大厅、俱乐部列表、俱乐部桌子、房间头像及公共头像组件，因此未对各页面重复打补丁，也未复制头像进 Creator 包。

- 2026-09-15：修复俱乐部进入房间点击链路的两处串行拥塞：俱乐部桌面不再在每秒房间刷新时为每张实体桌重复执行 Hall 查询与 Creator 资源解析，改为每个俱乐部仅预热首个共享房间族；俱乐部模板创建已由服务端完成创建及入座后，客户端不再重复查询活动房间，而是并行准备房间元数据与一次性连接票据。新增 `club-room-prewarm-priority.test.mjs`，相关俱乐部等待入房、无缝房间切换门禁 17/18 通过；剩余 1 项为既有表层组件数量断言（期望 2、当前实现 3），与本次路径无关。Creator 3.8.8 已运行且 Preview 可访问；因当前无头专属 Profile 无俱乐部测试账号会话，真实桌子点击计时仍待账号会话复验。

只记录本岗位任务，禁止跨岗位混写。每个新问题须立即在“记忆”的待处理队列登记问题、顺序、状态和相关路径，并在开始处理、完成或阻塞时同步状态；完成后从队列移除并按时间在本章追加日期、用户要求、根因/结论、修改范围、验证结果、未完成项。记录须精简，不复制全文，不删除或改写历史记录，纠错须追加注明。

### 2026-09-08｜入口初始化

- 用户要求：创建框架 AI 独立入口和固定七章规范。
- 根因/结论：此前无本岗位入口，现按项目模板建立。
- 修改范围：仅本文档。
- 验证结果：岗位边界、必读文档、执行验收、冻结与记录规则已核对。
- 未完成项：无。

### 2026-09-08｜文件名规则更正

- 用户要求：取消入口文件的 `AI_` 前缀。
- 根因/结论：最新明确命名要求替换此前文件名。
- 修改范围：仅本文档文件名和自引用。
- 验证结果：已改为 `框架执行提示词.md`，无前缀副本。
- 未完成项：无。

### 2026-09-08｜命名与层级规范

- 用户要求：补充精简一致的命名、清晰层级和引用同步规则。
- 根因/结论：原规范只有泛化命名要求，现已去重并细化。
- 修改范围：仅本文档。
- 验证结果：冻结边界归入第 3 章，命名、层级和同步验收归入第 4 章。
- 未完成项：无。

### 2026-09-08｜详细规范归档更正

- 用户要求：详细命名与层级规则迁入独立规范，岗位入口只保留必读引用。
- 根因/结论：详细条款不应驻留岗位入口，现已去重迁出。
- 修改范围：本文档与 `Client/docs/前端框架规范/命名与层级规范.md`。
- 验证结果：入口仅保留一条必须遵守的引用，详细规则已独立归档。
- 未完成项：无。

### 2026-09-08｜命名规范引用与整改边界

- 用户要求：统一命名规范必读引用，并限制历史不合规整改范围。
- 根因/结论：相对引用已统一，专项整改边界已并入冻结规则。
- 修改范围：仅本文档。
- 验证结果：引用归入第 2 章，整改边界归入第 3 章。
- 未完成项：无。

### 2026-09-08｜框架审计待解决台账

- 用户要求：将本次框架审计问题写入待解决分类，以后只在用户明确要求时处理。
- 根因/结论：原文档体系无统一待解决台账；已按框架职责、跨岗位协同、静态风险和历史架构债分类记录，并明确未授权修改。
- 修改范围：新增 `Client/docs/前端框架规范/历史记录与证据/待解决/框架审计待解决项.md`，并追加本开发记录。
- 验证结果：台账包含责任人、证据、影响、验证状态和建议下一步；未新增 AI 员工平级章节，“记忆”仍位于文档最后。
- 未完成项：台账全部问题均待用户后续明确授权，本次未修改任何业务代码。

### 2026-09-08｜稽查协调规则

- 用户要求：统一跨岗位冲突与多 AI 配合事项的报告和调度入口。
- 根因/结论：原阻塞规则缺少统一协调归属，现已去重合并为更新记录和安全断点后向 aoo稽查报告，由其统一调度和验收。
- 修改范围：仅本文档“阻塞报告”。
- 验证结果：协调规则仅保留一条，岗位 AI 自行协调和越权修改均已禁止。
- 未完成项：无。

### 2026-09-08｜问题串行、状态记录与进度规则

- 用户要求：问题按顺序串行处理，明确插队指令才可中断，并持续记录状态、精简必要进度。
- 根因/结论：原规范缺少新问题排队、插队判定、状态同步及 Token 节制要求，现已归入既定章节。
- 修改范围：仅本文档“执行与验收”“开发记录”和“记忆”。
- 验证结果：规则已去重合并，七章顺序不变，“记忆”仍位于末尾。
- 未完成项：无。

### 2026-09-08｜越界问题报告与无头验收环境

- 用户要求：疑似误发或部分越界问题须暂停越界处理并交由 aoo稽查核对；岗位自行执行的浏览器验收统一使用任务专属无头 Chrome。
- 根因/结论：现有跨岗位阻塞规则未完整覆盖禁止执行、测试和自行转交，入口也缺少无头验收的隔离要求；现已去重归入既定章节并保持问题严格串行。
- 修改范围：仅本文档“修改边界”“执行与验收”“阻塞报告”和本开发记录。
- 验证结果：越界判断、部分越界依赖处理、稽查报告字段、CDP `1234`、独立临时 Profile、端口冲突协调、可见窗口授权例外及静态任务例外均已核对。
- 未完成项：无。

### 2026-09-08｜公共头像文字与语音气泡方向越界阻塞

- 用户要求：按头像在当前 Canvas 的实际坐标统一决定文字和语音气泡展开方向，并用无头 Chrome/CDP `1234` 验收。
- 根因/结论：气泡视觉节点位于公共 `Common_Head.prefab`，但当前唯一方向控制实现位于跑得快 `SeatPresenter.ts`，公共层无可独立修改并生效的方向组件；任务超出框架岗位修改边界。
- 修改范围：仅本开发记录和“记忆”待处理队列；未修改 PDK 代码、Prefab、场景、资源或其他业务。
- 验证结果：已静态确认 `CommonPdkPlayController -> SeatPresenter -> Common_Head` 调用链；`showQuickText()` 固定开启 `ChatBubbleLeft`，`showVoice()` 固定开启 `VoiceLeft`。因越界阻塞未启动浏览器验收。
- 未完成项：待 aoo稽查核对并由用户明确授权跑得快 AI 修改 `Client/assets/Games/Poker/PDK/Common/Code/Runtime/Room/SeatPresenter.ts`；安全恢复点为尚未产生任何业务文件改动。

### 2026-09-08｜公共头像气泡方向授权续作

- 用户要求：授权新增公共头像控制器、绑定 `Common_Head.prefab` 并最小接入 PDK，使文字、语音与 `Img_Ready` 统一按头像相对当前 Canvas 中心的实际坐标选择方向。
- 根因/结论：原 PDK 展示层固定开启 Left 节点且隐藏路径含重复斜杠；现将节点缓存、坐标判定、显示入口和清理集中到公共控制器，左半屏使用 Right 节点并将就绪标识放到右侧，右半屏反向处理，不读取座位号。
- 修改范围：新增 `Client/assets/Common/Code/UI/CommonHeadController.ts` 及 Creator 生成的 `.meta`，绑定 `Client/assets/Common/Prefab/Common/Common_Head.prefab`，最小修改 `Client/assets/Games/Poker/PDK/Common/Code/Runtime/Room/SeatPresenter.ts`，新增方向契约测试。
- 验证结果：Cocos Creator 3.8.8 已正常导入脚本、丢弃旧内存恢复数据、从磁盘重开并在 Inspector 确认组件绑定，保存后无未保存标记；TypeScript 与方向、社交、座位相关测试通过。组件注册和公共引用全局门禁分别被 38 个既有缺失/重复资源 UUID、既有重复目录 `assets/Common/Prefab/common` 阻塞，本次未改这些范围。
- 未完成项：CDP `1234` 被其他任务的专属无头 Chrome（PID 59227，Profile `/tmp/aoo-pdk-back.8sHJSC`）占用，遵守隔离规则未终止、接管或复用；待端口释放后从 Creator Preview 对左右头像的文字、语音、`Img_Ready` 完成真实 E2E。安全恢复点为代码、Prefab 与 Creator 同步均已完成，尚未宣称任务完成。

### 2026-09-09｜公共头像方向坐标实现纠错

- 用户要求：根据真实房间错误遮罩修复 `Common_Head 无法取得头像或 Canvas 的 UITransform`。
- 根因/结论：`Head/Avatar` 没有 `UITransform`，动态挂载链也不应强依赖祖先 `Canvas` 组件；已改为直接读取头像节点 `worldPosition.x`，并与 `view.getVisibleOrigin()`、`view.getVisibleSize()` 算出的当前可见区域中心比较。
- 修改范围：仅 `Client/assets/Common/Code/UI/CommonHeadController.ts` 及方向契约测试，未改 Prefab、头像坐标、资源、节点层级或其他玩法逻辑。
- 验证结果：Creator 3.8.8 已重新编译并刷新 Preview；TypeScript、公共头像方向、PDK 社交与座位相关 7 项测试全部通过。
- 未完成项：CDP `1234` 当前被其他任务 Profile `/tmp/aoo-pdk-share-e2e.LJIiIz` 占用，未接管或终止；待释放后执行真实房间左右头像、文字、语音和 `Img_Ready` E2E。

### 2026-09-09｜精简回答规范

- 用户要求：回答精简，不回复无用内容。
- 根因/结论：已与现有 Token 和进度规则去重合并，仅保留问题结论及必要信息。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：未新增章节或重复条款。
- 未完成项：无。

### 2026-09-09｜公共 ChatPanel 节点命名统一（业务验收阻塞）

- 用户要求：仅统一公共 `ChatPanel.prefab` 不规范节点名并同步必需引用，保持层级、布局、组件、资源与聊天业务不变，完成 Creator 同步和表情、快捷文字、自定义文字双端真实发送验收。
- 根因/结论：权威资源实际位于 `Games/Common/Prefab/ChatPanel.prefab`；旧表情节点下划线、大小写和编号格式不一致，`Popup`、`Composer`、`Input`、`Expressions`、`QuickText` 职责不够明确；运行时注册、路径映射和测试仍指向不存在的 `Common/ChatPanel`。节点名与必需引用已统一，但现有聊天业务链路无法完成三类发送。
- 修改范围：仅 ChatPanel 节点 `_name`，ChatPanel 的资源注册地址、原生路径映射、PDK 节点查找路径、专项测试、本文档及验收证据；未改节点层级/布局/组件/资源、服务器或聊天业务。
- 验证结果：Creator 3.8.8 已正常重新导入，源文件与 Library 排序 JSON 哈希一致；TypeScript 和 38643 UUID 原生资源门禁通过；真实 Canvas 成功加载新节点名。快捷文字请求被服务端 `3008` 拒绝；表情节点无 Button，自定义发送按钮无业务绑定，均未产生聊天请求。
- 未完成项：表情、快捷文字、自定义文字的本机显示与对方送达均未通过；Prefab 既有缺少 `CloseButton` 导致专项测试 1 项失败；同项目 5 个 Creator 实例下未占用用户屏幕执行可见编辑器关闭重开。安全断点和证据位于 `Client/docs/前端框架规范/历史记录与证据/ChatPanel节点命名/2026-09-09/验收记录.md`。

### 2026-09-09｜唯一公共设置接线（真实点击验收阻塞）

- 用户要求：立即接通 `Client/assets/Common/Prefab/Common/Set.prefab` 作为唯一公共设置，保持节点层级不变并统一不规范命名。
- 根因/结论：原资源注册把位于 `common-prefab` 的设置错误指向 `games-common-prefab/Set`，PDK 内嵌控制器仍查找预制体中不存在的旧节点，实际无法稳定加载和操作；现保留原 UUID，将资源与根节点统一迁移为 `SettingsPanel`，节点仅语义化改名，公共控制器集中负责关闭、分类、音量、音乐主题与扑克外观偏好，PDK 只通过逻辑键 `room/SettingsPanel` 打开。
- 修改范围：公共设置 Prefab/`.meta` 的资源名和节点 `_name`、公共设置控制器、公共资源注册/加载描述、原生路径映射、PDK 最小接线及专项测试；未改节点父子关系、兄弟顺序、坐标、尺寸、组件或美术资源，也未修改玩法私有设置实现。
- 验证结果：Creator 3.8.8 已正常重导入并完成一次 Web Desktop 构建；原 UUID `a36d2c66-fd95-446f-bc4a-b2c4cdc7045e` 保持不变，Library 根节点及构建产物均为 `SettingsPanel`，`common-prefab/config.json` 已包含该资源；TypeScript 和设置专项契约测试通过。构建同时输出项目既有缺失资源警告，本次未越界处理。
- 未完成项：CDP `1234` 在验收启动瞬间被其他任务 Chrome（PID 23535）占用，已立即终止本任务新实例且未接管或终止对方进程；尚待端口释放后在真实 PDK 房间点击设置、切换分类、拖动音量、切换主题/牌面并关闭弹窗。玩法私有设置入口是否清理仍须按具体玩法影响范围单独核对，当前未删除。

### 2026-09-09｜快捷文字语音地区化目录（E2E 端口阻塞）

- 用户要求：按公共普通话、地区方言和男女声整理快捷文字语音，保留 UUID、同步引用并实现地区缺失回退普通话。
- 根因/结论：仅旧 `boy/girl 1-10` 有完整 `FastVoice` 配置证据；已迁入公共音频 Bundle。`11-12` 无快捷文字配置，`ay_1-16` 虽有安岳文案但无项目稳定地区编码，均保持原位。
- 修改范围：新增 `Games/Common/Audio/Chat/Mandarin/Boy|Girl` 与 `Dialect/CD|NJ|LS/Boy|Girl`；移动 20 个 MP3 及原 `.meta`；更新对应 `Sound.json` 路径；新增集中地区回退播放器和专项测试。未改 ChatPanel 发送业务、玩法音效或开房规则表。
- 验证结果：Creator 3.8.8 正常导入并成功构建 `games-common-audio`，构建包含 20 个音频；专项测试 5 项、38660 UUID 原生资源门禁通过；项目 TypeScript 全量检查仅被 `CommonHeadController.ts` 两个既有错误阻塞，本次文件无新增错误。
- 未完成项：CDP `1234` 被其他任务 PID 23535、Profile `/tmp/pdk-wide-touch.7RssrX` 占用，已向 aoo稽查报告且未接管；待端口释放后完成普通话男女声真实加载播放及 CD/NJ/LS 缺失方言回退验证。证据位于 `Client/docs/前端框架规范/历史记录与证据/快捷文字语音地区化/2026-09-09/验收记录.md`。

### 2026-09-09｜公共 ChatPanel 三类聊天交互接通（E2E 阻塞）

- 用户要求：在用户已为 `Emoji01～Emoji20` 添加 Button 后，接通表情、快捷文字与输入文字；发送后立即向右滑回隐藏，打开时从右侧快速滑出，点击窗口外同样取消收起。
- 根因/结论：按钮事件、头像 `FaceAnimation` 显示链路和公共房间协议直发均缺失；已集中补齐，并将三类发送改为本机立即显示、0.16 秒右侧收起。
- 修改范围：公共头像控制器、PDK 聊天/社交/运行时/座位展示最小接线、针对性测试与本记录；未修改 ChatPanel Prefab、Server、规则表或其他项目。
- 验证结果：Creator 3.8.8 TypeScript 通过，针对性测试 9/9 通过；扩大测试中 1 项结算断言因无关并行改动失败，本次未越界修改。
- 未完成项：CDP `1234` 先后被其他任务 PID 23535、39742 占用，两个 Aoo Creator 3.8.8 实例导致 Preview 未刷到最终脚本缓存，已向 aoo稽查报告。待环境释放后执行真实房间 E2E；自定义文字跨客户端送达仍需服务端聊天岗位明确授权。证据位于 `Client/docs/前端框架规范/历史记录与证据/ChatPanel节点命名/2026-09-09/聊天交互接通验收.md`。

### 2026-09-09｜ChatPanel 表情资源更正

- 用户要求：表情点击后使用 `Games/Common/Spine/Emoji` 目录内的动画。
- 根因/结论：上一次将表情显示误接到静态 SpriteAtlas；已更正为 `emoji_super` SkeletonData，20 个按钮一一播放 `emoji_1～emoji_20` Spine 动画。
- 修改范围：公共头像控制器、Spine Emoji 目录 Bundle 配置、针对性测试与证据更正；未改 Common_Head/ChatPanel Prefab 布局。
- 验证结果：待 Creator 重导入、TypeScript 和针对性测试后补充。
- 未完成项：真实房间 E2E 仍受 CDP `1234` 与多 Creator 实例环境影响。

### 2026-09-09｜ChatPanel Spine 表情同步验证补充

- 用户要求：续作确认 Spine 表情资源可正常加载。
- 根因/结论：`Games/Common/Spine/Emoji` 原未配置可动态加载的 Bundle；已保留原 UUID 并配置为 `games-common-emoji-spine`。
- 修改范围：Emoji 目录 `.meta`、公共头像表情加载逻辑、针对性测试和证据；未改 Prefab 层级与布局。
- 验证结果：Creator 3.8.8 已正常重导入并更新 Preview chunk；针对性测试 10/10 通过；无头 Chrome/CDP `1234` 从真实 Preview 成功加载 `emoji_super` 及 `emoji_1～emoji_20`。全量 TypeScript 仅被无关登录并行改动的未登记错误码阻塞。
- 未完成项：因用户正在操作 Creator，未抢占编辑器执行目标 Bundle 构建；待真实 PDK 房间点击表情并保存头像旁 Spine 动画截图。

### 2026-09-09｜框架审计公共 UI 基础问题整改

- 用户要求：处理框架已记录的问题和需要优化项。
- 根因/结论：`AssetLoader` 使用集合导致重复资产引用无法对称释放；`LifecycleScope` 会被单个清理异常中断；两种公共对象池没有容量上限，且清空时遗漏借出对象。现分别改为引用次数账本、完整清理后聚合异常、默认容量 32 且清空终结全部对象。
- 修改范围：仅 `Client/assets/Common/Code/UI/Infrastructure.ts`、`Client/assets/Common/Code/Runtime/core/LifecycleScope.ts`、`Client/assets/Common/Code/UI/Presentation.ts`、新增专项测试并更新框架审计台账；未修改网络、登录、大厅、服务端、玩法编码或冻结兼容层。
- 验证结果：资源重复引用、异常清理、对象池容量和借出对象销毁共 4 项回归通过；公共社交/设置相关 5 项回归通过；TypeScript 全量检查通过。
- 未完成项：F-001、F-002、F-005、C-001、C-002、R-001、R-002 涉及服务端握手、公共网络、登录/大厅、玩法协议或冻结兼容层，超出当前 Aoo UI 线程修改边界；既有真实 E2E 项仍因 CDP `1234` 被其他任务占用而等待。

### 2026-09-09｜快捷文字语音地区化续验完成

- 用户要求：端口释放后继续完成普通话男女声与 `CD`、`NJ`、`LS` 地区回退的真实音频验收。
- 根因/结论：先前阻塞仅为其他任务占用 CDP `1234`；端口释放后已用本任务独立临时 Profile 验证，20 个普通话资源全部实际播放，3 个地区的男女声均在方言缺失时回退同 ID、同性别普通话并播放成功。
- 修改范围：仅补充验收证据与本记录；未再修改音频、业务代码、ChatPanel、玩法规则或开房规则表。
- 验证结果：普通话 20/20、地区回退 6/6 通过；Bundle、config、import、native 请求成功，未捕获 Runtime 异常；截图及明细位于 `Client/docs/前端框架规范/历史记录与证据/快捷文字语音地区化/2026-09-09`。
- 未完成项：无；无法确认地区归属的 `11-12` 与 `ay_1-16` 继续原位保留，等待用户提供稳定映射证据后另行处理。

### 2026-09-09｜框架审计剩余七项整改

- 用户要求：继续处理框架审计台账中 F-001、F-002、F-005、C-001、C-002、R-001、R-002。
- 根因/结论：已分别收口无敏感 URL 的 WSS 握手、单次重连恢复、精确生命周期门禁、地区化 PDK 写协议、登录退出 Hall 清理与安全随机数、房间推送围栏、Bundle 独占释放。V2 兼容门禁要求 `poker.pdk.dispatch` 历史定义保留，但客户端和服务端均已禁止其形成新写入。
- 修改范围：公共网络/状态/错误类型/兼容 Bundle 服务、登录路由与鉴权、协议生成源及产物、Gateway/Poker/兼容桥、定向测试和审计台账；未修改开房规则表、登录场景布局或其他项目。
- 验证结果：客户端 TypeScript 通过，56 项定向测试通过；Gateway 握手集成测试 1/1 通过；Gateway、Poker、GameSPI 与 LegacyCommon 所在 gameServer 等受影响模块编译通过；Creator 3.8.8 自动导入后状态栏 0 错误，真实 Preview Canvas 已加载。
- 未完成项：真实 Preview 在 `http://127.0.0.1:8080/api/v2/version/check` 的 CORS 预检处被既有 Gateway Origin 配置阻断，未进入账号登录、业务 WSS、退出和断网恢复；待服务端运行配置允许当前 `http://127.0.0.1:7458` Origin 后补真实 E2E。协议全局 `check.mjs` 另被既有 `GatewayEntryPolicy.ts` 动态局域网 HTTP 字面量门禁阻断，本次未改该运行策略。

### 2026-09-09｜框架审计真实登录与礼物入口续作

- 用户要求：允许继续处理框架审计整改与真实环境问题。
- 根因/结论：Gateway 本地 Origin 未包含当前 Creator `7458`；礼物服务已有 `/v1/gifts/inbox`，但网关未发布 `/api/v2/gifts/**`、本地编排未启动 Gifting，且礼物模块仍使用独立 HMAC 客户端令牌。现已补齐 Preview Origin、礼物上游代理与路由白名单、本地服务编排，并让礼物认证复用统一账号会话。
- 修改范围：Gateway 路由与本地 Origin、Gifting HTTP 认证边界、Bootstrap、本地核心服务脚本、客户端礼物请求身份头、契约测试与审计台账；未修改开房规则表、登录场景布局或玩法代码。
- 验证结果：受影响生产模块完整编译通过；无头 Chrome 真实 Canvas 完成游客登录并进入 `MainScene`，WSS URL 无查询参数；礼物服务健康检查通过，`/api/v2/gifts/inbox?after=0` 已由 404 变为正确到达上游，无效会话返回 401。
- 未完成项：有效会话下礼物收件箱 200 的本轮无头复验未完成；重新启动本地全套服务时被既有只读规则表七列表头门禁阻断，已用已有运行快照恢复 Gateway、Version、Account、Hall、Social 并单独启动 Gifting。聚合测试仍被既有孤儿扑克测试类缺少 `AypdkGameProvider`、`HbpdkSession` 阻断，本轮未越权处理；真实退出确认弹窗仍待补点击闭环。

### 2026-09-09｜礼物收件箱与会话退出闭环更正

- 用户要求：继续处理完前述剩余链路。
- 根因/结论：有效会话首次复验由 404 进展为 502，进一步确认 `GiftingService.inbox()` 无数据时会服务端轮询 20 秒，超过 Gateway 8 秒上游超时；客户端已有 1.5 秒轮询，因此删除重复服务端等待并立即返回查询结果。
- 修改范围：仅 Gifting 收件箱等待策略、本地 Gifting 重启编排和本验收记录；未修改规则表、场景、Prefab 或玩法代码。
- 验证结果：有效账号会话访问 `/api/v2/gifts/inbox?after=0` 返回 HTTP 200、`data` 为数组，实测约 56ms；账号退出返回 `logged_out`，原访问令牌随后访问礼物接口返回 401。Gifting 模块测试通过，Gateway 契约与握手测试 5/5 通过，全部本轮测试账号、会话及关联数据已清理且残留为 0；核心六服务及 Gateway authority 均在线。
- 未完成项：UI 退出确认弹窗仍需在独占 CDP `1234` 后补真实 Canvas 点击证据；当前端口由其他任务 Chrome 占用，未终止或复用。房间规则 watcher 仍被用户只读规则表七列表头门禁阻断，不属于本轮代码整改授权。

### 2026-09-09｜跑得快出牌语音接线（真实点击验收阻塞）

- 用户要求：接通 `Games/Poker/PDK/Common/Audio/Game`，按实际出牌牌型播报对应语音。
- 根因/结论：公共 PDK 出牌展示此前只播放牌型动画，没有音频消费端；现新增独立音频 Presenter，在权威新动作与旧 `OpCard` 入口按玩家性别、牌型和必要牌点精确解析，重连历史不重复播报，无对应文件时不误播。
- 修改范围：`Game.meta` Bundle 配置、新增 `PdkGameAudioPresenter.ts`、最小接入 `CommonPdkPlayController.ts`、专项测试与验收证据；未修改规则表、出牌判定、请求协议、音频文件或其他玩法。
- 验证结果：TypeScript、专项测试 4/4、Creator 原生资源门禁通过；Creator 3.8.8 正常导入并完成 Web Desktop 构建，`pdk-common-game-audio` 产物包含 92/92 个 MP3。
- 未完成项：CDP `1234` 被其他任务 PID 19298、Profile `/tmp/pdk-cocos-touch-chrome.iIiauu` 占用，已报告 aoo稽查且未接管；待释放后从真实 PDK 房间点击出牌，核对牌型、请求和 AudioSource。证据位于 `Client/docs/前端框架规范/历史记录与证据/跑得快出牌语音/2026-09-09/验收记录.md`。

### 2026-09-09｜跑得快房间操作声音接线

- 用户要求：按 `Games/Poker/PDK/Common/Audio/Room` 内文件语义接通房间操作声音；缺少任一声音时静默不播放，不能报错。
- 根因/结论：该目录此前没有运行时消费端；现新增独立房间音频 Presenter，将背景、入座、洗牌、定庄、发牌、有效选牌、出牌、最后三秒、余牌报警和本地胜负接到现有房间事件，并对重连历史、动作和倒计时做防重放。
- 修改范围：`Room.meta` Bundle 配置、新增 `PdkRoomAudioPresenter.ts`、最小接入 `CommonPdkPlayController.ts`、专项测试与验收证据；未修改玩法规则、协议、Prefab、音频文件或其他玩法。
- 验证结果：TypeScript、音频专项测试 7/7、Creator 原生资源门禁通过；Creator 3.8.8 正常重启资源库并完成 Web Desktop 构建，`pdk-common-room-audio` 产物包含 11/11 个 MP3。缺 Bundle、缺 Clip、节点销毁和延迟退出均静默返回，不抛异常或记录错误。
- 未完成项：尚待真实多人 PDK 房间逐项触发并采集 AudioSource 证据；构建内既有 Prefab 缺失资源告警不属于本次音频改动。证据位于 `Client/docs/前端框架规范/历史记录与证据/跑得快房间操作声音/2026-09-09/验收记录.md`。

### 2026-09-09｜框架审计 UI 退出与礼物链路最终闭环

- 用户要求：继续处理完框架审计剩余真实 UI 验收。
- 根因/结论：统一 `Message` 模板选中后会把按钮提升到根节点，退出逻辑仍只查找旧 `image01/btnSure|btnCancel`；同时旧版按钮在 Web Preview 可只产生触摸结束而不合成 Button Click。礼物客户端又使用运行配置中的回退设备号而非账号签发设备号，成功收件箱循环没有节流。
- 修改范围：退出确认按钮根路径兼容及触摸结束兜底、礼物设备身份来源、收件箱成功/失败统一 1.5 秒轮询间隔、审计台账和验收证据；未修改场景、Prefab、规则表或玩法逻辑。
- 验证结果：Creator 3.8.8 刷新 Preview 后，无头 Chrome/CDP `1234` 真实 Canvas 完成游客登录、进入 `MainScene`、点击退出与确认，最终进入 `Login_Scene`；本地会话和账号提示清除，账号退出接口 200，礼物收件箱有效会话 200，运行时异常 0。登录生命周期测试 15/15 通过；本轮设备对应测试账号 528～540 及关联数据已清理，残留 0。证据位于 `Client/docs/前端框架规范/历史记录与证据/框架审计/2026-09-09-退出验收`。
- 未完成项：无；只读规则表 watcher 的七列表头门禁仍不属于本轮授权。

### 2026-09-09｜框架岗位执行规范与音效职责纠正

- 用户要求：后续以本文档作为框架岗位执行规范；跑得快出牌音效和房间操作音效不由框架岗位处理，其余五项继续。
- 根因/结论：两项跑得快音效真实 E2E 属于具体玩法职责，先前将其保留在框架待处理队列不符合岗位边界；历史开发记录保留，本岗位不再续作、修改或验收相关玩法音效。
- 修改范围：仅本文档“开发记录”和“记忆”待处理队列；未修改玩法代码、音频、Prefab、Scene 或测试。
- 验证结果：七章及顺序保持不变，“记忆”仍为末章；框架队列已只保留五项职责内任务。
- 未完成项：按队列顺序继续处理五项框架任务。

### 2026-09-09｜剩余框架任务真实房间续验阻塞

- 用户要求：继续处理完框架队列任务后统一报告，并新增除进入登录界面、进入游戏房间外统一使用 `WaitForm.prefab` 的任务。
- 根因/结论：CDP `1234` 释放后已使用本任务独立无头 Chrome 建立两个真实账号并通过 Canvas 创建、加入房间；权威房间随后被服务端结束，客户端收到 `3008: room does not exist`，无法继续完成公共头像气泡、ChatPanel 和 SettingsPanel 的稳定真实房间验收。加载房间时还出现既有玩法 Prefab 缺失 UUID `4ea6c8dd-1da2-4fb6-9db1-75998bd41c4d`、`71906fbc-fb71-4863-9e7f-f5826e13e1e5`，均不属于框架岗位可修改范围。
- 修改范围：仅本文档队列和本阻塞记录；未修改房间生命周期、玩法 Prefab、玩法资源、框架代码、Scene 或规则表。
- 验证结果：真实 Canvas 创建房间 `647294` 用时约 5.7 秒，第二账号加入用时约 6.5 秒；另一次创建房间 `604538` 后复现同类结束状态。左右头像已真实装载，左侧头像 `Img_Ready` 激活时位于头像右侧；右侧头像、文字/语音气泡、ChatPanel 和 SettingsPanel 因房间被结束未形成完整稳定证据。
- 未完成项：须由 aoo稽查协调房间/玩法责任岗位先恢复稳定且不会自行结束的三人测试房间，并处理或确认上述缺失玩法资源；安全恢复点为框架代码零改动、后续仍从队列第 1 项真实 E2E 继续。

### 2026-09-09｜公共回合分与结算特效预制体

- 用户要求：公共回合分直接加入唯一 `Common_Head.prefab`，独立保留 `Common_SettlementFx.prefab`；不接业务代码，Creator 同步后等待后续明确接线指令。
- 根因/结论：已撤销并删除未引用的独立 `Common_RoundScore.prefab`；回合分按既定层级落在 `Common_Head/Settlement/Lb_RoundScore`，默认隐藏；结算特效保留独立 `FxLayer/BeanTemplate` 结构，豆子节点默认隐藏并复用现有占位资源。
- 修改范围：仅 `Client/assets/Common/Prefab/Common/Common_Head.prefab`、新增 `Client/assets/Common/Prefab/Common/Common_SettlementFx.prefab` 及 `.meta`、本文记录；未注册、未接业务代码，未修改原有节点命名、坐标和层级。
- 验证结果：Creator 3.8.8 对 Common 目录执行正常重新导入后，已搜索并分别重新打开两个 Prefab；Creator 可见 `Common_Head/Settlement` 与 `Common_SettlementFx/FxLayer`，原生资源门禁扫描 1367 个场景/预制体且阻塞为 0，两个 Prefab 内部 `__id__` 引用均完整。
- 未完成项：按用户要求等待后续明确指令再接业务数据与播放逻辑；全项目既有重复 Prefab 根目录、ChatPanel 节点契约和玩法资源缺失已另报 aoo稽查，本任务未越权修改。

### 2026-09-09｜屏幕适配职责只读清点

- 用户要求：在不依赖稳定房间的范围继续剩余框架任务。
- 根因/结论：对客户端 TypeScript 中尺寸、坐标、锚点、缩放、Widget/Layout、可见区域与安全区调用清点出 627 个候选；静态适配主要集中在公共全屏表单和大厅方向布局，动态安全区、牌桌状态与动画坐标必须保留运行时实现，不能机械迁入 Prefab。
- 修改范围：仅新增 `Client/docs/前端框架规范/历史记录与证据/屏幕适配审计/2026-09-09/审计记录.md` 与本文记录；未修改屏幕适配代码、Prefab、Scene 或规则表。
- 验证结果：已区分公共框架、大厅、亲友圈、玩法、登录只读和动态平台适配边界，并确定后续按单页串行迁移与桌面/移动 E2E 的安全恢复点。
- 未完成项：队列要求前四项真实房间验收后才能开始实际迁移；当前只完成清点，不提前改变全局布局基线。

### 2026-09-09｜唯一公共 WaitForm 只读审计

- 用户要求：除进入登录界面和进入游戏房间外，需要加载时统一使用唯一 `Common/Prefab/Common/WaitForm.prefab`。
- 根因/结论：大厅普通 Form 已在加载超过 800ms 时显示逻辑 `UIWaitForm`，并能规范化到唯一公共 `WaitForm`；但普通 Prefab 加载仍会先显示 HTML 过渡遮罩，游戏返回大厅也继续使用该遮罩，尚未达到唯一加载界面目标。
- 修改范围：仅新增 `Client/docs/前端框架规范/历史记录与证据/统一加载界面/2026-09-09/审计记录.md` 与本文记录；未提前修改全局过渡协调器、场景、Prefab 或业务代码。
- 验证结果：已明确普通大厅表单、返回大厅、登录例外、进入房间例外及亲友圈列表内容态的边界，并列出最小接线和门禁方案。
- 未完成项：本任务按队列位于屏幕适配之后；须等待前置真实房间与适配基线稳定后实施，并完成 Creator Preview 桌面/移动真实点击验收。CDP `1234` 当前由其他任务 Profile `/tmp/aoo-pdk-final-e2e-another` 占用，本任务未接管。

### 2026-09-09｜唯一公共 WaitForm 首批接线

- 用户要求：按已授权审计结果持续整改，普通加载统一使用公共 Creator `WaitForm`，保留登录和进入游戏房间例外。
- 根因/结论：大厅 FormManager 原同时触发 DOM 遮罩和延迟 800ms 的 `UIWaitForm`；现将大厅标记为外部 Creator 加载呈现，DOM 协调器只保留超时、失败和首帧提交能力，并将正常 WaitForm 阈值调整为 120ms；`UIWaitForm` 显式固定到 `common-prefab/WaitForm`。
- 修改范围：`ModulePrefabRegistry.ts`、`LegacyFormManager.ts`、`LegacyLobbyScreen.ts`、专项测试与本文记录；未修改登录 Scene、玩法逻辑、视觉设计或 WaitForm 层级。
- 验证结果：专项契约 3/3 通过，Creator 原生资源门禁 1367 个场景/预制体且阻塞 0；Creator 3.8.8 脚本热刷新状态栏未新增错误。登录与进入房间仍使用默认过渡例外。
- 未完成项：CDP `1234` 仍由跑得快任务占用，桌面/移动真实点击待端口释放后补验；返回大厅的跨 Scene WaitForm 仍需在房间环境稳定后处理，避免场景销毁期间黑屏。

### 2026-09-09｜公共房间弹窗路径阻塞续作

- 用户要求：修复 `room/DissolveRoom` 与 `game/CommonPdk/pdk_UISharePanel` 的公共 Prefab 加载阻塞，不改玩法业务和布局。
- 根因/结论：`DissolveRoom.prefab` 位于 `games-common-prefab` 根目录，注册却错误添加 `Common/` 前缀，已更正。用户确认旧代码不再作为依据，因此删除指向已不存在资源的 `pdk_UIShareMore` 公共注册与 NativeMaps 陈旧条目，不从旧构建恢复历史 Prefab。跑得快岗位随后确认当前 `pdk_UISharePanel` 节点契约，框架按该契约新建唯一公共 Creator Prefab。
- 修改范围：修正公共 `CommonPrefabRegistry.ts` 的 DissolveRoom 地址；删除 `GamePrefabRegistry.ts`、NativeMaps 和迁移测试中的失效 ShareMore 声明；新增 `Client/assets/Games/Common/Prefab/pdk_UISharePanel.prefab` 及 `.meta`，以新 UUID `9d5c3b27-4ab8-4d4a-9e43-7c82c160f412` 注册为 `game/CommonPdk/pdk_UISharePanel`，Bundle 为 `games-common-prefab`；未复用旧资源、未修改解散或分享业务逻辑。
- 验证结果：Creator 3.8.8 已正常导入并重新打开目标 Prefab，资源元数据 `imported=true`，编辑器状态未新增错误；专项契约 4/4 通过；Creator 原生资源门禁检查 1368 个场景/预制体、阻塞 0，Prefab 54 个序列化对象且资源引用异常 0。跑得快岗位随后在真实房间 `680276` 点击微信邀请，确认面板完整挂载全部契约节点并可由 `btn_closeshare` 关闭；`DissolveRoom` 同场景真实加载成功且不再报告缺失资源，旧 `ShareMore` 静态引用为 0，TypeScript 检查通过。
- 未完成项：本弹窗路径任务已闭环；多人分享送达等玩法业务验收仍属于跑得快岗位，不进入框架待处理队列。

### 2026-09-09｜唯一公共 WaitForm 大厅真实验收

- 用户要求：在房间链路阻塞期间继续处理独立框架任务，除进入登录界面和进入游戏房间外统一使用唯一公共 `WaitForm.prefab`。
- 根因/结论：首次真实点击脚本已正确输入账号密码，但输入框原生层退出焦点吞掉首次“确定登录”点击，导致停留登录弹窗并超时；按真实用户行为补第二次 Canvas 点击后进入大厅。在网络延迟 800ms、上下行 64KB/s 条件下点击“创建房间”，加载期间唯一公共 Creator `WaitForm` 可见，随后创建房间表单正常打开。
- 修改范围：仅补充任务专属真实 E2E 脚本与本文记录；未修改登录 Scene、`WaitForm.prefab` 层级、玩法逻辑、规则表或本轮已冻结框架接线代码。
- 验证结果：Creator Preview `3.8.8`、无头 Chrome/CDP `1234` 真实 Canvas 点击通过，断言 `publicWaitFormVisible=true`、`destinationLoaded=true`，页面异常 0；截图、请求和报告位于 `Client/docs/前端框架规范/历史记录与证据/证据/waitform-lobby-2026-09-09T12-57-49-693Z`。
- 未完成项：普通大厅 Form 加载已闭环；仅余“游戏返回大厅”的跨 Scene 公共 `WaitForm` 真实验收，需稳定真实房间恢复后继续。

### 2026-09-09｜统一规则表映射框架权限阻塞

- 用户要求：现在处理地区规则表新增、删除和修改规则行后自动进入生成产物及公共创建房间 UI 的通用映射，规则表只读，禁止中文规则名白名单和地区特判。
- 根因/结论：当前地区工作簿已统一为六列并新增“托管次数”，删除旧“是否可选/是否必选”；`Server/tools/room-rules-publisher.rb` 仍强制旧七列表头，并通过 `PLAY_IDENTITIES`、`FIELD_KEYS_BY_CODE` 和中文标签映射字段，因此成都、内江、凉山三表当前均在表头校验阶段失败，新增“小结算”等普通规则行无法生成。工作簿没有稳定业务键列，需由外部持久化注册表为首次出现的规则/选项分配稳定技术键并在显示文字、选项和顺序变化后保持不变。
- 修改范围：本轮仅只读检查工作簿 XML、现有发布器、Watcher、生成物和公共创建房间 Presenter，并记录安全断点；未修改或重新保存任何 `.xlsx`，未修改服务端发布器、数据库、玩法规则、结算逻辑或 UI 代码。
- 验证结果：对成都、内江、凉山工作簿分别执行现有 `validate`，均明确失败于“必须且只能存在一组七列表头”；只读 XML 确认当前真实表头为“界面显示文字、控件类型、可选项显示文字、默认勾选、托管次数、是否显示”，且三表均已有“小结算/多选/小局结算弹窗/默认1/托管次数3/显示是”。
- 未完成项：项目级 `AGENTS.md` 将本线程限制为 Aoo UI 修复与更换；完整修复必须获准修改 `Server/tools/room-rules-publisher.rb`、相关 Watcher/测试及 `Server/work/generated/room-rules` 生成产物并执行本地发布。已向 aoo稽查申请该精确目录和操作授权，获准后从“六列表头与稳定外部键注册表设计”继续。

### 2026-09-09｜统一规则表映射框架授权续作

- 用户要求：明确继续执行并授权规则生成器、Watcher、测试、稳定键注册表、生成产物和本地发布范围；普通规则行不得依赖小结算特判、中文标签或地区代码白名单。
- 根因/结论：发布器已改为解析当前六列表头，将控件、选项、默认序号、托管次数和显示状态全部写入通用 Schema；规则字段和非数值选项由每个玩法的外部持久化键注册表首次分配，后续属性修改保持技术键不变，删除后保留墓碑禁止复用。玩法业务身份移至独立注册表，发布器代码不再包含逐规则或逐玩法映射常量。
- 修改范围：`Server/tools/room-rules-publisher.rb`、`Server/tools/watch-room-rules.rb`、`Server/tools/room-rule-play-identities.json`、`Server/tools/test-room-rules-watcher.rb`、新增 `Server/tools/test-room-rule-schema-evolution.rb`、三地区 `.generated.json/.keys.json` 产物，以及客户端 Schema 类型中的 `trusteeCount`；未修改任何规则表、玩法规则、结算逻辑或玩法客户端文件。
- 验证结果：临时工作簿新增、选项/默认/托管/显示修改、删除与稳定墓碑门禁通过；Watcher 全部契约通过；成都、内江、凉山分别生成 8/10/9 个字段，三者“小结算”均为多选、默认 1、托管 3、显示；本地发布更新活动索引和缓存，Watcher PID `98460` 仅作为受影响服务启动，三地区确认 hash 分别为 `dfca4c2c…`、`9346afeb…`、`bf3902ea…`。
- 未完成项：CDP `1234` 仍由亲友圈专属 Profile `/tmp/aoo-club-records-e2e.iZfv2H` 占用，已由 aoo稽查协调完成后释放；框架未接管。待释放后完成三地区真实创建页“小结算”验收及 LS201 权威房间快照，再关闭本项。

### 2026-09-09｜平台级底分建房契约

- 用户要求：继续处理框架队列中的平台级 `baseScore`，先解除 Hall 创建房间对该字段的拒绝，并完成成功、非法值及房间配置证据。
- 根因/结论：地区规则 Schema 只允许规则表字段，导致公共协议字段 `baseScore` 被当作未知地区规则拒绝。现将其定义为独立于地区规则表的公共房间字段：缺省为 1，仅接受不超过 32 位整数上限的正整数，地区 Schema 的未知字段拒绝策略保持不变。
- 修改范围：`Server/server/Hall/.../RoomRuleSchemaValidator.java`、对应定向测试，以及 `Server/tools/local-dev-services.sh` 的 Hall 单服务启动/重启入口；未修改规则表、客户端玩法文件或具体玩法逻辑。
- 验证结果：Hall 及依赖主代码 Reactor 编译成功；只重启 Hall，PID `11719`、健康状态 `UP`。经真实 Gateway API，LS201 使用底分 1 创建房间 `977606`、底分 5 创建房间 `603621`，查询所得 `rules.baseScore` 分别为 1、5；0、-1、1.5 和文本均以 HTTP 400、`HALL_RULE_VALUE_INVALID` 拒绝，两个测试房间均已退出清理。证据位于 `Server/docs/verification/evidence/base-score-20260909`。
- 未完成项：跑得快岗位继续补 LS201 权威运行快照及玩法侧重连、结算、战绩和回放验收；标准 Reactor 定向 JUnit 被仓库既有 Poker 旧验收测试缺少 `AypdkGameProvider`、`HbpdkSession` 阻断，本次未越界修改。

### 2026-09-09｜跨资源锁顺序死锁风险待审计

- 用户反馈：游戏服可能出现亲友圈锁与房间锁被不同线程反向持有并互相等待，导致业务线程堆积、连接超时及客户端停留加载界面。
- 风险判断：Aoo 框架若存在跨亲友圈、赛事、房间事务的多锁嵌套且没有统一加锁顺序、超时释放和死锁监测，也可能发生同类问题；当前仅作为待审计风险，不视为已确认故障。
- 后续要求：审计服务端锁顺序、锁作用域、阻塞线程与超时日志；发现问题先报告 aoo稽查，未经用户明确授权不得重启或修改服务。

### 2026-09-09｜跨资源锁顺序优先审计权限阻塞

- 用户要求：优先检查所有可能锁死的风险，存在合理可能即优化，并在完成后恢复原框架队列。
- 根因/结论：只读扫描确认服务端存在需立即专项核查的真实风险面：Gateway `RoomOrderedExecutor.execute` 使用无界 `ReentrantLock.lock()` 并在持锁期间执行任意 `Callable`；旧 `gameServer` 的房间任务锁同样无超时，`ClubRoom`、`UnionRoom` 在房间锁内调用 `roomPos.leave` 等跨对象流程，且亲友圈/联盟管理器包含大量数据库访问。当前证据足以确认无界等待和线程池饥饿的合理可能，但尚不足以断言已形成“亲友圈锁→房间锁→亲友圈锁”的完整等待环。
- 修改范围：仅只读扫描当前服务端源码并更新本文安全断点；未修改 Gateway、旧 gameServer、亲友圈、联盟、房间、数据库或部署状态。
- 验证结果：已排除单纯存在 `synchronized` 即视作故障的做法，并将首个明确风险定位到 `Server/server/Gateway/.../RoomOrderedExecutor.java` 的无界获取与锁内任意任务，以及 `Server/server/gameServer/.../AbsBaseRoom.java`、`ClubRoom.java`、`UnionRoom.java` 的无界房间任务锁调用链。
- 未完成项：项目级 `AGENTS.md` 将本线程限制为 Aoo UI 修复与更换，且本文禁止未经授权修改服务端、亲友圈和数据库；须由 aoo稽查取得并下达 `Server/server/Gateway`、`Server/server/gameServer` 及相应测试/单服务重启的精确授权后，才能建立完整锁顺序图、实施修复、并发故障注入和部署。原真实房间队列安全断点保留在三客户端登录校准阶段。

### 2026-09-09｜现行框架并发、饥饿与无限等待全量审计完成

- 用户要求：持续检查整个现行 Aoo 框架，发现死锁、活锁、线程池饥饿或无限等待风险即做最小框架级修复，全部关闭后再报告。
- 根因/结论：确认并修复 Gateway 同池阻塞代理、Room Authority 租约与生命周期共用调度器、两处无界锁、公共 JDBC 无网络等待边界、客户端原始 HTTP 与资源/Bundle 僵死 Promise；补查 Matchmaking、Media、ExternalIntegration、Share、LuckDraw、IdentityVerification、Referral、Inventory、Club、AdminApi 后，现行现代服务链路无剩余无界锁等待或无超时同步 HTTP。旧 `gameServer`/Legacy/AooKernel 的 64 个残留仅分类留档，当前未部署且未越权修改。
- 修改范围：Gateway、Bootstrap Room Authority、Account、GameCommon 公共 DataSource、客户端公共网络与资源加载、审计脚本、专项测试和服务端审计报告；未修改玩法规则、亲友圈业务、数据库结构、Prefab/Scene 或开房规则表。
- 验证结果：LOCK01～LOCK12、LOOP07～LOOP16 全部通过；Gateway 6/6、Account 10/10、GameCommon 183 项通过（9 项外部集成跳过）、客户端专项 9/9 通过；核心服务重建重启后 Gateway、Authority、Version、Account、Hall、Social、Gifting 全部健康。详细报告见 `Server/docs/verification/现行框架并发与无限等待审计-2026-09-09.md`。
- 未完成项：聚合测试编译被既有扑克测试引用已删除类型阻断，属玩法测试职责，未恢复旧代码；框架原 UI 队列从已记录安全断点继续。

### 2026-09-09｜登录/WS 阻塞与公共门禁复核

- 用户要求：不重启服务，核查账号登录 502、游戏 WebSocket 403，并审计跑得快上报的 9 项公共门禁失败。
- 根因/结论：登录 502 是 Account 上游瞬时断连，当前直连及 Gateway 代理均恢复 200；WS 403 是房间销毁后旧一次性票据被再次使用。9 项旧失败中 6 项为过期断言，更新后确认动画 Bundle/路径、背景 Bundle/编号、ChatPanel `Popup` 路径共 3 项为真实跑得快接线缺口。
- 修改范围：仅更新三个相关单元测试与本记录；未重启服务，未修改房间生命周期、玩法实现、Prefab 或资源。
- 验证结果：账号 16 经 Account/Gateway 登录均 200，退出 200；相关门禁 26 项中 23 项通过，3 项以精确新契约继续失败。
- 未完成项：三项玩法接线缺口已报告 aoo稽查，待其转交跑得快岗位处理；稳定三人房仍需协调后方可恢复框架真实 E2E。

### 2026-09-09｜登录代理修复部署阻塞更正

- 用户要求：真实浏览器再次复现登录 502 后继续定位，获维护窗后仅重启 Gateway 并完成连续点击验收。
- 根因/结论：共享 JDK HttpClient 复用上游已关闭的空闲连接导致首个 POST 无响应头；已缩短可配置的上游连接池保活时间并固定 HTTP/1.1，禁止自动重试登录 POST。此前记录中“无需代码修复”的判断仅基于单次 curl，不足以闭环，现追加更正。
- 修改范围：Gateway 上游 HttpClient 初始化及契约测试、本记录；未修改 Account、Hall、Poker、房间状态或数据库。
- 验证结果：Gateway 编译通过，代理隔离与 HTTP 契约测试 5/5 通过；完整父门禁被既有只读规则表监听器检查阻塞。
- 未完成项：获准仅重启 Gateway 后，启动恢复被历史测试房 967030 的 `rule snapshot mismatch` 阻断，8080 当前未恢复；已向 aoo稽查报告并等待房间岗位通过正式恢复/解散流程处理，之后立即续作 CDP 1234 真实登录验收。

### 2026-09-09｜登录代理部署闭环与剩余旧门禁清理

- 用户要求：Gateway 恢复后完成真实登录复验，并继续处理稽查下发的其余 6 项旧契约门禁。
- 根因/结论：Gateway 空闲连接保活修复已随单服务重启生效；6 项失败均为测试仍引用已删除协议、迁移前地区目录、旧结算表达式、旧公共 Prefab 名称或旧 Poker_Card 路径/Bundle，并非要求恢复旧代码。
- 修改范围：更新 5 个客户端契约测试及本记录；未修改玩法实现、Prefab、Scene、规则表或房间数据。
- 验证结果：独立 CDP `1234` 无头 Chrome 对真实 Cocos Canvas 连续完成 3 次账号登录，耗时 473/468/480ms，其中第 2 次登录前空闲 5200ms，均进入 `MainScene` 且 Gateway 未新增 Account 上游 502；6 项旧失败修正后相关门禁 39/39 通过。Gateway 编译及定向测试此前已分别通过，单服务已恢复健康。
- 后续边界：三项跑得快资源接线缺口及其真实房间 E2E 继续由稽查分派玩法岗位；框架恢复本文“记忆”中的 UI 队列，仍依赖稳定真实房间的项目不得虚报完成。

### 2026-09-09｜账号 21 首次登录复现与二次修复待部署

- 稽查反馈：CDP `1235` 全新上下文、账号 21 首次真实登录仍返回 Account HTTP 502，要求按完全相同页面链复现；此前账号 16 三次成功不足以证明全条件闭环。
- 复现结果：独立 CDP `1234`、全新 browserContext、账号 21、真实 Canvas 点击得到相同失败；预检 204，POST `identity=21&password=1&mode=multi` 于约 33ms 返回 502，Gateway trace `8799f1cf-9c49-4c3c-b990-27c28e3b9d24`，首个明确错误仍为 `HTTP/1.1 header parser received no bytes`。请求 URL、Origin、UA、设备和页面实例头与稽查链路一致，问题与账号 16/21 的业务数据无关。
- 更正与修复：前条“登录代理部署闭环”结论撤回。仅缩短 JDK 全局保活时间不能可靠排除已固化或复用的半关闭连接；现改为 Account 非 GET/HEAD 写请求使用独立 HttpClient/连接池，保证每次新建上游连接，仍禁止重试非幂等 POST；其他读取与服务继续共享连接池。
- 验证状态：Gateway 编译通过，HTTP 契约及代理隔离测试 6/6 通过；未重启 Gateway，未触碰稽查使用的 CDP `1235`。已向稽查申请仅重启 Gateway 的维护窗口，部署后必须以账号 21 全新上下文再次真实登录才可闭环。

### 2026-09-10｜账号 21/22 空响应最终根因更正

- 复核结果：Gateway 重启后，账号 21 等价真实登录仍为 502；直连 Account 证明账号 16 返回 200，而账号 21/22 均为空响应。只读数据库核对确认当前仅有测试别名 16，21/22 尚未注册。
- 最终根因：不存在的登录身份由 `UnifiedIdentityService.resolve` 抛出 `UnifiedIdentityService.NotFound`，未被 `JdbcAccountSessionService.login` 转换为账号域 `Unauthorized`，也不在 `AccountHttpRoutes` 捕获集合内，导致 JDK HttpServer 直接关闭 Exchange；Gateway 因上游无响应头才显示 502。该链路不是连接池复用问题，前条临时连接隔离方案已从源码撤回。
- 最小修复：`JdbcAccountSessionService` 将缺失身份统一转换为 `Unauthorized("invalid credentials")`，保证 HTTP 层返回 401；新增缺失身份回归测试。Account 测试 7/7、Gateway 原契约测试 5/5 通过。
- 部署状态：现有维护授权只允许重启 Gateway，明确禁止 Account，故修复尚未部署。已向稽查申请仅重建并重启 Account 与 Gateway 的维护窗；部署后账号 21 未注册时的正确结果是 401 并留在登录页。如要求账号 21 进入大厅，须由测试数据责任方先正式注册该账号。

### 2026-09-10｜规则默认索引与大厅 Prefab 适配续作

- 用户要求：开房规则表的默认勾选必须自动映射到创建房间 UI；常规屏幕适配必须由 Prefab 的 Widget、Layout、UITransform 或 Mask 表达，删除代码中的固定边距、坐标搬移和 Widget 覆盖。
- 根因/结论：规则发布器现显式区分单选与多选；单选单元格 `6` 作为一个完整的 1 基索引，仅映射第 6 项。凉山“比金花”因此应唯一默认“不比”。当前整表仍被独立的第 16 行“玩法”阻断：工作簿按 `｜` 分隔只有 5 个选项，但多选默认原值为 `123456`。大厅四向常规适配已迁入 `Lobby_Main.prefab`，旧运行时代码适配已删除。
- 修改范围：`Server/tools/room-rules-publisher.rb`、`Server/tools/test-room-rule-schema-evolution.rb`、`Client/assets/Lobby/EnabledPrefab/Lobby_Main.prefab`、`Client/assets/Lobby/Code/Runtime/LegacyLobbyScreen.ts` 及大厅专项测试；未修改或重新保存任何规则表，未修改亲友圈文件、玩法逻辑、节点命名或层级。
- 验证结果：6 项单选且默认 `6` 的映射回归通过；规则 Schema 演进测试和 Watcher 20 项门禁通过。大厅 6 项专项测试通过，Creator 3.8.8 已正常导入并重开 Prefab；同一 Preview 在 1920×900、1280×720、844×390 三种视口均进入 MainScene，移动触摸可打开 Create_Room，控制台无新增错误。
- 未完成项：凉山整表发布、缓存失效和“比金花默认不比”的真实 UI/建房全链验收，必须先由用户明确第 16 行应补回哪个第 6 项，或将其默认范围改为现有 5 项；框架不得猜测、吞错或修改权威规则表。

### 2026-09-10｜LS201 规则发布阻塞复核

- 稽查要求：在不修改规则表的前提下，复核 LS201 旧 Schema 被保留是否还存在发布框架缺陷。
- 根因/结论：发布器在生成任何文件前完成整表解析和默认索引校验；第 16 行“玩法”只有 5 个选项而默认原值为 `123456`，因此按 fail-closed 原则拒绝整表。Watcher 仅在发布脚本成功、工作簿前后哈希一致且生成物哈希等于来源哈希后写入确认状态；本次失败不会覆盖生成物、确认状态或活动数据库索引。未发现需要追加修复的发布框架缺陷。
- 修改范围：仅本开发记录；规则表、生成物、稳定键注册表、数据库、客户端和玩法代码均未修改。
- 验证结果：Schema 演进回归通过，Watcher 20 项契约通过；直接以只读 LS201 工作簿执行生成稳定复现 `第16行 玩法/默认勾选(D16)（控件类型=checkbox，可选项数=5，默认原值="123456"） 默认序号越界`。上一有效生成物哈希仍为 `27d446fb4ffd81518a11067f432023c08b083c01ff61d5cccd34ba3c05ea96a9`，修改时间保持 `2026-09-09 19:19:56`。
- 未完成项：唯一阻塞仍是权威表格含义冲突；须由用户明确第 6 项的实际文字，或将默认序号限定为现有 1～5 项。获得明确内容并由用户更新规则表后，框架再自动发布并完成缓存、UI 与真实建房验收。

### 2026-09-10｜NJ201 平台必填规则字段门禁

- 稽查要求：定位 NJ201 创建房间因 `playerCount is required` 返回 Hall 500 的统一规则发布原因，并从框架层处理所有同类字段映射。
- 根因/结论：内江权威表第 8 行为表头，第 9～18 行规则中没有“人数”；稳定注册表仍保留停用墓碑 `playerCount=人数`，并非发布器丢失已有映射。框架缺陷是发布器此前允许缺少平台必填字段的 Schema 发布。现由玩法技术身份注册表集中声明 `playerCount/人数`、`roundCount/局数` 两项平台必填字段，首次生成与墓碑恢复均使用稳定键；任一缺失时在写生成物、注册表和数据库前 fail-closed。
- 修改范围：`Server/tools/room-rule-play-identities.json`、`Server/tools/room-rules-publisher.rb`、`Server/tools/test-room-rule-schema-evolution.rb` 及本记录；未修改或重新保存规则表，未修改大厅、玩法代码、生成物、数据库或运行服务不得硬编码。
- 验证结果：全新注册表生成时 `人数→playerCount`、`局数→roundCount` 映射通过；删除平台必填字段会失败且不覆盖上一生成物；Schema 演进测试与 Watcher 20 项契约全部通过。当前 NJ201 只读校验精确返回 `内江跑得快.xlsx 权威 Excel 缺少平台必填规则字段: playerCount（历史显示名=人数）`。
- 未完成项：无法在不知道权威人数选项及默认值时恢复 NJ201 发布。须由用户明确并维护“人数”行的可选项、默认勾选、托管次数和是否显示；规则表更新后再发布、验证生成物/注册表/Hall 契约，并通知大厅岗位用 CDP `1236` 复验。

### 2026-09-10｜NJ201 用户冻结

- 用户要求：内江玩法暂不处理，由用户自行配置规则表；等待用户明确配置完成后再恢复。
- 执行结果：已冻结 NJ201 规则表、发布映射、创建房间 Hall 500 及相关代码，不再修改、重发或继续测试；通用平台必填字段门禁保留为已完成框架修复。
- 修改范围：仅本任务状态记录；未修改规则表、NJ201 代码、生成物、数据库或服务。
- 未完成项：NJ201 后续发布与大厅真实建房验收暂停，不再作为当前可执行队列；仅在用户明确通知规则表配置完成后恢复。

### 2026-09-10｜多选默认容错与 LS201 重新发布

- 用户要求：多选默认索引超出当前选项数时忽略越界项并对有效项稳定去重；单选纯数字默认无效时规范化为第 1 项并输出警告。重新发布 LS201，NJ201 保持冻结。
- 根因/结论：发布器原先将多选历史紧凑默认 `123456` 中不存在的第 6 项视为整表错误，导致 LS201 原子保留旧 Schema。现多选只保留 1 基有效索引、按首次出现顺序去重；单选合法索引保持原值，`5项+6`、`5项+16` 等纯数字无效值统一回退第 1 项并写入 stderr 审计警告；非法字符仍拒绝。
- 修改范围：`Server/tools/room-rules-publisher.rb`、`Server/tools/test-room-rule-schema-evolution.rb`、LS201 生成物/稳定键注册表/本地活动规则索引及本记录；未修改规则表、NJ201、大厅或玩法代码。
- 验证结果：`多选5项+123456→[1,2,3,4,5]`、`单选5项+6→[1]`、`单选5项+16→[1]`、合法 `单选6项+6→[6]` 与非法字符拒绝均通过；Schema 演进及 Watcher 20 项通过。LS201 已发布 sourceHash `7de4853ddef66be667bc238146b5d8ac41d2c52274bceb44e4d23fbd2e35aa98`，活动 generation `2026090729`、release `92031090729`、cacheEpoch `27`；UI 9 字段、validator 10 字段（含平台治理字段），注册表无 inactive 墓碑，重复小结算旧项已清除；比金花默认唯一第 6 项“不比”，玩法默认保留第 1～5 项。
- 未完成项：须由跑得快岗位从真实 Canvas 重新打开 LS201 创建页，验证新 sourceHash、无重复小结算、比金花默认“不比”、真实建房及权威房间快照；NJ201 按用户要求继续冻结。

### 2026-09-10｜全客户端 Widget 动态适配专项初审

- 用户要求：扫描全客户端运行时 Widget 四边及 UITransform 尺寸、坐标适配；能由 Prefab/Scene 表达的迁回资源，只保留真实安全区、动态内容和平台实时数据，并完成 Creator 与三视口验收。
- 初审结论：明确发现公共 `LegacyFormManager` 全屏覆盖、亲友圈 `LegacyClubLandscapeAdapter`、PDK Canvas/ChatPanel，以及 15 个 CompatibilityApp/麻将 SceneBootstrap 的运行时缩放或 Widget 禁用；安全区、Manifest 动态渲染、头像方向和动态列表/牌张尺寸属于可保留项。完整清单与分批计划见 `历史记录与证据/Widget动态适配审计/2026-09-10/清单与计划.md`。
- 规范去重：`命名与层级规范.md`“适配”章节已完整覆盖“能用 Prefab Widget 就禁止代码适配”和删除双重适配，本次未重复新增同义条款。
- 修改范围：仅审计清单、计划与本记录；未修改登录场景、亲友圈、PDK、CompatibilityApp、Prefab、Scene 或业务逻辑。
- 未完成项：先建立 `LegacyFormManager` 路径到 Prefab 的完整映射并选择无冲突首批；亲友圈与 PDK 项存在岗位/冻结冲突，须由 aoo稽查协调后才能写入；Prefab 变更后按批完成 Creator 3.8.8 同步和 CDP `1234` 三视口验收。

### 2026-09-10｜玩法规则变更主动归档协作

- 用户要求：以后修改、修复或确认玩法流程、牌型、计分、结算、庄家、操作限制、开房规则语义或地区差异后，必须主动报告给“Aoo 文档”任务。
- 执行结果：协作要求已去重归入“执行与验收”；已向任务 `01a088d4-fe1b-7e92-a939-04785e1bec61` 补报 LS201 默认索引、重复小结算清理、活动 Schema 发布及 NJ201 平台必填字段门禁与冻结状态，包含前后规则、影响、证据和测试。
- 修改范围：仅本文档及跨任务事实通知；未修改玩法、规则表、Prefab 或服务。
- 未完成项：无；后续满足触发条件时持续主动执行。

### 2026-09-10｜正式文档变更主动归档范围扩大

- 用户要求：主动报告范围从玩法规则扩大到所有需要写入或更新正式文档的修改。
- 根因/结论：前条仅覆盖玩法语义，范围不足；现已以最新要求替换为框架、协议、数据库、资源、Prefab/Scene、部署、验收、业务、配置、迁移和重要修复的统一主动报告规则，并保留纯内部重构只记岗位开发记录的例外。
- 修改范围：仅本文“执行与验收”和本更正记录；未修改代码、配置、资源或其他岗位文档。
- 验证结果：旧窄条款已被完整新条款替换，没有重复规则；当前 Widget 动态适配专项清单与计划已主动补报“Aoo 文档”任务。
- 未完成项：无；后续持续按扩大后的统一范围执行。

### 2026-09-10｜玩法规则变更主动同步

- 用户要求：所有岗位完成玩法规则相关修改或确认后，主动向固定“Aoo 文档”任务提交结构化报告。
- 根因/结论：已写入固定任务ID、报告字段和无法直发时的稽查转发路径。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜正式文档同步范围扩展

- 用户要求：将主动同步从玩法规则扩大到全部需要新增或更新正式文档的变更。
- 根因/结论：已合并覆盖架构、组件、接口、数据库、资源、场景、构建、验收、业务、规则、配置、迁移和重要修复；纯内部无外部影响的重构仅记岗位记录。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜aoo产品更名与点对点协作更正

- 用户要求：原“aoo稽查”更名为“aoo产品”；职责明确时直接点对点协作，明确文档事项直发Aoo文档，仅例外情形上报aoo产品。
- 根因/结论：已删除重复文档同步条款并替换旧泛化上报规则；历史记录旧称保留为当时名称。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变。
- 未完成项：无。

### 2026-09-10｜业务域全栈职责更正

- 用户要求：框架岗位负责公共前后端框架、公共协议、公共资源和发布工具链，并跨配置、数据迁移、部署和真实 E2E 闭环。
- 根因/结论：旧规范只定义前端框架并排除数据库和业务协议，现已替换为公共框架业务域全栈边界。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜本地测试账号隔离更正

- 用户要求：框架岗位仅使用本地账号`11`至`20`，密码为`1`；不得占用其他岗位账号，预留账号须经aoo产品分配。
- 根因/结论：已加入岗位账号段、并行换号、实际账号留证和跨岗位账号/数据禁止操作规则。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜持续执行与idle条件更正

- 用户要求：统一已授权任务的持续执行、阻塞续作和唯一合法idle条件，不得把未授权历史审计项自动入队。
- 根因/结论：有已授权可执行任务时必须持续执行；当前项阻塞时点对点联系并继续独立任务；仅全部完成或全部等待决定、授权、外部状态时可idle。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜停止报告强制更正

- 用户要求：任何停止、idle、异常结束或准备不再继续时必须额外向aoo产品报告，普通阶段进度仍无需报告。
- 根因/结论：已补齐停止报告七项字段和禁止带已授权可执行任务停止的门禁，替换“正常idle无需报告”的冲突口径。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜所有玩法未发牌统一退出规则

- 用户要求：所有玩法在未发牌时，无论玩家已准备或未准备，均可点击房间左上角返回按钮退出。
- 根因/结论：旧公共规则错误地把抢庄等发牌前阶段视为游戏中；权威扑克会话还会将该阶段离房玩家保留为托管占位。现统一以服务端 `cardsDealt` 为退出边界，准备状态不参与判断；抢庄阶段离房会取消尚未展示的牌局并真实移除成员，已发牌对局仍禁止普通退出。
- 修改范围：公共客户端房间退出策略、PDK 公共房间接入、GameSPI 房间成员生命周期契约、扑克权威状态机与测试，以及现行进出房间规范；未修改任何玩法规则表、Prefab、Scene 或节点层级。
- 验证结果：客户端公共退出策略与 PDK 返回按钮测试 7 项通过，Client TypeScript 全量检查被同文件既有 `includes(number)` 类型错误阻塞；服务端主代码编译通过，定向测试编译被既有缺失 `AypdkGameProvider`、`HbpdkSession` 阻塞。
- 未完成项：待部署受影响的 Poker/GameSPI 服务，并以真实房间分别验证未准备退出、已准备退出、抢庄阶段退出和发牌后禁止退出；完成后从待处理队列移除。

### 2026-09-10｜所有玩法未发牌统一退出验收完成

- 完成内容：公共客户端与 GameSPI 均以 `cardsDealt` 作为唯一退出边界；准备状态不限制退出，未发牌抢庄会撤销未展示牌局并真实移除成员，房主离开时转移房主，空房关闭；已发牌后仍保留房间并提示不可退出。
- 部署结果：GameSPI、Poker 生产代码已完成依赖构建，Gateway 已仅重启受影响实例并通过 `8080/18080` 健康检查；本次未修改 Prefab、Scene、Meta 或节点层级，无 Creator 资源数据库同步项。
- 验收结果：公共客户端专项测试 7 项通过；真实 Cocos Canvas 点击完成未准备直接退出、已准备未发牌直接退出以及发牌后房间保留三条边界，使用框架账号 `11/12`，证据目录 `pdk-back-button-2026-09-10T02-29-03-033Z` 与 `pdk-back-button-2026-09-10T02-30-27-516Z`。抢庄阶段真实移除由服务端权威状态专项测试覆盖。
- 已知旁支：旧 E2E 脚本仍按过期提示文字等待“游戏中不能退出”，实际发牌后房间已正确保留；该提示断言不属于本次退出能力判定，不影响本任务结论。
- 未完成项：无；该任务已从待处理队列移除。

### 2026-09-10｜公共 Canvas/CDP 事件坐标换算修复

- 根因/结论：Creator Preview 的 `SHOW_ALL` 策略会把 Canvas 节点平移，节点 `worldPosition` 已包含该平移；旧验收脚本再次减去 `Canvas.worldPosition`，导致窄屏真实渲染位置与 CDP 点击点横向错位。公共换算现只使用节点世界坐标一次，不修改 Creator 场景、Prefab 或运行时布局。
- 修改范围：新增 `Client/tools/cocos-cdp-coordinate.mjs` 与对应专项测试，作为各岗位真实 Canvas/CDP 验收的统一坐标契约。
- 验证结果：专项测试 3/3 通过；`852×393` 移动触摸下“确定登录”、加入房间、准备和左上角返回均按修正坐标真实命中，证据目录 `pdk-back-button-2026-09-10T02-30-27-516Z`，运行时异常为 0。
- 未完成项：无；已通知亲友圈岗位按公共换算恢复模板房 E2E，并从框架待处理队列移除。

### 2026-09-10｜UIMessage_Drift 销毁节点竞态修复

- 根因/结论：漂浮消息的延迟回调与场景退出并发时，Cocos 已清空销毁节点的内部 `children`，但 `LegacyForm` 仍持有 JavaScript 引用；旧 `find` 未检查节点有效性便调用 `getChildByName`。现公共查找逐级校验 `isValid`，漂浮队列发现根节点失效时取消定时器并安全结束。
- 修改范围：`LegacyFormManager.ts`、`MessageDriftLifecycle.ts` 与公共提示专项测试；未修改 Prefab、Scene、节点层级、快捷文字内容或玩法协议。
- 验证结果：公共提示、Canvas 坐标和房间退出组合专项测试 12/12 通过；原 `children=null` 调用路径已被有效性门禁覆盖。
- 未完成项：框架修复无未完成项；已直接通知跑得快岗位恢复 QuickText 真实房间验收，玩法侧消息发送结果由该岗位闭环。

### 2026-09-10｜Modules 资源归档与迁移门禁补齐

- 用户要求：完成 `Client/assets/Modules` 下各模块资源引用本地化，按模块归置到 `Modules/*/Atlas/Imported`，并补齐迁移工具链与门禁测试。
- 根因/结论：原引用存在跨模块共享与同名覆盖风险；已将 218 个图片/图集资源重定向到对应模块目录（Activity、CreateRoom、Download、Profile、Ranking、Records、Rewards、Store、Support、Tournament），Prefab UUID 已更新，模块内引用统一闭环。
- 修改范围：新增/更新 `Client/tools/localize-module-atlas-assets.mjs`、`Client/work/module-atlas-localization.json`、`Client/tests/unit/module-atlas-localization.test.mjs`；调整 `Client/assets/Modules/*/Atlas/Imported` 与相关模块 `Prefab` 资源映射；仅文档与资源引用结构，未改业务代码逻辑。
- 验证结果：Creator 目录 `.meta` 补齐后已重新导入并保存 `Create_Room.prefab`；门禁（`club-union-create-room-flow.test.mjs`）通过，显示 `btn_next` 已接入；`btn_next` 与 `ui/club/UIUnionPLSet` 绑定链路已完成文档确认。
- 未完成项：未新增运行时行为测试，不代表所有真实点击场景已复验；待后续任务按有权限范围补充用户路径级验证。

### 2026-09-10｜Modules 资源归档记录更正

- 更正说明：上一条“Modules 资源归档与迁移门禁补齐”误加入了不相关项（`btn_next`、`ui/club/UIUnionPLSet`、`club-union-create-room-flow`），该内容不属于本次模块资源本地化任务。
- 正确范围：仅完成 `Client/assets/Modules` 下 10 个模块（`CreateRoom` 等）相关 `Prefab` 的图片/图集资源本地化，资源以 `Modules/<模块名>/Atlas/Imported` 存放；使用独立 UUID 复制资源并重写 `Prefab` 引用（保留来源相对路径，避免同名冲突）。
- 任务要素（本次）：新增/更新 `Client/tools/localize-module-atlas-assets.mjs`、`Client/work/module-atlas-localization.json`、`Client/tests/unit/module-atlas-localization.test.mjs`；补齐目录 `.meta`；在 Creator 3.8.8 重新导入并重开 `Create_Room.prefab`；专项门禁 `module-atlas-localization.test.mjs` 通过。
- 验证与结论：本次记录仅涉及资源本地化链路，不包括亲友圈赛事建房按钮绑定或 `UIUnionPLSet` 相关验收内容。

### 2026-09-10｜任务转交先入队与字段清单标准化

- 用户要求：所有新任务与转交任务在处理前先写入“记忆”待处理队列。
- 根因/结论：新增“任务摘要、来源、状态、职责范围、安全断点”入队字段清单；完成任务后移除，阻塞或停止时保留队列状态，避免任务丢失与重复认领。
- 修改范围：仅本文档“执行与验收”和“开发记录”。
- 验收结果：七章顺序未变，历史记录未改写，未引入代码或配置改动。
- 未完成项：无。

### 2026-09-10｜代码分类原则落库

- 用户要求：将“用在哪里就归到对应 code 文件夹”写入岗位规范并执行，禁止 A 类功能在 B 类目录引用。
- 根因/结论：现有两个代码整理线程需执行严格职责归属，不再允许跨域引用或转发路径掩盖，分类应以真实使用域为准并同步 `.meta` 与引用。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验收结果：规则落库，无代码更动；后续代码整理需按本约束逐条自检并在完成项中回填。
- 未完成项：无。

### 2026-09-10｜游戏配置目录、Bundle 边界与 Poker 旧引用收口

- 用户要求：统一游戏大类配置目录与 Creator Bundle 边界；Poker 公共配置使用 `Games/Poker/Common/Config`，并永久保持用户已删除的 `Games/Poker/Packs` 不恢复。
- 根因/结论：麻将配置已从 `Packs/Pack01/Config` 迁至 `Packs/Config`；现存 NJPDK 公共配置已迁至 `Poker/Common/Config`，原目录及资源 UUID 保持不变。`Games/Common`、`LongCard`、`Mahjong`、`WordCard` 配置为大类 Bundle；Poker 根目录保持非 Bundle，其九个直属子域分别配置为独立 Bundle。
- 修改范围：相关目录及 `.meta`、公共目录规范、`LegacyLobbyScreen`、`CommonPdkSwitchCoordinator`、Prefab 注册与 NativeMaps、旧 `SceneInfo.json`、目录/Bundle/迁移门禁；未恢复 `Poker/Packs`，未修改玩法规则、开房规则表或 UI 层级。
- Bundle 结果：大类为 `games-common`、`games-longcard`、`games-mahjong`、`games-wordcard`；Poker 子域为 `poker-common`、`poker-cx`、`poker-ddz`、`poker-gd`、`poker-nn`、`poker-pdk`、`poker-sg`、`poker-sj`、`poker-zjh`；均使用 `priority: 1`、`bundleConfigID: default`，全项目名称无冲突。
- 验证结果：目录、Bundle、快捷语音、PDK 公共房间及迁移专项门禁 28/28 通过；现行资源/代码中旧 `Games/Poker/Packs/Pack01`、`poker01-prefab`、`PaoDeKuaiReplay/Record` 引用为 0。Creator 3.8.8 已重新导入并重开 `GameRoom2D.scene`，控制台 0 Error；仅有 4 条既有 Activity PNG 缩略图 Warning，与本次改动无关。
- 未完成项：无；已通知跑得快和俱乐部岗位恢复真实 E2E。

### 2026-09-10｜Common Atlas 单 Bundle 方案取消

- 更正说明：用户最新明确决定覆盖此前“`Common/Atlas` 合并为单一 `common-atlas` Bundle”的要求，禁止使用 `common-atlas` 或 `common-atlas-*`，Creator 当前已配置的 Bundle 边界为唯一权威。
- 执行结果：框架线程在收到更正前尚未修改 Atlas 元数据、资源、NativeMaps、PrefabRegistry 或加载合约；已立即通知资源归档和公共代码线程停止扁平化、移动、改名、合并、删除子 Bundle 及旧 Bundle 名替换。
- 保留边界：不得为未完成方案改写现有 `loadBundle` 合约；其他线程若已产生方案性改动，仅可撤回自身未完成改动，不得覆盖用户或其他 AI 的修改。

### 2026-09-10｜主协调暂停断点

- 暂停原因：aoo产品要求等待 Lobby 代码精简与 Games 公共代码整理两个 AI 全部完成，未明确恢复前禁止继续修改、刷新、测试或启动新工作。
- 已完成内容：Modules 20 个 Prefab 的 25 个无引用顶层 `null` 序列化槽已移除并重映射 `__id__`，Creator 3.8.8 于 12:15:42 重新导入成功、后续无 `_children` TypeError；Common Atlas 单 Bundle 方案确认本线程零代码/资源写入并已取消；已完成 `RoomNetworkControllerMount` 公共房间职责与 UUID 所有权只读确认。
- 当前任务：Creator Preview 登录后报 `MainScene` 未在 build settings。已只读确认 `assets/Lobby/Scenes/MainScene.scene.meta` UUID 为 `aba41764-79e6-4d3d-80b3-c199cf5fd49f`，且 `profiles/v2/packages/builder.json` 的 web-desktop 场景清单已包含同 URL/UUID；尚未定位 Preview 实际场景清单为何未生效，未修改设置、场景或代码。
- 共享文件状态：Lobby 代码 AI 正迁移职责文件，Games 公共代码 AI 正整理 `Games/Common/Code`；框架不再触碰 `Lobby/Code`、`Games/Common/Code`、Builder 配置、Atlas、Registry 或 Creator。安全恢复点为读取两 AI 最终文件清单与门禁结果后，再从 Preview 运行时场景清单/构建 Worker 状态继续定位。

### 2026-09-10｜Games/Common/Code 职责归类（真实 E2E 阻塞）

- 用户要求：只保留当前公共游戏层实际使用的代码，按职责重分类并清理可证明无用项。
- 根因/结论：9 个 TypeScript 均有运行时、Creator 组件或契约测试入口，不能删除；已分别迁入 `Catalog`、`Audio`、`Room`、`Settlement`、`Card`，且原 `.meta` UUID 保持。`Other/resources` 实含 1,937 个由 `legacy-ui/**`、`native-ui/game/**` 与 NativeMaps 动态加载的资源，不属于无用代码，已保留并通知资源归档岗位避免冲突。
- 修改范围：`Client/assets/Games/Common/Code` 下上述 TS 与 `.meta` 的移动；所有静态 import 与专项测试路径修复；未修改玩法规则、协议、UI、资源动态路径或 `Other/resources`。
- 验证结果：44 项目录、目录注册、房间、重连、快捷语音、结算及公共扑克运行时契约测试通过；Creator 3.8.8 自动重新导入并生成新目录 `.meta`，控制台 Error 为 0。CDP `1234` 被 PID `97488`（Profile `/tmp/aoo-framework-exit.mXqQBP`）占用，未接管或终止，故真实公共流程 E2E 待端口释放。
- 未完成项：释放 `1234` 后，以任务独立临时 Profile 从 Creator Preview 完成公共 PDK 入房、公共扑克牌、重连与结算模板加载真实 E2E；通过后才继续下一项 `Common/Code` 整理。

### 2026-09-10｜代码实际使用域归属纠正

- 用户要求：模块专用代码归模块 Code，同游戏共享代码归 `Games/<Game>/Common/Code`，跨游戏公共才留 `Games/Common/Code`；禁止以转发层或兼容路径掩盖跨域放置。
- 修改范围：将仅由扑克 PDK 房间与回放使用的 `Poker_Card_Factory.ts`、`Poker_Card_Presenter.ts` 及原 `.meta` 从 `Games/Common/Code/Card` 移至 `Games/Poker/Common/Code/Card`，并修复调用和测试路径；未改类名、UUID、牌值协议、图集 Bundle 或运行时行为。
- 验证结果：PDK/card/room/reconnect 定向测试 27/27 通过。发现 `RoomNetworkControllerMount.ts/.meta` 已由并行写入从 Lobby 移至 `Games/Common/Code/Room`，非本批移动，已停止触碰并报 aoo产品确认所有权。
- 未完成项：Creator 与静态门禁待合并当前并行写入后复验；真实 E2E 仍受 Preview 缺少 MainScene build setting 阻塞。

### 2026-09-10｜Games 公共代码迁移引用复验

- 完成内容：职责归档任务已将 `RoomNetworkControllerMount.ts/.meta` 归回 `Lobby/Code/Runtime` 并保留 UUID；本任务复核发现三份已迁移脚本仍带旧目录层级的相对 import，已仅修正至真实全局公共路径，未改变业务行为、资源 Bundle、场景或 Prefab。
- 验证结果：10 个迁移运行时文件的相对 import 静态解析全部命中；目录、目录注册、房间、重连、快捷语音、结算及公共扑克运行时契约测试 44/44 通过。大厅 UI 专项另有 4 个现存失败（缺少 `NewMain.prefab`、三项过期 Lobby 断言），已回报其责任岗位，未在公共代码整理中修改。
- Creator/E2E：任务专属无头 Chrome 使用独立 Profile/CDP `1239`。刷新预览仍显示之前错误层级的扑克 import；磁盘源码已为正确路径，Creator 正被用户操作，未抢占编辑器强制重导入。待用户空闲后由 Creator 正常导入、复查控制台与 Preview，再继续已知 `MainScene` build settings 阻塞后的真实大厅/房间流程。
- 未完成项：本项不得标记完成，直至 Creator 重新导入后无脚本错误，并完成公共 PDK 入房、扑克卡、重连和结算的真实 Canvas E2E。

### 2026-09-10｜全局公共代码首个无消费者转发清理

- 完成内容：删除 `Runtime/CompatibilityApp/bootstrap/LoginScreenBootstrap.ts` 及原 `.meta`（UUID `50e67376-9126-45d9-9caf-93b465bfc990`）。该文件只有一行从 Login 域回导 `LoginScreenBootstrap` 的转发，违背实际使用域归属；全项目路径引用和 UUID 序列化引用均为 0。
- 验证结果：删除后路径、UUID 均不再存在，公共游戏专项回归 44/44 通过。
- 未完成项：删除仍待 Creator 在用户空闲后正常重新导入验证；其余 `CompatibilityApp` 项均有大厅或玩法运行时消费者，不按同名误删。

### 2026-09-10｜MainScene Bundle 场景加载修复

- 根因：Creator 的 `web-desktop` 构建任务已勾选 `MainScene`（UUID `aba41764-79e6-4d3d-80b3-c199cf5fd49f`），但 Preview 真实加载的 `assets/main/config.json` 仅有 `Boot_Strap`、`Login_Scene`；`MainScene` 在 `lobby` Bundle 的 `config.json` 中。`director.loadScene('MainScene')` 在 Bundle 尚未注册时会误报未加入 build settings。
- 修改范围：新增全局导航运行时 `BundleLoader.ts`；登录路由和 PDK 回大厅兜底路径均在预加载/跳转前显式加载 `lobby` Bundle，再使用原有 `director.loadScene` 生命周期。未修改场景、Prefab、Bundle 元数据、资源或规则表。
- 验证结果：运行时 Preview 的 main/lobby 场景清单与 Bundle UUID 已通过 CDP 实测；变更文件相对 import 均可解析；针对性测试 41 项通过。另有 `Poker_Common.plist.meta` 缺失导致 1 项既有扑克资源测试失败，已留给资源归档职责处理。
- 未完成项：Creator 正被用户操作，尚未将新 `BundleLoader.ts` 导入 Preview 编译记录；待编辑器空闲后重新导入、刷新 Preview，并以真实 Canvas 登录进入大厅后完成公共 PDK E2E。

### 2026-09-10｜MainScene Bundle 导航与大厅首次加载闭环

- 根因/结论：`MainScene` 已在 `lobby` Bundle，而公共表单登记还将实际嵌套于 Bundle 根目录的 `Lobby_Main`、`NoticeBar` 与 `WaitForm` 写成不可发布的根路径或嵌套 Bundle。现由集中 `BundleLoader` 先加载 `lobby`/`common-prefab`，并将三项登记分别解析到 `lobby/Prefab/Lobby_Main`、`common-prefab/Common/NoticeBar`、`common-prefab/Common/WaitForm`。
- 修改范围：全局导航装载器、登录及 PDK 返回大厅路由、公共 Prefab 登记与定向契约；未修改登录场景、Prefab 内容、Bundle 元数据、规则表或用户编辑器布局。
- Creator/E2E：Creator 3.8.8 内置“清除代码缓存”后正常重新编译，Preview 产物已包含全部新路径。任务专属 Chrome Profile（CDP `1239`）清除临时会话后，从 `Login_Scene` 对真实 Canvas 输入账号、密码并点击“确定登录”，进入 `MainScene`，`Lobby_Main` 活跃；登录、会话、用户资料、WebSocket 票据、活跃房间及大厅目录请求均返回 200。
- 已知旁支：运行中 Creator 报告 `WaitFormAction.anim`、`shopAni.anim`、`btn_yuanAni.anim`、`ani_yuan01.png` 等资源已被其他资源操作删除；本任务未重建、改绑或掩盖这些 UUID 错误，后续 PDK 资源级 E2E 须先由资源责任域恢复。
- 未完成项：大厅首次加载闭环已完成；公共 PDK 入房、扑克卡、重连与结算的真实验收仍按 Games 公共代码队列继续。

### 2026-09-10｜CD201 公共房间 Bundle 发布修复

- 根因/结论：成都 CD201 活动发布索引 `2026090709` 的 `ui_schema.bundle` 仍为已退役的 `poker01-prefab`；内江、凉山均已发布 `pdk-common-room`。问题来自服务端房间发布记录，非 Creator 场景或客户端目录。
- 修改范围：新增正式迁移 `Server/database/migrations/V20260910_01__publish_cd201_pdk_common_room_bundle.sql`，以当前 CD201 发布内容生成新的不可变 release/index `2034091001/2026091001`，只将资源路由收敛至 `pdk-common-room/GameRoom2D`；未更新或删除任何既有房间。客户端新增集中、可审计的历史 Bundle 读取映射，旧 `poker01-prefab` 仅在读取历史房间路由时解析为 `pdk-common-room`，不进入玩法启动器或新写入路径。
- 验证结果：本地运行库已应用迁移，活动 CD201 索引实测返回 `pdk-common-room/GameRoom2D`；此前 E2E 房 `294730` 仍能读取其旧 release/index，随后已自动解散且成员状态为 `LEFT`，无测试房残留。Creator 3.8.8 已导入并编译新增 TypeScript（Preview 编译产物包含 resolver）；新增集中映射专项测试通过。相关定向测试 13/15 通过，剩余两项均为既有缺失资源 `pdk_UISharePanel.prefab` 与 `VoiceRecording.anim`，未重建或改绑。
- 未完成项：本轮重新打开的创建房页面规则加载正常，但 CreateButton 未收到任何真实鼠标/触摸回调，未产生新房间；该大厅点击链并非本次 Bundle 发布改动，已保持冻结，待其责任任务单独复验。

### 2026-09-10｜PDK Bundle 拓扑与真实入房更正

- 更正：上一条记录中的 `pdk-common-room` 是未发布的嵌套目录名，不能作为 Creator Bundle。当前权威 Bundle 为 `paodekuai-common`、`paodekuai-deskbg`、`paodekuai-liangshan`、`paodekuai-neijiang`；PDK 父目录不是 Bundle，历史 `poker01-prefab` 与 `pdk-common-room` 仅在集中读取映射中兼容。
- 修改范围：四个 PDK Bundle 元数据与相关公共 Prefab/结算/音频/房间资源索引；正式发布迁移 `V20260910_02__publish_cd201_pdk_common_bundle.sql` 已将 CD201 活动索引发布至 release `2034091002`、generation `2026091002`，路由为 `paodekuai-common/GameRoom2D`。同时补齐公共 `common-prefab` 父 Bundle 声明、Numpad 实际路径和 PDK 结算预加载路由。
- 验证结果：Creator 3.8.8 已正常导入四个 PDK Bundle 和 `common-prefab`，Preview 配置均可访问；真实 Canvas 创建房间后进入 `GameRoom2D`，加载 `games-common` 与 `paodekuai-common`，并通过真实 `Btn_Back` 返回大厅清理测试房。
- 未完成项：最新 Numpad 与结算预加载修复需在刷新后重测；当前刷新卡在受保护登录场景的 `LoginScreenBootstrap` UUID 无效问题，未修改场景，等待其责任范围按 Creator 修复后从 JoinRoom/建房断点恢复。

### 2026-09-10｜CD201/LS201 范围收敛与公共 Prefab 缺失阻塞

- 用户要求：当前仅处理 CD201、LS201；麻将、亲友圈和其他玩法暂停并保留断点。
- 根因/结论：`LoginScreenBootstrap` 的脚本 UUID、Meta 与三个场景序列化引用一致，所谓 UUID 无效是全局脚本加载失败的连带错误。当前首个真实运行错误为 `common-prefab/index.js` 加载失败；磁盘 `Client/assets/Common/Prefab` 仅剩 `.DS_Store`，CD201/LS201 登录及房间链路依赖的 `WaitForm`、`NoticeBar`、`Numpad`、`Message`、`SettingsPanel` 均不存在。
- 修改范围：仅只读核对磁盘、Creator 日志和专属 CDP Preview；未修改 Common Atlas、Common Prefab、Registry、场景或玩法代码。
- 验证结果：Preview 服务 200，`paodekuai-common` 的 `index.js/config.json` 为 200，`common-prefab` 的两项均为 404，场景停在 `Boot_Strap`。
- 未完成项：紧急共享资源指令禁止自行恢复或移动 Common；需 aoo产品/用户明确权威恢复来源和授权，按原 UUID 恢复 CD201/LS201 必需公共 Prefab 后继续登录、入房、结算与 E2E。

### 2026-09-10｜Modules 权威路由与登录启动解阻

- 用户要求：禁止恢复旧 `Common/Prefab` 副本，以 `Modules` 当前现有 Prefab 为权威，仅修改统一注册、NativeMaps、Bundle 路由和测试。
- 根因/结论：登录与大厅预加载仍强制加载已不存在的 `common-prefab`，造成启动前 404；现移除该启动依赖，并将唯一可一一对应的 `UINoticeBar` 映射至 `activity-ui/Social/Prefab/Notice`。Modules 中不存在 WaitForm、Numpad、SettingsPanel 和通用 Message 的等价 Prefab，未做错误替代。
- 修改范围：`LoginScreenBootstrap.ts`、`SceneRouter.ts`、`ModulePrefabRegistry.ts`、`prefab-path-map.json` 及对应测试；未修改任何 Prefab、节点、UUID、视觉或 Common Atlas。
- 验证结果：启动相关测试 7 项通过；另 6 项既有大厅测试因已迁移资源/过期断言失败。Creator 热编译后的专属 CDP Preview 已进入 `Login_Scene`，Canvas 有效，运行时异常 0；已通知登录大厅和跑得快从安全断点续验 CD201/LS201。
- 未完成项：WaitForm、Numpad、SettingsPanel、通用 Message 在 Modules 无对应现有 Prefab，待真实链路首次需要时按具体缺失项报告，禁止恢复旧副本。

### 2026-09-10｜公共 Prefab 权威清单与 Bundle 恢复

- 用户最终确认：`Client/assets/Common/Prefab` 只保留 `Common_Head`、`Message_Drift`、`Message`、`NoticeBar`、`Numpad`、`SettingsPanel`、`WaitNet` 七个根层 Prefab；其他历史 Prefab 均视为主动废弃，不恢复、不兼容、不继续引用。
- 修改范围：恢复 `Prefab.meta` 的 `isBundle: true` 与 `common-prefab` 声明；公共注册表将历史消息逻辑键统一映射至 `Message`，`UIWaitForm/UIWaitNet` 统一映射至 `WaitNet`，并同步 NativeMaps 根路径。未修改七个 Prefab 的节点、层级和布局。
- Creator/验证：Creator 3.8.8 已识别七个根层 Prefab；Preview 的 `assets/common-prefab/config.json`、`index.js` 均返回 200，配置清单恰好包含七项；相关公共加载契约通过，另两项失败属于缺失的 PDK 分享面板与语音动画旁支。
- 依赖审计：七个 Prefab 中 `Message`、`Numpad`、`SettingsPanel` 依赖完整；`Common_Head` 仍有 13 个动画 UUID、`Message_Drift` 仍有 1 个动画 UUID、`NoticeBar` 仍有 1 个材质 UUID、`WaitNet` 仍有 1 个动画 UUID 指向当前磁盘不存在的资源。依照“当前磁盘权威、禁止历史恢复、Prefab 布局只读”规则，仅报告具体节点/组件，等待用户在 Creator 中决定删除残留引用或亲自补资源。

### 2026-09-10｜代码目录责任口径补充

- 根因/结论：按“用在哪里就归类到对应 `Code` 目录”的原则写入框架岗位执行规范，要求不允许 `A` 类功能长期运行于 `B` 类目录，通过兼容路径/转发层掩盖边界。
- 修改范围：补充 `第 3 章`“修改边界”一条硬约束；未改任何代码、资源、场景、服务端文件。
- 验证结果：规范已补齐并保持七章结构不变。

### 2026-09-10｜废弃动画/材质/Prefab治理同步

- 用户要求：凡用户已删除的动画、材质、Prefab 资源一律视为废弃，不从 Git、废纸篓、备份、Library、构建产物或旧版本恢复、复制、重建或兼容。
- 根因/结论：框架资源门禁同步为“现存磁盘与 Creator 为权威，失效引用只报不补建”，防止历史恢复路径污染公共加载链路。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验收结果：规则与历史失效引用处理方式继续与当前 `common-prefab`/公共路由门禁一致。
- 未完成项：无。

### 2026-09-10｜亲友圈 Club Prefab Bundle 解阻

- 根因/结论：当前 `ClubList.prefab` 及公共根路径候选均存在，缺失提示来自 `Club/Prefab.meta` 仅填写 Bundle 名却未启用 Bundle，导致 Preview 无 `club-prefab` 发布配置。
- 修改范围：仅为 `Client/assets/Club/Prefab.meta` 补充 `isBundle: true`，并增加 Bundle 名与启用状态契约断言；未修改亲友圈 Prefab、节点、业务脚本或历史资源。
- Creator/验证：Creator 3.8.8 已正常重新导入并刷新 Preview；`/assets/club-prefab/config.json`、`index.js` 均返回 200，配置明确发布根资源 `ClubList`（UUID `e4eb1156-33b4-4956-8d40-455e736c15a3`）。完整亲友圈入口测试另被并行新增 `LegacyUnionPLSetController.ts` 的过期控制器清单断言阻断，与本次 Bundle 修改无关。
- 未完成项：亲友圈账号31真实列表与保存验收由亲友圈岗位从原断点继续。

### 2026-09-10｜亲友圈 Club Atlas Bundle 解阻

- 根因/结论：当前 `Club/Atlas` 资源存在，但目录 Meta 与 Prefab 目录此前相同，仅填写 `club-atlas` 名称而未启用 Bundle，导致进入亲友圈后图集 Bundle 的 config/index 均为 404。
- 修改范围：仅为 `Client/assets/Club/Atlas.meta` 补充 `isBundle: true`，并增加 Bundle 名与启用状态契约断言；未恢复、移动、新建或修改任何图片、图集、Prefab、节点与业务脚本。
- Creator/验证：Creator 3.8.8 已重新导入 `Club/Atlas` 并刷新 Preview；`/assets/club-atlas/config.json`、`index.js` 均返回 200，未新增导入错误。
- 业务复验：亲友圈岗位使用账号31完成真实保存，大厅已显示 CD201 模板桌；证据为 `Client/docs/前端框架规范/历史记录与证据/证据/club-template-room-2026-09-10T09-44-01-980Z/after-save.png`。
- 未完成项：无。

### 2026-09-10｜身份判断权威字段统一

- 用户要求：身份判断统一使用服务端 `user_type`，不得从 `ppk` 或其他展示、格式特征推断。
- 根因/结论：身份值由服务端权威产生，公共协议与框架只负责传递和消费，不得生成平行口径。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验证结果：规则已归入既定章节，七章顺序保持不变；未修改代码、资源或规则表。
- 未完成项：无。

### 2026-09-10｜游戏公共 Prefab Bundle 恢复

- 用户要求：恢复公共预制体发布链路。
- 根因/结论：`Games/Common/Prefab` 的现行 Prefab 与 `games-common-prefab` Bundle 声明均存在，但 Creator Preview 刷新后未发布该 Bundle，导致 CD201、LS201 创建房间时请求 `config.json/index.js` 返回 404。
- 修改范围：仅触发 `Client/assets/Games/Common/Prefab.meta` 的 Creator 重新导入并刷新 Preview；未恢复历史资源、未修改 Prefab 内容、节点层级或大厅业务代码。
- 验证结果：Preview 的 `/assets/games-common-prefab/config.json` 与 `/assets/games-common-prefab/index.js` 均恢复 HTTP 200；已通知登录大厅岗位从账号21至24的 CD201/LS201 断点继续真实入房复验。
- 未完成项：最终真实创建、入房结果由原登录大厅验收任务继续回传。

### 2026-09-10｜资源归档过期队列清理与 Games 公共 Bundle 更正

- 用户要求：清理已由资源 AI 闭环的过期待办，继续 `Games/Common/Code` 与 `Common/Code` 的真实职责归类。
- 根因/结论：Modules、Lobby、Common 图片与图集归档及 Modules/Common 重复资源去重已由资源职责闭环，不再作为框架待办；`Games/Common` 根目录是唯一 `games-common` Bundle，`Prefab` 是普通子目录，前条记录中临时出现的 `games-common-prefab` 不是最终 Bundle。
- 修改范围：仅更新本岗位开发记录和记忆队列；未修改资源、Prefab、业务代码或 Creator 配置。
- 验证结果：已移除四项过期资源队列；Games 公共代码任务独占 `Games/Common/Code`、公共 PDK 场景路由、Bundle 引用和结算注册，框架线程只审计 `Common/Code`，避免重复写入。
- 未完成项：两项公共代码职责归类仍须完成静态门禁、Creator 串行同步和真实公共流程验收。

### 2026-09-10｜废弃公共游戏弹窗路由清理

- 用户要求：按当前磁盘资源权威继续 `Common/Code` 职责整理，不得保留跨域旧 Bundle 或已删除资源兼容。
- 根因/结论：`GamePrefabRegistry` 与 NativeMaps 仍登记不存在的 `PaoDeKuaiSportPointDialog`，并指向已取消的 `games-common-prefab`；全项目无实际消费者，属于无效公共路由。
- 修改范围：仅删除 `GamePrefabRegistry.ts` 和 `prefab-path-map.json` 中对应登记，增加防回归断言；未恢复资源、未修改麻将专属加载器或 Creator 内容。
- 验证结果：公共扑克房间与迁移定向测试 17/17 通过，NativeMaps JSON 解析通过，现行公共代码和 NativeMaps 中该资源名及旧 Bundle 引用均为零。
- 未完成项：麻将 `HzmjPrefabLoader.ts` 的同名旧 Bundle 引用属于麻将职责，已保持只读并交由对应责任范围确认。

### 2026-09-10｜框架岗位越权巡检与调度边界收紧

- 用户要求：框架 AI 已越界，须在规范中收紧权限。
- 根因/结论：原“公共框架全栈、持续执行、点对点协作”口径过宽，导致框架岗位巡检其他 AI、监督 Creator、追溯并拟恢复大厅业务场景。现明确框架岗位不是稽查或协调者，只能执行用户或 aoo产品下发的具体公共框架任务。
- 修改范围：仅本文档“修改边界”“执行与验收”“阻塞报告”“开发记录”和“记忆”。
- 新边界：业务专属目录默认只读；不得从任何历史来源恢复业务 Scene/Prefab；不得调度其他 AI；Creator 仅服务于自身已授权公共文件的同步验收。
- 未完成项：无。

### 2026-09-11｜刷新后恢复最后成功位置

- 用户要求：登录、游戏大厅、游戏房间、亲友圈刷新后分别恢复原位置，并确认亲友圈是否需要独立场景。
- 根因/结论：公共 `HostRoute` 已定义四类位置但没有持久化消费者；启动只恢复登录会话和服务端活动房间，亲友圈的 `legacy_last_club_*` 仅保存资料。亲友圈继续挂载于 `MainScene`，无需新增 Scene。
- 修改范围：新增公共 `HostRouteStore`；最小接入登录启动/退出、大厅成功挂载、亲友圈进入/返回及 PDK 房间成功挂载；新增契约测试。未新增或修改 Scene、Prefab、节点和资源。
- 验证结果：刷新恢复、亲友圈、大厅及房间状态相关测试 8/8 通过；Creator 3.8.8 已生成新脚本 `.meta` 并完成 Editor/Preview 编译，控制台新增错误 0。
- 未完成项：四位置真实刷新 E2E 仍被当前 `Client/assets/Lobby/Scenes/MainScene.scene` 空壳状态阻断；该业务场景不属于框架岗位，保持安全断点，不能宣称最终闭环。

### 2026-09-11｜游戏 Catalog 能力配置

- aoo产品要求：在唯一游戏 Catalog 中增加设置、聊天、语音和解散能力声明，本阶段不接线业务面板。
- 根因/结论：原 Catalog 只有玩法族与展示元数据，缺少统一能力模型；现新增类型安全的四项能力和按稳定 `game_code` 精确查询入口，未登记或未知编码统一安全返回全 `false`。
- 修改范围：仅 `Games/Common/Code/Catalog` 的现有类型与权威 Catalog、针对性单元测试及本记录；未修改 Scene、Prefab、Meta、业务面板、玩法实现、登录、大厅、亲友圈、规则表或服务端。
- 验证结果：`CD201`、`NJ201`、`LS201` 显式登记四项能力为 `true`；Catalog 定向 TypeScript 编译通过，Catalog 新旧契约测试 5/5 通过。项目全量 TypeScript 检查仍有 6 个既有业务目录错误，本次范围未新增错误。
- 未完成项：能力尚未接入 `SettingsPanel`、`ChatPanel`、`VoicePanel`、`DissolvePanel`；其余玩法因缺少可靠稳定编码或权威能力事实保持默认关闭，等待逐项确认后登记。

### 2026-09-11｜Catalog 房间能力接线

- aoo产品要求：继续将 Catalog 的设置、聊天、语音、解散能力接入 CD201、LS201 公共房间，NJ201 玩法保持暂停。
- 根因/结论：PDK 公共房间此前无能力门禁，进入房间即创建四类控制器并绑定入口；现统一从 `getGameCapabilities(gameCode)` 读取，关闭能力时不创建对应控制器、不订阅聊天/语音事件、不绑定请求入口，并直接停用现有 Anchor 下的对应按钮。
- 修改范围：仅 PDK 公共协调器、房间展示控制器、社交控制器、Catalog/公共功能针对性测试与本记录；未修改 Prefab、Scene、Meta、节点、资源、协议、服务端、规则表、NJ201 专属实现或其他玩法。
- 验证结果：针对性测试 23/23 通过；项目 TypeScript 检查仅保留 6 个既有 Club/PDK/Login 错误，本次无新增。Creator Preview 已热编译，账号11以真实 Canvas 创建 CD201 房间 `207876`、LS201 房间 `156034`，两者均实测设置打开/关闭、聊天打开、语音入口触发、未发牌解散弹窗打开且无新增页面错误；测试房已退出，专属 CDP `1234` Profile 已清理。证据目录为 `Client/docs/前端框架规范/历史记录与证据/证据/catalog-room-capabilities-2026-09-11`。
- 未完成项：浏览器环境没有 `aooMedia` 录音实现，语音面板按原行为提示平台未提供录音并立即关闭；能力入口和禁止请求门禁已验证，真实录音上传不属于本次 Catalog 接线。

### 2026-09-11｜亲友圈积分统一命名

- aoo产品要求：旧 `GameCent`/比赛分统一改为 `ClubCent`，含义固定为亲友圈内的游戏积分，各亲友圈独立。
- 修改结果：全部客户端 Prefab 的旧积分节点命名已统一使用 `ClubCent` 标识，公共 `CommonRoom.prefab` 保持原层级并保存为 `ClubCent`；客户端相关字段、事件、节点查询、提示和注释以及现行 Club 服务字段、接口和账本原因已同步统一，中文语义固定为“俱乐部积分”。
- 隔离保证：现行权威状态仍以 `club_id` 为聚合键；新增集成测试覆盖同一玩家同时加入两个亲友圈并分别持有 `120`、`35` 俱乐部积分，确认互不串用。
- 验证结果：556 个 Prefab 均通过 JSON 解析，Prefab 及现行客户端代码不再残留 `GameCent`、`SportsPoint`、`sportsPoint` 或“比赛分”；Creator 3.8.8 已重新显示无未保存标记的 `ClubCent` 节点；Club 及依赖模块构建成功，Club 测试 5/5 通过。Creator Web Desktop 阶段构建被既有 `verify-no-empty-asset-shells` 门禁拦截，首错为项目中 12 个既有空资源目录，未越权删除这些目录；直接 TypeScript 检查仅保留 6 个任务前既有错误，本次重命名未新增错误。

### 2026-09-11｜公共 Prefab 去下划线索引同步

- aoo产品已在 Creator 将唯一公共头像与漂字 Prefab 重命名为 `CommonHead`、`MessageDrift`；框架同步更新公共注册表、原生路径映射、PDK 头像加载/清理、魔法表情头像加载、契约报错文字及针对性测试。
- 两个 Prefab 的原 UUID `6a38d2de-c1f4-46f4-81f9-a1aa4dc6f403`、`4960876b-bf92-4692-bb2f-78fd6ab1c8df` 保持不变，根节点和 `syncNodeName` 与文件名一致；逻辑 Form ID `UIMessage_Drift` 属于协议键，保持不变。
- 验证：两个 Prefab JSON 解析通过，旧资源加载路径无残留，相关专项测试 19/19 通过；全量命名门禁被既有缺失台账 `Client/docs/verification/121-naming-inventory.json` 阻塞，本次未新建或伪造该历史台账。

### 2026-09-11｜废弃公共 NoticeBar 代码链路清理

- aoo产品要求：用户已在 Creator 删除公共 `NoticeBar`，同步删除它在代码侧的全部专属链路，不影响其他消息和通知功能。
- 修改范围：删除公共注册表与 NativeMaps 当前公共路径、大厅加载/实例化/表单登记、专属控制器及 Meta，并移除对应专属测试断言；公共组件当前清单同步调整为六项。
- 隔离保证：`MessageDrift`、普通 `Message`、聊天、语音、社交消息/红点刷新及玩法专属历史通知映射均未改动；`social.notice.changed` 仍由 `SocialController` 消费，因此保留。
- 验证结果：公共 `UINoticeBar`、`LegacyNoticeBarController`、`Common/Prefab/NoticeBar` 现行引用均为零；NativeMaps JSON 解析通过，定向测试 6/6 通过；项目 TypeScript 检查仅保留 6 个任务前既有业务错误，本次无新增。Creator 3.8.8 已识别更新后的 `CommonPrefabRegistry.ts`，控制台无新增 NoticeBar/脚本反序列化错误。

### 2026-09-13｜俱乐部断线重连模板桌保留

- 用户要求：修复俱乐部断线重连后模板桌子不显示。
- 根因/结论：Hall 重连监听在候选连接仍为 `RECOVERING` 时查询模板，并且查询前隐藏桌子容器；短暂失败后连接仍进入 `READY`，但桌子没有后续刷新。现将 UI 投影查询延后至重连提交后，并在刷新失败时保留上一份桌面快照。
- 修改范围：`LegacyClubMainController.ts`、俱乐部模板恢复专项测试及本记录；未改 Prefab、Scene、节点层级、视觉、规则或服务端。
- 验证结果：俱乐部模板定向回归 9/9 通过；Creator 3.8.8 热编译控制台 0 Error；项目 TypeScript 仅保留 3 个任务前既有错误，本次文件无新错误。
- 未完成项：独立无头 Chrome/CDP `1234` 已到达真实 `LoginScene`，但当前登录按钮的 Canvas 坐标与公共 CDP 换算失配，无法打开账号面板，因此最终断网重连 E2E 尚未完成。

### 2026-09-13｜全局数值键盘小数输入

- 用户要求：所有数值输入位置的数字键盘均可正常输入小数点，例如 `1.23`，不局限于俱乐部。
- 根因/结论：公共 `Numpad` 的小数点只在消费端显式提供 `decimal` 回调时才生效；创建房间底分和房费等数值入口未提供该回调，原实现还把底分强制限制为正整数。现统一接通创建房间底分、通用比例/积分/费用/分数字段、俱乐部房费及分成区间，小数最多两位；首键小数点规范化为 `0.`。
- 修改范围：公共数字表单适配、创建房间底分、俱乐部房费与分成区间的键盘消费逻辑、专项测试及本记录；未修改公共键盘 Prefab、节点或层级。房间号、回放码、玩家/俱乐部 ID、页码和房卡数量属于整数业务标识或计数，继续保持整数协议。
- 验证结果：创建房间底分专项测试 3/3、俱乐部分成契约测试通过；Creator 3.8.8 已热编译，本次文件未产生 TypeScript 新错误，项目仍只有 3 个任务前既有错误。

### 2026-09-13｜唯一公共数字键盘输入状态机

- 用户要求：全项目数字输入永久只允许使用公共 `Numpad` 组件，不得由创建房间、俱乐部或其他页面分别维护按键输入代码。
- 架构结果：数字、小数点、删除、前导零及小数位限制统一由 `NumpadService` 处理；业务页面只提供当前值、写回函数、整数位上限、总显示长度和允许的小数位数。
- 输入规则：房间号、玩家/俱乐部 ID、回放码、页码和数量等整数入口不声明小数位，点击小数点不生效；积分、费用、比例和底分等数值入口显式声明小数位，可输入 `0.xx`。
- 清理结果：业务页面的 `digit`、`backspace`、`clear`、`decimal` 回调及遗留的逐键 `push/clear` 实现永久删除；专项门禁禁止再次引入第二套页面输入状态机。
- 验证结果：公共键盘唯一状态机、整数/小数策略、全局 Legacy 数值输入路由、创建房间底分及加入俱乐部专项测试 9/9 通过；Creator 3.8.8 当前控制台 Error 为 0。项目 TypeScript 仅保留 3 个任务前既有错误，本次文件无新增错误。

### 2026-09-13｜全项目数字入口 Button + lb 统一

- 用户要求：全项目所有调用公共数字键盘的 Prefab 入口统一改为 `Button + lb`，不再使用原生 EditBox 唤起平台键盘。
- 修改结果：完成 45 个 Prefab、85 个数字入口迁移，范围包含俱乐部、麻将大结算页码和手机绑定；文字搜索、昵称、备注和聊天等普通文字入口保持 EditBox。旧 EditBox 仅作为旧业务代码的值存储且已禁用，可视交互和点击入口均为 `Button + lb`。
- 验证结果：专项门禁 2/2 通过，迁移脚本二次运行为零改动；Creator 3.8.8 已重启并重导入全部 45 个 Prefab，Library 对应资源全部包含 Button/lb；真实移动视口 Canvas 从登录进入大厅、俱乐部加入界面，点击公共数字按钮后 `lb` 显示 `1`，未唤起隐藏原生输入框，页面异常 0。项目 TypeScript 仅保留 3 个任务前既有错误，本次无新增。

### 2026-09-13｜房间顶部积分按联盟房显示

- 用户要求：公共房间顶部“积分”只在联盟房间显示，俱乐部房和大厅普通房均隐藏。
- 修改结果：`CommonRoom/RoomInfo/ClubCent` 默认隐藏；PDK 公共房间仅当权威进房票据 `entryOrigin === 'UNION'` 或 `unionId > 0` 时显示，并写入本地玩家的俱乐部积分。不以 `fromClub` 作为联盟房判定，避免俱乐部房误显示。
- 验证结果：显示规则与 Prefab 层级专项测试 2/2 通过；Creator 3.8.8 已重启并重导入 `CommonRoom.prefab`，Library 数据确认 `ClubCent` 默认 `_active=false`；TypeScript 仅保留 3 个任务前既有错误，本次无新增。

### 2026-09-13｜房间双向无感场景交接

- 用户要求：进入房间和退出房间均不显示进度条，不闪黑屏，保持原场景画面直到目标场景首帧完成。
- 实现结果：公共展示事务新增静默首帧保留模式；建房、加入/恢复进房以及房间返回大厅/俱乐部均冻结当前 Cocos Canvas，目标场景完成就绪检查、引擎绘制和浏览器绘制后才移除；加载失败会立即释放冻结帧并显示原有可重试错误界面。
- 验证结果：专项测试 2/2 通过；Creator 3.8.8 热编译后使用专属无头 Chrome/CDP `1234`、Apple iPhone 14 Pro 移动视口真实触摸创建房间与返回，进入采样 724 次、返回采样 384 次，进度层出现次数均为 0，且两个方向均捕获到冻结帧从源场景保持至目标场景首帧后释放。
- 修改范围：公共展示事务、PDK 公共房间启动/返回、场景返回路由、创建房间交接及专项测试；未修改 Scene、Prefab、Meta、节点层级、玩法规则、协议或服务端。

### 2026-09-13｜房间切换黑屏回归修正

- 用户反馈：首版无感交接在进出房间时仍显示黑屏。
- 根因/修正：浏览器对未启用 `preserveDrawingBuffer` 的 WebGL Canvas 临时复制会得到全黑像素；改为在 `Director.EVENT_AFTER_DRAW` 中将当前相机渲染至原设计尺寸 `RenderTexture`，读取已完成的合成帧，并在像素有效性检查通过后才显示冻结层。退出事务提前到任何离房请求和界面清理之前；进入事务等待 `CommonRoom` 与 `PDK_CommonRoom` 都挂载后再提交，嵌套 Prefab 加载不得覆盖静默交接。
- 真实验证：Creator 3.8.8 热编译并整页重载后，专属无头 Chrome 移动视口真实触摸创建、进入和退出房间；进入冻结帧非黑像素采样 `2157`、退出 `4864`，两个方向进度层均未出现，目标场景首帧后冻结层均已释放。截图和采样记录保存于 `Client/docs/前端框架规范/历史记录与证据/证据/room-seamless-transition-2026-09-13`。

### 2026-09-13｜刷新恢复俱乐部房间退出修正

- 用户反馈：从俱乐部进入房间后刷新页面，再点击退出约等待 20 秒，并错误返回游戏大厅。
- 根因/修正：Hall 的活动房间恢复票据不携带原始进入来源，刷新启动时现仅在 `roomId` 一致时把已成功提交的 `HostRoute.returnTo` 合并回恢复票据，保留权威 `clubId`；退出时不再向刷新过程中已经关闭的旧 Hall 连接发送心跳并等待网络超时，目标大厅连接统一由 `SceneRouter` 在挂载返回页面时重建。
- 验证结果：刷新来源恢复、俱乐部 ID 返回、退出不等待旧 Hall、房间覆盖层单一关闭及无黑屏交接针对性测试 7/7 通过；Creator Preview 使用框架账号 `11` 完成真实登录和俱乐部页面恢复检查。该账号现有俱乐部均无可进入的模板桌，未伪造模板或越权修改俱乐部业务数据，因此“俱乐部模板桌进入→刷新→真实点击退出”的最终 Canvas 计时仍待具备现成模板桌的俱乐部会话复验。
- 修改范围：`SceneRouter`、PDK 公共房间切换协调器及刷新恢复专项测试；未修改 Scene、Prefab、Meta、玩法规则、俱乐部业务实现或服务端。

### 2026-09-13｜房间双向切换延迟优化

- 用户反馈：房间已实现无黑屏交接，但进入和退出仍有可感知卡顿。
- 优化结果：进房所需 `common-prefab`、`games-common` 与玩法 Bundle 从串行改为并行加载；房间基础节点就绪后不再额外空等两轮浏览器帧，由公共展示事务统一等待并提交目标首帧；退房时 `SceneRouter` 复用房间侧已经捕获的同一展示事务，删除重复冻结与重复首帧提交等待。
- 即时响应补充：大厅启动阶段已读取的玩法目录和规则配置采用 60 秒短缓存，点击进房不再重复请求；Hall 确认入座后，房间详情与一次性游戏票据改为并行获取，将原来的五段串行网络链缩减为必要的两段权威提交。请求失败会清除对应缓存，避免把失败长期保留。
- 深度优化补充：大厅空闲阶段后台预载公共 PDK Bundle 与 `GameRoom2D` 场景；实体俱乐部桌展示时只读预热对应房间详情、玩法配置和发布资源，不提前占座、不提前签发可复用票据。点击时优先命中预热结果，仅执行权威占座和一次性票据。冻结画面 GPU 回读由原设计分辨率限制为最高 `960×540` 等比快照，并直接抽样回读数据，删除第二次整屏 `getImageData`，避免点击瞬间阻塞主线程。
- 即时壳层重构：区域跑得快桌子点击后立即切入已预热的 `GameRoom2D` 并挂载公共房间壳层，Hall 权威占座、一次性票据和房间连接在后台并行完成后绑定同一界面，不再让网络往返阻塞点击视觉响应。权威入房失败时取消迟到的壳层任务，按原入口返回俱乐部/联盟/大厅并展示准确提示，禁止残留实体桌或错误占座界面。
- 闪屏修正：预热范围由场景扩展到 `CommonRoom` 与 `PDK_CommonRoom` 两个实际房间根 Prefab；点击切换期间静默保留已绘制的入口画面，必须等两个根节点挂载且房间基础层完成首帧提交后才揭开，禁止暴露空 `GameRoom2D` 中间帧。
- 房间状态原子首显：公共房间壳层不得在权威状态到达前显示默认按钮、空头像或空手牌；首次进入与断线重连统一在保留画面后隐藏挂载，等待权威快照写入并完成头像、手牌、桌面牌和操作区异步渲染，再以同一展示事务一次性揭幕，禁止任何房间出现默认态到真实态的二次闪动。
- 边界：权威入房、房间详情、一次性票据、WebSocket 建连、场景就绪检查和无黑屏保护均保留，不以跳过服务端校验换取速度。
- 验证结果：资源预热/并行、单一退出事务、俱乐部权威入房、Hall 网络预热/并行、低成本冻结帧和无黑屏失败保护专项测试 9/9 通过；Creator Preview 已热编译相关脚本且项目日志无新增编译错误。未修改 Scene、Prefab、Meta、玩法规则或服务端。

### 2026-09-13｜离房玩家头像权威清理

- 用户反馈：一个玩家已经退出未发牌房间，其他玩家界面仍显示该玩家在座位中。
- 根因/修正：公共座位展示仅清空空座文字但保留旧头像实例，且旧头像异步加载可能在较新的离房快照后重新挂载；同版本推送去重指纹也未包含成员席位。现以权威 `playerId` 清理空座头像和点击层，为每个座位增加异步修订号，并将成员席位纳入状态指纹。
- 修改范围：公共 PDK 权威状态投影、房间头像展示、座位 Presenter 和专项测试；未修改俱乐部页面、Prefab、Scene、玩法规则或服务端离房逻辑。
- 验证结果：离房清理、席位指纹、房间退出及无感切换定向测试 11/11 通过；Creator 3.8.8 Editor/Preview 编译产物已包含本次逻辑；项目 TypeScript 仅保留 3 个任务前既有错误，本次无新增。
- 未完成项：CDP `1234` 被其他任务 Profile `/tmp/aoo-social-e2e.kgxKnY` 占用，未接管；Creator 同时存在用户未保存 Scene 提示，未保存或丢弃。待端口和编辑器现场释放后，以账号 11～20 完成双客户端真实离房同步 E2E。

### 2026-09-13｜离房权威状态分裂修正

- 用户复验：刷新后离房玩家头像仍存在，证明上一轮客户端空座清理不是完整根因。
- 根因/修正：`HallRoomGateway.leave()` 在首次权威离房请求失败后，仅凭 Hall 的 `active=false` 伪造成功并直接返回俱乐部，可能留下 Poker 权威座位；权威端又把“座位已不存在”的重试当成失败，无法安全补偿。现改为失败后以新幂等键再执行一次离房，未确认时禁止导航；权威座位已不存在统一返回 `ALREADY_LEFT`，使请求/响应丢失后的补偿可收敛。
- 修改范围：Hall 公共房间生命周期客户端、Gateway 权威房间成员生命周期、专项契约测试；保留上一轮成员指纹、空座头像清理和异步失效保护。
- 验证结果：客户端离房补偿及头像投影测试 11/11 通过；Creator Preview 已热编译新客户端；Bootstrap/Gateway 受影响模块编译成功，本地 Gateway 已于 06:18 重启，authority 端口 `18080` 与业务端口 `8080` 正常。
- 未完成项：CDP `1234` 仍由 `/tmp/aoo-social-e2e.kgxKnY` 占用，未接管；待释放后使用账号 11～20 完成双客户端“退出→对端即时消失→对端刷新仍不出现”的真实 E2E。

### 2026-09-13｜退出后再次进入同一房间黑屏修正

- 用户复验：玩家退出后再次点击同一实体桌，房间 Canvas 黑屏，控制台连续报告 `The asset has been destroyed!`。
- 根因/修正：房间后台预热只缓存了完成 Promise，未持有两个公共房间 Prefab；正常离房销毁运行时 UI 后，二次进入仍把旧 Promise 当作资源可用。与此同时 Bundle 可能保留 `SceneAsset` 外壳，但其中 Scene 节点已被上次场景切换销毁。现由启动器持有预热 Prefab 引用直至启动器销毁，失效 Prefab 自动逐出并重载；运行房间场景前验证真实 Scene 节点，发现失效外壳时释放并重新解析一次。
- 修改范围：公共 PDK 场景启动器及无感进出房间专项测试；未修改俱乐部页面、Prefab、Scene、Meta、节点、玩法规则或权威入退房协议。
- 验证结果：退出后重入资源生命周期、无感场景切换、成员投影与权威离房补偿定向测试 9/9 通过；Creator 3.8.8 Editor 编译产物已包含 Prefab 引用持有、失效资源逐出及 Scene 重载逻辑。项目 TypeScript 仅保留 3 个任务前既有错误，本次无新增。
- 未完成项：CDP `1234` 仍由其他任务专属 Profile 占用，未接管；待该端口释放后执行双客户端“玩家 2 退出→再次点击同桌→非黑屏进入→控制台无 destroyed asset”的真实 Canvas E2E。

### 2026-09-13｜离房座位再次占用部署修正

- 用户复验：玩家退出后再次点击实体桌提示“房间人数已满”。
- 运行证据：Hall 日志对房间 `276642` 连续报告 `Duplicate entry '276642-0' for key 'aoo_hall_room_member.uk_hall_room_seat'`；权威离房已把成员标记为 `LEFT`，但运行中的旧 Hall 未在新占座前清理该历史行，数据库唯一座位索引拒绝复用空椅子。
- 处理结果：源码已有按房间清理 `LEFT` 运行行再计算座位的实现，本次未重复改写；已仅编译并重启受影响 Hall 服务。部署后的 `JdbcHallRepository.class` 已核验包含该删除语句，Hall PID `32545`，`8093/health/hall` 返回 `UP`；Gateway/Authority 未重启。
- 未完成项：等待当前客户端再次点击房间 `276642`，确认历史空座可正常复用且不再产生重复索引错误。

### 2026-09-13｜即时房间壳层重构回退更正

- 用户复验：前两轮缓存与场景失效保护完成后问题仍存在，并明确指出即时入房重构前流程正常。
- 根因更正：回归边界是大厅 `enterRoom()` 新增的 `presentDefaultRoomShell()` 提前切场景，以及启动器新增的 optimistic shell/rollback 并行链路；该设计让视觉壳层启动和正式权威入房共同拥有场景生命周期。此前仅修复资源缓存和同场景单飞不足以恢复原来的单一所有权。
- 回退结果：已删除提前切场景、optimistic shell、rollback 及相关状态；保留无状态后台资源预热。现行顺序恢复为“权威加入成功→取得一次性 handoff→唯一正式场景启动→挂载权威房间状态”。
- 验证结果：定向测试 9/9 通过，TypeScript 未新增错误；Creator 3.8.8 Editor 热编译产物已确认不再包含 `presentDefaultRoomShell`、`rollbackRoomShell`、`shellPending` 或 `shellTransition`。待当前双客户端现场复验后闭环。

### 2026-09-13｜二次入房黑屏根因更正：场景双重启动

- 更正原因：用户在资源持有修正后复验仍黑屏，证明上一条仅覆盖失效缓存，未覆盖实际触发黑屏的并发条件。
- 最终根因/修正：桌子点击同时启动即时房间壳层与权威入房，两条流程在热缓存条件下可能于同一帧对同一个 `GameRoom2D` Scene 对象各调用一次 `runScene`；第二次启动会先销毁当前场景，再尝试运行同一已销毁对象。现将同名场景加载和启动收敛为单飞 Promise，并以 Director 的 `onLaunched` 回调作为完成点，壳层和权威流程只能共享一次真实场景启动。
- 同步与验证：相关定向测试 9/9 通过，TypeScript 未新增错误；已通过 Creator 3.8.8“刷新预览”同步，Editor 编译产物明确包含 `sceneLoadPending`、单次 `runScene` 和启动完成回调。仍待用户当前双客户端现场复验。

### 2026-09-13｜公共玩家头像本地资源服务接入

- 用户要求：所有玩家必须有头像，开发阶段从 `/Users/aoo/Pictures/头像` 读取；头像不进入 Creator 包，后续迁移到正式资源服务器。
- 实现：新增公共 `PlayerAvatarService`，真实 `headImageUrl` 优先；为空或加载失败时按玩家 ID 稳定映射 `tx1.png` 至 `tx10000.png`，自动跳过图库缺失的 `tx2258.png`、`tx2259.png`。公共 `CommonHead` 的方形/圆形头像与大厅本人头像均接入该规则，图片按 URL 缓存。
- 本地资源边界：新增 `tools/local-avatar-server.py`，把原图库通过 `0.0.0.0:8765` 只读提供并附加 CORS；客户端本地预览按当前页面主机名访问，正式环境后续只需配置 `__aoo_RUNTIME_CONFIG__.avatarBaseUrl`。未把任何头像复制到 `assets`，未修改原图库。
- 验证：本地资源 `tx1.png`、`tx10000.png` 均返回 200、PNG `100×100`；抽查玩家 ID 1～30000 的映射缺失数为 0；Creator 3.8.8 Preview 已编译新脚本，控制台仍为任务前既有 3 条 `bg1.jpg/bg1.png` 动态 URL 冲突，未新增错误；房间切换定向测试 6/6 通过。

### 2026-09-13｜公共头像图库缺号更正

- 更正原因：用户已补齐 `tx2258.png`、`tx2259.png`。
- 更正结果：公共头像映射恢复为完整的 `tx1.png` 至 `tx10000.png`，删除缺号跳过分支；历史开发记录保留，不回写改动。

### 2026-09-13｜全局玩家头像加载入口统一

- 用户纠正：头像必须作为公共能力统一处理，不能只修大厅或游戏房间；所有实际引用头像组件的位置都应显示玩家头像。
- 根因：俱乐部桌子 `PlayerState/Avatar` 过去只切换占座状态和昵称，从未调用头像加载；大厅、俱乐部列表、保险箱记录和兼容房间还各自维护重复的远程图片加载代码。
- 处理结果：唯一 `PlayerAvatarService` 提供 URL 解析、真实头像失败回退、缓存和 Sprite 赋值；公共 `CommonHead`、大厅本人、俱乐部列表、俱乐部保险箱、俱乐部桌子座位及兼容跑得快头像入口全部复用该服务。俱乐部桌子按座位玩家 `pid` 映射本地头像，不改 `ClubDesk.prefab` 层级。
- 验证结果：头像统一专项测试与房间切换回归 8/8 通过；源码中头像专用 `loadRemote<ImageAsset>` 已收敛到公共服务，剩余 `LegacyRemoteImageService` 是非头像限定的通用远程图片设施；Creator 3.8.8 Preview 已热编译俱乐部桌子和公共服务代码，未新增控制台错误。

### 2026-09-13｜联盟与俱乐部身份称谓统一

- 用户要求：项目内联盟业务的“创建者”统一为“盟主”，俱乐部业务的“创建者”统一为“圈主”。
- 处理结果：客户端职务、提示、审核表头和三个相关 Prefab，后台统一词汇，以及服务端联盟/俱乐部中文注释与两个运行时提示均按业务上下文完成替换；稳定英文协议、数据库字段和枚举名保持不变。
- 验证结果：词汇与大厅迁移测试 5/5、头像及无感房间回归 8/8 通过，Prefab JSON 解析通过；Creator 3.8.8 已刷新预览，Library 已导入新文案且无旧文案，控制台仅保留任务前既有 3 条 `bg1` 动态 URL 冲突。
- 未完成项：无；管理员账号、普通房间、PK 房间和比赛场专属场中的通用“创建者”语义不属于联盟/俱乐部身份，按语义保留。

### 2026-09-13｜刷新后全界面资源无感预热

- 用户要求：刷新恢复后点击任何按钮均无感响应，不再由各按钮首次加载界面资源。
- 根因/修正：公共 `LegacyFormManager` 过去只在 `show()` 时首次解析 Bundle/Prefab，并在大厅超过 120ms 后打开 `UIWaitForm`。现新增刷新表面统一预热：注册表、公共房间、玩法弹窗与当前已注册 Form 并发进入 Creator 缓存；后台预热和真实点击共用 Prefab/Bundle 单飞 Promise；大厅单按钮加载不再触发第二套加载界面；登录提示和房间公共弹窗复用同一入口，房间在揭幕前完成预热。
- 验证结果：预热契约及无感房间回归 10/10 通过，扩大词汇回归合计 13/13 通过；Creator 3.8.8 Preview 已自动热编译并可在产物中确认大厅、登录、房间三处接线，本次未新增编译错误。
- 未完成项：真实刷新后逐按钮计时 E2E 尚未执行；CDP `1234` 被其他任务独立 Profile `/tmp/aoo-social-e2e.kgxKnY`（PID `91045`）占用，未接管或终止。Creator 当前 `ClubDesk.prefab*` 存在用户未保存状态，本任务未保存、关闭或重导入该 Prefab。

### 2026-09-13｜刷新预热顺序纠正

- 用户复验：大厅场景加载后仍停留“正在挂载界面”约 3 秒，要求先显示界面、再按顺序后台预加载。
- 根因/修正：上一轮在大厅启动返回前 `await` 了整批预热，并以 6 路并发解码资源，阻塞当前界面提交且争抢主线程。现删除前台等待：大厅、登录和游戏房间均先显示当前界面，从下一轮事件循环开始以并发度 1 顺序预热；房间 Form 同时改为静默展示模式，按钮不再触发独立加载层。
- 验证结果：顺序后台预热及无感房间回归 10/10 通过；Creator Preview 自动热编译产物已确认包含三处 `preloadRefreshSurface(..., 1)` 且大厅不存在预热 `await`。全量 TypeScript 仅保留任务前 3 个既有错误，本次文件无新增错误。
- 未完成项：CDP `1234` 仍由上述其他任务占用，未执行独立无头刷新计时；用户当前可直接刷新复验 3 秒前台阻塞是否消失。

### 2026-09-13｜刷新加载文案精简

- 用户要求：刷新进度条下方不再显示重复状态文字，加载层只显示“正在加载中......”。
- 处理结果：公共加载协调器与 Web Desktop/Mobile 启动模板统一为单行固定加载文案；删除进度条下方 `aoo-startup-percent` 节点及更新逻辑，并在运行时清理浏览器可能残留的旧节点；失败提示与重试逻辑保持不变。
- 验证结果：加载文案、刷新预热和房间无感切换专项测试 11/11 通过；Creator 3.8.8 Preview 已自动热编译，产物中仅保留单行文案和旧节点清理逻辑。本次未修改 Scene、Prefab 或用户未保存的 `ClubDesk.prefab`。

### 2026-09-13｜公共启动进度与界面首帧同步

- 用户要求：刷新和首次进入加速，进度条不得停顿；进度到 100% 必须与目标界面完成挂载严格同步，不能提前或延后。
- 根因/处理：公共加载层过去只响应离散阶段上报，并依赖 CSS 过渡，阶段间会静止；提交时又直接关闭遮罩。现由公共协调器逐帧平滑追赶，加载期间最多推进到 94%，仅在目标 Canvas 已挂载并完成引擎帧、浏览器帧后快速补齐最后 6%，100% 实际绘制一帧后立即揭示目标界面；失败时停止动画。桌面、移动模板删除重复 CSS 过渡，避免双重插值卡顿。
- 验证结果：加载进度、刷新预热和房间切换专项测试 12/12 通过；TypeScript 本次文件无新增错误，全量仍仅有任务前 3 个既有业务目录错误。
- 未完成项：Creator 当前尚未重新生成本次脚本的 Preview chunk，且规定 CDP `1234` 仍被其他任务 PID `91045` 占用；未接管、终止或复用，待资源数据库正常热编译且端口释放后执行真实首次进入/刷新逐帧 E2E。

### 2026-09-13｜大厅最终帧硬切闪屏修复

- 用户反馈：公共进度条最后一帧进入游戏大厅时会闪一下。
- 根因/处理：目标大厅已完成首帧绘制，但公共 DOM 遮罩在 100% 后直接切换 `hidden=true`，浅色加载层与高对比大厅形成单帧硬切。现于 100% 时立即解除遮罩交互拦截，并仅对遮罩执行 80ms 透明退场；新过渡启动会取消未完成退场，失败和取消仍立即关闭。
- 验证结果：最终帧交接、进度、刷新预热和房间切换专项测试 13/13 通过；TypeScript 本次公共文件无新增错误，全量仍保留任务前 3 个既有业务目录错误。
- 更正（2026-09-13）：用户反馈“仍然闪”时，Preview chunk 时间仍早于透明交接源码，实际运行的是旧脚本；随后 Creator 于 07:56:23 正常生成新 chunk，已确认包含 `is-revealing`、交互解除及 80ms 退场。真实最终帧录制仍待 CDP `1234` 释放后继续。

### 2026-09-13｜大厅最终帧闪屏根因二次更正

- 用户复验：80ms 透明退场版本仍会闪一下。
- 根因/修正：静默事务结束路径会调用 `ensureDom()`，导致此前从未显示的通用过渡遮罩在结束时以默认可见状态插入页面一帧；同时透明退场会让已显示的大厅再次被浅色遮罩覆盖后变亮。现将过渡遮罩改为创建前即 `hidden`，静默结束只读取既存遮罩、禁止创建；目标界面完成就绪检查、引擎绘制和浏览器绘制后原子隐藏遮罩，不再执行透明叠加退场。
- 验证结果：新增“遮罩插入前隐藏、静默结束不创建遮罩、绘制后原子移除”门禁通过；本次 TypeScript 文件无新增错误，全量仍仅有任务前 3 个既有业务目录错误。CDP `1234` 仍被其他任务独立 Profile 占用，未接管，真实逐帧 E2E 待端口释放。

### 2026-09-13｜首次启动双进度条所有权收口

- 用户要求：刷新首次进入只显示一套进度条。
- 根因/修正：Web 模板的 `aoo-startup-cover` 与 Cocos `BootStrap` 的 `BootstrapStatus/ProgressTrack/ProgressPercent` 同时消费启动进度。现由 Startup 展示边界明确返回是否实际接管可见界面；Web 接管时运行期隐藏三项重复 Cocos 展示，无 DOM 的原生端继续使用 Cocos 进度界面。
- 修改范围：公共展示协调器、Startup 展示适配器及 `LoginScreenBootstrap` 最小接线；未修改登录 Scene、Prefab、节点层级或业务路由。
- 验证结果：单一首次启动展示专项测试 4/4 通过；本次 TypeScript 文件无新增错误，全量仍为任务前 3 个既有业务错误。真实刷新 Canvas E2E 因 CDP `1234` 被其他任务 Profile 占用尚未执行。

### 2026-09-13｜MainScene Bundle 导航错误修正

- 用户反馈：进入房间失败后的回退界面提示“场景 MainScene 未加入构建列表”。
- 根因/修正：`MainScene` 文件和 UUID 已正确列入桌面、移动构建配置，但资源实际归属 `lobby` Bundle；`SceneRouter` 仍从主包调用 `director.loadScene()`，并把同步拒绝一律误报为未加入构建列表。现统一由 SceneRouter 按场景所有权从 `lobby` Bundle 加载、校验并运行 `MainScene`，失效 SceneAsset 逐出后仅重载一次；Login 主包场景保留主包加载。
- 修改范围：`SceneRouter.ts` 与专项门禁；未修改大厅/登录 Scene、Prefab、Bundle 配置或房间玩法。
- 验证结果：Bundle 所有权加载专项测试 1/1 通过，Creator 3.8.8 Preview/Editor 已生成包含 `loadOwnedScene` 的最新 chunk；本次 TypeScript 无新增错误，全量仍为任务前 3 个既有业务错误。房间首次失败的另一独立原因仍是当前磁盘缺少 `pdk_UISharePanel`，须按已记录任务另行决定。

### 2026-09-13｜取消 common-prefab 后公共资源索引收口

- 用户/转交要求：Creator 已取消 `Common/Prefab.meta` 的独立 `common-prefab` Bundle，全部代码必须服从当前 `common` Bundle，不得重新启用或保留兼容分支。
- 根因/修正：公共注册表、表单加载 fallback、俱乐部桌子玩家状态、PDK 座位头像和魔法表情头像仍引用旧 Bundle。现集中以 `COMMON_ASSET_BUNDLE` 及 `Prefab/*` 资源路径注册 `CommonHead/PlayerState/Numpad/SettingsPanel/Message/MessageDrift/WaitNet`，删除 LegacyFormManager 两条旧 Bundle fallback，业务消费点只引用公共目录常量。
- 同步证据：当前 Preview `assets/common/config.json` 已确认包含上述全部 `Prefab/*` 项；源码精确检索已无 `common-prefab` Bundle 字符串（麻将的 `games-common-prefab` 是另一现行 Bundle，不属于本次范围）。相关定向断言 4/4 通过，TypeScript 本次无新增错误。
- 未完成项：Creator 已生成公共注册表新 chunk；PDK 分享控制器后续最小修正尚待下一次 Preview 刷新生成最新 chunk，真实入房 Canvas E2E 仍受 CDP `1234` 占用阻塞。

### 2026-09-13｜删除已废弃跑得快分享面板强依赖

- 用户反馈：进入房间时加载失败，首个错误为缺少 `game/CommonPdk/pdk_UISharePanel`。
- 根因/修正：当前磁盘已不存在该 Prefab，公共房间也已删除分享面板入口，但跑得快仍注册该表单；房间揭幕前的全表面预热会因此阻断每次入房。现删除表单键、注册和面板生命周期，不恢复旧资源；结算与战绩分享按钮直接调用既有 `shareLink()` 能力。
- 验证结果：源码已无 `pdk_UISharePanel` 或其 `formKey` 引用，TypeScript 本次相关文件无新增错误；真实 Creator Preview 入房待最新 PDK chunk 生成后复验。

### 2026-09-13｜公共导航进度背景接通

- 用户要求：修复导航进度条未显示指定背景图。
- 根因/结论：`TransitionBg` 已正确归属 `common` Bundle，但运行时把相对 `nativeUrl` 直接写入 CSS，且异步取图结束前先显示纯色遮罩；现按 Bundle 基址生成绝对 URL，并在背景准备完成后才首次显示遮罩。
- 修改范围：仅公共 `PresentationTransitionCoordinator.ts`、专项测试和本记录；未修改 Scene、Prefab、登录布局或业务模块。
- 验证结果：背景资源路径与 Preview 原生文件均可访问；专项测试新增项通过，相关测试 10/11，唯一失败为 Preview 模板被其他并行改动还原导致的既有断言；TypeScript 仅剩 3 个既有跨业务错误，本次文件无新增错误。
- 未完成项：Creator 3.8.8 当前尚未生成包含本次源码的新 Preview chunk，且 CDP `1234` 被其他任务占用，真实导航 Canvas 复验待编辑器同步后执行。

### 2026-09-13｜俱乐部无大厅中间帧与统一延迟导航

- 用户要求：俱乐部列表进入俱乐部不得先露出游戏大厅；所有界面先显示目标页，再后台加载其他界面资源；普通加载超过 0.5 秒才显示导航界面；首次刷新只显示启动进度。
- 根因/结论：俱乐部入口先关闭列表再异步挂载目标页，造成约一秒大厅中间帧；导航事务没有避让仍可见的 Startup；场景与 Form 的显示延迟分别为 0ms/120ms。现改为目标俱乐部页挂载成功后再关闭列表，Startup 可见期间抑制导航层，并以公共 `NAVIGATION_REVEAL_DELAY_MS=500` 统一可见导航阈值；后台预加载始终静默、单线程执行。
- 修改范围：公共展示协调器、Startup 展示边界、场景导航、Form 管理器、俱乐部入口最小接线及专项测试；未修改 Scene、Prefab 层级、登录布局、俱乐部数据或玩法业务。
- 验证结果：专项测试 8/8；TypeScript 本次改动无新增错误，并顺带消除了俱乐部入口回调的既有 `Promise<boolean>` 类型错误，当前仅剩跑得快目录 2 个既有错误。
- 未完成项：Creator 3.8.8 当前尚未生成包含最终源码的新 Preview chunk，CDP `1234` 被其他任务占用，真实刷新和俱乐部点击复验待编辑器同步后执行。

### 2026-09-13｜刷新导航、闪屏与黑屏根因更正

- 用户要求：修复刷新后依次出现导航界面、闪屏和黑屏。
- 根因/结论：公共协调器错误地允许任意 Form/Prefab 事务在提交时释放首次启动遮罩，导致后台事务先结束时 Startup 被提前撤下；现只有显式标记 `releaseInitialPresentation` 的启动路由事务能够交接首次展示权，Startup 所有权也不再依赖瞬时 DOM hidden 状态。
- 修改范围：公共展示协调器、Startup 展示边界、SceneRouter 标记及专项测试；未修改 Scene、Prefab、登录布局或玩法业务。
- 验证结果：Startup 唯一所有权、0.5 秒导航阈值、背景接通和俱乐部原子切换专项测试 8/8；TypeScript 本次无新增错误，仅剩跑得快目录 2 个既有错误。
- 未完成项：Creator 3.8.8 尚未生成包含本次最终修正的新 Preview chunk，CDP `1234` 仍被其他任务占用，真实刷新逐帧复验待同步后执行。

### 2026-09-13｜Creator Preview 启动遮罩恢复

- 用户复验：刷新后导航界面、闪屏和黑屏仍存在。
- 根因/结论：Creator `preview-template/index.ejs` 当前已被还原为原生模板，不含持久 Startup HTML 遮罩；BootStrap Scene 切换会销毁场景内进度节点。现补回 Preview 唯一启动遮罩，并增加与 DOM 是否存在无关的启动阶段所有权，直到唯一启动路由提交目标首帧。
- 修改范围：Preview 模板、公共展示协调器及专项测试；未修改 Scene、Prefab 或业务 UI。
- 验证结果：Preview HTTP 实际响应已包含唯一 `aoo-startup-cover`，Startup 图片 URL 返回 200，Creator Preview chunk 已包含 `initialPresentationActive`；专项测试 8/8。
- 未完成项：CDP `1234` 仍由其他任务占用，未接管其浏览器；待用户当前 Preview 刷新复验最终视觉。

### 2026-09-13｜导航进度条底部定位

- 用户要求：公共导航进度条距离屏幕底部 100px。
- 根因/结论：原导航卡片使用全屏 Grid 居中；现将导航卡片固定为 `bottom:100px`，错误文字和重试按钮改为绝对定位，保证正常及失败状态都不改变进度条基线。
- 修改范围：仅公共导航运行时样式和专项测试；启动进度、Scene、Prefab 与业务界面未改。
- 验证结果：专项测试 8/8。
- 未完成项：Creator 3.8.8 尚未生成含最终样式的新 Preview chunk，待同步后视觉复验。

### 2026-09-13｜启动进度条底部定位

- 用户要求：启动进度条同样距离屏幕底部 100px。
- 根因/结论：三个启动模板仍使用居中卡片；现统一固定 `bottom:100px`，错误和重试区域采用绝对定位，不改变进度条基线。
- 修改范围：Creator Preview、Web Desktop、Web Mobile 启动模板样式及专项测试；未修改登录 Scene/Prefab。
- 验证结果：专项测试 8/8，Preview HTTP 响应已包含最终样式。
- 未完成项：无。

### 2026-09-13｜启动与导航进度底部位置更正

- 用户要求：将启动和导航进度条由距底部 100px 统一改为 30px。
- 根因/结论：最新位置要求替代此前数值；四个公共展示入口现统一使用 `bottom:30px`。
- 修改范围：公共导航运行时 CSS、Preview/Web Desktop/Web Mobile 启动模板及专项测试。
- 验证结果：专项测试 8/8，Preview HTTP 实际模板已返回 `bottom:30px`。
- 未完成项：无。

### 2026-09-13｜导航与启动进度条宽度和底板

- 用户要求：导航和启动进度条长度统一为 1200，并增加底板。
- 根因/结论：原最大宽度为 420px，轨道颜色过浅；现统一使用 `min(1200px, calc(100vw - 48px))`，并增加实体灰色底板和内描边，窄屏保留 24px 两侧安全边距。
- 修改范围：公共导航运行时样式、Preview/Web Desktop/Web Mobile 启动模板及专项测试。
- 验证结果：Preview HTTP 已返回最新启动样式，专项测试 8/8。
- 未完成项：无。

### 2026-09-13｜进度条黑色半透明底板

- 用户要求：导航和启动进度条底板改为黑色半透明。
- 根因/结论：实体灰色底板不符合最终视觉；现统一为 `rgba(0,0,0,.38)` 并保留半透明内描边。
- 修改范围：公共导航运行时样式及 Preview/Web Desktop/Web Mobile 启动模板样式。
- 验证结果：Preview HTTP 已返回最新样式，专项测试 8/8。
- 未完成项：无。

### 2026-09-13｜俱乐部入口原子揭幕

- 用户要求：修复俱乐部列表进入俱乐部时闪屏。
- 根因/结论：俱乐部主界面以空桌首帧显示后才异步恢复权威模板，导致背景、按钮和桌子跳变；现让列表保持更高层级，目标界面在下方完成权威模板恢复后才关闭列表并一次性揭幕。
- 修改范围：俱乐部入口最小接线及专项测试；未修改 Prefab、模板协议和业务数据。
- 验证结果：Creator Preview 最新入口 chunk 已确认顺序为“挂载目标→恢复模板→关闭列表”；相关测试 12/13，唯一失败为既有测试仍断言旧版 `renderDeskPlayers(node, room)` 两参数签名，当前实现已使用三参数且与本次改动无关；本次无新增类型错误。
- 未完成项：待当前 Preview 真实点击视觉复验。

### 2026-09-13｜导航背景资源类型修复

- 用户要求：修复公共导航遮罩加载 `TransitionBg/spriteFrame` 时的 Bundle 缺失警告。
- 根因/结论：Creator 资源数据库将 `TransitionBg.png` 发布为 `ImageAsset` 和 `Texture2D`，并没有 `SpriteFrame`；现改为从 `common` Bundle 加载 `Texture/TransitionBg/texture`。
- 修改范围：公共导航资源 Catalog、展示协调器及专项断言；未修改图片、Meta、Bundle 或 Prefab。
- 验证结果：Preview `common/config.json` 确认存在 `/texture` 且不存在 `/spriteFrame`；Creator 3.8.8 最新 Preview chunk 已同步为 `Texture2D` 加载；相关新断言通过，全组专项测试 11/13，两项失败为已有启动模板/旧阈值断言；TypeScript 仅保留两个既有 PDK 错误。
- 未完成项：当前 CDP `1234` 由其他任务占用，未进行本次独立 Canvas E2E。

### 2026-09-13｜房间交接遮罩残留修复

- 用户要求：修复进入房间后卡在公共导航界面。
- 根因/结论：并发展示事务中，可见事务完成后若仍有静默房间交接事务，旧实现只重绘事务不隐藏已完成的 DOM 遮罩，导致房间首帧已就绪但仍被导航层遮挡。现在剩余事务全部不可见时立即原子隐藏 DOM 遮罩，同时保留静默交接的旧帧快照。
- 修改范围：仅公共 `PresentationTransitionCoordinator` 和专项门禁；未修改房间业务、Prefab、Scene 或服务端。
- 验证结果：专项测试 9/9；Creator 3.8.8 最新 Preview chunk 已包含遮罩释放分支；TypeScript 仅保留两个既有 PDK 错误。
- 未完成项：CDP `1234` 仍由其他任务占用，待当前 Preview 真实进房复验。

### 2026-09-13｜启动恢复房间交接修复

- 用户要求：修复刷新后卡在启动页。
- 根因/结论：启动期直接恢复活动房间的分支没有持有并提交带 `releaseInitialPresentation` 权限的启动路由事务，房间虽已绘制，HTML 启动层仍无法释放。现由认证恢复路由统一建立启动事务，房间首帧确认后显式提交。
- 修改范围：仅公共 `SceneRouter` 启动所有权交接及专项门禁；未修改房间玩法、Scene 或 Prefab 结构。
- 验证结果：启动/房间交接专项测试 15/15；Creator 3.8.8 Preview chunk 已同步包含活动房间分支的启动事务提交；TypeScript 仅保留两个既有 PDK 错误。
- 未完成项：CDP `1234` 由其他任务占用，待当前 Preview 真实刷新复验。

### 2026-09-13｜入房首帧关键路径缩短

- 用户要求：大厅和俱乐部点击房间后立即进入，消除 3–4 秒停顿。
- 根因/结论：两个入口的公共房间接管器在建立 WebSocket 和显示房间前，会启动并串行等待结算 Bundle 与聊天、语音、表情、设置等多个非首帧 Prefab；另外入房仍重复执行已完成的场景预加载。现改为“连接→房间基础层→首帧提交”优先，其余资源在首帧后逐个让帧预热，并复用大厅/俱乐部已完成的场景预热。
- 修改范围：公共 PDK 房间场景启动器、公共房间接管器和专项门禁；未修改大厅/俱乐部 Prefab、玩法或服务端。
- 验证结果：入房交接专项测试 6/6；TypeScript 本次无新增错误，仅保留两个既有 PDK 错误。
- 未完成项：Creator 已触发运行预览，当前 PDK Bundle 的按需 chunk 需在下次真实进房时由 Preview 重编译；CDP `1234` 由其他任务占用，未执行本任务独立真实计时 E2E。

### 2026-09-13｜启动与导航提示文字样式

- 用户要求：启动界面和导航界面的提示文字改为白色字加黑色描边。
- 根因/结论：用户明确纠正目标为两套公共加载展示，不是俱乐部大厅；已完整撤销误加到 ClubMain 的样式。
- 修改范围：Preview/Web Desktop/Web Mobile 启动 DOM、公共导航 DOM，以及无 DOM 时的 Creator `BootstrapStatus` Label 后备显示。
- 验证结果：启动/导航样式专项测试 9/9；Creator 3.8.8 Preview 最新 chunk 已同步 `text-shadow` 黑色描边与 Cocos `LabelOutline`。
- 未完成项：无。

### 2026-09-13｜启动与导航提示描边宽度

- 用户要求：启动界面和导航界面的提示文字黑色描边统一改为 `0.5`。
- 修改范围：Preview/Web Desktop/Web Mobile 启动 DOM、公共导航 DOM，以及无 DOM 时 Creator `BootstrapStatus` 的 `LabelOutline`；未修改业务界面文字。
- 验证结果：DOM `text-shadow` 与 Creator `LabelOutline.width` 均已统一为 `0.5`；专项测试 9/9，Creator 3.8.8 Preview 最新 chunk 已同步。
- 未完成项：无。

### 2026-09-13｜未正式对局不生成战绩

- 用户要求：游戏没有正式打过时不记录战绩，也不在战绩列表显示。
- 根因/结论：公共 Gateway 将等待阶段的加入、准备等写操作提前登记为回放参与数据，公共战绩查询又允许无结算数据的参与记录进入列表，形成“版本 -、无成员”的空战绩；现统一以“房间已正式开局”作为回放写入门槛，并以“至少存在一局正式结算”作为战绩列表与总数门槛。
- 修改范围：公共 Gateway 回放持久化门槛、公共战绩查询服务及专项测试；未修改玩法、俱乐部或战绩 UI。
- 验证结果：GameCommon 查询专项测试 2/2；Gateway 写入门槛四分支探针通过；受影响服务端 41 模块跳过测试编译成功，Hall/Gateway 已重启且健康；本地现有 179 个无结算空房间已被查询门槛排除，57 个有结算房间继续可见。
- 未完成项：全量测试被既有规则文档目录 `.DS_Store` 门禁和 Poker 测试缺失 `AypdkGameProvider`、`HbpdkSession` 阻塞，本次未修改这些无关范围。

### 2026-09-13｜公共 MsgDrift 命名与连续提示队列

- 用户要求：将公共 `MessageDrift.prefab` 按规范重新命名；每次触发必须提示一次，上一条完全消失后才播放下一条；初始停顿为 0 秒，上滑到顶部后停留 0.5 秒。
- 根因/结论：Prefab 已调整为根节点直接包含 `sprite`、`LabelMessage`，公共生命周期仍查找已不存在的 `bg1/LabelMessage`；首次加载期间多个 `show()` 又会覆盖前一组参数。现保留稳定协议键 `UIMessage_Drift`，物理资源和生命周期统一命名为 `MsgDrift`，并让首次加载并发触发逐条进入唯一公共队列。
- 修改范围：公共 `MsgDrift` Prefab（原 UUID 保持不变）、生命周期、公共 Prefab 注册与原生路径映射、登录/大厅公共注册入口、公共组件规范及专项测试；未修改任何业务页面的提示实现或登录场景布局。
- 验证结果：Creator 资源数据库已自动导入，Preview 已生成 `MsgDriftLifecycle`、登录、大厅和公共注册表最新 chunk；原生资源门禁通过（560 个 Scene/Prefab、20347 个资源/meta、28665 个 UUID，阻塞 0）；专项测试 11/11；真实 Creator Preview 连续点击两次空房间号“确定”，两条提示依次完成“立即上滑—顶部停留—淡出”，第二条只在第一条消失后开始，运行期错误为空。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/msg-drift-queue-2026-09-13/`。
- 未完成项：Creator 当前打开的 `ClubMain.prefab*` 存在用户未保存内容，为避免覆盖用户现场未强制切换编辑器标签；资源导入、Preview 编译和真实 Canvas 运行均已验证。

### 2026-09-13｜公共 MsgDrift 改为点击即时并发提示

- 用户更正：连续触发时不能等待上一条消失；每点击一次必须立刻提示一次。
- 根因/结论：上一版按“上一条消失后再提示”实现为串行队列，不符合用户最终确认。现将公共生命周期改为隐藏模板加独立运行实例：每个触发立即实例化、立即上滑，各自到顶停留 0.5 秒后淡出销毁，互不等待、互不重启动画。
- 修改范围：仅公共 `MsgDriftLifecycle.ts` 与对应专项断言；未修改业务页面、Prefab 层级和登录场景布局。
- 验证结果：Creator Preview 已生成并实际运行多实例版本；空房间号“确定”按钮以 80ms 间隔连续触发两次均立即响应，以 500ms 间隔触发可同时看到两条处于不同上滑位置；运行期错误为空，专项测试 2/2，原生资源门禁阻塞 0。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/msg-drift-parallel-2026-09-13/`。
- 未完成项：无。

### 2026-09-13｜启动/导航单排提示与房间首帧事务解锁

- 用户要求：启动与导航界面的提示文字只能保留一排，并同时解决进入房间停留在上一界面后超时的问题。
- 根因/结论：启动、导航各自同时维护状态行和错误行；房间进入期间建房与游戏两个静默展示事务并发提交时共用同一个进度动画帧，互相取消完成回调，导致保留的上一帧无法释放并在 30 秒后误报“正在提交房间首帧超时”。
- 修改范围：启动三套 Web 模板、启动展示视图、公共展示事务协调器、公共 Form 异步事务取消、跑得快公共房间首帧等待与启动器、专项测试；未修改登录场景布局、房间 Prefab 层级或玩法规则。
- 验证结果：Creator 3.8.8 已重新加载并生成最终 Preview chunk；专项测试 10/10；原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）；独立无头 Chrome 真实点击“游客登录→创建房间→创建”，3.628 秒取证时已进入 `GameRoom2D`，`CommonRoom` 与 `PDK_CommonRoom` 均存在，启动/导航错误行数量 0、状态行数量 1、导航遮罩和保留帧均已隐藏，建房与游戏事务均 `commit` 且失败数 0。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/loading-single-line-room-entry-2026-09-13/`。
- 未完成项：无；全项目 TypeScript 仍有两处既有跑得快类型错误，本次修改文件未新增类型错误。

### 2026-09-13｜建房界面先关闭与房间即时首帧

- 用户要求：创建或进入房间不能再等待约 3 秒，点击后应立即进入；创建房间进入前必须关闭创建界面。
- 根因/结论：建房事务保留了包含创建页的大厅截图，创建页又在完整房间交接结束后才关闭；房间首帧还额外等待头像、手牌图片和动画任务最多 1.5 秒。现由房间启动器独占画面交接，服务端建房成功后先关闭创建页，再启动房间切换；公共房间 Form 挂载即作为首帧边界，头像、牌面与动画在后台继续收敛。
- 修改范围：公共建房提交顺序、跑得快公共房间首帧等待边界及专项测试；未修改玩法规则、服务端、Prefab 层级或登录场景。
- 验证结果：Creator 3.8.8 已自动导入并生成两个最终 Preview chunk；专项测试 17/17，原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）；独立无头 Chrome 真实点击“游客登录→创建房间→创建”，创建页在 307ms 关闭，`CommonRoom` 与 `PDK_CommonRoom` 在 458ms 完成挂载，保留帧在 592ms 原子释放，控制台错误 0，最终画面仅显示房间。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/room-entry-immediate-2026-09-13/create-room-committed.png`。
- 未完成项：无；尝试重新加入已退出且随即解散的单人房间时服务端按预期返回 409，该业务结果不属于本次框架问题。

### 2026-09-13｜建房点击当帧响应与嵌套首帧合并

- 用户复验：创建房间仍未达到立即进入，需要进一步优化。
- 根因/结论：创建页虽已提前到建房成功后关闭，但接口往返期间仍保持可见；公共房间两层 Form 又分别执行完整的引擎绘制和浏览器绘制屏障，与外层房间场景事务形成三次串行首帧等待。现创建页在点击当帧隐藏且失败自动恢复；`CommonRoom`、`PDK_CommonRoom` 明确由外层房间事务统一提交，只挂载不再各自等待重复首帧。
- 修改范围：公共建房即时反馈、公共 Form 外层展示所有权选项、跑得快两层公共房间接线及专项测试；未修改服务端、玩法规则、Prefab 层级或其他普通 Form 的展示屏障。
- 验证结果：Creator 3.8.8 已同步生成最新 Preview chunk；专项测试 17/17、原生资源门禁通过；独立无头 Chrome 真实创建流程中，创建页在点击后 14ms 隐藏，房间两层在 391ms 挂载，最终画面在 488ms 完整交接，控制台错误 0。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/room-entry-immediate-2026-09-13/create-room-immediate-v2.png`。
- 未完成项：无。

### 2026-09-13｜创建界面直接交接房间

- 用户复验：创建界面关闭后暴露大厅约一秒，要求创建后直接进入房间。
- 根因/结论：上一轮为即时关闭创建页主动激活了大厅根节点，房间事务随后抓取大厅作为交接帧。现先保留创建页已绘制的最后一帧，再关闭真实创建节点；大厅不主动激活，房间首帧完成后直接替换保留帧，因此视觉路径只有“创建中→房间”，不再经过大厅。
- 修改范围：公共建房保留帧与大厅可见性顺序、专项断言；未修改服务端、玩法规则、Prefab 层级和普通大厅导航。
- 验证结果：Creator 3.8.8 Preview 已同步；专项测试 17/17、原生资源门禁通过；独立无头 Chrome 逐帧取证中，203ms 仍显示创建中末帧，402ms 已显示房间，652ms 完成交接，整个序列大厅根节点未作为可见画面出现，控制台错误 0。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/create-room-direct-handoff-2026-09-13/`。
- 未完成项：无。

### 2026-09-13｜大厅创建进入房间保留帧无空档

- 用户复验：大厅创建进入房间的间隙会闪屏。
- 根因/结论：公共保留帧把场景相机切到离屏纹理后，主 Canvas 当帧不再绘制，而 HTML 保留 Canvas 要等帧末 GPU 读回完成后才建立，二者之间存在一个空帧。现相机切换前同步复制浏览器当前已显示的 Canvas 并立即置顶，帧末再用 GPU 读回结果更新同一保留帧，房间首帧完成后原子释放。
- 修改范围：公共 `PresentationTransitionCoordinator` 保留帧抓取及专项测试；未修改大厅/房间 Prefab、玩法和服务端。
- 验证结果：Creator 3.8.8 Preview 已同步；专项测试 17/17、原生资源门禁通过；独立无头 Chrome 创建房间后约每 10ms 采样，保留帧全程最低非空有效像素比例为 `0.7489114659`，未出现透明帧或黑帧，房间首帧完成后正常释放。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/room-transition-no-flash-2026-09-13/`。
- 未完成项：用户当前资源中另有缺失字体和 SpriteFrame 404，与本次公共交接无关，依照边界未处理。

### 2026-09-13｜大厅创建房间闪屏回归修正

- 用户复验：大厅创建进入房间仍会闪屏，要求对照昨晚已正确的实现修复。
- 根因/结论：上一条记录中新增的浏览器 `drawImage(source)` 与已验证的 WebGL `preserveDrawingBuffer=false` 边界冲突，可读到无效/黑帧；现已撤回该回归路径，恢复为 `Director.EVENT_AFTER_DRAW + RenderTexture + readPixels + 有效像素校验` 的单一保留帧实现，并保留“创建页末帧→完整房间首帧”的直接交接顺序。本条为对上一条开发记录中技术结论的更正，历史记录按规范保留。
- 修改范围：公共 `PresentationTransitionCoordinator` 保留帧捕获和专项回归断言；未修改大厅/房间 Prefab、玩法规则或服务端。
- 验证结果：Creator 3.8.8 已重新导入并生成最终 Preview chunk；专项测试 17/17，原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）。独立无头 Chrome 真实点击“游客登录→创建房间→创建”后，72–470ms 始终显示创建页有效保留帧，580ms 原子切换为完整 `GameRoom2D`，全程无大厅帧、黑帧、白帧和控制台错误。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/room-create-flash-regression-2026-09-13/`。
- 未完成项：无。

### 2026-09-13｜清缓存账号登录恢复房间无登录回闪

- 用户复验：清理缓存后使用账号密码登录并恢复活动房间时会闪出登录界面，同时偶发 Creator 全屏错误 `ProductionApiError: hall request failed`。
- 根因/结论：认证成功后的公共导航仍沿用 0.5 秒延迟显示；房间内层静默事务又会保留来源 `LoginScene` 快照，外层导航提交时该快照会短暂重新露出；运行期房间退出回调还直接丢弃 `leave()` Promise，使 Hall 竞争失败升级为未捕获异常。现账号登录离开 `LoginScene` 时导航立即接管画面，外层可见导航存在时禁止内层重复抓取来源帧，并在目标首帧提交时立即释放旧快照；退出统一走带错误收敛的 `requestLeave()`。
- 修改范围：公共认证路由、公共展示事务协调器、跑得快公共运行期退出接线与专项测试；未修改登录 Scene/Prefab 布局、玩法规则、服务端或当前编辑中的 Prefab。
- 验证结果：Creator 3.8.8 已重新索引并热编译脚本；专项测试 18/18，原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）。专属无头 Chrome 清空 Cookie、LocalStorage、SessionStorage 与 IndexedDB 后，通过真实 Canvas 输入账号 `11`、密码 `1`，恢复活动房间 `917425`；逐帧画面为“导航遮罩→完整 `GameRoom2D`”，导航释放后未再出现登录画面，继续等待 8 秒未出现 `pageerror`、`hall request failed` 或未捕获 Promise。证据见 `Client/docs/前端框架规范/历史记录与证据/证据/account-room-recovery-flash-2026-09-13/final-release-retained/`。
- 未完成项：无。

### 2026-09-13｜启动页不再等待后台预加载

- 用户复验：应用卡在启动界面。
- 根因/结论：认证路由已完成目标界面首帧提交后，只要还有静默后台预加载事务，公共协调器就会从引用计数分支提前返回，连带把启动页释放延迟到全部后台任务结束；某个预加载慢或挂起时表现为启动页一直不消失。现将 Startup 所有权释放移动到后台事务判断之前，目标界面提交即立即退出启动页，剩余预加载继续静默运行。
- 修改范围：仅公共 `PresentationTransitionCoordinator` 和专项断言；未修改登录 Scene/Prefab、业务页面、玩法或服务端。
- 验证结果：Creator 3.8.8 显示脚本编译完成；专项测试 19/19，原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）；专属无头 Chrome 冷启动进入 `LoginScene`，启动与导航遮罩均成功隐藏、控制台无启动错误。新增门禁明确保证 Startup 释放发生在“仍有后台记录”的提前返回之前。
- 未完成项：无。

### 2026-09-13｜扑克公共节点发牌动画归档

- 用户要求：从 Creator 2.2.2 版本提炼扑克发牌动作，明确为 Cocos 节点动画而非 Spine，并以精简名称归档，暂不接入玩法。
- 处理结果：误迁的 Spine 资源已撤回；旧版唯一 `Deal()` Tween 逻辑整理为 `PokerDealNodeAnim`，一套参数化实现支持 16、8、3 及任意手牌数量，保留批次间隔、移动时长、起始缩放和批次音效回调。
- 修改范围：仅新增 `assets/Games/Poker/Common/Spine/PokerDealNodeAnim.ts` 及 Creator 自动生成的同名 `.meta`；未修改旧项目、玩法代码、Scene 或 Prefab。
- 验证结果：Creator 3.8.8 已正常导入 UUID `1ea5f49e-1721-4e70-9e87-89536b079897` 并生成 Editor/Preview 编译产物，未出现该脚本编译错误；尚未接入玩法，等待用户后续指令。

### 2026-09-13｜房间进入关键路径深度并行化

- 用户要求：从框架层继续深度优化大厅及俱乐部点击桌子后的房间进入速度。
- 根因/结论：进入链路仍存在三类可消除的串行等待：场景切换前额外等待来源帧、后台预热整体等待及 `preloadScene→loadScene` 重复阶段；此外公共房间 Prefab 解码原来排在 Game WebSocket 连接之后，三个 Bundle 清单又互相等待后才启动房间 Scene。现统一移除前三类等待，让两个必需房间 Prefab 与 Game WebSocket 并行，并让房间 Scene 在其所属 Bundle 就绪后立即加载，与其余 Bundle 清单并行。
- 修改范围：公共跑得快场景启动器、公共跑得快房间协调器及专项门禁；未修改服务端、玩法规则、俱乐部业务、Scene/Prefab 层级或权威房间状态。
- 验证结果：Creator 3.8.8 已刷新预览，Editor/Preview 编译产物均包含最终并行化实现；专项测试 7/7、原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）。
- 未完成项：当前没有可加入的俱乐部真实房间，双账号真实 Canvas 点击计时尚未闭环；剩余不可并行的硬依赖为 Hall 入房/票据、Game WebSocket 建连和权威房间快照，不能用假数据或提前展示旧头像绕过。

### 2026-09-13｜俱乐部进入后的房间预加载优先级

- 用户要求：确认所有界面统一预加载，并在进入俱乐部后优先加载房间。
- 处理结果：统一预加载仍只覆盖已注册、当前流程可到达的界面，禁止扫描废弃资源；公共后台队列固定为“游戏房间 Prefab → 公共房间 Prefab → 当前已注册页面 → 可选模块页面”。跑得快房间 Scene/Bundle 继续使用独立预热链，并在俱乐部桌子渲染时按真实房间发布信息补充预热。
- 修改范围：公共 `LegacyFormManager` 预加载优先级与专项门禁；未修改俱乐部业务、Prefab 层级、服务端或玩法规则。
- 验证结果：Creator 3.8.8 Editor/Preview 已热编译最终队列顺序；专项测试与资源门禁通过。

### 2026-09-14｜取消 Lobby/Prefab 独立 Bundle 后同步索引

- 用户要求：`assets/Lobby/Prefab` 已取消独立 Bundle，框架同步修改索引。
- 处理结果：保留仍由大厅使用的 `LobbyMain.prefab` 与 `ClubList.prefab` 及原 UUID；`UILobbyMain`、`UILobbyClubList` 和公共兜底加载均改为父级 `lobby` Bundle 的 `Prefab/LobbyMain`、`Prefab/ClubList` 路径，活跃代码不再引用 `lobby-prefab`。
- 修改范围：公共模块 Prefab 注册表、公共 Form 加载兜底及相关专项断言；未删除 Prefab、未修改其层级或大厅业务。
- 验证结果：Creator 3.8.8 Editor/Preview 已热编译最终索引；大厅俱乐部列表专项测试通过，原生资源门禁通过（560 个 Scene/Prefab、阻塞 0）。

### 2026-09-14｜跑得快小结算 Prefab 路径迁移同步

- 用户要求：跑得快小结算已移动到 `assets/Games/Poker/PDK/Common/Prefab/SmallSettlement.prefab`，同步框架索引。
- 处理结果：跑得快小结算逻辑路由、模板解析、预加载、原生路径映射及 29 个当前地区注册项统一改为 `paodekuai-common/Prefab/SmallSettlement`，UUID 使用现存资源 `af823342-845f-4b26-9343-c2c95712a829`；控制器同步现存 Prefab 的 `RemCards` 节点路径。旧扑克公共小结算仍供其他扑克玩法使用，未删除。
- 修改范围：公共结算注册、解析与预加载配置、跑得快小结算控制器节点索引、专项测试及本记录；未修改 Prefab 层级、布局、资源内容或其他扑克玩法。
- 验证结果：Creator 3.8.8 Editor 热编译产物已包含新路径；结算与房间切换专项测试 15/15，通过原生资源门禁（563 个 Scene/Prefab、阻塞 0）。
- 未完成项：无。

### 2026-09-14｜整场结束后退出仍保留玩家座位定位

- 用户反馈：大厅创建的跑得快房间完成整场后，一名玩家退出，其他客户端仍显示其头像和牌。
- 根因/结论：服务端 `PokerAuthoritativeSession.leave()` 只按 `state != null` 判断，整场已结束且 `roundNo >= roundLimit` 时仍把退出玩家替换成负数托管占位并保留手牌、出牌和统计，而不是删除座位；客户端权威适配与 `SeatPresenter` 对 `playerId/pid <= 0` 已有清头像逻辑，Hall 离开入口也会广播新权威状态，因此根因属于跑得快服务端玩法成员生命周期。
- 修改范围：仅只读检查公共退出、权威广播、客户端投影和座位清理链路并更新本记录；未修改跑得快服务端、玩法客户端、Prefab、Scene 或规则。
- 验证结果：静态调用链确认 `JdbcGatewayRoomAuthority.leave()` 提交 `common.room.leave_req` 后保存快照并广播；`PokerAuthoritativeSession.leave()` 在非空 `state` 分支写入负数玩家；客户端适配把非正玩家投影为 `pid=0`，座位 Presenter 会清头像。
- 未完成项：需跑得快岗位在 `Server/server/Poker/src/main/java/com/aoo/bcg/poker/PokerAuthoritativeSession.java` 将“整场结束”退出改为彻底移除座位及其手牌、出牌、托管和统计状态，并补多客户端真实 E2E；当前框架 UI 岗位无权修改玩法状态机。

### 2026-09-14｜跑得快大结算 Prefab 接入

- 用户要求：跑得快大结算引用 `assets/Games/Poker/Common/Prefab/BigSettlement_0.prefab`。
- 处理结果：跑得快大结算逻辑路由、默认模板解析、原生路径映射及 29 个当前地区注册项统一指向 `poker-common/Prefab/BigSettlement_0`，保留现存 UUID `997768e2-c9b5-4e2b-addb-6f22cd54c0fc`；其他扑克玩法配置未修改。
- 修改范围：公共结算注册与解析、原生路径映射、跑得快结算注册数据、专项测试及本记录；未修改 Prefab 层级、节点、布局、组件或资源。
- 验证结果：Creator 3.8.8 Editor 热编译产物已包含最终路径；结算与房间切换专项测试 15/15，通过原生资源门禁（562 个 Scene/Prefab、阻塞 0）。
- 未完成项：无。

### 2026-09-14｜房间重连黑屏修复

- 用户反馈：活动房间断线重连进入时会闪黑屏。
- 根因/结论：房间首帧门禁被简化成 Form 挂载后立即完成，启动/导航遮罩会在权威头像、手牌和桌面牌的异步呈现结束前释放，因而短暂暴露空场景底帧；现只等待当前首屏必需呈现任务全部结束，音效和后续 Prefab 预热仍在后台执行。
- 修改范围：仅跑得快公共房间首帧呈现门禁、专项测试及本记录；未修改玩法规则、权威房间状态、Prefab、Scene 或资源。
- 验证结果：Creator 3.8.8 Editor/Preview 已热编译最终逻辑；相关专项测试 16/16、原生资源门禁通过（563 个 Scene/Prefab、阻塞 0）。专属无头 Chrome/CDP `1234` 使用账号 `11` 真实登录并恢复活动房间 `402536`，逐帧状态为 `LoginScene+遮罩 → GameRoom2D+遮罩 → 完整房间+遮罩释放`，未出现无遮罩黑帧；头像和牌面已在最终画面显示。
- 未完成项：无；本任务临时浏览器进程已停止，专属 Profile 已移入废纸篓，CDP `1234` 已释放。

### 2026-09-14｜房间重连残留一秒黑屏修复

- 用户反馈：上一轮首帧门禁修复后，房间重连仍残留约一秒黑屏。
- 根因/结论：公共导航遮罩把“显示遮罩”错误地串行依赖于背景纹理解码；冷启动或重连时场景已清屏、背景图却仍未完成，空档会暴露黑色 Canvas。现改为遮罩先用固定底色同步取得画面所有权，背景纹理只异步补齐；Creator Preview 模板也在 Canvas 前内置同一启动遮罩和非黑底色，覆盖引擎脚本加载前的第一帧。
- 修改范围：公共展示过渡协调器、Creator Preview 模板及专项门禁；未修改房间数据、玩法规则、登录 Scene 布局或 Prefab。
- 验证结果：Creator 3.8.8 Editor/Preview 已热编译最终逻辑，Preview HTTP 已返回新模板；专项测试 10/10、原生资源门禁通过（563 个 Scene/Prefab、阻塞 0）。专属无头 Chrome/CDP `1234` 从导航提交起每 50ms 采样 1 秒，全部为 `rgb(242, 241, 237)` 且启动遮罩持续可见，没有黑帧。
- 未完成项：无；专属无头浏览器已停止，临时 Profile 已移入废纸篓，CDP `1234` 已释放。

### 2026-09-14｜刷新首帧恢复完整启动界面

- 用户纠正：刷新后的第一个界面不能只是灰白底，必须直接显示完整启动界面。
- 处理结果：Creator Preview 首屏模板直接引用现有 `StartupBg` 原生资源并以最高优先级预取；启动背景、提示文字和进度条均在 Canvas 之前进入 HTML，Cocos 尚未初始化时也不会出现灰白占位。
- 验证结果：Preview HTTP 返回的首屏模板包含有效启动图 URL（状态 200）；专属无头 Chrome 在导航提交后 50ms 截图已显示完整启动背景、`正在加载中......` 和进度条，图片本机加载耗时 24.5ms。
- 未完成项：无；本任务没有修改登录 Scene/Prefab 布局。

### 2026-09-14｜启动与导航进度条连续性修复

- 用户反馈：启动界面刚出现时进度条停约一秒，尾段未跑完又停顿一秒多后直接进入房间。
- 根因/结论：引擎接管前的 HTML 进度条没有自主运动，公共运行时进度又固定停在 94% 等待目标场景，且启动遮罩释放前没有绘制 100%。现统一为“HTML 首帧立即运动 → 运行时继承当前进度 → 稀疏加载阶段持续逼近 98.5% → 目标首帧就绪后显示 100% → 下一绘制帧揭开目标界面”。
- 修改范围：启动展示适配器、公共导航展示协调器、Web 桌面/手机构建模板、Creator Preview 模板及专项门禁；未修改玩法、房间权威数据、Scene 或 Prefab。
- 验证结果：Creator 3.8.8 已刷新 Preview，Editor/Preview 编译产物均包含最终逻辑；相关专项断言 3/3、原生资源门禁通过。专属无头 Chrome/CDP `1234` 每 50ms 采样启动进度，从 1.67% 连续增长至遮罩释放前 59.07%，回退 0 次、停帧 0 次。
- 未完成项：无；全文件测试中另有一条地区桌子启动次数断言受并行工作区改动从 2 变成 3，与本次进度条改动无关，按职责边界未修改。

### 2026-09-14｜公共解散房间终态与无黑屏交接修复

- 用户反馈：解散发起者仍显示同意/拒绝；全员同意后部分客户端已退出，但发起者仍留在房间；退出交接出现黑屏或卡住。
- 根因/结论：发起者只被设置为不可点击而未隐藏操作按钮；部分 Gateway 在销毁房间权威后不会向发起者补发 `CommonPdk_DissolveRoom`；离房清理又可能早于当前房间画面的保留帧捕获。
- 修改范围：公共解散投票投影、公共 PDK 离房协调器、场景返回启动器和公共展示事务；未修改服务端、玩法规则、Scene 或 Prefab 层级。
- 修复结果：发起者及已投票玩家不显示同意/拒绝；全员同意的权威投票快照与服务端解散终态统一进入幂等离房路径；离房前等待当前房间帧真实保留，目标界面首帧完成前不释放；删除旧的一秒绘制等待。
- 结果提示：解散终态通过返回目标的 `startupMessage` 统一携带“房间已解散”，在大厅或俱乐部目标界面完成挂载后展示，避免房间弹层清理吞掉提示或重复提示。
- 二次更正：权威协议把发起解散本身视为发起者的隐式同意，`posAgreeList` 不保证为发起座位写入 `1`；客户端终态判定现统一为“发起座位隐式同意，其他在座玩家全部显式同意”，避免发起者停留在已销毁房间。
- 验证结果：新增专项断言通过，Creator 原生资源门禁阻塞 0；Creator 3.8.8 已刷新两个脚本并输出 `DISSOLVE_REFRESH_OK`，控制台未新增脚本编译错误。双账号 E2E 已改用规范账号 11、12 并真实创建房间 `402536`，但现行加入房间 UI 已不再暴露旧脚本识别的数字键盘节点，测试停在加入步骤，证据位于 `docs/前端框架规范/历史记录与证据/证据/pdk-dissolve-vote-2026-09-14T07-33-25-045Z`；未把测试脚本兼容问题虚报为功能验收通过。

### 2026-09-14｜公共解散终态按 2.2.2 业务链路更正

- 用户复验：一名玩家同意后已返回大厅，解散发起者仍停留在房间投票界面。
- 对照结论：2.2.2 以服务端 `Dissolve` 终态作为退房依据，不由客户端按座位投票数组推断；现行 V2 的同意响应只是确认数据，不是可投影的完整房间快照。
- 更正结果：删除“客户端全员同意推断”和同意响应快照解析；同意成功后立即查询权威状态，只接受 `DISSOLVED`、正式解散广播，或活跃投票期间服务端明确返回房间已不存在/玩家已离座作为同一终态，统一进入幂等退房流程。
- 验证结果：解散生命周期专项测试 11/11、Creator 原生资源门禁通过（563 个 Scene/Prefab、阻塞 0）；尚待 Creator 热编译及双客户端真实点击复验。

### 2026-09-15｜公共数字键盘无回弹滑入滑出

- 用户要求：公共数字键盘从屏幕上方快速滑入，点击确定后向下滑出屏幕；删除数字键盘窗口的回弹效果。
- 修改范围：仅调整公共 `NumpadService` 的唯一出入场事务，并在 `LegacyFormManager` 中禁止 `Numpad` 叠加原有 `elasticOut` 居中缩放；未修改业务输入规则、Prefab 层级或玩法代码。
- 实现结果：打开以 `cubicOut` 单向滑入 0.18 秒，确定以 `cubicIn` 单向滑出 0.16 秒；确认阶段禁用重复点击，滑出完成后才执行原业务回调；缓存表单再次挂载时重新从上方入场。数字键盘路径不存在 `elastic`、`bounce` 或 `back` 回弹曲线。
- 验证结果：专项测试 9/9、Creator 原生资源门禁阻塞 0；Creator 3.8.8 已通过资源数据库刷新并生成最新 Editor/Preview 编译产物，控制台返回 `NUMPAD_SLIDE_REFRESH_OK`。
- 阻塞记录：专属无头 Chrome/CDP `1234` 已启动，但清洁会话使用规范账号 11 登录后仍停留 `LoginScene`，无法进入大厅真实点击数字键盘；未调用内部 Controller 冒充 E2E，当前只差登录环境恢复后的逐帧真实 Canvas 验收。

### 2026-09-15｜公共数字键盘改为即时显示与关闭

- 用户最新更正：取消此前公共数字键盘滑入、滑出方案，改为调用后直接显示、点击确定后直接关闭。
- 修改结果：从 `NumpadService` 删除全部 Tween 引用、动画时长、屏幕位移、重复挂载动画监听和延迟确认回调；确认点击现在同步执行原业务回调。`LegacyFormManager` 继续排除 `Numpad` 的旧 `elasticOut` 动画，确保公共键盘完全没有出入场或回弹效果。
- 验证结果：数字键盘与创建房间相关专项测试 9/9、Creator 原生资源门禁阻塞 0；Creator 3.8.8 Editor/Preview 编译产物均已包含即时确认代码，且旧滑动常量与位移实现不存在。

### 2026-09-15｜清除本地数据同步清理权威房间状态

- 用户要求：BootStrap 勾选“是否清除本地数据”时，除浏览器缓存外还必须清理测试账号卡住的房间数据。
- 根因/结论：原清理流程只删除本地 Storage、IndexedDB 和 Cache，无法清除 Hall 保存的活动房间成员关系；现改为启动时阻止旧会话自动恢复，待用户显式登录取得新令牌后查询权威活动房间、执行离房并再次确认，清理失败则阻止进入且允许下次登录重试。
- 修改范围：新增公共 `StuckRoomCleanupService.ts`，并接入 `LoadingEnvironmentPolicy.ts`、`ClientBootstrap.ts`、`AuthSession.ts`；未修改登录 Scene 布局、玩法规则或服务端。
- 验证结果：专项契约测试通过；Creator 3.8.8 已导入新脚本并生成 `.meta`，Preview 热编译产物包含完整清理链路；本地 Gateway/Hall 的活动房间接口实测可访问且账号 21 返回 `active=false`，无测试房可用于本轮离房突变验收。

### 2026-09-15｜公共解散同意按钮事件恢复

- 用户反馈：公共解散弹窗点击“同意”再次不生效。
- 根因/结论：按钮自身监听之外又存在弹窗根节点捕获阶段命中检测，同一手势可能先被根节点处理并改变按钮状态，干扰原生 Button 事件；已删除弹窗级捕获层，只保留同意/拒绝按钮自身的 CLICK、TOUCH_END、MOUSE_UP 与单手势去重。
- 修改范围：仅 `CommonPdkDissolveController.ts` 及专项测试；未修改 Prefab 层级、玩法规则或服务端。
- 验证结果：专项契约测试通过；Creator Preview 热编译产物已确认不含旧 `onFormPointerEnd`/`bindInputRoot`，并包含三个按钮事件监听。双客户端权威解散真实 E2E 仍依赖可用测试房。

### 2026-09-15｜最后同意者未退出房间更正

- 用户复验：两名玩家全员同意解散后，一端已回大厅，最后操作的一端仍停留在房间弹窗。
- 根因/结论：服务端向最后同意者的响应已携带 `roomTerminal=true`/`phase=DISSOLVED`，客户端却丢弃响应并只做后续状态查询；房间已销毁后查询返回 `room is dissolved` 或 `player is not a room member`，旧补偿分支未识别，导致终态导航缺失。现直接消费服务端明确终态响应，非终态响应才继续对账，同时补全销毁后权威错误识别。
- 修改范围：仅公共 `CommonPdkRuntime.ts`、`CommonPdkDissolveController.ts` 及专项测试；未修改服务端、玩法规则或 Prefab。
- 验证结果：解散生命周期专项测试 13/13，Creator 原生资源门禁通过（564 个 Scene/Prefab、阻塞 0）；Creator Preview 已热编译并包含最终响应终态处理。双客户端真实复验需使用修复后的新 Preview 会话，旧页面不会自动替换已加载脚本。

### 2026-09-15｜解散后 3008 房间不存在终态补全

- 用户复验日志：未退出端持续报 `3008: room does not exist: 124461`，解散弹窗和房间界面未关闭。
- 根因/结论：房间已由服务端销毁，但客户端解散期间的权威对账只识别 `room not found`，没有识别服务端实际返回的 `room does not exist`，导致 500ms 轮询持续告警且不进入终态导航。现仅在活动解散投票上下文中精确识别该消息为服务端终态；没有把通用错误码 3008 整体视为解散，避免误判“未轮到操作”等其他 3008。
- 修改范围：仅公共 `CommonPdkRuntime.ts` 与解散专项测试；未修改服务端、玩法、Prefab 或 Scene。
- 验证结果：专项测试 13/13、Creator 原生资源门禁阻塞 0；Creator 3.8.8 已重新加载并生成包含 `room does not exist` 分支的最新 Preview 编译产物。

### 2026-09-15｜Git 仓库地址与用户授权边界

- 用户要求：把客户端、服务端和管理后台 Git 地址写入每个 AI 员工规范；用户测试完成后会主动邀请对应 AI 提交，未收到明确指令禁止推送。
- 根因/结论：统一记录三个固定仓库地址；取消 AI 自主提交和定时推送授权，明确邀请后默认只推送对应仓库 `develop`，`main` 及历史改写仍需当次单独授权。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：规则已归入既定章节，历史开发记录未改写，“记忆”仍为最后一章。
- 未完成项：无。

### 2026-09-15｜暂停跨任务文档消息与自动整理

- 用户要求：所有 AI 暂时不再给文档任务发送消息，也不再自动整理正式文档。
- 根因/结论：新增临时覆盖规则；暂停期间不执行此前主动发送文档报告的条款，各岗位只维护自身开发记录与记忆，文档 AI 仅响应用户直接下达的具体文档任务。
- 修改范围：仅本文档“执行与验收”和本开发记录；历史记录不删除、不改写。
- 验证结果：临时规则已归入既定章节，“记忆”仍为最后一章。
- 未完成项：等待用户后续明确恢复文档协作。

### 2026-09-15｜退出后重进同一房间误报满员修复

- 用户反馈：账号从俱乐部房间退出到俱乐部后，再次点击同一桌提示“房间人数已满”。
- 根因/结论：公共 Hall 普通加入流程直接执行新增 join，没有先读取当前账号的权威活动房间；退出展示与共享桌投影交接期间账号仍保有原座时，重复 join 会被房间容量校验误判为新成员。现所有普通加入先查询账号权威活动房间：目标相同则恢复原成员并签发新的一次性票据，目标不同则提示先退出，账号无活动房间时才新增占座。
- 修改范围：仅公共 `HallRoomGateway.ts` 及俱乐部等待/重入契约测试；未修改俱乐部规则、玩法人数、服务端或 Prefab。
- 验证结果：相关房间加入与解散专项测试 17/17、Creator 原生资源门禁阻塞 0；已只重启 Aoo Creator 3.8.8（未触碰并行的 3.8.5 项目），Preview 编译产物包含最终“恢复原座优先”逻辑。

### 2026-09-15｜退出低号座位后重进误报满员补正

- 用户复验：玩家退出后再次点击同一桌仍提示“房间人数已满”，并明确授权框架岗位修改本问题相关前后端；三带二显示问题已交由其他 AI，本任务不再处理。
- 根因/结论：Hall 清除历史离席行后仍以 `MAX(seat_no)+1` 分配新座位；退出 0 号位而 1 号位仍占用时，两人房重进会得到越界的 2 号位并被游戏权威按满员拒绝。现统一改为分配最小空闲座位号。
- 修改范围：公共 Hall `JdbcHallRepository`、对应专项测试及本文档；未修改三带二规则、牌面展示或其他玩法代码。
- 验证结果：Bootstrap/Hall 跳过测试的生产编译通过；新 Hall 已部署且健康检查为 `UP`。使用框架专属账号 11、12 完成真实 HTTP 权威链路：账号 12 占用 1 号位、账号 11 退出 0 号位后重进成功复用 0 号位，不再返回满员；测试房 `580376` 与两账号成员状态已清理。Maven 聚合测试仍被既有缺失类 `AypdkGameProvider`、`HbpdkSession` 和规则文档目录 `.DS_Store` 阻塞，与本次改动无关。

### 2026-09-16｜持续 develop 提交权限

- 用户授权：所有 AI 可将各自职责范围内已形成闭环、可明确归属的改动提交并推送到对应仓库 `develop`，无需逐次申请；仍禁止 `main`、强推、改写历史和夹带无关改动，每次结果必须报告。

### 2026-09-16｜Git 权限更正：仅明确指令或每日快照

- 更正原因：用户最新明确要求撤回 AI 自主提交权限；此前“持续 develop 提交权限”记录不再作为执行依据。
- 最终规则：只有用户针对具体任务明确说“提交”才允许提交该任务；否则仅由北京时间 `02:00` 的每日固定任务统一提交。

## 7. 记忆

- 当前用户授权任务：任务摘要“检查并修复俱乐部点击桌子进入房间非常慢”；来源“用户 2026-09-15 明确要求”；状态“进行中”；职责范围“俱乐部入房入口与公共房间交接关键路径的计时、串行依赖和最小修复，不修改玩法规则或三带二问题”；安全断点“尚未修改业务文件，准备核对入房请求、票据、资源准备、WebSocket 和场景挂载的等待关系”。
- 当前用户授权任务：任务摘要“刷新后俱乐部桌子上的玩家头像未渲染”；来源“用户 2026-09-15 明确反馈”；状态“待处理”；职责范围“俱乐部桌面刷新投影与公共头像组件接线”；安全断点“尚未检查或修改，排在当前俱乐部入房性能任务之后”。




- 已完成用户授权任务：公共数字键盘按用户 2026-09-15 最新要求改为直接显示、点击确定直接关闭；此前滑入滑出方案已撤销，公共 `NumpadService` 不含 Tween、位移、延迟或回弹，旧 `elasticOut` 入口仍明确排除 `Numpad`；专项测试 9/9、资源门禁阻塞 0，Creator Editor/Preview 已同步。

- 当前用户授权任务：任务摘要“大厅创建的房间完成整场后，一名玩家退出但其他房间客户端仍保留该玩家头像和牌”；来源“用户 2026-09-14 明确反馈”；状态“阻塞，根因属于跑得快服务端玩法状态机”；职责范围“仅完成公共退出、权威成员状态、广播和公共座位 UI 清理链路定位，不越界修改玩法规则”；安全断点“`PokerAuthoritativeSession.leave()` 在整场结束时仍因 `state != null` 写入负数托管占位；应由跑得快岗位改为完整移除座位关联状态并完成双客户端 E2E，框架代码与 UI 未修改”。

- 当前用户授权任务：任务摘要“从框架层深度优化俱乐部/大厅进入房间流程，点击桌子后立即进入”；来源“用户 2026-09-13 明确要求”；状态“代码、Creator 热编译与专项门禁完成，等待具备可加入俱乐部房间的双账号真实点击计时”；职责范围“公共房间场景启动关键路径与 UI 交接，不修改俱乐部规则、房间权威状态或玩法逻辑”；安全断点“已移除进入前额外绘制等待、后台预热整体等待以及 `preloadScene→loadScene` 重复串行阶段；两个必需房间 Prefab 改为与 Game WebSocket 建连并行，房间 Scene 在所属 Bundle 就绪后即开始加载并与其余 Bundle 清单并行；专项门禁 7/7、原生资源门禁阻塞 0，Creator Editor/Preview 最终 chunk 已确认；剩余硬依赖为 Hall 入房/票据、Game WebSocket 和权威房间快照，当前没有现成可加入俱乐部房间，尚未虚报真实 E2E 完成”。

- 已完成用户授权任务：Creator 2.2.2 的扑克发牌动作已精简为 `PokerDealNodeAnim`，明确为 Cocos 节点 Tween、非 Spine；单一参数化实现覆盖 16/8/3 及任意张数，存放于 `assets/Games/Poker/Common/Spine`，Creator 3.8.8 已导入并编译，尚未接入具体玩法，等待用户后续指令。


- 当前用户授权任务：任务摘要“公共导航进度条固定在距离屏幕底部 100px”；来源“用户 2026-09-13 明确要求”；状态“阻塞，代码与静态验证完成，等待 Creator Preview 同步”；职责范围“公共导航 DOM 遮罩样式，不调整启动进度、业务 Scene 或 Prefab”；安全断点“导航卡片已固定 `bottom:100px`，错误与重试区域不会推移进度条；专项测试 8/8，Creator 尚未生成最终 chunk”。

- 当前用户授权任务：任务摘要“统一加载超过 0.5 秒才显示导航界面，阈值后续可集中调整”；来源“用户 2026-09-13 明确确认继续处理”；状态“阻塞，代码与静态验证完成，等待 Creator Preview 同步和真实复验”；职责范围“公共展示事务延迟常量、场景导航与 Form 加载接线；后台预加载始终静默”；安全断点“已集中为 `NAVIGATION_REVEAL_DELAY_MS=500`，普通 Form 与场景导航统一使用，后台 preload 静默；专项测试 8/8，本次无新增类型错误；Creator 尚未生成最终 chunk”。

- 当前用户授权任务：任务摘要“修复俱乐部列表进入俱乐部时先返回大厅，并统一确认进入当前界面后再串行后台预热其他界面资源”；来源“用户 2026-09-13 明确要求深度检查并统一处理”；状态“阻塞，代码与静态验证完成，等待 Creator Preview 同步和真实复验”；职责范围“公共 Form 切换可见性、俱乐部入口最小接线及现有后台资源预热顺序，不改俱乐部 Prefab 层级与业务数据”；安全断点“目标俱乐部 Form 挂载成功后才关闭列表，列表关闭也不会在俱乐部 Form 可见时激活大厅；`preloadRefreshSurface(true, 1)` 保持当前界面完成后的单线程静默后台预热；专项测试通过”。

- 当前用户授权任务：任务摘要“刷新首次进入只能显示启动界面进度，禁止再显示导航界面进度，并消除后续闪屏、黑屏”；来源“用户 2026-09-13 连续反馈”；状态“阻塞，最终代码与 Creator Preview 同步完成，等待用户当前 Preview 视觉复验”；职责范围“公共 Startup 与导航展示所有权，不修改登录 Scene/Prefab 布局”；安全断点“Preview 已恢复唯一持久 `aoo-startup-cover`，公共协调器以 `initialPresentationActive` 覆盖 DOM/Cocos 启动阶段且只有启动路由可释放；图片 URL 200、最终 chunk 已生成、专项测试 8/8；CDP `1234` 被其他任务占用”。

- 当前用户授权任务：任务摘要“修复公共导航进度条未显示 `TransitionBg` 背景图”；来源“用户 2026-09-13 明确反馈”；状态“阻塞，代码与静态验证完成，等待 Creator Preview 同步和真实 Canvas 复验”；职责范围“公共场景过渡背景资源 URL 解析与遮罩首次显示时序，不修改业务场景、Prefab 层级或登录布局”；安全断点“已按 `bundle.base` 归一化相对 `nativeUrl`，并让遮罩等待背景准备后首次显示；专项新增测试通过，本次 TypeScript 无新增错误；Creator 3.8.8 尚未生成新 chunk，CDP `1234` 被其他任务占用”。

- 当前用户授权任务：任务摘要“同步取消 `Common/Prefab.meta` 的 `common-prefab` Bundle 后的全部代码引用”；来源“Aoo 任务 `01a01f07-cab4-73f2-b9c6-15b911804a98` 2026-09-13 明确转交”；状态“阻塞，代码与静态验证完成，等待 Creator 生成全部最新业务 chunk 和真实 Canvas E2E”；职责范围“公共 Prefab 注册表、AssetLoader/Catalog 与必要业务消费点的集中接线，不改 Prefab 层级和业务行为”；安全断点“所有旧 Bundle 精确引用已删除；Preview `common/config.json` 含全部目标 `Prefab/*`，定向断言 4/4、TypeScript 本次无新增错误；Creator 已生成公共注册表 chunk，但 PDK/俱乐部消费 chunk 尚未全部更新，CDP `1234` 被其他任务占用”。

- 当前用户授权任务：任务摘要“修复刷新首次进入时 HTML Startup 与 Cocos BootStrap 同时显示两套进度条”；来源“用户 2026-09-13 明确确认处理”；状态“阻塞，代码与静态验证完成，等待真实刷新 Canvas E2E”；职责范围“Startup 首次启动展示所有权和 BootStrap 最小展示接线，不修改登录 Scene/Prefab 层级”；安全断点“Web Startup 接管时运行期隐藏 `ProgressTrack/ProgressPercent/BootstrapStatus`，原生无 DOM 时保留 Cocos 进度；专项测试 4/4、本次 TypeScript 无新增错误；CDP `1234` 被其他任务 PID `91045` 占用，不接管其他任务浏览器”。

- 当前用户授权任务：任务摘要“拆分首次启动与通用场景过渡视觉，并规范迁移启动/场景过渡背景资源”；来源“Aoo 任务 01a01f07-cab4-73f2-b9c6-15b911804a98 2026-09-13 明确转交”；状态“进行中”；职责范围“Startup 首次启动视觉、Common 场景过渡视觉、SceneRouter 编排边界、Web/原生唯一资源与构建同步”；安全断点“已确认唯一源图 UUID `f60d7021-4ffc-4350-bc4e-c2b8afc4dd04`，当前名称 `Startup/Texture/background.png`；尚未移动、复制或改代码，不修改 `LoginScene.scene`、业务路由和当前编辑中的 Prefab”。

- 当前用户授权任务：任务摘要“修复公共进度条最后一帧进入游戏大厅时的闪屏”；来源“用户 2026-09-13 连续复验反馈”；状态“进行中，已完成根因二次更正，等待当前 Preview 复验和真实逐帧 E2E”；职责范围“公共启动遮罩最终帧与大厅 Canvas 首帧交接”；安全断点“已删除 80ms 透明叠加退场；通用遮罩插入 DOM 前即隐藏，静默事务结束不再调用 `ensureDom()`；目标首帧完成引擎和浏览器绘制后原子隐藏；定向门禁 3/3，TypeScript 本次无新增错误；CDP `1234` 被其他任务 PID `91045` 占用，不改大厅 Scene/Prefab、不接管其他任务浏览器”。

- 当前用户授权任务：任务摘要“优化刷新和首次进入速度，使公共进度条持续丝滑且只在目标界面完成挂载时到达 100% 并立即交接”；来源“用户 2026-09-13 明确要求”；状态“阻塞，公共实现和静态回归已完成，等待 Creator 热编译与真实逐帧 E2E”；职责范围“公共启动加载协调器、真实进度聚合和首次/刷新启动交接”；安全断点“专项测试 12/12；TypeScript 本次文件无新增错误；Preview chunk 时间仍早于源码，CDP `1234` 被其他任务 PID `91045` 占用，本任务未接管或覆盖用户未保存的 `ClubDesk.prefab`”。

- 当前用户授权任务：任务摘要“刷新恢复后先显示当前界面，再按先后顺序后台预加载，按钮无感打开”；来源“用户 2026-09-13 复验纠正”；状态“阻塞，顺序修正、Creator 热编译和静态回归已完成，等待真实计时 E2E”；职责范围“公共 Form/Bundle/Prefab 单飞缓存、界面提交后的顺序后台预热及大厅/登录/房间最小接线”；安全断点“测试 10/10；Creator Preview 已含最终代码；CDP `1234` 被 `/tmp/aoo-social-e2e.kgxKnY` PID `91045` 占用，且 `ClubDesk.prefab*` 有用户未保存状态，本任务未接管或覆盖”。

- 永久架构约束：所有数字键盘输入唯一由公共 `NumpadService` 状态机维护；业务页面只能提供 `value/setValue`、整数位与总长度，并以 `decimalPlaces` 声明整数或小数。禁止在页面中新增 `digit/backspace/clear/decimal` 按键回调、逐键拼接、前导零或小数点处理逻辑。

- 永久场景交接约束：运行期进入或退出游戏房间均使用公共静默首帧保留事务，不显示加载进度；目标场景首帧提交后才释放上一场景画面，失败时必须释放冻结帧并显示可重试错误界面。应用首次启动与登录启动流程仍可使用正常加载界面。

- 当前用户授权任务：任务摘要“修复玩家离房后其他客户端刷新仍显示其头像，并修复退出后再次点击同桌的黑屏”；来源“用户 2026-09-13 连续复验”；状态“阻塞，权威离房补偿、Gateway 部署、客户端空座投影及重入资源生命周期均已完成，等待双客户端真实 E2E”；职责范围“Hall/Gateway 公共成员生命周期、公共 PDK 权威成员投影和场景资源生命周期”；安全断点“本轮重入相关定向测试 9/9，Creator Editor 热编译已同步；Gateway PID `26747` 已载入 06:18 构建；CDP `1234` 被 `/tmp/aoo-social-e2e.kgxKnY` 占用，不接管其他任务”。

- 已完成用户授权任务：全项目 45 个 Prefab、85 个公共数字键盘入口已统一为 `Button + lb`，文字键盘字段未改；Creator 3.8.8 完成重导入，专项门禁和真实移动 Canvas 点击验收通过。

- 当前用户授权任务：任务摘要“全项目数值键盘统一支持小数点并可输入 `1.23`”；来源“用户 2026-09-13 明确纠正范围”；状态“客户端实现、专项测试及 Creator 热编译完成，等待真实 Canvas 点击和服务端协议闭环”；职责范围“公共数值键盘消费端、创建房间底分、比例/积分/费用/分数字段”；安全断点“房间号、回放码、玩家/俱乐部 ID、页码和房卡数量保持整数；Hall 当前仍以正整数校验 `baseScore`，不属于本线程 UI 修改边界，客户端底分小数提交前必须由对应服务端岗位同步协议”。

- 当前用户授权任务：任务摘要“修复俱乐部断线重连后模板桌子不显示”；来源“用户 2026-09-13 明确反馈”；状态“进行中”；职责范围“俱乐部大厅模板桌恢复显示的最小客户端接线、定向测试与真实重连验收”；安全断点“`LegacyClubMainController` 重连恢复会先隐藏桌子容器，恢复请求失败时容器保持隐藏且连接恢复仍继续完成；尚未修改业务文件，不改 Prefab 层级、视觉、规则或服务端”。

- 当前授权任务：㉑任务摘要“删除旧 `PlayerCityCurrency` 地区资产和全部圈卡体系，统一以 `accountId` 个人 `RoomCard` 承担普通房、圈主亲友圈房、盟主联盟房扣退”；来源“aoo产品 2026-09-11 三次转交用户明确授权，最新要求覆盖历史只读兼容，圈卡历史数据与表结构也彻底删除”；状态“进行中”；职责范围“Client/Server/Admin 活跃资产模型、所有者付款解析、Billing/Repository/DAO/缓存/协议/配置、幂等扣退、破坏性正式数据库迁移与专属测试”；安全断点“旧 `PlayerCityCurrency` 类已不在当前源码，现行 `PlayerWallet` 仍按 pid 且 Billing 仍接受 `CITY_ROOM_CARD`，尚未修改业务文件；已识别 48 个候选文件，先证明共享字段归属并快照冲突文件；`classificationRegionId`、房卡/钻石/战绩/回放、开房规则表及 Prefab/Scene 层级保持不动，不连接或执行线上数据库”。
- 当前授权任务：⑲框架旧比赛分 `GameCent` 已统一为俱乐部独立游戏积分 `ClubCent`；全部客户端 Prefab 使用 `ClubCent` 节点标识，代码中文语义为“俱乐部积分”，现行 Club 服务按 `club_id` 独立持久化并有双俱乐部隔离集成测试；实现与针对性验证完成，Creator Web Desktop 完整构建被既有空资源目录门禁阻塞，未虚报全量闭环；规则表和无关赛事积分保持只读。
- 当前用户授权任务：⑰任务摘要“统一刷新后恢复最后成功位置：登录、游戏大厅、游戏房间、亲友圈”；来源“用户 2026-09-11 明确要求统一处理”；状态“阻塞，公共实现、Creator 编译和静态回归已完成，等待大厅岗位修复当前空壳 `MainScene.scene` 后执行真实四位置刷新 E2E”；职责范围“公共 `HostRoute` 持久化、启动恢复判定及登录/大厅/房间/亲友圈最小提交点接线，不新增亲友圈 Scene”；安全断点“`HostRouteStore` 与四类成功提交点已接入，测试 8/8、Creator 新增错误 0；未修改任何 Scene/Prefab，真实 E2E 尚未执行”。
- 已完成插队任务：`Client/assets/Common/Prefab` 当前按六项权威清单保留根层路由与 `common-prefab` Bundle；用户后续删除的 `NoticeBar` 已同步废弃代码链路，历史 Prefab 不再恢复或引用。现存六项中的残留动画 UUID 只报告给用户在 Creator 中处理，框架不得从历史来源补回。
- 暂停任务：任务摘要“重新核验并统一所有玩法开局前左上角返回退出规则：未发牌时无论准备状态均可退出，发牌后保持原限制”；来源“用户 2026-09-10 明确要求”；状态“进行中，因公共 Prefab 紧急恢复暂存安全断点”；职责范围“公共客户端退出判定、共用房间返回接线、公共服务端成员生命周期及真实 UI E2E”；安全断点“当前代码审计无回归，公共客户端专项测试 7/7，通过 GameSPI/Poker 定向 Maven 构建；尚待公共 Prefab 恢复后重新执行真实房间点击复验”。
- 岗位边界：全栈负责公共客户端与服务端框架、公共协议、公共数据库结构与迁移能力、资源与配置映射、公共组件、发布工具链、部署和真实 E2E；不处理具体玩法、亲友圈和登录大厅的业务专属实现。跑得快出牌音效及房间操作音效不属于本岗位。
- 调度边界：本岗位不是稽查或主协调，不巡检、跟踪或指挥其他 AI；只执行用户或 aoo产品明确下发的单项公共框架任务，其他岗位报告不得自动转为任务。
- 业务文件边界：登录、大厅、亲友圈和具体玩法目录默认只读；不得恢复其 Scene、Prefab、Meta、资源或代码，Creator 也不得用于检查、保存、关闭或重导入这些岗位的现场。
- 记录边界：开发记录与记忆只保存框架岗位信息，禁止跨岗位混写。
- 权威源：以当前磁盘和 Creator 现存对象为准；用户删除的 Prefab、节点、动画和材质即废弃，不从任何历史来源恢复或兼容，当前代码和验收不依赖它们；缺失新增仅由用户在 Creator 中创建。
- 待处理队列：①公共头像文字/语音气泡及 `Img_Ready` 按当前可见区域坐标左右展开，状态“两个真实客户端已进入房间，但权威房间随后被服务端结束并报 `3008`，等待房间/玩法岗位恢复稳定三人测试房间后续验”，相关路径 `Client/assets/Common/Code/UI/CommonHeadController.ts`、`Client/assets/Common/Prefab/Common/Common_Head.prefab`、`Client/assets/Games/Poker/PDK/Common/Code/Runtime/Room/SeatPresenter.ts`；②审计本地网页版在游戏资源已有缓存时创建/加入房间能否直接进入，状态“已取得单次创建约 5.7 秒、加入约 6.5 秒证据，冷/热完整对照等待第 1 项稳定环境后继续”，只做冷启动与缓存命中真实 E2E、关键网络请求、加载表现和耗时对比，不修改代码；③公共 ChatPanel 三类消息真实发送，状态“接线已完成，等待稳定真实房间 E2E；自定义文字跨客户端送达待服务端授权”，相关路径及证据见本章开发记录；④唯一公共 `SettingsPanel`，状态“资源迁移、公共控制器、PDK 接线、Creator 重导入和构建已完成，等待稳定真实房间点击 E2E”，相关路径 `Client/assets/Common/Prefab/Common/SettingsPanel.prefab`、`Client/assets/Common/Code/UI/CommonSettingsController.ts`、`Client/assets/Games/Poker/PDK/Common/Code/Runtime/CommonPdkSwitchCoordinator.ts`；玩法私有设置入口尚未删除，须按玩法影响范围单独核对；⑤客户端屏幕适配职责迁移审计，状态“大厅四向常规适配已迁入 `Lobby_Main.prefab` 并完成 Creator 与三视口 E2E；其余 627 个候选仍按单页串行审计”，`Client/assets/Login/Scenes/LoginScene.scene` 只报告不写入，详见屏幕适配审计证据；⑥统一公共加载界面，状态“大厅普通 Form 的唯一公共 `WaitForm` 已完成真实 Canvas 点击验收，仅余游戏返回大厅的跨 Scene 加载链路”，等待稳定真实房间后继续，登录和进入游戏房间保持例外。队列仅保留尚未完成的问题，并按提出顺序记录状态与相关路径；已完成结果移入“开发记录”，过期信息及时清除。
- 当前插队任务：⑦统一规则表映射框架，状态“多选越界过滤去重、单选无效回退首项并警告、平台必填稳定键及原子保护均已完成；LS201已重新发布并清除重复小结算旧项，等待跑得快真实Canvas创建与权威快照验收；NJ201地区身份已由用户纠正并解除冻结，等待跑得快岗位完成最新发布、真实创建和权威快照”；规则表保持只读，禁止修改玩法规则和结算逻辑。
- 后续串行任务：⑧平台级底分 `baseScore` 全链路，状态“公共 Hall 建房协议、默认值、合法性、房间配置持久化与单服务部署已完成；玩法侧重连、结算、战绩和回放真实验收由跑得快岗位继续”；缺省为 1，禁止玩法/地区特判和客户端视觉倍乘。
- 当前协同任务：⑩Lobby/Code 职责归档边界审核，来源“职责归档 AI 点对点交接”；已确认 `RoomRecoveryStore` 迁入公共运行时、`RoomNetworkControllerMount` 迁入 Games/Common、`HallRoomGateway` 保留大厅域，并批准删除 Common 侧 6 个无消费者 Lobby 转发兼容文件；状态“主协调暂停，等待职责归档 AI 完成并由 aoo产品明确恢复，框架不触碰 Lobby/Code”。
- 当前紧急任务：⑪核验 PDK 移动端重载恢复失败，来源“跑得快 AI 2026-09-10 点对点阻塞报告”；职责范围“公共 Creator 场景加载、启动恢复与 Gateway 鉴权链路”，不修改场景布局、玩法业务或废弃资源；状态“进行中：web-desktop/web-mobile-001 均已包含 MainScene，所谓未加入构建列表是 `director.loadScene()` 返回 false 的泛化误报；首次真实错误为 Gateway WS 403，已要求跑得快岗位用全新移动上下文复现并回传握手、AuthNavigation 与当前场景证据”；安全断点“尚未修改代码或构建配置，等待精确复现后仅修复公共恢复竞争或鉴权根因”。
- 已完成优先任务：⑨现行框架跨资源锁顺序、锁死、线程池饥饿与无限等待审计已完成并部署；旧 `gameServer`/Legacy/AooKernel 仅留档，不属于当前运行链路。原 UI 队列从三客户端登录校准安全断点继续。
- 快捷文字语音稳定目录：公共普通话使用 `Games/Common/Audio/Chat/Mandarin/Boy|Girl`，地区方言使用 `Dialect/<地区代码>/Boy|Girl`；地区资源缺失时回退同 ID、同性别普通话。
- 账号隔离：本地测试仅用`11`至`20`，密码为`1`；并行任务记录实际账号，被占用时只在本段换号。禁止操作其他岗位账号、会话和房间数据；`51`至`100`未经aoo产品分配不得使用。
- 停止条件：只要存在已授权、可执行任务就持续执行；仅全部闭环，或只能等待用户决定、授权且无其他独立任务时可idle。任何停止、idle、异常结束或准备不再继续都须向aoo产品提交包含当前任务、完成/未完成项、阻塞、队列、安全断点和原因的停止报告；未授权历史审计项不自动入队。
- 当前范围：仅处理 CD201、LS201 直接依赖的公共启动、登录、资源、Bundle、结算与真实 E2E；麻将、亲友圈及其他玩法全部暂停并保持只读断点。
- 已解阻任务：CD201/LS201 所需 `common-prefab` 已按当前六项权威清单发布，Preview 的 index/config 为 200；登录大厅与跑得快岗位已获准从原 E2E 断点继续。
- 上下文与重读：对话过长时自动压缩旧内容，删除重复、过期和无用信息，仅保留已确认需求、架构决定、重要路径、未完成任务及最近 10 轮对话原文，不询问用户或中断任务，压缩后立即继续。仅在上下文压缩后、用户要求写入规范且更新完成后、准备切换新的业务问题时，或记忆不确定、规则冲突、职责边界不清时，重新读取本岗位执行提示词；同一问题连续处理中不得重复读取。
- 待处理队列：⑫任务摘要“整理 `Client/assets/Games/Common/Code`，仅保留已证实使用的公共游戏代码并按真实职责重分类”；来源“aoo产品转交”；状态“阻塞，等待 Creator 在用户空闲后正常重新导入并继续真实公共流程 E2E”；职责范围“目录与原 `.meta` 移动、全链路引用修复、Creator 同步和公共游戏流程验证”；安全断点“9 个 TS/.meta 已迁入 Catalog、Audio、Room、Settlement，扑克卡已归入 `Games/Poker/Common/Code/Card`；10 个迁移运行时文件相对 import 静态解析通过、专项测试 44/44；`Other/resources` 因 1,937 个动态资源与资源归档岗位冲突而原样保留；专属 CDP `1239` Preview 当前仍缓存旧扑克 import 错误，磁盘源码已修正，且后续仍有 `MainScene` build settings 阻塞”。
- 待处理队列：⑬任务摘要“在⑫完成后整理 `Client/assets/Common/Code`，仅保留已证实使用的全局公共代码并按真实职责重分类”；来源“aoo产品追加转交”；状态“进行中，等待 Creator 与真实公共流程统一验收”；职责范围“目录与原 `.meta` 移动、全链路引用修复、Creator 同步和公共流程验证”；安全断点“已删除无消费者的 Login 转发及其原 `.meta`；已删除无消费者、资源不存在的 `PaoDeKuaiSportPointDialog` 注册和 NativeMaps 旧路径，定向测试 17/17、JSON 解析通过；其余 CompatibilityApp 项已有大厅或玩法运行时消费者”。
- 待处理队列：⑭任务摘要“补齐 `Client/assets/Games/LongCard.meta` 与 `Client/assets/Games/WordCard.meta` 的现有 Bundle 门禁”；来源“aoo产品转交”；状态“等待亲友圈桌子样例释放 Creator”；职责范围“仅按 Creator 当前实际 Bundle 名补齐 `userData.isBundle:true`，随后 Creator 重导入验证”；安全断点“已静态确认两个 `.meta` 缺少 `isBundle`，未写入，不改 Bundle 名、不移动资源、不处理麻将”。
- 待处理队列：⑯任务摘要“取消 `Games/Poker/Common/Prefab/SmallSettle`、`BigSettle` 的旧子 Bundle 边界”；来源“aoo产品转交”；状态“等待 Creator 串行窗口及结算编辑无未保存确认”；职责范围“仅移除两个目录 `.meta` 的旧子 Bundle 声明，保留目录、Prefab 与 UUID，使父 `poker-common` 成为唯一权威 Bundle”；安全断点“尚未写入；完成后须正常重导入并确认 `poker-common/config.json` 收录大小结算 Prefab，再通知跑得快和登录大厅续验”。
