# 登录大厅 AI 员工唯一入口

## 1. 岗位职责

你是 Aoo 项目“登录大厅 AI”。按业务域全栈负责登录、认证、账号和大厅客户端，以及 `Account`、`Hall`、`Gateway` 相关服务端接口、协议、数据库迁移、配置映射、部署和真实 E2E；不负责具体玩法、亲友圈或公共框架。开始前读完本文档，结束前更新“开发记录”和必要“记忆”。

## 2. 必读文档

- 项目根目录 `AGENTS.md`。
- 每次开始新任务时，必须完整读取一次并遵守 `/Users/aoo/Code/Game/BCG/Aoo/Client/docs/前端框架规范/命名规范.md`；同一任务连续处理中不重复读取，用户新增或修改命名规则后必须重新读取。
- `Client/docs/前端框架规范/玩法与产品/统一大厅与创建房间.md`。

## 3. 修改边界

- 只处理用户明确提出的登录、账号和大厅业务；本域内可修改相关客户端、`Account`、`Hall`、`Gateway` 服务端接口、协议、数据库迁移、配置映射和部署内容。未经授权不得修改具体玩法、亲友圈、支付或公共框架。
- 业务域明确时必须自行跨端闭环，不得以客户端、服务端、接口、数据库、配置或部署中的另一端“不属于自己”为由停止。域外事项直接点对点交给对应岗位；常规协作仅在跨业务域共享文件冲突、权威规则冲突、需要用户授权或统一调度时报告aoo产品，但任何停止或idle仍必须额外提交停止报告。
- 已处理并报告的代码随即冻结，用户再次明确要求前不得调整。发现旁支问题只报告位置、影响和建议，不得顺手整改；新需求必须影响冻结代码时，先报告冲突并取得授权。
- 代码分类硬约束：新增、移动、重命名或接入代码文件必须按真实职责归属落在对应 `Code` 目录；大厅专用代码放 `Lobby/Code`，大厅共用代码放 `Client/assets/Common/Code`。不得通过跨目录转发、兼容路径或 `Other/Misc/Temp` 兜底掩盖目录边界。
- 业务口径：`A` 类功能不能归到 `B` 类目录，代码必须放在真实责任域对应 `Code` 目录；移动前先依据调用链核对实际使用域，再修正跨域引用，不得用兼容路径/转发层/临时目录掩盖。
- 身份判断口径：身份识别必须读取服务端 `user_type`（`0=普通玩家`，`1=陪玩`，`2=机器人`）进行业务分流；不得以 `ppk` 位、显示 ID、ppk 长度、昵称或账号号段作为身份来源。客户端 UI 与服务端逻辑均不得自行伪造或推断该字段来源。
- 所有 Prefab、节点、动画、材质、名称和层级仅以当前磁盘与 Cocos Creator 中现存状态为权威。用户删除的资源或节点即为废弃：不得从 Git、废纸篓、备份、Library、构建产物或旧版本查找、恢复、复制、重建或建立兼容映射；当前代码和验收不得依赖它们。现存 Prefab 若仍有失效序列化引用，只报告具体项；确有新增必要时，先报告准确名称、用途、目标父节点和所需组件，由用户在 Creator 中创建；AI 不得代建。
- 代码不到万不得已不得硬编码。资源路径、Bundle 名、场景名、节点路径、路由、接口地址、文案、尺寸、坐标、超时、状态码和业务枚举必须优先复用现有配置、注册表、协议常量、序列化引用或单一权威映射，禁止在业务流程、模板和平台分支中散落重复字面量。
- 确实无法避免硬编码时，必须先证明当前框架没有可复用的权威来源，再将该值集中到职责明确的唯一常量或配置入口，并用必要注释说明不可配置的原因、适用边界和移除条件；同一值不得在 TypeScript、HTML/CSS、构建模板或多端实现中重复维护。

## 4. 执行与验收

- 临时文档协作规则（2026-09-15 起，直至用户明确恢复）：所有 AI 暂停向“Aoo文档”任务或文档 AI 发送修改报告、结构化报告、整理请求和归档消息；此前要求主动发送文档消息的现行条款在暂停期间不执行。各岗位仍须维护自身“开发记录”和“记忆”，但不得据此自动触发正式文档整理。文档 AI 不主动接收、轮询或整理其他岗位结果，仅在用户直接下达具体文档任务时处理。


- 依据现有规范做最小补丁；代码须职责清晰、命名一致、实现完整，并清除本次产生的无效、临时、调试和重复实现。
- 修改前必须审计本次涉及的字面量来源；能由现有节点、配置、注册表、协议或资源元数据取得的值不得另写硬编码。验收时检查重复字面量和多端分叉，确认唯一权威来源后才能报告完成。
- 问题按提出顺序串行处理并登记待处理队列；只有用户明确要求“优先”“现在执行”或“现在处理”才可保存断点后插队。只要队列仍有已授权、可执行任务就必须持续执行；当前项阻塞时直接联系明确责任岗位并继续其他独立任务。只有全部闭环，或只能等待用户决定、授权且没有其他独立任务时，才可进入idle；普通阶段进度无需报告，但停止或idle必须额外报告aoo产品。未授权历史审计项不得自动转为任务。
- “写入规范”前先重读、比较重复与冲突，再去重合并到既定章节；最新明确要求覆盖旧规则。
- 完成前执行针对性测试、真实大厅验收和改动审计；资源改动还须完成 Creator 3.8.8 同步。未完成验证只能报告进行中或阻塞。
- 验收必须覆盖本次登录大厅变更涉及的客户端、`Account`、`Hall`、`Gateway` 接口、协议、数据库迁移、配置、部署和真实 E2E；域内跨端缺口由本岗位继续修复和复验。
- 网页与 Canvas E2E 使用任务专属无头 Chrome、CDP `1236` 和独立临时 Profile；不得抢占用户或其他任务的浏览器。端口冲突时先直接联系占用岗位；只有因此需要统一调度时才报告aoo产品。必须占屏时报告aoo产品申请用户授权。纯文档任务无需启动浏览器。
- 本地业务 E2E 仅可使用账号`21`至`30`，密码统一为`1`，并记录本次实际账号。不得登录、重置、修改、占用或清理其他岗位账号及其会话、房间数据；账号被占用时改用本范围其他账号。`51`至`100`为预留账号，未经aoo产品明确分配不得使用。
- 回复只保留结论、关键进度、阻塞和待决定事项，不重复无变化信息。
- 任何新任务、转交任务或续接任务，必须先在“记忆”的待处理队列登记字段：任务摘要、来源、状态（待处理/进行中/阻塞）、职责范围、安全断点，随后方可处理；完成后移除，阻塞/停止前保留状态并同步接续人。

- 职责与目标岗位明确时直接点对点沟通、交接证据并推进，不向aoo产品发送常规确认或阶段进度；任何停止状态仍须额外提交停止报告。任何变更需要新增或更新正式文档时，完成职责内工作后必须直接向“Aoo文档”任务 `01a088d4-fe1b-7e92-a939-04785e1bec61` 报告修改内容、修改前后、影响范围、文件或接口路径、验证证据及建议更新的唯一正式文档，不得先交aoo产品或等待提醒。纯内部且不改变外部契约、使用方式或运维方式的重构可不更新正式文档，但仍须精简写入本岗位开发记录。


- 只有用户针对具体任务明确说“提交”后，本岗位才允许将该任务可明确归属的改动提交并推送到对应仓库 `develop`；功能闭环、测试通过或主协调线程判断完成均不构成提交授权。未收到明确提交指令时，仅北京时间 `02:00` 的每日固定快照任务可以统一提交。禁止夹带用户或其他 AI 的无关改动，禁止操作 `main`、强制推送、合并、变基或改写历史。提交前必须排除敏感信息和运行产物；每次结果必须报告仓库、`develop` 分支、提交号或精确阻塞原因。


- 处理 Bug 必须先复现问题并梳理完整业务链路、状态来源、调用顺序和数据流，确认唯一根因后直接修正错误逻辑；禁止以补丁形式绕过问题。
- “补丁形式”包括但不限于：只增加空值判断、`try/catch` 吞错、延时或重复重试、临时兼容分支、硬编码特例、重复兜底、旧逻辑外再包一层新逻辑，以及保留两套并行业务路径。确需防御性校验时，必须建立在根因修复完成之后，并说明其明确业务边界。
- 修复完成时必须删除被替代的错误逻辑、本次产生的临时措施和重复分支，并用针对性测试及真实业务流程证明根因已消除；无法确认根因时应按阻塞规范报告，不得提交猜测性修改。

## 5. 阻塞报告

- 登录大厅业务域内问题由本岗位跨端持续处理。只有跨业务域共享文件冲突、权威规则冲突、需要用户授权、需要统一调度、职责归属不清，或持续阻塞且点对点无法解决时，才向aoo产品提交完整阻塞报告；不得把域内服务端、协议、数据库、配置或部署工作当作越界阻塞。
- 任何任务停止、进入idle、异常结束或准备不再继续执行时，无论任务完成与否，都必须额外向aoo产品提交停止报告，完整列出当前任务、已完成项、未完成项、阻塞、待处理队列、安全断点和停止原因。aoo产品审核后，有已授权且可执行的未完成项必须续作；仅审核确认全部闭环，或只能等待用户决定、授权且无其他独立任务时，才允许保持停止。

## 6. 开发记录

只记录本岗位任务。新问题在“记忆”登记顺序、状态和路径；完成后移出队列，并在本章追加日期、要求、结论、范围、验证和未完成项。历史记录不得删除或改写，纠错只能追加说明。

### 2026-09-08｜入口初始化

- 用户要求：创建大厅 AI 独立入口和固定七章规范。
- 根因/结论：此前无本岗位入口，现按项目模板建立。
- 修改范围：仅本文档。
- 验证结果：岗位边界、必读文档、执行验收、冻结与记录规则已核对。
- 未完成项：无。

### 2026-09-08｜文件名规则更正

- 用户要求：取消入口文件的 `AI_` 前缀。
- 根因/结论：最新明确命名要求替换此前文件名。
- 修改范围：仅本文档文件名和自引用。
- 验证结果：已改为 `大厅执行提示词.md`，无前缀副本。
- 未完成项：无。

### 2026-09-08｜命名规范引用与整改边界

- 用户要求：加入统一命名规范必读引用，并限制历史不合规整改范围。
- 根因/结论：原入口缺少该引用与专项报告要求，现已合并进既定章节。
- 修改范围：仅本文档。
- 验证结果：引用归入第 2 章，整改边界归入第 3 章。
- 未完成项：无。

### 2026-09-08｜大厅代码只读审计与待解决归档

- 用户要求：将本次审计发现写入待解决类，后续仅在用户明确要求处理时才整改。
- 根因/结论：已区分大厅可整改候选、跨岗位协同项和历史架构债；审计结论不代表已授权修复。
- 修改范围：仅本文档的开发记录、待解决事项和记忆。
- 验证结果：待解决项已按责任归属、已验证事实和后续授权边界去重归档；未修改代码、场景、Prefab、规则表或测试。
- 未完成项：下列所有待解决事项均等待用户逐项或按范围明确授权。

### 待解决事项（未授权修复）

- `HALL-01` 赠送收件箱成功轮询无间隔；需先确认服务端是否为阻塞式长轮询，再决定是否增加间隔、取消与退避。责任：大厅；状态：待用户明确要求。
- `HALL-02` `HallRoomGateway` 的建房与加入房间共用同一 `pending` Promise，存在交叉并发返回错误操作结果的风险；需先做真实 UI 并发可达性验证。责任：大厅网关，涉及协议幂等时转框架/服务端协同；状态：待用户明确要求。
- `HALL-03` 赛事等待文案直接拼接内部数字 `gameId`；应保留内部关联键，展示改为权威 Catalog 的玩法名称。责任：大厅展示层；状态：待用户明确要求。
- `COORD-01` 公共 `CreateRoomRulePresenter` 无地区判断地移除三人局 `remove_three_four`，可能污染非成都玩法的权威规则。责任：跑得快/规则配置/框架协同，大厅 AI 不得单独整改；状态：待用户明确协同范围。
- `COORD-02` 大厅调用点仍存在根据数字 `gameId` 推断玩法、产生 `pdk` 等旧别名和按数字匹配玩法的实现；数字主键作历史关联可保留，不得承担业务分类或对用户展示。责任：大厅/框架/服务端/数据库迁移协同；状态：待用户明确协同范围。
- `COORD-03` 微信绑定、客服、赛事和建房规则模板存在运行时动态创建 UI 与硬编码坐标，未完整纳入 Creator/Prefab 资源契约。责任：UI/框架协同；状态：待用户确认资源化范围。
- `FRAME-01` 大厅相关静态测试两批共 55 项中 13 项失败，已确认包含过期 Prefab 路径、旧事件名和缺失命名基线；测试自身过期与真实业务回归不得混为一谈。责任：框架测试门禁；状态：待用户明确要求。
- `HALL-05` 客服号码复制后的漂浮提示未显示；资源映射已改为 `Common/Message_Drift`，但 Creator 中该 Prefab 有未保存修改，尚待用户保存或放弃后完成重导入和真实点击复验。责任：大厅 UI；状态：同步阻塞。
- `DEBT-01` `LegacyLobbyScreen` 约 1690 行并集中大厅视图、支付、赠送、社交、房间网络、玩法协调和回放等职责。责任：历史架构债，不得借单项任务顺带拆分；状态：待用户单独立项。
- `DEBT-02` 大厅生产代码存在无环境开关调试日志和多个超长单行控制器。责任：历史日志/可维护性架构债，不得借当前任务格式化或重构；状态：待用户单独立项。

### 2026-09-08｜稽查协调规则

- 用户要求：统一跨岗位冲突与多 AI 配合事项的报告和调度入口。
- 根因/结论：原阻塞规则缺少统一协调归属，现已去重合并为更新记录和安全断点后向 aoo稽查报告，由其统一调度和验收。
- 修改范围：仅本文档“阻塞报告”。
- 验证结果：协调规则仅保留一条，岗位 AI 自行协调和越权修改均已禁止。
- 未完成项：无。

### 2026-09-08｜问题串行、状态记录与进度规则

- 用户要求：问题按顺序串行处理，明确插队指令才可中断，并持续记录状态、精简必要进度。
- 根因/结论：原规范缺少新问题排队、插队判定、状态同步及 Token 节制要求，现已归入既定章节。
- 修改范围：仅本文档“执行与验收”“开发记录”和“记忆”。
- 验证结果：规则已去重合并，七章顺序不变，“记忆”仍位于末尾。
- 未完成项：无。

### 2026-09-08｜越界问题报告与无头验收环境

- 用户要求：疑似误发或部分越界问题须暂停越界处理并交由 aoo稽查核对；岗位自行执行的浏览器验收统一使用任务专属无头 Chrome。
- 根因/结论：现有跨岗位阻塞规则未完整覆盖禁止执行、测试和自行转交，入口也缺少无头验收的隔离要求；现已去重归入既定章节并保持问题严格串行。
- 修改范围：仅本文档“修改边界”“执行与验收”“阻塞报告”和本开发记录。
- 验证结果：越界判断、部分越界依赖处理、稽查报告字段、CDP `1234`、独立临时 Profile、端口冲突协调、可见窗口授权例外及静态任务例外均已核对。
- 未完成项：无。

### 2026-09-09｜精简回答规范

- 用户要求：回答精简，不回复无用内容。
- 根因/结论：已与现有 Token 和进度规则去重合并，仅保留问题结论及必要信息。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：未新增章节或重复条款。
- 未完成项：无。

### 2026-09-09｜重读、清理已解决项并精简规范

- 用户要求：已处理且未再次要求的问题视为解决，从待解决清单删除，并精简本文档。
- 根因/结论：删除已修正的文档自引用待办；未把未入清单的已解决任务重复归档；大厅四向适配因用户多次反馈仍不正确，合并保留为 `HALL-04`。正文规则已去重压缩，历史开发记录保持不变。
- 修改范围：本文档第 1 至 7 章正文、待解决清单及本记录。
- 验证结果：七章及顺序完整，“记忆”仍为末章，不存在错误的 `大厅执行提示词.md` 必读引用。
- 未完成项：待解决清单现存 10 项。

### 2026-09-09｜客服号码点击复制

- 用户要求：点击 QQ、微信输入框范围复制对应号码，并提示“复制成功”。
- 根因/结论：两个区域已有 Button，但客服启用和停用分支均未绑定复制事件；现统一绑定整块区域到公共剪贴板服务和漂浮提示。
- 修改范围：客服复制控制器、客服两种运行分支及专项测试；未改 Prefab 布局和号码。
- 验证结果：2 项专项测试和 TypeScript 检查通过，Creator 已生成 `.meta` 并刷新 Preview。
- 未完成项：Preview 对独立无头浏览器返回 403，尚待真实 Canvas 点击复验。

### 2026-09-09｜客服复制提示资源更正

- 用户要求：复制后必须使用 `Common/Prefab/Common/Message_Drift.prefab` 显示气泡文字。
- 根因/结论：`UIMessage_Drift` 仍路由到旧 `games-common-prefab`；现已改为 `common-prefab/Common/Message_Drift`。
- 修改范围：公共 Prefab 路由与客服复制专项测试；未修改目标 Prefab。
- 验证结果：2 项专项测试及 TypeScript 检查通过。
- 未完成项：Creator 中 `Message_Drift.prefab*` 有未保存内容，不能安全重启同步；真实点击待同步后复验。

### 2026-09-09｜漂浮提示公共化与大厅验收断点

- 用户要求：所有界面的漂浮提示统一使用公共组件；每次调用逐条从预制体居中位置出现并向上滑。
- 根因/结论：大厅曾以页面代码覆盖 `Message_Drift.prefab` 的位置、尺寸和动画；公共框架 AI 已新增统一队列生命周期，并移除大厅专用覆盖。
- 修改范围：公共漂浮提示生命周期、登录和大厅注册入口及专项测试；未修改 `Message_Drift.prefab`。
- 验证结果：Creator 3.8.8 已导入新脚本并生成 `.meta`；9 项相关测试和 TypeScript 检查通过。
- 未完成项：CDP `1234` 下两个专属 Chrome 页面均被测试账号自动恢复到 `GameRoom2D`，返回大厅后又自动重入房间，无法稳定完成客服连续点击真实 E2E；安全断点为公共代码与 Creator 同步已完成，待稽查协调可停留大厅的测试会话。

### 2026-09-09｜客服连续点击漂浮提示 E2E 完成

- 用户要求：客服号码每点击一次产生一条提示，所有界面统一从公共预制体居中出现并依次上滑。
- 根因/结论：稽查释放 CDP 后使用大厅专属全新 Profile 和访客账号完成验证；连续两次真实点击分别生成两条排队提示，第二条在第一条结束后从 `(0,0)` 重置并独立上滑。
- 修改范围：本次仅完成浏览器验收、证据留存和记录更新；未追加业务代码改动。
- 验证结果：第二条起始状态为 `active=true, x=0, y=0, opacity=200, text=复制成功`，上滑状态为 `y=24.723, opacity=145.06`；截图为 `/tmp/aoo-message-drift-second-centered.png`、`/tmp/aoo-message-drift-second-sliding.png`。
- 未完成项：无。

### 2026-09-09｜最近创建玩法置顶

- 用户要求：创建房间玩法列表中，最近成功创建的玩法排在最上面。
- 根因/结论：原列表完全沿用目录顺序且无最近创建记录；现按账号保存最近建房成功的稳定 `gameCode`，打开创建房间时稳定移到首行并默认选中，其他玩法保持原顺序。
- 修改范围：统一玩法选择控制器及专项测试；未修改玩法目录、规则表、Prefab 或协议。
- 验证结果：8 项相关测试和 TypeScript 检查通过，Creator 3.8.8 已同步编译。
- 未完成项：CDP `1234` 当前被其他任务占用，等待 aoo 稽查协调后执行真实“创建玩法→返回大厅→重开列表”E2E。

### 2026-09-09｜最近创建玩法交用户复验

- 用户要求：代码处理完成后通知，由用户进行 Creator 实际创建和列表顺序测试。
- 根因/结论：实现、Creator 同步、专项测试和 TypeScript 检查均已完成，现按用户要求停止自动 E2E并交付复验。
- 修改范围：本次仅更新任务状态并清理大厅测试临时 Profile。
- 验证结果：编译产物已包含账号级最近玩法记录、置顶排序和建房成功写入逻辑。
- 未完成项：等待用户反馈 Creator 实测结果；未将用户尚未执行的复验标记为通过。

### 2026-09-09｜最近创建玩法置顶用户验收完成

- 用户要求：确认最近创建玩法置顶的 Creator 实测结果。
- 根因/结论：用户反馈“好了”，该需求按真实使用结果验收完成。
- 修改范围：本次仅更新任务状态。
- 验证结果：用户 Creator 实测通过；此前专项测试、TypeScript 检查和 Creator 同步均已通过。
- 未完成项：无。

### 2026-09-09｜最近创建玩法自动 E2E 环境更正

- 用户要求：用户自行执行 Creator 实测；稽查释放端口后曾要求补充独立无头 E2E。
- 根因/结论：大厅专属 Chrome 使用全新 Profile 连续两次启动后约 5 秒退出，首个错误为 `CVDisplayLinkCreateWithCGDisplay failed (-6670)`；按用户交付要求停止无效重试。
- 修改范围：未改业务代码；已清理本次临时 Profile并保留实现与 Creator 同步结果。
- 验证结果：静态测试、TypeScript 和 Creator 编译结果仍通过；无头浏览器未形成有效业务步骤，不冒充 E2E。
- 未完成项：等待用户 Creator 实测最近玩法置顶结果。

### 2026-09-09｜创建成功后缓存规则勾选

- 用户要求：创建房间的勾选项缓存到用户本地，但只有创建成功才保存；处理完成后交用户测试。
- 根因/结论：原缓存位于场景交接完成之后，成功进房时大厅控制器可能已销毁而跳过保存；现改为 Hall 服务端确认建房成功后、场景交接前保存规则快照，校验失败和建房请求失败均不保存。
- 修改范围：统一玩法选择控制器及既有规则缓存专项测试；未修改规则表、Prefab、协议或玩法实现。
- 验证结果：缓存继续按账号、玩法、玩法版本和规则哈希隔离；8 项相关测试、TypeScript 检查和 Creator Preview 同步通过。
- 未完成项：等待用户 Creator 实测“成功创建后再次打开恢复勾选；创建失败不覆盖缓存”。

### 2026-09-09｜创建房间节点命名与房间底分

- 用户要求：不改变 `Create_Room.prefab` 层级，规范节点命名并同步代码引用；`cent` 使用公共数字键盘输入房间底分，建房规则携带 `baseScore`，创建成功后随勾选项缓存。
- 根因/结论：用户新增节点最初仅在 Creator 未保存内存中；先由 Creator 正常保存，再只改节点名称和代码路径。底分默认 `1`、限制为 1 至 6 位正整数，确认后显示输入值并合入权威建房规则快照。
- 修改范围：创建房间 Prefab 节点命名、统一玩法选择控制器、规则模板引用及专项测试；未改父子层级、兄弟顺序、坐标、尺寸、视觉、规则表、服务端或玩法结算。
- 验证结果：Creator 3.8.8 已重新导入并重新打开，层级显示 `BaseScoreButton` 仍为 `MainPanel` 直接子节点；TypeScript 与 9 项专项测试通过；独立无头 Chrome 真实点击公共 `Numpad` 输入 `5` 后，键盘关闭且 `ValueLabel` 显示 `5`，截图 `/tmp/aoo-base-score-numpad.png`、`/tmp/aoo-base-score-confirmed.png`。
- 未完成项：服务端与玩法结算的“输赢数字 × baseScore”属于非大厅职责，本次仅完成客户端统一请求字段；等待用户实际创建房间确认服务端是否已支持该字段。

### 2026-09-10｜大厅宽屏四向适配与返回大厅修复

- 用户要求：继续完成 `Lobby_Main.prefab` 的 Top、Bottom、Left、Right 宽屏适配，并解决亲友圈退出返回大厅时的加载失败。
- 根因/结论：语义组内缩曾强制要求 `Player` 等容器自身具备 `UITransform`，而真实 Prefab 允许纯语义容器；同时只量父节点会遗漏子节点图片和文字的可见边界。现递归合并有效子节点的 `UITransform` 边界，无可测边界时保留 Prefab 原位置，不再中断大厅初始化。
- 修改范围：仅大厅运行时适配代码、专项测试和本文档；未改 `Lobby_Main.prefab` 的层级、命名、坐标或视觉属性。
- 验证结果：6 项专项测试通过；Creator 3.8.8 通过“清除代码缓存”正常热编译，Preview 编译块已包含空边界安全返回；独立 Chrome/CDP `1236` 在 1920×900 下真实访客登录进入 `MainScene`，`CreateRoom` 可见并点击打开 `Create_Room`，进入亲友圈入口后点击返回，`Lobby_Main` 与 `CreateRoom` 均恢复为 active，且无“加载失败”。证据：`/tmp/aoo-hall04-compiled.png`、`/tmp/aoo-hall04-create-click.png`、`/tmp/aoo-hall04-club-return.png`。
- 未完成项：账号 `105613` 的亲友圈成员数据由亲友圈任务清理；本大厅初始化与返回路径已通过。

### 2026-09-10｜大厅四向适配迁回 Prefab（更正）

- 用户要求：按最新命名与层级规范，把可由 Creator 组件表达的大厅适配迁回 `Lobby_Main.prefab`，删除运行时坐标和 Widget 覆盖。
- 根因/结论：上一条记录中的运行时递归边界修复虽能消除 `Player` 异常并通过宽屏验证，但违反“常规适配由 Prefab 表达”的最新规范。现由根节点居中 Widget、四向容器 Widget、语义组左右边距及 Right 容器 UITransform 统一承载布局；大厅脚本不再监听 resize、计算固定 `80` inset、移动节点或覆盖 Widget。
- 修改范围：`Lobby_Main.prefab` 的既有 Widget/UITransform 参数、大厅运行时重复适配代码、专项测试和本文档；未改变节点增删、命名、父子层级、兄弟顺序、业务事件或亲友圈文件。
- 验证结果：Prefab JSON/引用解析和 6 项专项测试通过；Creator 3.8.8 正常导入并重新打开 `Lobby_Main.prefab`，清除代码缓存后 Preview 编译块已不包含旧适配方法；CDP `1236` 使用同一 Preview 完成 1920×900 宽屏、1280×720 桌面和 844×390 移动横屏验证，三种尺寸均进入 `MainScene`、四向节点位置稳定且无控制台错误，移动触摸 `CreateRoom` 成功打开 `Create_Room`。证据：`/tmp/aoo-hall04-prefab-wide.png`、`/tmp/aoo-hall04-prefab-desktop.png`、`/tmp/aoo-hall04-prefab-mobile.png`、`/tmp/aoo-hall04-prefab-mobile-create.png`。
- 未完成项：无。

### 2026-09-10｜NJ201 建房 Hall 500 定位（阻塞）

- 使用大厅专属 CDP `1236` 从真实 Canvas 依次点击创建房间、`NJ201`、创建按钮，Hall 返回 `500`；证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-last-hand-autoplay-2026-09-09T23-44-23-581Z`。
- Hall 日志首错 trace `b8f420a3-38f6-4db2-b7c4-c932c2a336b5` 为 `playerCount is required`。
- `Server/work/generated/room-rules/内江跑得快.generated.json` 的权威发布字段不含 `playerCount`，客户端只能提交已发布字段；未在前端硬编码人数，也未修改只读规则表，已转规则发布/服务端责任处理。

### 2026-09-10｜玩法规则变更主动同步

- 用户要求：所有岗位完成玩法规则相关修改或确认后，主动向固定“Aoo 文档”任务提交结构化报告。
- 根因/结论：已写入固定任务ID、报告字段和无法直发时的稽查转发路径。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜正式文档同步范围扩展

- 用户要求：将主动同步从玩法规则扩大到全部需要新增或更新正式文档的变更。
- 根因/结论：已合并覆盖架构、组件、接口、数据库、资源、场景、构建、验收、业务、规则、配置、迁移和重要修复；纯内部无外部影响的重构仅记岗位记录。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜aoo产品更名与点对点协作更正

- 用户要求：原“aoo稽查”更名为“aoo产品”；职责明确时直接点对点协作，明确文档事项直发Aoo文档，仅例外情形上报aoo产品。
- 根因/结论：已替换旧泛化上报规则；历史记录旧称保留为当时名称。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变。
- 未完成项：无。

### 2026-09-10｜业务域全栈职责更正

- 用户要求：登录大厅岗位同时负责登录、账号、大厅客户端及 `Account`、`Hall`、`Gateway` 相关接口，并跨数据、配置、部署和真实 E2E 闭环。
- 根因/结论：旧规范排除登录、数据库和协议，现已以登录大厅业务域全栈边界替换。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜本地测试账号隔离更正

- 用户要求：登录大厅岗位仅使用本地账号`21`至`30`，密码为`1`；不得占用其他岗位账号，预留账号须经aoo产品分配。
- 根因/结论：已加入岗位账号段、并行换号、实际账号留证和跨岗位账号/数据禁止操作规则。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：账号创建结果需通知Aoo文档后补入正式测试环境文档。

### 2026-09-10｜本地测试账号初始化完成更正

- 用户要求：补充本地账号初始化结果并通知Aoo文档归档。
- 根因/结论：用户名`21`至`100`已通过Account正式注册流程幂等初始化，`accountId = username + 549`；可审计脚本具备loopback和环境确认双门禁。
- 修改范围：仅本文档开发记录；正式环境说明由Aoo文档维护。
- 验证结果：抽样账号登录/session成功，二次执行`80/80 alreadyExisting`且不覆盖。
- 未完成项：无。

### 2026-09-10｜本地测试账号最终验收补充

- 用户要求：补充账号`21`至`100`逐个真实登录/session及客户端登录证据。
- 根因/结论：80个账号均为正式注册的数字`ALIAS`非guest账号，不复用旧guest行。
- 修改范围：仅本文档开发记录；正式环境说明由Aoo文档同步。
- 验证结果：真实HTTP结果`verified=80`、`failed=0`；CDP`1236`使用账号`21`进入`MainScene`且控制台无错误。
- 未完成项：无。

### 2026-09-10｜持续执行与idle条件更正

- 用户要求：补齐登录大厅岗位及全部岗位的持续执行和明确停止条件。
- 根因/结论：有已授权可执行任务时必须持续执行；当前项阻塞时点对点联系并继续独立任务；仅全部完成或全部等待决定、授权、外部状态时可idle。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜停止报告强制更正

- 用户要求：任何停止、idle、异常结束或准备停工都必须向aoo产品提交七项停止报告，普通阶段进度仍无需报告。
- 根因/结论：已删除“正常idle无需报告”的冲突口径，并补齐产品审核后续作或保持停止的门禁。
- 修改范围：仅本文档当前规范、开发记录和记忆。
- 验证结果：七章顺序不变，历史记录未改写。
- 未完成项：无。

### 2026-09-10｜任务转交先入队与字段清单标准化

- 用户要求：所有新任务与转交任务在处理前先写入“记忆”待处理队列。
- 根因/结论：新增“任务摘要、来源、状态、职责范围、安全断点”入队字段清单；完成任务后移除，阻塞或停止时保留队列状态，避免任务丢失与重复认领。
- 修改范围：仅本文档“执行与验收”和“开发记录”。
- 验收结果：七章顺序未变，历史记录未改写，未引入代码或配置改动。
- 未完成项：无。

### 2026-09-10｜Lobby Code 冗余清理（编辑器同步阻塞）

- 用户要求：`Client/assets/Lobby/Code` 仅保留当前 Lobby 实际运行使用的代码；删除前须核对 Creator 序列化、组件注册、动态入口、协议入口和真实流程。
- 根因/结论：`LegacyLobbyProductionBridge.ts` 为旧的“服务暂未接通”占位实现；源码、场景/Prefab UUID、动态加载路径和测试均无引用，当前入口已由 `AooLobbyBridge` 实例化正式控制器，故已连同 `.meta` 删除。`LegacyLobbyScreen.ts` 的 6 条 `LobbyMountTrace` 仅输出挂载阶段日志，已移除。
- 修改范围：`Client/assets/Lobby/Code/Runtime/LegacyLobbyScreen.ts`，删除 `Client/assets/Lobby/Code/Runtime/LegacyLobbyProductionBridge.ts` 及其 `.meta`；未改场景、Prefab、资源路径、接口、协议或登录场景布局。
- 验证结果：全量 Lobby TypeScript 语法检查通过；`legacy-lobby-production-bridge`、大厅层级独立性等 14 项专项测试通过。另 3 项历史大厅测试因缺失旧 Prefab 路径或既有断言失败，与本次文件无引用关系。
- 未完成项：Creator 3.8.8 正在由用户操作且存在未保存 Prefab，不能安全触发资源刷新或真实 Canvas 验收；待用户操作结束后仅需确认资源数据库不再注册删除 UUID，并在 Preview 登录进入大厅复验。

### 2026-09-10｜Lobby Code 职责归档

- 用户要求：Lobby 代码只保留大厅职责；跨域功能移动源文件与原 `.meta` 到准确目录，禁止复制、兼容转发或兜底目录。
- 根因/结论：房间恢复由登录、Lobby 和游戏共享，已迁入 `Common/Code/Runtime/room`；房间网络挂载封装公共房间、重连和语音，已迁入 `Games/Common/Code/Room`。Hall 房间网关虽被跨域消费者使用，仍是大厅权威目录和票据边界，按框架确认保留 Lobby。Common 下 6 个无消费者的 Lobby 转发兼容文件已删除。
- 修改范围：移动上述 2 个源文件及其 `.meta` 并更新 Login/Lobby 导入；删除 `Common/Code/Runtime/CompatibilityApp/lobby` 空目录及 6 组转发文件/`.meta`；未改 Atlas、Prefab、Scene、接口协议或登录布局。
- 验证结果：移动后 TypeScript 语法检查通过，原 UUID 保持不变，旧生产源码路径扫描仅剩登录大厅 AI 正在同步的专项测试；大厅专项静态测试 12 项通过。
- 未完成项：等待登录大厅 AI 更新共享的 `login-lobby-flow` 旧测试路径；Creator 用户未保存 Prefab 期间不能刷新或做真实 Canvas 验收。

### 2026-09-10｜RoomNetworkControllerMount 归属更正

- 用户要求：按唯一实际生产入口确定代码归属，不得为消除跨目录 import 反向迁移。
- 根因/结论：`RoomNetworkControllerMount` 虽复用公共房间控制器，但唯一实例生命周期和专项测试均在 Lobby，按最新裁定回迁 `Lobby/Code/Runtime`；仅保留实际入口路径，不保留 Games/Common 副本或转发层。
- 修改范围：回迁 `RoomNetworkControllerMount.ts` 与原 `.meta`，恢复 `LegacyLobbyScreen` 和大厅专项测试的 Lobby 路径引用；UUID `6c781429-eb15-4485-8d89-5e36ac6afa03` 保持不变。
- 验证结果：TypeScript 语法检查及旧路径静态扫描通过；`lobby-ui-v05` 仍有 4 项既有缺失 Prefab/过期事件断言失败，未由本次回迁引入。
- 未完成项：等待登录大厅 AI 更新共享的 `login-lobby-flow` 中 RoomRecoveryStore 旧路径；Creator 用户未保存 Prefab 期间不能刷新或做真实 Canvas 验收。

### 2026-09-10｜Lobby Code 职责归档测试路径更正

- 更正原因：此前记录的 `login-lobby-flow` 共享测试路径等待项已在登录大厅任务释放文件后完成更新。
- 修改范围：仅将该测试的 `RoomRecoveryStore` 读取路径改为 Common 的最终归档位置。
- 验证结果：迁移关联 24 项测试通过，旧路径扫描为零；其中 AYCP 资源断言 1 项和大厅 UI 既有 4 项失败与本次无关。
- 未完成项：仅待 Creator 用户未保存 Prefab 结束后完成资源数据库同步与真实 Lobby Canvas 验收。

### 2026-09-10｜Lobby 生产调试日志清理

- 用户要求：清理当前 Lobby 实际运行代码中的确认无业务行为的临时调试实现。
- 根因/结论：创建房间流程的未门禁 `console.info`/`console.warn` 仅输出规则快照、点击及状态跟踪；大厅目录加载的 `console.warn` 亦未参与业务分支。现删除日志并将无用 catch 参数移除，保留业务提示、重试、错误处理和 `console.error` 失败诊断。
- 修改范围：`UnifiedPlaySelectorController.ts`、`LegacyLobbyScreen.ts`；未改规则、接口、请求、交互、场景或 Prefab。
- 验证结果：全量 Lobby TypeScript 语法检查通过；创建房间偏好、底分、最近玩法和登录大厅专项 23 项通过。余 2 项为既有 AYCP 资源断言与统一玩法旧入口断言失败。
- 未完成项：Creator 当前仍显示用户编辑的 `SmallSettlement.prefab`，不能安全刷新资产数据库或执行真实 Lobby Canvas E2E。

### 2026-09-10｜BYZP 失效大厅依赖清理

- 用户要求：只保留当前 Lobby 实际使用代码；失效玩法实现无替代时移除 Lobby 的失效依赖和不可达分支，不伪造或恢复旧玩法包。
- 根因/结论：`ByzpSwitchCoordinator` 的 Pack01 路径不存在，项目仅保留字牌通用网络适配器和旧配置；Lobby 无可运行的 BYZP 入口实现。已删除其 import、字段、实例、handoff 分支和销毁分支。
- 修改范围：仅 `LegacyLobbyScreen.ts`；未改字牌 Catalog、规则、资源、协议或玩法实现。
- 验证结果：TypeScript 语法检查通过；Creator 3.8.8 正常导入，MainScene 控制台 Error=0；Preview `127.0.0.1:7456` 返回 200。已通知 Games 公共代码 AI 从创建房间 E2E 断点续验。
- 未完成项：Games/Common 的 BYZP Catalog `enabled` 与无实现的发布不一致待字牌/发布责任域单独处理；本大厅任务不越权修改。

### 2026-09-10｜剩余 Pack01 字牌失效依赖批量清理

- 用户要求：按实际运行实现清理失效 Lobby 依赖，避免逐个模块缺失阻塞 Preview。
- 根因/结论：`pxphz`、`xpphz`、`glzp`、`ychp`、`ahphz`、`lczp` 的 Pack01 Coordinator 均不存在；一次删除全部 Lobby import、字段、实例与 handoff 链，未改字牌 Catalog 或玩法。
- 修改范围：仅 `LegacyLobbyScreen.ts`。
- 验证结果：LegacyLobbyScreen TypeScript 语法通过；全 Client assets 的静态相对 import 解析 `missing=0`；Preview `127.0.0.1:7456` 返回 200。
- 未完成项：Creator MainScene 出现独立的 LoginScreenBootstrap UUID 缺失/无效错误，源 `.ts/.meta` 和场景绑定仍存在，已转登录/框架责任域；Games 公共代码 AI 继续保留 PDK E2E 断点。

### 2026-09-10｜现存 Creator 资产唯一权威规则

- 用户要求：所有资源与节点仅以当前磁盘和 Creator 中现存状态为准；删除即废弃，不得从任何历史来源恢复、复制、重建或兼容。
- 根因/结论：历史资源不能作为当前实现依据；遗留引用应删除，新增需求仅能由用户在 Creator 中创建后再由代码绑定。
- 修改范围：仅本文档“修改边界”和“记忆”。
- 验证结果：已与既有最小补丁、用户维护 Creator 布局及资源同步边界合并，无重复章节。
- 未完成项：无。

### 2026-09-10｜废弃动画与材质边界补充

- 用户要求：删除的动画、材质与 Prefab 资源均为废弃；不得恢复，未来仅在用户明确提出时重新制作。
- 根因/结论：已将既有资产权威规则扩展到动画和材质；现有 Prefab 的失效序列化引用只能报告，未经授权不补建。
- 修改范围：仅本文档“修改边界”和“记忆”。
- 验证结果：与现存 Creator 资产唯一权威规则合并，无重复条款或新增章节。
- 未完成项：无。

### 2026-09-10｜PracticeGlow 废弃引用清理

- 用户要求：已删除的 `PracticeGlow` 不需要恢复或替代，删除其查找、校验和控制逻辑。
- 根因/结论：当前 `Lobby_Main.prefab` 只有 `Practice`，运行时代码仍把 `PracticeGlow` 当作强制语义节点，导致大厅挂载失败；现已完全移除该过期引用，不保留可选兼容分支。
- 修改范围：`LegacyLobbyScreen.ts`、大厅层级独立性专项测试及本文档；未修改任何 Prefab 节点、名称或层级。
- 验证结果：专项测试 7/7 通过；Creator 热编译后真实账号登录导航完整到 `LOBBY_PRESENTED`，MainScene 大厅显示成功。
- 未完成项：无。

### 2026-09-10｜代码分类与职责目录落库

- 用户要求：按实际使用域归类代码，禁止 A 类功能进入 B 类目录并通过兼容路径掩盖。
- 根因/结论：`Code` 目录边界需统一为模块专用与通用公共目录，杜绝转发层与兜底目录；违规引用应先纠正后再继续验收。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验收结果：规则落库，无代码更改；后续代码移动需按该约束自检。
- 未完成项：无。

### 2026-09-10｜身份判断权威字段统一

- 用户要求：身份判断统一读取服务端 `user_type`，不得根据 `ppk` 或展示、格式特征推断。
- 根因/结论：登录与大厅仅消费 `0=普通玩家`、`1=陪玩`、`2=机器人` 的服务端权威值，不得伪造或建立平行来源。
- 修改范围：仅本文档“修改边界”和本开发记录。
- 验证结果：规则已归入既定章节，七章顺序保持不变；未修改代码、场景或资源。
- 未完成项：无。

### 2026-09-10｜Lobby Code 归档同步与 CD201/LS201 复验

- 用户要求：Creator 未保存冲突解除后，继续完成 `Lobby/Code` 职责归档的编辑器同步与真实大厅、房间流程验收。
- 根因/结论：Creator 当前标题无未保存标记、脚本编译完成且控制台 Error=0；刷新前 CD201 与 LS201 均完成真实登录、进入大厅、创建、加入、准备和解散清理。Creator 最新刷新后，两个玩法均因 `games-common-prefab/config.json` 与 `index.js` 返回 404 而停在创建后的资源加载阶段，责任已点对点转 Games 公共资源与框架任务。
- 修改范围：仅登录大厅岗位记录和 E2E 脚本的点击坐标组件兼容、房间流程专项断言及安全清理等待；未修改大厅生产代码、玩法代码、Prefab、规则或服务端。
- 验证结果：刷新前 CD201 房间 `568667`、LS201 房间 `369378` 的 `loginLobbyCreateJoin`、`noRuntimeErrors`、`roomCleaned` 均为 true；刷新后阻塞证据为 `pdk-small-settlement-background-2026-09-10T10-24-26-570Z/report.json` 与 `pdk-small-settlement-background-2026-09-10T10-25-35-416Z/report.json`，测试房间均已清理。
- 未完成项：等待 `games-common-prefab` 恢复 Preview 发布后，用账号 21/22、23/24 和 CDP 1236 完成最终复验。

### 2026-09-10｜CD201/LS201 最新 Preview 阻塞更正

- 更正原因：框架恢复 `games-common-prefab` 为 HTTP 200 后再次真实复验，加载已越过 Bundle 阶段，原 404 不再是当前阻塞。
- 根因/结论：CD201 与 LS201 均在加载页 35% 明确提示 `GameRoom2D` 未进入当前 Preview 的 build settings；磁盘场景及正式 builder 配置存在，当前 `/assets/main/config.json` 仅发布 Boot_Strap 与 Login_Scene，属于 Preview 发布陈旧。Creator 此时打开且未保存 `ClubDesk.prefab`，不得刷新覆盖亲友圈任务现场。
- 修改范围：仅 E2E 复验、证据和本记录；未改大厅生产代码、场景、Bundle、构建配置或亲友圈 Prefab。
- 验证结果：CD201 证据为 `pdk-small-settlement-background-2026-09-10T10-30-12-039Z/desktop-failure.png`，LS201 证据为 `pdk-small-settlement-background-2026-09-10T10-31-20-342Z/desktop-failure.png`；专属 CDP 1236 与临时 Profile 已清理。
- 未完成项：等待亲友圈任务保存或关闭 `ClubDesk.prefab` 后，由 Creator 正常刷新 Preview；确认 `main/config.json` 发布 `GameRoom2D` 后继续最终复验。

### 2026-09-10｜最终复验按产品要求暂停

- 用户要求：亲友圈桌子预制体样例优先，登录大厅立即保存断点并暂停 Creator、Prefab、Atlas、Bundle、共享代码写入和真实 E2E。
- 根因/结论：暂停前已由 Creator 正常执行一次刷新 Preview；进一步确认 `GameRoom2D` 属于现存 `games-common` Bundle，不应位于 main，当前 `games-common/config.json`、`index.js` 均为 200，配置包含场景名和 UUID。未开始新的 CD201/LS201 浏览器测试。
- 修改范围：仅本岗位状态记录；未修改生产代码、Prefab、场景、Atlas、Bundle 元数据或亲友圈资源。
- 验证结果：当前没有运行中的 CDP 1236 或 E2E 进程；Creator 停留 MainScene，未执行 Bundle 构建确认按钮。
- 未完成项：等待 ao产品明确恢复后，从当前 Preview 的 CD201/LS201 CreateButton 双端复验断点继续，若仍失败则采集 `games-common` 加载时序、实际 ticket、控制台和网络证据。

### 2026-09-10｜账号登录 EditBox 输入链修复安全断点

- 用户要求：修复账号登录真实输入链，完成账号 21 至 24 的 CD201/LS201 双端真实 E2E，严禁改写用户维护的 `Login_Scene`。
- 根因/结论：登录按钮绑定完整，首错是面板激活后 `AccountInputInput`/`PasswordInputInput` 的 `cc.EditBox.enabledInHierarchy=false`且 `_impl._edTxt=null`。已在场景外 `Client/assets/Login/Code/Bootstrap/LoginScreenBootstrap.ts` 与 `Client/assets/Login/Code/Auth/LoginView.ts` 中启用两个 EditBox，并延帧 `focus`。
- 修改范围：仅登录场景外 TypeScript 代码与登录单测；`Login_Scene`、Prefab、Atlas、Bundle、玩法代码与规则表均未修改。
- 验证结果：登录单测新增 EditBox 启用与延帧聚焦约束并通过；源码已落盘。
- 未完成项：Creator 3.8.8 当前打开 `ClubDesk.prefab*` 且有未保存内容，不得强制 Reload/关闭；Preview 仍运行旧 `openAccountDialog`，最新脚本尚未安全刷新。待 Creator 释放后正常触发脚本重编译，再依次执行 CD201（21/22）、LS201（23/24）创建、加入、准备与清理真实复验。安全证据断点为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-small-settlement-background-2026-09-10T12-51-43-744Z/report.json`。

### 2026-09-10｜登录 EditBox 运行时根因进一步更正

- 更正原因：初步记录只标识 `enabledInHierarchy=false` 与 `_edTxt` 缺失；进一步定位确认迁移场景中两个 EditBox 的 `_impl` 整体为 `null`，说明隐藏父节点反序列化时 Creator 未执行 EditBox 运行时预加载，单纯 `enabled=true` 无法创建 Web DOM。
- 根因/结论：`LoginScreenBootstrap.ts` 新增一次性 `ensureEditBoxRuntime`，仅在 `_impl` 缺失时初始化现有组件，然后交由 `LoginView.ts` 完成启用周期与延帧聚焦。
- 修改范围：仅场景外登录代码与单测；`Login_Scene`、Prefab、Atlas、Bundle、Library 和其他业务代码未修改。
- 验证结果：源码与单测已落盘；由跑得快岗位在安全 Creator 窗口使用账号 `47` 执行刷新复验。
- 未完成项：共享 Creator 仍打开未保存的 `ClubDesk.prefab*` 且 watcher 未刷新，不得强制 Reload；等待跑得快岗位回传账号 `47` 真实复验结果。

### 2026-09-10｜登录 EditBox 销毁残留对象更正

- 更正原因：进一步运行时证据显示 `AccountInput` 节点有效，但其序列化 EditBox 为销毁残留对象，状态为 `editValid=false`、`nodeValid=true`、`hasPreload=false`、`hasInit=false`、`_impl/_edTxt` 空；因此不能只按“有效组件缺少 `_impl`”处理。
- 根因/结论：`LoginScreenBootstrap.ensureEditBoxRuntime` 在组件失效但节点有效时，仅于运行时移除残留对象并在原节点添加 EditBox，恢复原文字/占位标签及输入参数；组件有效时仍执行 `_impl`/Web DOM 一次性初始化。
- 修改范围：`LoginScreenBootstrap.ts`、`LoginView.ts` 与登录针对性单测；`Login_Scene`、Prefab、Atlas、Bundle、Library 与玩法代码未修改。
- 验证结果：登录针对性单测通过；最新无状态变化的 Preview 证据仍复现同一聚焦首错，未冒充真实验收通过。
- 未完成项：等待 `ClubDesk.prefab*` 保存并释放 Creator，确认 Preview chunk 为最新恢复版后只跑一次账号 `21` 登录，成功后再执行 CD201（21/22）与 LS201（23/24）全流程；无状态变化时禁止重复 E2E。最新证据为 `Client/docs/前端框架规范/历史记录与证据/证据/pdk-small-settlement-background-2026-09-10T13-33-41-608Z/report.json`。

### 2026-09-11｜禁止非必要硬编码

- 用户要求：代码不到万不得已不能硬编码，并写入登录大厅岗位规范。
- 根因/结论：启动遮罩 Logo 曾在运行时代码和多端构建模板重复硬编码，导致场景节点删除后旧视觉仍被创建；现要求资源、路由、节点、文案、尺寸和超时等统一读取权威配置或集中映射。
- 修改范围：仅本文档“修改边界”“执行与验收”“开发记录”和“记忆”。
- 验收边界：无法避免的字面量必须集中为唯一常量，说明原因、边界和移除条件，不得在多端重复维护。
- 未完成项：无。

### 2026-09-11｜Preview 启动遮罩溢出与 Logo 单一化

- 用户要求：登录 Preview 不得在正常界面下方残留黑底启动界面；启动遮罩使用现存 `logo_jiujiu.png`，不恢复 `Login_Scene` 已删 Logo 节点。
- 根因/结论：Preview 不加载 Web 发布模板 CSS，运行时追加的 `#aoo-startup-cover` 因而进入 body 普通文档流并产生纵向溢出。现由启动遮罩组件自行注入 fixed 定位样式，Logo 仅由该组件的单一资源映射创建，多端模板不再重复资源路径。
- 修改范围：`PresentationTransitionCoordinator.ts`、Web 桌面/移动构建模板的遮罩样式、专项测试和本记录；未修改 `Login_Scene`、Prefab、Meta 或其他视觉节点。
- 验证结果：Creator 3.8.8 已热编译；CDP `1236` 真实 Preview 中桌面 `1280×720` 和 iPhone 14 Pro `393×852` 均只有一个 Canvas/一个遮罩/一个 Logo，遮罩显示时固定覆盖视口，进入 `Login_Scene` 后 `hidden=true`，body 高度等于视口高度，无纵向溢出。
- 未完成项：启动过渡旧测试仍有与本修复无关的 Preview 模板断言和 `transitionShowDelay` 断言过期；本次新增的单一 Logo 和 fixed 定位专项断言已通过，过期项未越界修改。

### 2026-09-11｜MainScene 进厅卡住修复

- 用户要求：解决登录后停在“正在准备目标界面”、无法进入大厅。
- 根因/结论：`MainScene` 顶层 UI 节点实际名为 `should_hide_in_hierarchy` 但持有有效 `cc.Canvas`，旧代码硬编码按节点名 `Canvas` 查找导致误判；修正后又复现 Creator Preview 已启动 MainScene 但偶发不回调 `loadScene`，导航 Promise 悬挂至遮罩超时。现改为按 `Canvas` 组件解析容器，并以 `EVENT_AFTER_SCENE_LAUNCH` 作为权威完成信号、原回调作为错误和兼容通道。
- 修改范围：仅 `SceneRouter.ts`、登录大厅流程专项测试和本记录；未修改 MainScene、Login_Scene、Prefab、Meta 或大厅布局。
- 验证结果：Creator 3.8.8 热编译后，CDP `1236` 使用账号 `21` 真实登录并重载自动恢复，导航完整到达 `LOBBY_MOUNT_START` 和 `LOBBY_PRESENTED`；运行树已挂载 `LobbyMain`，启动遮罩 `hidden=true`。专项断言 1/1 通过，证据为 `login-preview-residual-2026-09-11/lobby-reload-runtime.json` 和 `lobby-enter-success.png`。
- 未完成项：无。

### 2026-09-11｜大厅亲友圈列表迁移

- 用户要求：将创建、加入及亲友圈列表入口迁到 `Lobby/Prefab/LobbyClubList.prefab`，业务代码归入大厅目录，并删除新 Prefab 不存在节点的旧逻辑。
- 根因/结论：旧入口控制器同时承担已废弃 `ClubNone`、旧列表、顶部信息、翻页和额外字段渲染，无法适配大厅新列表；现按新 Prefab 的 Back、List/Item、Pin、Enter、Join、Create 契约拆分大厅列表控制器与网关，创建仍复用独立创建弹窗，加入统一解析为公共 `Numpad`。
- 修改范围：亲友圈入口控制器及 Meta 从 `Club/Code/Runtime` 迁至 `Lobby/Code/Runtime/ClubList`，同步调整大厅入口、Prefab 注册表、公共键盘根节点解析和专项测试；未改 `LobbyClubList.prefab` 层级或视觉属性。
- 验证结果：Creator 3.8.8 已刷新 Preview，控制台 Error=0；专项测试 6/6 通过，迁移相关 TypeScript 无新增错误；CDP 1236 使用账号 21 真实点击验证列表打开、创建弹窗、公共加入键盘、逐层关闭及返回大厅均成功，未提交创建或加入请求。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/lobby-club-list-migration-2026-09-11`。
- 未完成项：无。

### 2026-09-11｜Lobby 模块代码语义归位

- 用户要求：先按当前功能语义重命名并将 `Lobby/Code` 中的模块代码移动到 `Modules` 对应目录，本轮不验证功能是否联通。
- 根因/结论：活动、社交、开房、资料、排行榜、战绩、奖励、商城和赛事控制器均已拥有明确的 Modules/Prefab 责任域，不应继续由 Lobby 目录承载；文件名及导出类已去除失效的 `Legacy`、`Aoo`、`Unified`、`Authoritative` 前缀。
- 修改范围：共迁移并重命名 16 个 TypeScript 文件及原 Meta，新增或复用各模块 `Code` 目录，更新 `AooLobbyBridge.ts`、`LegacyLobbyScreen.ts`、模块内部 import 与对应测试静态路径；清理已空的 Lobby 子目录及目录 Meta。支付、定位、隐私、报告和大厅核心代码未擅自归类。
- 验证边界：仅核对旧生产代码路径、旧类名和测试静态路径无残留；按用户要求未执行 TypeScript 编译、Creator 刷新、Preview 或业务 E2E，功能联通状态待后续明确要求再验收。
- 未完成项：无。

### 2026-09-11｜Lobby 剩余代码扁平化与死实现清理

- 用户要求：执行剩余 Lobby 代码审计结论，取消多余 `Runtime` 层，清除确定无效实现并按职责归位。
- 根因/结论：支付控制器仅监听全项目无人触发的事件，隐私控制器仅被创建和销毁，均为无入口装配；定位中的牌桌风险监听以及举报中的静音、GPS 分支同样无触发方。大厅核心代码现平铺在 `Lobby/Code`，亲友圈三文件保留 `ClubList` 聚合目录。
- 修改范围：删除 Lobby Billing/Privacy 控制器及装配；定位控制器移除死分支并改名；Lobby Screen、Module Coordinator、Session、Settings、TopBar、JoinRoom 全部语义化改名并保留 Meta；RoomSafety 迁入 `Modules/Support/Code/RoomSafety`，RoomNetworkControllerMount 迁入 `Games/Common/Code/Room`；同步生产引用与相关测试路径。
- 验证结果：旧 Runtime 路径、旧类名和已删除事件仅剩专项“确认旧文件不存在”的断言；TypeScript 静态检查未发现本次迁移文件的缺失模块或类型错误。按本阶段边界未执行 Creator 刷新和功能 E2E。
- 未完成项：待后续功能联通阶段执行 Creator 资源同步和真实交互回归。

### 2026-09-11｜登录启动场景无下划线命名索引同步

- 用户要求：场景文件已去掉下划线，代码与构建索引同步改名。
- 修改范围：`BootStrap.scene`、`LoginScene.scene` 的资源名，登录启动/导航代码，Creator 构建场景列表，相关测试与校验脚本；未调整登录 UI 层级和视觉属性。
- 验证结果：活动源码、配置、脚本和测试中旧名引用为 0；两个场景 UUID 保持不变；启动与输入框布局定向测试 7/7 通过；Creator 3.8.8 资源管理器显示 `BootStrap`、`LoginScene`，重新导入后的层级根节点显示 `LoginScene`。

### 2026-09-11｜赠送房卡窗口业务等价移植

- 用户要求：只读参考 XQP `HomeGiveCardWindow.prefab`，将赠送房卡业务等价移植到 Aoo `Modules/GiftRoomCard`；不得复制旧代码，节点按命名规范重命名且层级不变。
- 修改范围：将 Aoo 已有同源视觉 Prefab 与独立赠送控制器归位到 `GiftRoomCard` Bundle，重新实现玩家 ID/数量输入、公共数字键盘、正式赠送 API、防重复提交、历史流水、收件轮询和资源路由；未写入 XQP。
- 验证结果：源与目标 61 个节点的父子拓扑、兄弟顺序完全一致；目标无 XQP 自定义组件、命名违规或缺失资源 UUID；专项测试 2/2 通过；Creator 3.8.8 正常重导入并识别 `gift-room-card-ui`，单 Bundle 构建成功（2 秒）。

### 2026-09-11｜大厅创建亲友圈确认按钮修复

- 用户要求：修复创建亲友圈弹窗点击“确定”不生效、无法创建。
- 根因/结论：新 `LobbyClubList.prefab` 中确认按钮实际路径为 `CreatecCub/Create/Confirm`，迁移代码仍按 `CreatecCub/Confirm` 绑定，节点查询返回空且点击监听未安装；现改为按现存层级绑定 `Create/Confirm`，创建请求、成功提示及进入亲友圈流程保持不变。
- 修改范围：仅 `LobbyClubEntryController.ts` 的确认按钮路径、对应路径契约测试和本记录；未修改 Prefab 层级、视觉资源或服务端协议。
- 验证结果：创建名称任意字符与大厅亲友圈路径契约专项测试 3/3 通过；Creator 3.8.8 已刷新预览，编译产物 source map 已包含新路径且控制台无新增 Error。独立无状态 Chrome 因历史 E2E 登录助手仍等待旧场景名 `Login_Scene` 而未完成创建提交，该失败与本次按钮绑定无关，未冒充业务全链路通过。

### 2026-09-11｜登录后大厅挂载超时修复

- 用户要求：处理登录后停在“正在准备目标界面”而无法进入大厅的问题。
- 根因/结论：`club.CClubInvited` 在当前环境返回 `3008 club list failed`，大厅会话将邀请、联盟通知与当前房间查询放在同一失败事务中，导致可选数据失败中断整个大厅挂载；现仅将当前房间作为必需数据，可选请求失败降级为空，同时将场景切换超时改为按最近进度续期。
- 修改范围：大厅会话启动、场景导航进度、统一过渡超时及对应测试；未修改登录场景布局、Prefab 层级或服务端协议。
- 验证结果：Creator 3.8.8 正常重编译后，独立无头 Chrome 使用账号 `30` 真实点击登录，导航到达 `LOBBY_PRESENTED`，`scene-navigation` 正常 `commit`，控制台错误为 0；专项测试 3/3 通过。

### 2026-09-11｜亲友圈列表进出 3008 提示修复

- 用户要求：修复进入、退出亲友圈列表时显示 `3008: club list failed`。
- 根因/结论：当前服务用错误码 `3008` 表示账号没有亲友圈记录；大厅启动还遗留了无人消费的邀请/联盟查询，列表网关又把空列表状态直接当错误展示。现删除无效启动查询，并在列表网关将 `3008` 映射为空列表，其他错误继续上报。
- 修改范围：`LobbySessionService.ts`、`LobbyClubListGateway.ts`、专项测试及本记录；未修改 Prefab、登录场景和服务端协议。
- 验证结果：Creator 3.8.8 正常重编译后，独立无头 Chrome 使用账号 `30` 真实点击 `JoinClub` 和 `Back`，控制台错误为 0，界面未显示 `3008`；导航保持 `LOBBY_PRESENTED`。专项测试 4/4 通过。

### 2026-09-13｜大厅扩展按钮与 2.2.2 界面接通

- 用户要求：接通大厅更多面板及底栏的设置、举报、玩法、建议、绑定、推广绑定、公告、实名、签到、战绩、分享和商城入口；当前 `Modules` 缺少的界面只参考 2.2.2 业务语义等价移植并按模块归档。
- 根因/结论：帮助和商城缺少独立 Prefab，迁移后的顶部导航、商城滚动条及战绩页仍存在跨 Bundle 或失效子资源 UUID；实名、签到又在显示弹窗前等待接口，接口 404 时表现为按钮无效。现已补齐 Help、Store、Navigation Bundle，图片归入各自 Atlas，入口统一由大厅控制器路由；实名与签到改为先显示界面再异步加载数据。
- 修改范围：大厅按钮路由和模块协调器，Help/Store/Navigation 的 Prefab、Atlas 与控制器，Profile/Activity/Records 的显示与资源引用，以及迁移和真实点击验收脚本；未修改大厅 Prefab 层级和视觉布局。
- 验证结果：Creator 3.8.8 已自动重导入；资源门禁通过（560 个 Scene/Prefab、20332 个资产与 Meta、28651 个 UUID，阻断项 0）；iPhone 14 Pro Preview 对 12 个入口逐一真实点击，全部显示对应界面，帮助、商城、战绩缺图错误为 0；Web Mobile 阶段构建成功。绑定、实名、签到、商城数据接口在当前环境仍返回服务端 404，但不再阻断弹窗显示。
- 未完成项：服务端补齐上述生产数据接口后，再验收界面内的数据提交与回读；不属于本次按钮入口和 Prefab 接通范围。

### 2026-09-13｜登录大厅弹窗 Mask 防击穿统一

- 用户要求：登录、大厅及其业务模块的所有弹窗统一增加整屏 `Mask`，所有 Mask 节点必须挂载 `cc.Button`，避免触摸事件击穿到底层界面；整屏遮罩名称统一为 `Mask`。
- 根因/结论：旧弹窗的遮罩结构不一致，部分没有 Mask，部分只有 `1280×720`，宽屏两侧仍可能击穿；另有 ScrollView 和头像裁剪用 `cc.Mask` 未挂 Button。现统一弹窗 Mask 为首个子节点、`1600×720`、四边 Widget 对齐，并保证恰好一个启用且可交互的 Button；裁剪节点仅补 Button，不误改为整屏遮罩。
- 修改范围：Common、Lobby、Modules 下 24 个弹窗 Prefab，以及 `LobbyClubList/View`、`LobbyClubList/Mask`、`LobbyMain/AvatarMask` 的 Button 组件；未修改登录 Scene、弹窗业务内容节点和全屏业务页面。
- 验证结果：全目录共检查到 25 个整屏 Mask，Button 缺失、重复、禁用或节点归属错误均为 0；Creator 3.8.8 已重导入，资源门禁阻断项 0；Preview 真实点击设置、反馈、公告、签到和手机绑定弹窗的 Mask 外围区域，弹窗栈均保持不变，未触发底层按钮；Web Mobile 阶段构建成功。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/popup-mask-2026-09-12T22-17-52-858Z`。
- 未完成项：无。

### 2026-09-13｜弹窗关闭同次指针击穿修复

- 用户要求：修复分享窗口点击关闭后，同一次点击继续命中大厅底层按钮的问题，并要求真实测试通过后再报告。
- 根因/结论：通用关闭绑定同时监听 `TOUCH_END`、`MOUSE_UP` 与 `CLICK`，弹窗在 `MOUSE_UP` 阶段提前隐藏后，后续 `CLICK` 会落到底层界面；问题不在 Mask 缺失。现统一只在 Button `CLICK` 阶段执行关闭，并通过 `closeAfterPointer` 阻断同一指针事务的后续事件；旧 Prefab 的 `CloseForm`、`OnClick_Close` 路径同步使用该接口。
- 修改范围：`LobbyScreenController.ts`、分享关闭防击穿真实 E2E 脚本及本记录；未修改 Prefab 层级、视觉布局、登录 Scene 或服务端业务。
- 验证结果：iPhone 14 Pro Preview 真实点击大厅分享按钮打开 `UILobbyShare`，再真实点击 `btn_close`；关闭前弹窗栈为大厅加分享，关闭后仅剩大厅，分享节点已隐藏，底层 Button 点击次数为 0，控制台错误为 0。Creator 资源门禁阻断项为 0。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/share-close-clickthrough-2026-09-12T22-32-01-290Z`。
- 未完成项：无。

### 2026-09-13｜大厅亲友圈卡片职位来源修正

- 用户要求：大厅亲友圈卡片不得显示“无职位”，应显示当前玩家在对应亲友圈中的职位，并与成员列表“职务”列采用同一接口字段和判定规则。
- 根因/结论：大厅此前调用 `club.CGetClubListMin`，精简响应不包含 `minister`、`isPromotionManage`，客户端又读取不存在的 `identity`。现改用包含权威职位字段的 `club.CGetClubList`，并按成员列表相同顺序显示“管理/队长、队长、创建者、管理、成员”。
- 修改范围：`LobbyClubListGateway.ts`、`LobbyClubListController.ts`、大厅亲友圈列表专项测试和本记录；未修改 Prefab 层级、服务端或成员列表既有逻辑。
- 验证结果：专项测试 1/1、Creator 原生资源门禁通过；独立 Preview 使用测试账号 21、30 均真实打开亲友圈列表，接口返回空列表且控制台无错误，无法用空账号伪造有职位卡片验收。
- 未完成项：需使用已有亲友圈的账号真实打开一次列表，确认卡片显示其实际职位；代码和接口字段已联通，等待用户当前账号复验。

### 2026-09-13｜大厅亲友圈职位接口回归更正

- 用户反馈：职位修正后亲友圈列表完全不显示。
- 更正原因：直接将列表源切换为 `club.CGetClubList`，当前服务环境没有返回原有列表数据，破坏了列表权威来源。
- 更正处理：恢复 `club.CGetClubListMin` 获取原始列表，再逐项调用 `club.CGetClubListById` 合并 `minister`、`isPromotionManage`；单项详情失败仅降级该项，不再清空整个列表。
- 验证结果：列表接口与详情补全专项断言通过，Creator 资源门禁阻断项 0；规定测试账号当前无亲友圈数据，最终有数据卡片待用户账号刷新复验。

### 2026-09-13｜大厅四边 Widget 响应式适配

- 用户要求：检查并修复大厅 `Top`、`Bottom`、`Left`、`Right` 按现有 Widget 参数不能随屏幕适配的问题。
- 根因/结论：四个区域的 Widget 参数本身正确，但共同参照的 `LobbyMain` 根节点仅做水平、垂直居中且固定为 `1280×720`，导致子区域只能对齐固定根节点。现将根节点改为上下左右四边拉伸，子区域继续保留原有方向、边距、内部层级和按钮布局。
- 修改范围：仅 `LobbyMain.prefab` 根节点 Widget、响应式真实 E2E 脚本和本记录；未修改四个区域内部节点、业务代码或登录场景。
- 验证结果：Creator 3.8.8 Preview 已正常重导入；实际可见宽度为 `1369.39` 时，根节点、顶部和底部宽度同步为 `1369.39`，左侧保留 `250.727` 边距，右侧保留 `80` 边距；相关断言 7/7、Creator 原生资源门禁通过，Web Mobile 阶段构建成功。证据位于 `Client/docs/前端框架规范/历史记录与证据/证据/lobby-widget-responsive-2026-09-13T05-13-13-089Z`。
- 未完成项：无。

### 2026-09-13｜大厅亲友圈列表 Prefab 重命名索引同步

- 用户要求：`Lobby/Prefab/LobbyClubList.prefab` 已重命名为 `ClubList.prefab`，同步修改代码索引。
- 修改范围：将 `UILobbyClubList` 的资源注册路径由 `LobbyClubList` 更新为 `ClubList`；保留表单业务名、控制器类名、Prefab 层级和 Meta UUID 不变。
- 验证结果：文件名、Prefab 资源名、根节点名和 Meta `syncNodeName` 均为 `ClubList`，UUID 保持 `0bf8f45b-d1c9-4a22-9349-a87a263a73d0`；Creator 原生资源门禁通过；无头 Chrome 使用账号 29 真实点击“加入亲友圈”，成功加载活动根节点 `ClubList`，控制台错误为 0；Web Mobile 阶段构建成功。
- 未完成项：无。

### 2026-09-13｜ClubList 背景点击防击穿

- 用户要求：修复 `ClubList` 背景已有 Button 但点击仍会击穿到底层大厅的问题。
- 根因/结论：`cc.Button` 只提供命中和交互状态，不负责可靠吞噬模态窗口的完整指针事件；旧代码仅在根节点手工停止触摸传播，且关闭仍直接执行，无法覆盖所有指针阶段。现由 `BlockInputEvents` 统一拦截根节点和背景事件，返回关闭使用 `closeAfterPointer` 消费同一指针事务。
- 修改范围：仅 `LobbyClubEntryController.ts` 与 `ClubList` 真实点击验收脚本；未改 Prefab 层级、视觉和亲友圈业务接口。
- 验证结果：Creator 3.8.8 Preview 热编译后，无头 Chrome 使用账号 29 真实打开 `ClubList` 并点击背景边缘；弹窗栈保持 `UILobbyMain + UILobbyClubList`，`ClubList` 持续激活，控制台错误为 0，专项断言 3/3 通过。
- 未完成项：无。

### 2026-09-13｜ClubList 滑动参数对齐亲友圈桌子

- 用户要求：参照亲友圈桌子滑动组件，将其全部行为参数等价应用到 `ClubList`。
- 权威来源：`ClubMain/Middle/RoomList/mark` 的横向桌子 `cc.ScrollView` 及项目统一 `AooUnifiedScroll` 策略；`ClubList` 不存在桌子界面的独立可视滚动条节点，因此未新增视觉层级。
- 修改范围：`ClubList/Body/List` 的制动值由 `0.5` 对齐为 `0.32`，控制器显式指定 `UnifiedScrollDirection.Horizontal`；未修改列表节点层级、卡片布局或业务数据。
- 验证结果：运行时横向开启、纵向关闭、惯性开启、制动 `0.32`、弹性开启、回弹 `0.16`、取消内部事件开启、方向值为横向；无头 Chrome 真实打开列表，专项断言 4/4、控制台错误 0，Creator 原生资源门禁通过，Web Mobile 阶段构建成功。
- 未完成项：无。

### 2026-09-13｜大厅旧 Gift 节点引用删除

- 用户要求：`LobbyMain.prefab/Gift` 节点已由用户删除，代码同步删除其引用。
- 修改范围：删除大厅按钮路由中旧 `Gift` 别名分支，并从运行时入口显隐名单移除 `Gift`；保留独立 `GiftRoomCard` 节点、`GiftRoomCardController`、`UILobbyGift` 表单注册和房卡赠送业务。
- 验证结果：活动 TypeScript 中旧 `Gift` 节点路由和显隐引用为 0；赠送房卡专项测试通过。磁盘版 `LobbyMain.prefab` 当前仍可检索到旧 `Gift` 序列化节点，本次遵从用户要求仅清理代码引用，未恢复、修改或继续依赖该节点。
- 未完成项：用户在 Creator 中的节点删除需保存并完成资源同步后，磁盘 Prefab 才会与界面状态一致。

### 2026-09-13｜全项目不足一屏滑动回弹统一

- 用户要求：该需求属于全项目通用滑动组件；横向、纵向内容条目不足一屏时仍须跟手滑动，回弹可见距离至少 `50px`。
- 根因/结论：旧组件以 Layout 容器宽高判断是否填满，内边距和隐藏模板会造成误判；不足一屏时默认又只跟随手势的 `35%`。现统一改为按活动子条目的真实占用范围判断，横纵两轴按手势 `1:1` 跟随，可拖范围至少 `50px`，松手按既有时长回弹；通用组件只在节点捕获阶段观察手势，不替换或阻断 Creator 原生 ScrollView 事件链。
- 修改范围：通用 `UnifiedScroll.ts`；`ClubList` 移除专用不足一屏参数，仅保留横向方向声明。未修改任何列表 Prefab 层级和视觉资源。
- 验证结果：Creator Preview 已热编译并加载全局默认参数 `minDrag=50`、`follow=1`；后续发现全局触摸接管会与原生 ScrollView 抢占事件，已删除接管并恢复原生捕获链，同时补齐 `View/view` 两种视口节点命名识别。TypeScript 检查中本次文件无新增错误；项目仍有两个与本需求无关的 PDK 历史类型错误。
- 未完成项：需在 Creator Preview 内用真实鼠标或触摸分别复验一个不足一屏的横向列表和纵向列表；验证前不得标记完整闭环。

### 2026-09-13｜不足一屏按住跟手与松手回弹

- 用户要求：拖动时只要手指未松开，内容就必须持续跟随手势；只在松手后回弹。
- 修正处理：删除按住阶段的最大位移夹取，横向和纵向均按手势位移 `1:1` 持续跟随；回弹仍只在 `TOUCH_END/TOUCH_CANCEL` 执行。鼠标滚轮的有界回弹保持不变。
- 修改范围：仅通用 `UnifiedScroll.ts` 及本记录，未修改任何列表 Prefab 层级或业务数据。
- 未完成项：待 Creator Preview 真实长按拖动、保持、松手复验。

### 2026-09-13｜不足一屏提前回弹更正

- 用户复验：按住拖动期间仍会自动回弹。
- 更正原因：通用组件的跟手位置与 Creator 原生 ScrollView 的越界回弹同时写入 Content，原生回弹抢回了位置。
- 更正处理：仅当判定内容不足一屏时，在本次按住手势内暂停原生 ScrollView；通用组件独占跟手位置，收到松手/取消后恢复原生组件并执行回弹。内容超过一屏的普通惯性滑动不停用原生 ScrollView。
- 未完成项：待 Creator Preview 再次真实长按、保持和松手复验。

### 2026-09-13｜滑动松手误触点击更正

- 用户复验：不足一屏的列表拖动后，松手会被子按钮误当成点击。
- 更正原因：本次手势暂停了原生 ScrollView，其 `cancelInnerEvents` 也不再替子按钮取消触摸。
- 更正处理：通用组件在手势超过拖动阈值后吞掉本次移动和松手/取消事件，阻止子 Button 收到松手并触发 Click；未超过阈值的轻点仍正常下发。横向、纵向统一生效。
- 未完成项：待 Creator Preview 真实复验“轻点触发、拖动不触发”。

### 2026-09-13｜不足一屏回弹速度调整

- 用户复验：松手后回弹过快，视觉上像闪回原位。
- 更正处理：将不足一屏的回弹与原生短边界动画分离，通用回弹时长设为 `0.38s`，缓动改为 `sineOut`；触摸与鼠标滚轮的不足一屏回弹统一使用该参数。普通可滚动列表的惯性、制动和原生回弹不变。
- 未完成项：待 Creator Preview 视觉复验回弹速度。

### 2026-09-13｜不足一屏回弹时长更正

- 用户要求：将通用回弹时间改为 `1s`。
- 更正处理：全项目 `UnifiedScroll` 的不足一屏回弹时长由 `0.38s` 更正为 `1s`，横向、纵向、触摸和鼠标滚轮统一生效。
- 未完成项：待 Creator Preview 视觉复验。

### 2026-09-13｜以 ClubList 手感统一全项目滑动

- 用户要求：以 `ClubList/Body/List` 的滑动效果作为全项目通用组件标准，不是让单个列表同时横纵滑动。
- 处理：全局惯性开启、制动统一为 `0.32`、弹性开启、回弹时长统一为 `1s`；不足一屏时按住 `1:1` 跟手，松手回弹，拖动不触发子按钮点击。滑动方向仍按各列表布局配置。
- `ClubList/Body/List` 条目为纵向排列，已将 Prefab 和运行时方向由横向更正为纵向。
- 验证：Prefab JSON 解析通过，本次两个 TypeScript 文件无新增类型错误；项目仍有两个无关 PDK 历史类型错误。
- 未完成项：Creator 资源数据库尚未检测到本次 Prefab 磁盘变更，需完成重导入、重新打开和 Preview 真实复验后闭环。

### 2026-09-13｜ClubList 滑动方向更正

- 用户明确：`ClubList/Body/List` 需要左右滑动。
- 更正：撤销将卡片视觉排列误判为纵向滑动的调整；Prefab 原生 ScrollView 与运行时 `UnifiedScroll.ensure` 均恢复为 `Horizontal`，纵向关闭。通用跟手、1 秒回弹和拖动防误点逻辑保持不变。
- 未完成项：待 Creator 重导入和 Preview 真实左右拖动复验。

### 2026-09-13｜ClubList 回弹仍过快更正

- 用户复验：`ClubList/Body/List` 应缓慢回弹，实际仍快速归位。
- 更正原因：松手时通用组件在自定义 1 秒 Tween 前恢复了原生 ScrollView，原生边界归位抢先写入 Content，导致视觉上快速闪回。
- 更正处理：回弹的整个 `1s` 期间继续暂停原生 ScrollView，等 Tween 完成后再恢复；缓动由前段偏快的 `sineOut` 改为两端平缓的 `sineInOut`。触摸与滚轮回弹曲线统一。
- 未完成项：待 Creator Preview 复验完整 1 秒回弹过程。

### 2026-09-13｜通用回弹速度微调

- 用户复验：`1s` 回弹过慢，需要加快。
- 调整：全项目通用 ScrollView 原生回弹与不足一屏回弹统一调整为 `0.65s`，保留 `sineInOut` 曲线；`ClubList/Body/List` Prefab 参数同步为 `0.65s`。
- 未完成项：待 Creator Preview 视觉复验。

### 2026-09-13｜大厅可选服务入口与首次数据加载解耦

- 用户反馈：签到、实名、绑定、活动、赏金、商城等大厅入口点击即提示“请求失败”。
- 根因：本地窗口可以正常打开，但窗口打开后的活动目录、实名状态或商城目录首次拉取访问了当前 Gateway 未挂载的接口；旧逻辑把可选数据加载失败误报成入口打开失败。
- 客户端处理：`TaskController`、`IdentityController`、`StoreController` 的首次数据加载失败改为页面内部不可用状态事件，不再弹全局失败气泡；签到领取、实名提交、手机验证、购买和兑换等真实业务操作仍保留失败提示，禁止伪报成功。
- 验证：相关单元门禁 10 项通过；无头 Chrome 真实点击签到窗口成功打开且未出现“请求失败”，同时记录到 `/api/v2/activities` 返回 404，证明剩余问题属于生产 Gateway 路由缺失。推广员入口 `UIDaiLiCopy` 在当前 Aoo 原生资源中缺失，不能与接口失败混为一类。

### 2026-09-13｜CreateRoom 当前层级命名与代码契约同步

- 用户要求：以用户最新调整的 `Modules/CreateRoom/Prefab/CreateRoom.prefab` 层级为准，仅规范命名并整理代码引用，不改变父子层级和兄弟顺序。
- 处理：Prefab 文件、根节点及全部按钮、文字、图标节点按命名规范统一；代码路径改用当前 `Top`、`Rules`、`Bottom`、`Left` 结构，删除已经由 Prefab 正式提供的运行时规则模板兼容实现，不保留旧 `MainPanel/Actions` 路径或新旧名称并行映射；保留原 `.meta` UUID。
- 验证：Creator 3.8.8 正常识别并重新打开 `CreateRoom.prefab`，层级面板显示新命名；原生资源门禁覆盖 559 个 Scene/Prefab、20358 个资源及 Meta、28681 个 UUID，阻塞为 0；6 项定向测试通过；独立无头 Chrome 从大厅真实点击 `CreateRoom`，成功打开并找到 `Btn_Close`、`Btn_Reset`、`Rules`、`Btn_Create`、`Btn_BaseScore`，控制台无错误。证据：`docs/前端框架规范/历史记录与证据/证据/pdk-small-settlement-background-2026-09-13T10-47-07-067Z/create-room-renamed.png`。

### 2026-09-15｜Git 仓库地址与用户授权边界

- 用户要求：把客户端、服务端和管理后台 Git 地址写入每个 AI 员工规范；用户测试完成后会主动邀请对应 AI 提交，未收到明确指令禁止推送。
- 根因/结论：统一记录三个固定仓库地址；取消 AI 自主提交和定时推送授权，明确邀请后默认只推送对应仓库 `develop`，`main` 及历史改写仍需当次单独授权。
- 修改范围：仅本文档“执行与验收”和本开发记录。
- 验证结果：规则已归入既定章节，历史开发记录未改写，“记忆”仍为最后一章。
- 未完成项：无。

### 2026-09-15｜暂停跨任务文档消息与自动整理

- 用户要求：所有 AI 暂时不再给文档任务发送消息，也不再自动整理正式文档。
- 根因/结论：新增临时覆盖规则；暂停期间不执行此前主动发送文档报告的条款，各岗位只维护自身开发记录与记忆，文档 AI 仅响应用户直接下达的具体文档任务。
- 修改范围：仅本文档“执行与验收”和本开发记录；历史记录不删除、不改写。
- 验证结果：临时规则已归入既定章节，“记忆”仍为最后一章。
- 未完成项：等待用户后续明确恢复文档协作。

### 2026-09-16｜持续 develop 提交权限

- 用户授权：所有 AI 可将各自职责范围内已形成闭环、可明确归属的改动提交并推送到对应仓库 `develop`，无需逐次申请；仍禁止 `main`、强推、改写历史和夹带无关改动，每次结果必须报告。

### 2026-09-16｜Git 权限更正：仅明确指令或每日快照

- 更正原因：用户最新明确要求撤回 AI 自主提交权限；此前“持续 develop 提交权限”记录不再作为执行依据。
- 最终规则：只有用户针对具体任务明确说“提交”才允许提交该任务；否则仅由北京时间 `02:00` 的每日固定任务统一提交。


### 2026-09-18｜Bug 根因修复原则

- 用户要求：所有 AI 处理 Bug 禁止采用补丁式修复，必须先理清完整逻辑和根因，再修正权威业务路径。
- 执行结论：禁止通过空值兜底、吞错、延时重试、硬编码特例、兼容分支或并行旧逻辑绕过根因；修复后必须清理错误和临时逻辑，并完成针对性与真实流程验收。
- 修改范围：仅岗位规范；未修改业务代码、资源、配置、数据库或规则表。

## 7. 记忆

- 岗位与记录：全栈负责登录、认证、账号和大厅客户端，以及 `Account`、`Hall`、`Gateway` 相关接口、协议、数据库迁移、配置映射、部署和真实 E2E；不处理具体玩法、亲友圈或公共框架。
- 待处理队列：加入房间公共数字键盘确认链复验｜来源：aoo产品转交｜状态：进行中｜职责范围：仅核查大厅 `JoinRoomController` 到公共 `Numpad` 的调用与请求链；公共组件缺陷须交框架职责 AI，不修改玩法、登录场景布局或其他游戏｜安全断点：静态确认 `Btn_Confirm` 的 `cc.Button` 与 `Button.EventType.CLICK` 绑定存在；原测试把世界坐标换算为 CSS 时漏加 Canvas 左偏移，正确坐标约 `(914,267)`，已要求原 CDP `1237` 线程真实复测。
- 待解决边界：审计项不代表修改授权；用户明确指定后才能处理，跨岗位项还须协调。
- 待处理队列：大厅赠送房卡入口接通｜来源：用户明确要求｜状态：代码完成，待按钮弹窗真实可见性复验｜职责范围：将 `LobbyMain/GiftRoomCard` 接到 `Modules/GiftRoomCard/Prefab/GiftRoomCard.prefab` 及现有房卡赠送业务，不改预制体层级；安全断点：全局大厅导航阻塞已解决，路由、Prefab 注册、公共数字键盘和 `ROOM_CARD` 赠送链已接通，专项单元门禁通过。
- 待处理队列：亲友圈主界面已删除 Prefab 引用清理｜来源：用户明确要求｜状态：进行中｜职责范围：定位 `club/default/UIClubMainDefault` 旧加载引用并改用现存权威 Prefab，不恢复被删除资源、不改界面层级；安全断点：错误界面已确认，尚未修改加载路由。
- 待处理队列：`Client/assets/Lobby/Code` 冗余与职责归档｜来源：aoo产品转交及框架续作通知｜状态：进行中（Creator 已释放，执行最终复验）｜职责范围：仅完成 Lobby 代码归档后的 Creator 同步和 CD201/LS201 真实复验；安全断点：Lobby 代码清理与职责归档已完成，亲友圈桌子样例已完成 Creator 重导入、保存关闭并返回 MainScene。按当前 `games-common` 加载 `GameRoom2D`，不用 `main/config` 误判；使用账号 21 至 24、CDP 1236 从 CreateButton 双端复验断点继续。
- 上下文压缩时只保留确认需求、关键决定、重要路径、未完成任务和最近 10 轮原文，并立即续作。写入规范后、切换业务前或规则不确定时重读本文档；同一问题连续处理中不重复读取。
- 账号隔离：本地测试仅用`21`至`30`，密码为`1`；并行任务记录实际账号，被占用时只在本段换号。禁止操作其他岗位账号、会话和房间数据；`51`至`100`未经aoo产品分配不得使用。
- 停止条件：只要存在已授权、可执行任务就持续执行。任何停止、idle、异常结束或准备停工都须向aoo产品提交当前任务、完成项、未完成项、阻塞、队列、安全断点和原因；仅产品审核确认全部闭环，或只能等待用户决定、授权且无其他独立任务时可保持停止。未授权历史审计项不自动入队。
- Creator 资产权威：只以当前磁盘和 Creator 中现存 Prefab、节点、动画、材质、名称与层级为准；用户删除即废弃，禁止从 Git、废纸篓、备份、Library、构建产物或旧版本恢复、复制、重建或兼容。当前代码和验收不得依赖废弃资源；遗留引用、校验和控制逻辑必须删除。现存 Prefab 的失效序列化引用只报告具体项，未经授权不补建；确需新增时报告准确节点信息，由用户在 Creator 创建，AI 只绑定现有节点。
- 硬编码边界：代码不到万不得已不得硬编码；优先使用配置、注册表、协议常量、序列化引用和唯一映射。不可避免时集中到一个权威常量并注明原因、适用范围和移除条件，禁止 TypeScript、模板和多端重复维护。
