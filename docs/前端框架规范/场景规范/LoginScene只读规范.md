# LoginScene 只读规范

登录场景布局由用户在 Creator 中维护。除当次明确授权的视觉范围外：

- 禁止增删、重命名、移动节点或改变顺序、坐标、尺寸、锚点、显隐和层级。
- 禁止修改图片、字体、文字样式、布局组件和按钮点击区域。
- 禁止自动迁移、格式化或重新序列化登录场景。
- 代码只能按用户确定的最终节点名绑定现有节点。
- 缺失节点或组件只记录“待用户在 Creator 中处理”。

本规范优先于一般场景修改流程。
