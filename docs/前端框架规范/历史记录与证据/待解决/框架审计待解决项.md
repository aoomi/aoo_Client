# 框架审计待解决项

## 处理边界

- 本文档只记录待解决问题，不代表已获得代码、场景、预制体、资源、协议、数据库或规范文档的整改授权。
- 所有条目默认状态为“待用户明确授权”；只有用户后续明确指定具体问题和处理范围后，对应 AI 才可实施修改。
- 处理前必须重新核对当时代码、现行规范、责任归属和复现结果；未复现的静态风险不得直接当作已确认故障修复。
- 历史兼容层及其冻结代码不得借本台账整体搬迁、重构、格式化或清理。

## 已确认且属框架职责

### F-001｜WebSocket 票据写入 URL

- 优先级：P1。
- 状态：已解决（2026-09-09）。
- 责任人：框架网络 AI；更换鉴权方式时需服务端协同。
- 直接证据：`Client/assets/Common/Code/Runtime/network/ProtocolClient.ts:47-55` 与 `GatewayEntryPolicy.ts:85-90` 把 `ticket` 写入查询参数。
- 影响：可能进入代理、网关、浏览器网络记录和错误诊断，与“票据不放 URL”规范冲突。
- 验证状态：客户端改用标准 WebSocket 子协议头传递一次性票据和页面实例，升级 URL 禁止任何查询参数；Gateway 握手集成测试、客户端 TypeScript、定向门禁及受影响服务端模块编译通过。Gateway 已允许当前 Creator Preview Origin；无头 Chrome 在真实 Canvas 完成游客登录并进入 `MainScene`，业务 WebSocket URL 为 `/api/v2/gateway/ws` 且无查询参数，票据只存在于子协议头。

### F-002｜重连恢复监听存在两次调用路径

- 优先级：P1。
- 状态：已解决（2026-09-09）。
- 责任人：框架网络 AI。
- 直接证据：`StableTransportFacade.ts:149-157` 在 `RECOVERING` 阶段等待恢复监听；同文件 `175-198` 在新一代进入 `READY` 后再次调用相同监听。
- 影响：一次重连可能重复请求权威快照并重复触发恢复副作用。
- 验证状态：恢复监听已收口到 `RECOVERING` 单次调用；替换 generation 计数回归和相关重连测试通过。真实断网 E2E 随 F-001 的 Gateway 环境恢复后补测。

### F-003｜AssetLoader 重复加载时引用计数不对称

- 优先级：P1。
- 状态：已解决（2026-09-09）。
- 责任人：框架资源 AI。
- 直接证据：`Client/assets/Common/Code/UI/Infrastructure.ts:15-29` 每次加载都执行 `addRef()`，但使用 `Set<Asset>` 去重，`releaseAll()` 对同一对象只执行一次 `decRef()`。
- 影响：同一缓存资源被重复加载后会留下未对称引用。
- 验证状态：已按资产记录持有次数，单项释放和批量释放均与每次 `addRef()` 对称；重复加载回归及 TypeScript 通过。
- 后续观察：实际页面可补反复进入退出的 Creator 内存曲线，不影响本次引用正确性闭环。

### F-004｜LifecycleScope 清理被单个异常中断

- 优先级：P2。
- 状态：已解决（2026-09-09）。
- 责任人：框架生命周期 AI。
- 直接证据：`Client/assets/Common/Code/Runtime/core/LifecycleScope.ts:34-39` 清空集合后直接逐项调用，没有单项捕获和聚合错误。
- 影响：某个清理函数抛错后，后续监听、定时器或资源清理不再执行，且第二次 `close()` 无法补救。
- 验证状态：已逐项捕获并保证全部逆序清理完成，最后统一抛出 `AggregateError`；异常清理回归与幂等二次关闭测试通过。

### F-005｜客户端生命周期门禁存在假阳性

- 优先级：P2。
- 状态：已解决（2026-09-09）。
- 责任人：框架测试 AI。
- 直接证据：`Client/tests/unit/client-manager-lifecycle.test.mjs:109-118` 使用跨越整个文件的贪婪正则；`SceneRouter.ts:209-221` 的 `resetSession()` 实际只调用 `network.resetGame()`。
- 影响：门禁能在目标方法不包含 `network.reset()` 时仍通过，无法可靠防止回归。
- 验证状态：门禁改为提取 `resetSession()` 精确方法体，分别断言销毁 Lobby、调用 `network.reset()` 且禁止 `resetGame()`；生命周期测试通过。

## 需跨岗位协同

### C-001｜`pdk` 编码仍可形成客户端写请求

- 优先级：P1。
- 状态：已解决（2026-09-09）。
- 责任人：协议生成源维护者、跑得快玩法 AI；框架网络 AI 协同。
- 直接证据：`GeneratedProtocolIds.ts:79` 将 `poker.pdk.dispatch` 标记为客户端写请求；`CommonPdkReplayController.ts:154` 存在 `pdk.*` 实际调用；`ProtocolClient.ts:315` 会输出到 `poker.pdk.dispatch`。
- 影响：可能继续产生禁止的平行业务编码，地区身份不明确。
- 验证状态：公共 PDK Runtime 继续只按 `CD201/NJ201/LS201` 发送；客户端发送边界拒绝 `poker.pdk.dispatch`，Poker 服务端删除 NJ 旧别名接受路径，兼容桥仅允许三种地区写协议。V2 兼容门禁要求历史消息定义保留，故只保留机器可识别定义及只读推送兼容，不再接受新写入。协议生成、TypeScript、服务端相关模块编译与定向门禁通过。

### C-002｜登录、会话与 Hall 生命周期风险

- 优先级：待登录/大厅 AI 复核。
- 状态：已解决（2026-09-09）。
- 责任人：登录/大厅 AI；涉及公共连接所有权时由框架网络 AI 协同。
- 待复核证据：`UnifiedIdentityAuthGateway.ts:149` 缺少 Web Crypto 时游客凭据全零；`AuthSession.ts:275-280` 持久化认证凭据；`SceneRouter.ts:202-221` 退出只执行 `resetGame()`；会话轮换、认证并发和启动超时仍有迟到副作用风险。
- 影响：可能涉及账号凭据安全、旧连接未关闭、旧会话回写和认证竞态。
- 验证状态：`resetSession()` 已执行完整 `network.reset()`，会关闭 Hall socket、心跳、票据请求及 Hall/Game slot；游客凭据在缺少 Web Crypto 时改为失败关闭，不再使用弱随机回退。认证 epoch、单飞恢复、退出后迟到会话不可回写及持久化 refresh token 轮换策略已有回归覆盖，TypeScript 与登录生命周期测试通过。无头 Chrome 已从真实 Canvas 完成游客登录、进入 `MainScene`、打开退出确认弹窗并点击确定，最终进入 `Login_Scene`，本地会话与账号提示均清除，`/api/v2/account/logout` 返回 200，未捕获运行时异常。统一消息模板按钮根路径兼容、礼物设备身份头及收件箱成功轮询节流已同时修正；礼物收件箱有效会话返回 200。

## 仅静态风险待复现

### R-001｜公共推送的房间与版本过滤职责未确定

- 状态：已解决（2026-09-09）。
- 责任人：框架网络 AI 与相关玩法 AI。
- 证据：`ProtocolClient.ts:164-191` 未在协议分发边界校验当前 `roomId`、`playVersion` 和单调版本；但仓库已有 Room Store 和玩法适配层。
- 风险：如果下层未严格过滤，切房后迟到消息可能触发副作用。
- 验证状态：公共协议分发边界已绑定权威 `roomId/playVersion`，拒绝跨房间、跨版本及 `stateVersion` 回退推送；嵌套载荷与切房重绑回归通过。

### R-002｜Bundle 所有权与释放风险

- 状态：已解决（2026-09-09）。
- 责任人：框架资源 AI；具体玩法 AI 协同。
- 证据：`LegacySubgameBundleService.ts:43-48` 可对全局已存在的 Bundle 执行 `releaseAll/removeBundle`，未证明它由本服务独占。
- 风险：可能影响其他页面共享资源。
- 验证状态：释放逻辑只处理本服务成功加载并登记的 Bundle；全局预先存在但非本服务所有的 Bundle 只借用、不执行 `releaseAll/removeBundle`。所有权门禁与 TypeScript 通过。

### R-003｜公共对象池容量与销毁语义不完整

- 责任人：框架公共组件 AI。
- 状态：已解决（2026-09-09）。
- 证据：`Client/assets/Common/Code/UI/Presentation.ts:43-68` 和 `Runtime/core/LifecycleScope.ts:65-86` 没有容量上限，且清理不会终结所有借出对象。
- 风险：峰值对象数可能长期驻留，与公共组件性能底线不一致。
- 验证状态：`NodePool` 与 `ResettablePool` 已增加非负容量上限（默认 32）；超限对象立即销毁，`clear()` 同时终结池内和借出对象；容量、超限销毁和借出对象清理回归及 TypeScript 通过。

## 历史架构债

- `Client/assets/Common/Code/Runtime/CompatibilityApp` 含大量具体玩法和大厅兼容实现，Common 也存在反向依赖 Login/Games 的历史结构。
- 状态：只记录、冻结，不作为当前整改任务。
- 后续边界：用户如未明确指定目录、玩法、依赖边界和迁移批次，任何 AI 不得整体重构或清理。
