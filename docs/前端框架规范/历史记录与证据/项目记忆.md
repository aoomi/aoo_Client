# Aoo 项目记忆

## 2026-08-27：UI 职责与项目边界

- 唯一负责项目：`/Users/aoo/Code/Game/BCG/Aoo`。
- 当前及后续职责仅限 Aoo 项目的 UI 修复与更换，包括分层美术资源、Cocos Creator 场景同步、构建和视觉验证。
- 不得修改 `JJMJ` 或其他项目。此前误改 JJMJ 的内容只允许按修改前备份完整还原，还原后停止操作。
- 当前未完成任务：将用户确认的“玖玖”登录 UI 分层资源接入 Aoo 项目；资源源目录为 `/Users/aoo/Code/AI Agent/tmp/ui-login-layers`。
- 登录 UI 目标规格：背景 `1600×720`；项目 UI 设计区 `1280×720`；登录方式仅保留“账号登录、微信登录”；背景、人物、Logo、左右精灵、按钮、协议栏、勾选框、设置和客服均为独立层。
- 当前视觉决定：清雅休闲的滨水花园风格；真人感鹅蛋脸成年女性；深青色现代礼服；App 名为“玖玖”；禁止赌场、麻将牌、扑克、骰子和霓虹赌博平台视觉。
- Aoo 登录场景既有长期规则仍有效：除用户当次明确授权的视觉替换范围外，不得擅自改变业务节点名称、层级、组件和 TypeScript 契约；任何资源或场景变更必须同步到 Cocos Creator 3.8.8 并完成构建验证。

## 2026-08-27：玖玖登录 UI 接入结果

- JJMJ 误接入内容已依据开发前备份完整还原；`Loading.scene/.meta` 与 `loading.jpg/.meta` SHA256逐项一致，新增 `LoginV2` 和相关 UUID 零残留。
- 玖玖登录 UI 仅接入 Aoo：`Client/assets/Login/Scenes/LoginScene.scene` 与 `Client/assets/Login/Atlas/LoginV2/`。
- 正式资源共11层：背景、人物、Logo、左右精灵、账号按钮底、微信按钮底、协议面板、勾选框、设置和客服；按钮文字继续使用 Cocos Label，不烘焙到正式按钮底图。
- 原业务节点、组件及 TypeScript 路径保留；游客及非目标入口仅隐藏未删除。
- Cocos Creator 3.8.8 已导入资源并完成 Web Mobile 构建；独立只读稽查总体 PASS，P0/P1=0。
- P2提醒：Aoo 当前运行时为 `ResolutionPolicy.SHOW_ALL`，16:9满足1280×720设计区与1600×720背景横裁；非16:9会留边。若后续要求所有比例固定高度横裁，需要单独调整适配策略并回归全部场景。
- 原场景可恢复备份：`/Users/aoo/Code/AI Agent/tmp/ui-login-backup-aoo-20260827-0127`。

## 2026-08-27：登录人物比例与按钮文字首败修复

- 首败根因：人物透明图被场景节点按非原始宽高比显示，导致脸、肩和身体横向变形；按钮继续叠加场景 Label，字体资源显示为白色方块。
- 人物已更新为稍瘦鹅蛋脸版本，源图 `1181×1331`，场景显示 `575.8595041322315×649`，节点缩放 `(1,1)`，源图与显示宽高比误差为 `0`，禁止后续非等比修改。
- 账号与微信按钮正式改用各自 `360×88` 的“文字+底板”烘焙整图；原 Label 节点保留但隐藏，Button组件、点击区域、节点名和业务路径不变。
- Cocos Creator 3.8.8 已重新导入并完成最终 Web Mobile 构建；最终场景 SHA256 为 `c0d77da67a7e18889418a6c09e9ffdc4ffc2bf62b54f1b299c25f595bd299525`，构建前后未漂移。
- 独立稽查结论：PASS，P0/P1=0；原P2“构建早于Creator最终序列化”已通过最终场景同SHA重建补齐证据。

## 2026-08-27：创建房间规则窗口 V2 视觉复刻

- 正式目标仅为 `Client/assets/Lobby/Prefab/create_Room.prefab`；隐藏的 `AddGamePanel`、旧玩法库、收藏/预约等未实现入口继续保持隐藏，禁止因视觉复刻重新启用。
- 新增皮肤资源位于 `Client/assets/Lobby/Atlas/CreateRoomV2/`：全屏遮罩、弹窗面板、普通按钮、选中按钮、分割线，均为 Creator 3.8.8 导入的独立九宫格资源。
- 固定视觉规范：遮罩 `rgba(0,0,0,0.22)`；面板底色 `#F2F7F7`、14px 圆角、`#B8CDCF` 1.5px 描边、`0 4 12 rgba(0,0,0,0.15)` 柔影；普通按钮 `#F8FAFA/#B4C9CB/#263233`；选中按钮渐变 `#7BB0B2 → #689B9D`、描边 `#92C4C6`、白字；禁止内部遮罩、内发光、高饱和青绿和强立体高光。
- `CreateRoomRulePresenter.ts` 仅负责普通/选中独立 Sprite 的视觉切换及字号颜色；服务端 Schema、校验、快照、创建请求、重复事件去重等业务契约不得改变。
- 关键节点和 Button 组件全部保留：`btn_close`、`MainPanel/btn_create_room`、`MainPanel/Header/btn_rules`、`MainPanel/RulesPanel/ScrollView/view/ruleItem`、`content`。
- Creator 3.8.8 正式 Web Mobile 构建完成；最终 prefab SHA256 为 `e3dd74614b12c4084da33016f09f4f94dd06e700e9a99bbb4e729fbc189fd7b9`。定向测试 9/9 PASS，独立只读稽查 PASS，P0/P1=0。
- 可恢复备份：`/Users/aoo/Code/AI Agent/tmp/create-room-v2-backup-20260827-020551`；构建产物：`/Users/aoo/Code/AI Agent/tmp/create-room-v2-build-20260827-0218/web-mobile`。
- 既有 P2：`unified-create-room-ui.test.mjs` 仍引用已删除的旧 `UnifiedPlaySelector.prefab`；Creator 日志另含与本窗口无关的历史 Prefab missing-UUID 警告。二者均不得误判为本次视觉复刻首败。
