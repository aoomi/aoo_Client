# Aoo Client 迁移与构建记录

## 当前目标

来源：`QH_DFMJ/client-next-3.8.8`。目标：Creator 3.8.8 原生工程中的 `Common、Login、Lobby、Club、Games`。

首批范围：登录、大厅、亲友圈、内江跑得快（Poker Pack01）和成都血战麻将（Mahjong Pack01）。资源与 `.meta` 成对迁移，保留 UUID；Creator 2.x 资源必须转换，不能冒充 3.8.8 原生 Prefab。

## 已完成记录

- 已迁移首批业务代码、Prefab、UI 和资源并保存来源映射。
- 已保留依赖、Creator 设置、模块映射和逐文件映射记录。
- 旧跨模块代码暂由 `Common/Code/Runtime/CompatibilityApp` 编译兼容；新代码禁止引用，完成接口化后逐模块移除。

## 未关闭风险

- 历史记录指出认证目录曾因大小写合并丢失；补回并完成真实登录回归前，不得宣称登录迁移完整。
- 构建成功不代表业务通过，必须执行登录至大小结算的端到端测试。
- 历史目录方案、地区文件夹、`main/remote` 和全局横竖版空目录均已废弃，以现行《前端架构规范》为准。

## 构建门槛

- Creator 3.8.8 能导入且无缺失 UUID、脚本和资源引用。
- Bundle 清单无循环依赖、重复资源和大小写冲突。
- 主包只包含最低登录闭环，远程资源可校验、重试、恢复和回滚。
- iOS、Android、Web 的路径大小写一致。
- 依赖、插件、许可证和版本记录同步更新。
