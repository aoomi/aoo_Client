# 亲友圈刷新后桌面头像验收记录

- 时间：2026-09-15
- Creator：3.8.8，Preview `http://127.0.0.1:7456/`
- 无头 Chrome：任务专属 Profile，CDP `1237`，`1280×720` 视口（Apple iPhone 14 Pro 横屏预览模板）
- 运行配置：`environment=test`，API `http://127.0.0.1:8080/`，协议封包版本 `2.0`，网关路径 `/api/v2/gateway/ws`
- 测试账号：31（玩家 580，建桌）、32（玩家 581，观察桌面）；密码未写入证据
- 最终复验房间：`roomId=116065`；亲友圈桌面快照没有暴露 `operationId/stateVersion/buildId`，本次不伪造这些字段

## 结论

刷新前及连续两次整页刷新后，活动桌均使用 `CommonHead/Stat`，玩家 580 的 `Stat/SquareAvatar/Mask/Img_Avatar` 保持 `activeInHierarchy=true`。头像地址 `http://127.0.0.1:8765/tx580.png` 不可达时，Sprite 仍保留预制体默认帧 `bg_txk_f`（UUID `df55d4d9-facb-4405-82a0-b8bad14c9234@d571f`），纹理有效；昵称节点为 `Stat/Lb_PlayerName=玩家580`。两轮刷新均未产生 `pageerror`，每轮均观察到本地头像请求失败并进入默认头像降级路径。

## 证据

- 刷新前：`20-before-refresh-avatar.png`、`20-before-refresh-state.json`
- 第一次最终刷新：`25-final-refresh-1.png`、`25-final-refresh-1-state.json`
- 第二次最终刷新：`26-final-refresh-2.png`、`26-final-refresh-2-state.json`
- 运行配置：`runtime-config.json`
- Creator 同步：在资源管理器对 `LegacyClubMainController.ts` 执行“重新导入资源”，脚本热编译结束后 Preview 产物已包含 `CommonHeadController.useVariant('Stat')`、`Lb_PlayerName` 和 `showPlayerAvatar`；控制台未新增脚本注册、缺失 UUID 或反序列化错误。现存 `bg1.jpg/bg1.png` 动态加载 URL 冲突告警与本改动无关。
- 定向测试：`player-avatar-unification.test.mjs` 与 `club-template-unified-scroll.test.mjs` 共 `9/9` 通过。

## 清理

两次建出的测试房间均由账号 31 通过真实返回按钮退出，账号 32 侧确认活动房间节点归零；额外浏览器上下文已关闭。账号 32 为完成双账号观察加入了专用测试亲友圈 `549245`，该测试成员关系保留，避免在未获得删除确认时执行破坏性成员移除。
