# 队长子窗口监听隔离修复验收

- 根因：队长列表、查看战绩、查看报表等多个表单共用同一动态按钮清理数组；任一子窗口渲染或关闭都会解绑底层队长列表按钮。
- 修复：动态按钮清理器改为按表单根节点分组；列表刷新和表单关闭只清理当前表单拥有的监听，控制器销毁时仍统一清理全部监听。
- 静态验证：`club-promoter-report-response-contract.test.mjs` 3/3 通过；TypeScript 检查中本次文件无错误，剩余 8 项均为既有 PDK 类型错误。
- Creator 同步：Cocos Creator 3.8.8 已正常热编译，Preview chunk 中 `rowDisposers = new Map()` 生效，未修改 Scene、Prefab、meta 或布局。
- 真实界面：账号 31、亲友圈 549245 已通过真实 Canvas 点击进入队长列表，截图 `08-promoter-window.png`，页面错误为空。
- 首行规则：按用户确认，服务端列表第一行固定为本人；客户端保留服务端顺序并将首行无条件设为数据只读，隐藏展开入口和整个操作区。截图 `08-promoter-window.png` 中 PID580 首行只展示数据，无操作按钮。
- 最新同步：执行 Creator“刷新预览”后，Preview source map 已包含 `manager-list-render`、`rows.entries()` 和 `index === 0` 判断。
- 数据边界：未为测试增加、删除或修改成员/队长关系。
