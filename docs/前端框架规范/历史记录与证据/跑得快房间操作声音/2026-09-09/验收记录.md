# 跑得快房间操作声音接线验收记录

- 资源权威目录：`Client/assets/Games/Poker/PDK/Common/Audio/Room`，共 11 个 MP3 及原 `.meta`。
- 语义接线：`bg` 房间背景、`zuoxia` 入座、`xipai` 洗牌、`zhuang` 定庄、`fapai` 发牌、`xuanpai` 有效选牌变化、`chupai` 非不要出牌、`daojishi` 最后 3 秒、`baojing` 玩家余 1～2 张、`win/lose` 本地玩家局结算。
- 防重放：权威首次快照/重连历史动作不播放操作音；出牌与报警按新 `operationId` 消费；倒计时按座位和秒数去重。
- 容错：`PdkRoomAudioPresenter` 将 Bundle 缺失、单个音频缺失、节点已销毁和退出房间后的延迟播放全部降级为 `false`，不抛异常、不输出错误日志、不影响房间操作。
- 设置：背景音乐服从公共 `BackMusic/BackVolume`，操作声音服从 `SpSound/SpVolume`。
- Creator 同步：Creator 3.8.8 已正常重启资源库并重新打开 `Boot_Strap.scene`；导入/构建日志为 `Client/temp/asset-db/log/2026-9-9 14-09.log`。
- 验证：TypeScript 通过；房间音频与出牌音频专项测试合计 7/7；Creator 原生资源门禁通过（29074 资产/Meta、38912 UUID、blocking=0）；Web Desktop 构建于 14:12:09 成功，产物 `Client/build/web-desktop/assets/pdk-common-room-audio` 包含 11/11 个 MP3。
- 既有告警：全量构建继续输出项目既有 Prefab 缺失 SpriteFrame/Spine 告警，本次没有修改这些资源。
- 待真实 E2E：需进入真实多人 PDK 房间依次触发入座、开局、选牌、出牌、报警、倒计时和结算，并核对 AudioSource；静态/构建闭环不替代该项。
