# 跑得快出牌语音接线验收记录

- 资源权威目录：`Client/assets/Games/Poker/PDK/Common/Audio/Game/Boy|Girl`，共 92 个 MP3、92 个原 `.meta`。
- Bundle：`Game.meta` 已配置为 `pdk-common-game-audio`；Creator 3.8.8 正常导入日志为 `Client/temp/asset-db/log/2026-9-9 13-46.log`。
- 接线：`PdkGameAudioPresenter` 消费权威 `lastActions` 的新 `operationId` 和旧 `OpCard` 事件；重连初始历史动作不重复播报。
- 映射：单张、对子、三张按实际牌点选择；不要、顺子、三带一、三带二、四带二、炸弹、连对、飞机、三带一对、四带二对按牌型选择。资源没有精确对应文件的牌点或牌型返回空路径，不使用相近语音冒充。
- 性别：玩家 `sex/gender/userSex` 为女时使用 `Girl`，其余使用 `Boy`；播放服从公共 `SpSound`、`SpVolume` 设置。
- 验证：TypeScript 通过；专项测试 4/4；Creator 原生资源门禁通过，阻塞 0；Web Desktop 构建成功，`pdk-common-game-audio` 产物包含 92 个 MP3，主脚本包含控制器接线。
- 既有问题：扩大 PDK 回归 8 项失败，涉及已不存在的旧动画/背景目录和并行手势契约，不由本次音频改动引入，本次未越界修改；Creator 构建仍输出项目既有缺失 SpriteFrame/Spine 告警。
- E2E 阻塞：CDP `1234` 被 PID 19298、Profile `/tmp/pdk-cocos-touch-chrome.iIiauu` 占用，未终止、接管或复用。待端口释放后，以本任务独立临时 Profile 进入真实 PDK 房间，通过 Canvas 点击出牌并核对牌型、音频网络请求和 AudioSource 播放状态。
