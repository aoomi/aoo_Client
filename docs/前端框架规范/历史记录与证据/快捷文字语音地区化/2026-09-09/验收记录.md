# 快捷文字语音地区化验收记录

- 迁移：旧公共目录中具有 `FastVoice` 配置证据的普通话男声 10 个、女声 10 个，均连同原 `.meta` 移至 `Games/Common/Audio/Chat/Mandarin/Boy|Girl`，文件名统一为 `quick-text-01` 至 `quick-text-10`。
- 待确认：男女声 `11.mp3`、`12.mp3` 缺少快捷文字配置；男女声 `ay_1` 至 `ay_16` 的配置文案指向安岳，但项目未定义安岳稳定地区代码，全部保持原位。
- 地区目录：已建立 `Dialect/CD/Boy|Girl`、`Dialect/NJ/Boy|Girl`、`Dialect/LS/Boy|Girl`；当前无可确认地区方言文件，集中解析器按地区路径优先、普通话路径兜底。
- 引用：`Common/Config/Legacy/assets/njpdk/jsonData/Sound.json` 的 20 个 `FastVoice` 条目改为 `games-common-audio` Bundle 和新路径；ChatPanel 发送业务未修改。
- Creator：Cocos Creator 3.8.8 命令行重新打开项目并正常导入；导入日志 `Client/temp/asset-db/log/2026-9-9 00-18.log`；构建日志 `Client/temp/asset-db/log/2026-9-9 12-20.log`；输出 `Client/work/quick-text-audio-build/web-desktop/assets/games-common-audio`，含 20 个 MP3。
- 门禁：`quick-text-audio-catalog.test.mjs` 5 项通过；Creator 原生资源门禁通过（1367 个 Scene/Prefab、28621 个资源/Meta、38660 UUID、阻塞 0）；迁移目录 MP3/Meta 均为 20，UUID 无重复。
- 既有问题：全量 TypeScript 检查仅报 `CommonHeadController.ts:132,143` 两个既有类型错误；构建仍打印既有 Prefab/Scene 缺失资源告警，与本次音频 UUID 无关。
- E2E 阻塞：CDP `1234` 被 PID 23535、Profile `/tmp/pdk-wide-touch.7RssrX` 占用，已向 aoo稽查报告，未接管或终止。待端口释放后验证普通话男女声播放，以及 CD/NJ/LS 方言缺失时的普通话回退。

## 2026-09-09 续验更正

- 原占用进程释放 `1234` 后，本任务使用独立临时 Profile 启动无头 Chrome，以同一 Web Desktop 构建完成真实 Cocos/WebGL2 验收。
- 普通话男声、女声 `quick-text-01` 至 `quick-text-10` 共 20 个资源均通过 Bundle 加载、AudioClip 解码和 AudioSource 实际播放；播放时 `playing=true`、`currentTime>0`，音频时长范围为 1.907～3.866 秒。
- `CD`、`NJ`、`LS` 分别验证男女声，共 6 条地区解析链路：地区资源不存在时均选择同 ID、同性别的普通话资源并成功播放。
- 网络实测 `games-common-audio/index.js`、`config.json`、男女声 import/native 资源请求成功；未捕获 Runtime 异常。SwiftShader 与 WebGL ReadPixels 提示属于无头图形环境告警。
- 截图证据：`Client/docs/前端框架规范/历史记录与证据/快捷文字语音地区化/2026-09-09/e2e.png`。截图底层的 `MainScene` 启动所有者提示为现有启动入口问题，不影响本次已加载 Cocos 引擎后对独立音频 Bundle 的真实播放验收，本次未越界修改。
