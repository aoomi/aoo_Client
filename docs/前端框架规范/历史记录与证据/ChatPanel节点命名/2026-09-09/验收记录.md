# ChatPanel 节点命名验收记录

- 时间：2026-09-09。
- Preview：`http://127.0.0.1:7456/`，`buildId=aoo-preview-20260909-runtime-port-5`。
- 浏览器：Chrome 152，无头模式，CDP `1234`，独立 Profile `/tmp/aoo-chatpanel-e2e.ew735w`。
- 运行配置：API `http://127.0.0.1:8080/`，协议版本 `2.0`；Hall/Game WebSocket 地址、`operationId`、`stateVersion` 未由当前 Preview 暴露，未伪造记录。
- 测试账号与房间：游客 `513`，`game_code=CD201`，`roomId=604978`；测试结束已通过真实 Canvas 离开房间并返回大厅。

## Creator 与静态门禁

- Prefab UUID `fa402e34-21d4-43fe-a4b5-73df41d36227` 于 11:11 由 Creator 3.8.8 正常重新导入；磁盘源文件与 `library/fa/fa402e34-21d4-43fe-a4b5-73df41d36227.json` 的排序 JSON SHA-256 同为 `2449768aadcb8c11333cc3c9e1d4ebcb2c6e77b7c02c9e98dd8da1d160e7f3f3`。
- TypeScript `--noEmit` 通过；Creator 原生资源门禁通过：1367 个场景/预制体、28602 个资源与 meta、38643 个 UUID，阻断项 0。
- Client 全量阶段门禁在首项既有空目录检查处停止，首个错误为 12 个 `Modules/*/Atlas` 空目录；未修改这些越界目录。
- 专项测试 7 项中 6 项通过；失败项为 Prefab 既有缺少 `CloseButton`，本次未新增节点或删除断言掩盖问题。
- 同一项目当时存在 5 个 Creator 实例。为避免占用用户屏幕，未执行可见编辑器内的关闭并重开；该项标记为待编辑器同步确认。

## 真实 Canvas 结果

- ChatPanel 加载：实际点击房间聊天按钮后，`form:room/ChatPanel` 完成 begin/progress/commit；运行时层级显示 `MessagePanel`、`MessageComposer`、`MessageInput`、`EmojiGrid`、`Emoji01…Emoji20`、`QuickTextPanel`。
- 表情：实际点击 `Emoji01` 后没有聊天 WebSocket 请求，只有无关心跳帧；20 个 Emoji 节点均只有 `UITransform + Sprite`，没有 Button。
- 快捷文字：实际点击 `QuickText01` 发出 `quick_text` 请求，`requestId=69702f3b-5159-4261-b1d8-212fbf068575`；服务端返回 `code=3008`、`unsupported poker dispatch action: common.room.dispatch`，本机未显示，对方未收到。
- 自定义文字：实际点击 `MessageInput` 并通过键盘输入 `ChatPanel-E2E-20260909` 成功；实际点击 `SendButton` 后没有 WebSocket 请求，文字仍留在输入框，本机未显示，对方未收到。
- 浏览器控制台存在 PDK 房间既有缺失 UUID `4ea6c8dd-1da2-4fb6-9db1-75998bd41c4d`、`71906fbc-fb71-4863-9e7f-f5826e13e1e5`；ChatPanel 加载未新增缺失 UUID、反序列化或脚本错误。

## 截图

- `01-CD201房间.png`：进入测试房间。
- `02-ChatPanel新节点名加载.png`：ChatPanel 成功加载。
- `03-自定义文字输入后点击发送.png`：输入成功但发送后仍留在输入框。

## 阻塞结论

节点命名与必需引用已生效；三类消息双端送达验收未通过。补齐 Emoji/Button、发送绑定及服务端 `common.room.dispatch` 支持会改变聊天业务，超出本次“只改名称及必需引用、禁止修改聊天业务”授权。
