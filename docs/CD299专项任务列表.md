# CD299 专项任务列表

## CD299-E2E-001

- 目标：按 XQP 权威行为等价实现 CD299 扯旋的前后端完整真人流程、横屏 UI、操作按钮、座位视角、积分落座、芒果规则和结算，并完成真实端到端验收。
- 已确认产品规则：自己固定显示在本地 0 号位；两张牌后逐轮下注，补第三、第四张牌，敲后按权威规则自动补牌，最后分牌结算；快捷下注倍数、按钮显隐、动画与坐标以 XQP 为对照；开房规则以只读规则表为唯一权威。
- 允许修改范围：Aoo 的 CD299 专属客户端、服务端、协议、注册、本地测试数据，以及完成 CD299 所必需且已明确授权的 CommonRoom/Gateway 扩展点。
- 禁止修改范围：其他玩法行为、机器人、聊天和外围功能、开房规则表、冻结的竖屏资源、XQP 源码、未授权 Git 操作。
- 前置依赖：Creator 3.8.8 Preview、CDP 1234、CD299 服务端和本地测试账号可用。
- 验收终点：专项自动测试、Creator 同步、桌面和移动无头 Chrome 多账号真实 Canvas E2E 全部通过；覆盖混合下注、全敲、休芒、揍芒、后加入芒果、低分/仅一家有分、搭不动、快捷下注、规则选项、断线恢复、超时托管、结算守恒与 UI 坐标/显隐。
- 证据要求：记录 buildId、协议版本、地址、roomId、playerId、operationId、stateVersion、请求摘要、截图、日志和结算数据；不得含 Token。
- 当前状态：执行中。
- 安全恢复断点：六人混合下注/全敲真实 E2E 已通过，证据目录 `前端框架规范/历史记录与证据/证据/cd299-horizontal-full-2026-09-20T15-49-17-852Z`；继续执行芒果、低分、规则、超时、移动端和最终独立复验。
