import { ProductionApiClient } from '../Activity/ProductionApiClient';
export type PrivacyRequestStatus='PENDING'|'COOLING_OFF'|'ON_HOLD'|'RUNNING'|'COMPLETED'|'CANCELLED'|'FAILED';
export interface PrivacyRequest{id:string;type:'EXPORT'|'DELETE';status:PrivacyRequestStatus;requestedAt:string;coolingOffUntil:string|null;completedAt:string|null;resultManifest:string|null;version:number;allowedActions:readonly string[];}
/** The only player-side transport for privacy rights. */
export class PrivacyGateway{public constructor(private readonly api:ProductionApiClient){}public list():Promise<PrivacyRequest[]>{return this.api.get('/api/v2/privacy/requests');}public requestExport():Promise<PrivacyRequest>{return this.api.mutate('POST','/api/v2/privacy/exports',{},ProductionApiClient.operationKey('privacy-export'));}public requestDeletion():Promise<PrivacyRequest>{return this.api.mutate('POST','/api/v2/privacy/deletions',{},ProductionApiClient.operationKey('privacy-delete'));}public cancel(requestId:string,expectedVersion:number):Promise<PrivacyRequest>{return this.api.mutate('POST','/api/v2/privacy/requests/cancel',{requestId,expectedVersion},ProductionApiClient.operationKey(`privacy-cancel:${requestId}:${expectedVersion}`));}}
