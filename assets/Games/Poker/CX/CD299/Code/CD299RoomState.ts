import {CD299_GAME_CODE} from './CD299Rules';
export type CD299Phase='WAITING'|'BASE_AND_MANGO'|'DEALING'|'BETTING'|'ADD_CARD'|'SPLITTING'|'REVEAL'|'ROUND_SETTLEMENT'|'FINISHED';
export interface CD299Snapshot{gameCode:typeof CD299_GAME_CODE;roomId:number;ownerId:number;stateVersion:number;round:number;bankerSeat:number;phase:CD299Phase;roomStartedAtEpochMillis:number;roomEndsAtEpochMillis:number;terminalDeadlineEpochMillis:number;roundSettlementDeadlineEpochMillis:number;roundSettlementTriggerSeat:number;seatRetentionDeadlineEpochMillis:Readonly<Record<number,number>>;restartCarryScores:Readonly<Record<number,number>>;viewerRole:'SPECTATOR'|'SEATED';viewerSeat:number;rules:Readonly<{maxPlayers:number;openingBet:number;earthNineKing:boolean;roundCount?:10|20|30;roomDurationMinutes?:30|45|60;autoOperateAfterTimeouts:3|4|5;operationSeconds?:number;splitSeconds?:number;splitExtensionSeconds?:number;splitExtensionLimit?:number}>;players:Readonly<Record<number,number>>;readySeats:readonly number[];hands:Readonly<Record<number,readonly number[]>>;committed:Readonly<Record<number,number>>;droppedSeats:readonly number[];threeFlowerSeats:readonly number[];splitSeats:readonly number[];splitDeadlineEpochMillis:Readonly<Record<number,number>>;carryScores:Readonly<Record<number,number>>;scores:Readonly<Record<number,number>>;winCounts?:Readonly<Record<number,number>>;loseCounts?:Readonly<Record<number,number>>;bases:Readonly<Record<number,number>>;mangos:Readonly<Record<number,number>>;requiredMangos:Readonly<Record<number,number>>;mangoCursors:Readonly<Record<number,number>>;playedSeats:readonly number[];roundSeats:readonly number[];upMangoLevel:number;lastMangoEvent:string;bets:Readonly<Record<number,number>>;lastBetActions:Readonly<Record<number,import('./CD299Protocol').CD299BetAction>>;allInSeats:readonly number[];lastDelta:Readonly<Record<number,number>>;mangoPool:number;currentSeat:number;betTarget:number;followAmount:number;minimumRaiseTarget:number;quickRaiseTargets:readonly number[];viewerAvailableScore:number;operationDeadline:Readonly<{operationId:string;deadlineEpochMillis:number;seatId:number;open?:boolean}>;allowedBetActions:readonly import('./CD299Protocol').CD299BetAction[];}
export function acceptCD299Snapshot(current:CD299Snapshot|null,incoming:CD299Snapshot):CD299Snapshot{const seated=incoming.viewerRole==='SEATED';const settledBankerMayHaveLeft=(incoming.phase==='ROUND_SETTLEMENT'||incoming.phase==='FINISHED')&&incoming.bankerSeat>=0&&incoming.players[incoming.bankerSeat]===undefined;const validBetNumbers=Number.isSafeInteger(incoming.followAmount)&&incoming.followAmount>=0&&Number.isSafeInteger(incoming.minimumRaiseTarget)&&incoming.minimumRaiseTarget>=0&&Number.isSafeInteger(incoming.viewerAvailableScore)&&incoming.viewerAvailableScore>=0&&Array.isArray(incoming.quickRaiseTargets)&&(incoming.quickRaiseTargets.length===0||incoming.quickRaiseTargets.length===3)&&incoming.quickRaiseTargets.every(value=>Number.isSafeInteger(value)&&value>=0);const hasRoundCount=incoming.rules?.roundCount!==undefined;const hasDuration=incoming.rules?.roomDurationMinutes!==undefined;const validEndDimension=hasRoundCount!==hasDuration&&(hasRoundCount?[10,20,30].includes(incoming.rules.roundCount!):[30,45,60].includes(incoming.rules.roomDurationMinutes!));if(incoming.gameCode!==CD299_GAME_CODE||!Number.isSafeInteger(incoming.roomId)||incoming.roomId<=0||!Number.isSafeInteger(incoming.stateVersion)||incoming.stateVersion<0||!Number.isInteger(incoming.bankerSeat)||incoming.bankerSeat<-1||incoming.rules?.maxPlayers!==8||!validEndDimension||![3,4,5].includes(incoming.rules.autoOperateAfterTimeouts)||incoming.bankerSeat>=incoming.rules.maxPlayers||(incoming.bankerSeat>=0&&incoming.players[incoming.bankerSeat]===undefined&&!settledBankerMayHaveLeft)||typeof incoming.rules.earthNineKing!=='boolean'||!Array.isArray(incoming.allowedBetActions)||!validBetNumbers||(incoming.viewerRole!=='SPECTATOR'&&!seated)||(seated&&(!Number.isInteger(incoming.viewerSeat)||incoming.viewerSeat<0||incoming.viewerSeat>=incoming.rules.maxPlayers||incoming.players[incoming.viewerSeat]===undefined))||(!seated&&incoming.viewerSeat!==-1))throw new Error('[CD299] invalid authoritative snapshot');if(current&&current.roomId!==incoming.roomId)throw new Error('[CD299] room identity changed');if(current&&incoming.stateVersion<current.stateVersion)return current;if(current&&incoming.stateVersion===current.stateVersion&&incoming.viewerRole===current.viewerRole&&incoming.viewerSeat===current.viewerSeat)return current;return freeze(incoming);}
function freeze(s:CD299Snapshot):CD299Snapshot{return Object.freeze({...s,rules:Object.freeze({...s.rules}),players:Object.freeze({...s.players}),seatRetentionDeadlineEpochMillis:Object.freeze({...s.seatRetentionDeadlineEpochMillis}),restartCarryScores:Object.freeze({...s.restartCarryScores}),readySeats:Object.freeze([...s.readySeats]),hands:Object.freeze(Object.fromEntries(Object.entries(s.hands).map(([k,v])=>[k,Object.freeze([...v])]))),committed:Object.freeze({...s.committed}),droppedSeats:Object.freeze([...s.droppedSeats]),threeFlowerSeats:Object.freeze([...s.threeFlowerSeats]),splitSeats:Object.freeze([...s.splitSeats]),splitDeadlineEpochMillis:Object.freeze({...s.splitDeadlineEpochMillis}),carryScores:Object.freeze({...s.carryScores}),scores:Object.freeze({...s.scores}),winCounts:Object.freeze({...s.winCounts}),loseCounts:Object.freeze({...s.loseCounts}),bases:Object.freeze({...s.bases}),mangos:Object.freeze({...s.mangos}),requiredMangos:Object.freeze({...s.requiredMangos}),mangoCursors:Object.freeze({...s.mangoCursors}),playedSeats:Object.freeze([...s.playedSeats]),roundSeats:Object.freeze([...s.roundSeats]),bets:Object.freeze({...s.bets}),lastBetActions:Object.freeze({...s.lastBetActions}),allInSeats:Object.freeze([...s.allInSeats]),lastDelta:Object.freeze({...s.lastDelta}),quickRaiseTargets:Object.freeze([...s.quickRaiseTargets]),operationDeadline:Object.freeze({...s.operationDeadline}),allowedBetActions:Object.freeze([...s.allowedBetActions])});}
