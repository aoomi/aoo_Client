export const CD299_GAME_CODE = 'CD299' as const;
export const CD299_PLAY_VERSION = 'cd299-v1.0.0' as const;
export type CD299StandPolicy = 'LOSER_ONLY' | 'EVERYONE' | 'NOBODY';
export type CD299FlipMode = 'LADDER' | 'ROLLING';
export interface CD299RoomRules { roomDurationMinutes: 30|45|60; maxPlayers: 8; startPlayers: 2|4|6; operationSeconds: 10|15|30; standPolicy: CD299StandPolicy; mangoFlipMode: CD299FlipMode; mangoRaise: 0|1|2|3|4|5; mangoScore: number; openingBet: number; rounds: number; restMango: boolean; beatMango: boolean; everyHandMango: boolean; firstRoundCanRest: boolean; eachPlayerMustFollow: boolean; earthNineKing: boolean; fireproofCard: boolean; bigHeadKeepsBase: boolean; }
export const CD299_DEFAULT_RULES:Readonly<CD299RoomRules>=Object.freeze({roomDurationMinutes:30,maxPlayers:8,startPlayers:2,operationSeconds:10,standPolicy:'LOSER_ONLY',mangoFlipMode:'LADDER',mangoRaise:3,mangoScore:3,openingBet:3,rounds:8,restMango:true,beatMango:true,everyHandMango:true,firstRoundCanRest:true,eachPlayerMustFollow:false,earthNineKing:true,fireproofCard:true,bigHeadKeepsBase:false});
export function validateCD299Rules(r:CD299RoomRules):CD299RoomRules{const flags=[r.restMango,r.beatMango,r.everyHandMango,r.firstRoundCanRest,r.eachPlayerMustFollow,r.earthNineKing,r.fireproofCard,r.bigHeadKeepsBase];if(!([30,45,60] as const).includes(r.roomDurationMinutes)||r.maxPlayers!==8||!([2,4,6] as const).includes(r.startPlayers)||!([10,15,30] as const).includes(r.operationSeconds)||r.mangoRaise<0||r.mangoRaise>5||!Number.isFinite(r.mangoScore)||r.mangoScore<0||!Number.isFinite(r.openingBet)||r.openingBet<r.mangoScore||!Number.isInteger(r.rounds)||r.rounds<1||flags.some(value=>typeof value!=='boolean'))throw new Error('[CD299] invalid room rules');return {...r};}
