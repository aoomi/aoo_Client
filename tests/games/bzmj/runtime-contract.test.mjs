import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';import{fileURLToPath}from'node:url';const adapter=await readFile(fileURLToPath(new URL('../../../assets/Games/Mahjong/Common/Code/BzmjGameplayAdapter.ts',import.meta.url)),'utf8');test('BZMJ compatibility binding remains family-owned',()=>{assert.match(adapter,/extends BozhouMahjongFamilyAdapter/);assert.match(adapter,/code:'bzmj'/);assert.doesNotMatch(adapter,/private sequence|private current|private async send|private accept/);assert.ok(!adapter.includes('canWin(')&&!adapter.includes('calculateScore(')&&!adapter.includes('settlement('));});
