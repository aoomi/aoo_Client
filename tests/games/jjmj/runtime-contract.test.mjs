import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';import{fileURLToPath}from'node:url';const adapter=await readFile(fileURLToPath(new URL('../../../assets/Games/Mahjong/Common/Code/JjmjGameplayAdapter.ts',import.meta.url)),'utf8');test('JJMJ compatibility binding remains family-owned',()=>{assert.match(adapter,/extends MahjongIntentFamilyAdapter/);assert.match(adapter,/code:'jjmj'/);assert.doesNotMatch(adapter,/private sequence|private current|private async send|private accept/);assert.ok(!adapter.includes('canWin(')&&!adapter.includes('calculateScore(')&&!adapter.includes('settlement('));});
