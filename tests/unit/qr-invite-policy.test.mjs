import assert from'node:assert/strict';import fs from'node:fs';import vm from'node:vm';import ts from'../../../Admin/node_modules/typescript/lib/typescript.js';let source=fs.readFileSync(new URL('../../assets/Common/Code/Runtime/navigation/QrInvitePolicy.ts',import.meta.url),'utf8');source=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText;let sandbox={exports:{}};vm.createContext(sandbox);vm.runInContext(source,sandbox);let{QrScanResult:R,QrInvitePolicy}=sandbox.exports;let policy=new QrInvitePolicy();let view=policy.presentation({payload:'signed-payload',signature:'hmac'},100_000,10_000);assert.equal(view.minimumPixels,256);assert.equal(view.refreshAtEpochMillis,70_000);assert.equal(view.screenshotWarning,true);assert.equal(policy.scan('',false,false),R.UNREADABLE);assert.equal(policy.scan('x',false,false),R.INVALID_SIGNATURE);assert.equal(policy.scan('x',true,true),R.EXPIRED);assert.equal(policy.scan('x',true,false),R.OK);console.log('qr invite policy passed');
