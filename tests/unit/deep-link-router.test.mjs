import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';import ts from '../../../Admin/node_modules/typescript/lib/typescript.js';
let source=fs.readFileSync(new URL('../../assets/Common/Code/Runtime/navigation/DeepLinkRouter.ts',import.meta.url),'utf8');source=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2022}}).outputText;const sandbox={exports:{}};vm.createContext(sandbox);vm.runInContext(source,sandbox);const{DeepLinkAction:A,DeepLinkRouter}=sandbox.exports;const router=new DeepLinkRouter();const base={inviteVerified:true,installed:true,authenticated:true,versionCompatible:true,currentRoomId:0,targetRoomId:9};assert.equal(router.resolve({...base,inviteVerified:false}).action,A.REJECT_INVALID);assert.equal(router.resolve({...base,installed:false}).action,A.OPEN_INSTALL);assert.equal(router.resolve({...base,authenticated:false}).action,A.OPEN_LOGIN);assert.equal(router.resolve({...base,versionCompatible:false}).action,A.REQUEST_UPDATE);assert.equal(router.resolve({...base,currentRoomId:8}).action,A.CONFIRM_LEAVE_ROOM);const joined=router.resolve(base);assert.equal(joined.action,A.JOIN_TARGET_ROOM);assert.equal(joined.serverAdmissionRequired,true);console.log('deep link state matrix passed');
